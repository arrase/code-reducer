{
  "version": 1,
  "steps_hash": "61d594e4ff13def120bfbc4dbb8f7556458b5da07bca0af9ff454967f8afc215",
  "files": {
    "cmd/cmd_test.go": {
      "sha256": "028791d1a295f037bfbe579da61292cb99c3f25a61c3d8520eee233831c1deb8",
      "facts": "#### [API_SIGNATURES]\n* TestRootFlags(t *testing.T)\n* TestCheckInitStatus(t *testing.T)\n* TestPromptString(t *testing.T)\n* TestPromptStringList(t *testing.T)\n* TestPromptContextSize(t *testing.T)\n* TestPromptIgnores(t *testing.T)\n* TestLoadInitialSetupConfig(t *testing.T)\n* TestRunSetupFlow(t *testing.T)\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code manages the command-line interface (CLI) and interactive configuration lifecycle for project setup. It ensures that project initialization and update operations are performed in the correct state (e.g., preventing re-initialization of existing projects) and handles the persistence of user configuration settings.\n\n**High-Level Algorithm Steps**\n1.  **Parse CLI Arguments:** Extract configuration parameters from command-line flags.\n2.  **Verify Project State:** Determine if a project is initialized by checking for the existence of specific directories and metadata files.\n3.  **Validate Operation Mode:** \n    *   Allow initialization only if the project is uninitialized.\n    *   Allow updates only if the project is already initialized.\n4.  **Capture User Input:** Collect configuration values through interactive prompts, employing fallback logic for empty or invalid inputs.\n5.  **Manage Configuration Persistence:** Load or save configuration settings to the file system.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State**\n* `modelIDFlag`: Global variable modified in `TestRootFlags`.\n* `numCtxFlag`: Global variable modified in `TestRootFlags`.\n* `RootCmd`: Global variable modified via `.SetArgs()` and `.Execute()` in `TestRootFlags`.\n\n**Synchronization Mechanisms**\n* None identified.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O**\n* **Disk**:\n    * Directory creation via `t.TempDir()` and `os.MkdirAll`.\n    * File creation and writing via `os.WriteFile` (specifically `.metadata.json`).\n    * Configuration file persistence and retrieval via `config.SaveConfig` and `config.LoadConfig`.\n* **Input**:\n    * Simulated user input via `bufio.Reader`.\n\n**Error Propagation**\n* **Error Return Codes**: The functions `checkInitStatus`, `promptString`, `promptStringList`, `promptContextSize`, `promptIgnores`, `runSetupFlowWithReader`, `config.SaveConfig`, and `config.LoadConfig` propagate errors via return values.\n* **Swallowed Errors**: The return value from `RootCmd.Execute()` is swallowed in `TestRootFlags`."
    },
    "cmd/init.go": {
      "sha256": "995bf2f9da724383666ec4f58da72bdea798d316266511b84faae8447fdd4682",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface (cmd/init.go)\n\nThis file defines **no exported or public elements**. All identifiers follow Go's unexported naming conventions:\n\n| Element | Visibility | Notes |\n|---|---|---|\n| `initCmd` | Unexported (`var`) | Cobra command struct literal; lowercase |\n| `init()` | Unexported (`func`) | Registers the cobra command with `RootCmd`; references an external `RootCmd` not defined in this file |\n\n**Conclusion:** No public API surface is exposed by this file.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code establishes a CLI entry point for initializing project documentation. It triggers an engine operation that scans a repository and generates initial wiki markdown pages. The command is wired into a root cobra command tree, meaning the application supports multi-step operations (init being one of them).\n\n## High-Level Algorithm Steps\n\n1. Register an `init` subcommand under the root CLI command\n2. On invocation, delegate to an engine operation identified by `ModeInit`\n3. The engine scans the repository structure and produces a first-pass set of wiki markdown files\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis: `cmd/init.go`\n\n**No direct external side effects visible in this file.** This file is a thin CLI command wrapper. All actual work is delegated to `internal/engine.ExecuteCommand(engine.ModeInit)`. Since that module's source was not provided, its internal interactions (network calls, disk writes, database queries, etc.) cannot be analyzed here — those must be inferred from the engine package separately.\n\n**Dependencies:**\n- `github.com/arrase/code-reducer/internal/engine` — local package; external I/O behavior unknown without its source.\n- `github.com/spf13/cobra` — CLI framework; no external I/O in this file.\n\n## Error Propagation\n\nThe error handling pattern is straightforward and follows cobra conventions:\n\n```go\nRunE: func(cmd *cobra.Command, args []string) error {\n    if err := executeCommand(engine.ModeInit); err != nil {\n        return err  // propagate up to cobra's error handling\n    }\n    return nil  // success path\n},\n```\n\n- If `executeCommand` returns a non-nil error, it is returned directly to the caller (cobra), which will typically print a usage message and exit with status code 1.\n- No wrapping, retry logic, or panic recovery is present — errors pass through as-is."
    },
    "cmd/root.go": {
      "sha256": "d20db30dc9ae4b7208aecf6ccab88be4d36a2b4336adda7cbd6898a806923558",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `cmd` package (root.go)\n\n## Variables\n\n| Element | Type | Description |\n|---|---|---|\n| `RootCmd` | `*cobra.Command` | Root cobra command instance used as CLI entry point. |\n\n## Functions\n\n### Package-level functions\n\n| Function | Parameters | Return Type | Description |\n|---|---|---|---|\n| `init()` | — | — | Registers persistent flags (`--model-id`, `--num-ctx`) on `RootCmd`. |\n| `executeCommand` | `mode engine.Mode` | `error` | Validates git repo, configures setup if needed, resolves config, and runs the engine. |\n| `checkAndRunSetup` | `repoRoot string` | `error` | Checks whether a config file exists; if not and stdin is a TTY, invokes `RunSetupFlow`. |\n| `checkInitStatus` | `repoRoot, docsDir string`, `mode engine.Mode` | `error` | Validates init state based on the requested mode (`engine.ModeInit` or `engine.ModeUpdate`). |\n| `runEngine` | `repoRoot string`, `cfg *config.Config`, `mode engine.Mode` | `error` | Sets up signal handling, creates an engine runner, and executes documentation generation. |\n\n## Notes\n\n- All package-level functions in this file use lowercase identifiers (unexported). Only `init()` is a standard Go initialization function; the others are internal implementation details.\n- The only exported public element from this file is `RootCmd`.\n\n#### [BUSINESS_LOGIC]\n**Core Purpose:**  \nA CLI tool that drives an LLM-based documentation agent through three lifecycle modes: `init`, `update`, and one additional mode (likely `run`). It manages project state transitions—configuring a setup phase when needed, validating git repository requirements, and executing the engine while streaming status/error events to stdout/stderr.\n\n**High-Level Algorithm Steps:**\n\n1. **Resolve working directory** — capture current path for all subsequent relative operations.\n2. **Verify Git repo** — fail early if the project is not a git repository.\n3. **Auto-run setup (if needed)** — when no prior configuration file exists and stdin is a terminal, trigger the `setup` flow; otherwise report an error.\n4. **Resolve configuration** — load or construct config from disk + CLI flags (`--model-id`, `--num-ctx`).\n5. **Check init status against mode** — enforce that:\n   - `init` mode fails if already initialized (suggest `update`),\n   - `update` mode fails if not yet initialized (suggest `init`).\n6. **Execute engine with signal handling** — register interrupt/terminate handlers, instantiate the runner, and stream events of three types (`status`, `error`, other) to stdout or stderr as appropriate.\n\n#### [STATE_AND_CONCURRENCY]\n**Global Variables (Mutable State):**\n\n| Variable | Type | Mutation Location | Notes |\n|---|---|---|---|\n| `modelIDFlag` | string | `init()` via `StringVar` | Set once at startup. Read-only after that within this file. |\n| `numCtxFlag` | string | `init()` via `StringVar` | Set once at startup. Read-only after that within this file. |\n\n**Synchronization Mechanisms:** None observed in this file. No locks, mutexes, atomics, channels, or goroutine patterns visible here.\n\n**Shared State:** The two global flags (`modelIDFlag`, `numCtxFlag`) are shared across all calls to `executeCommand` and any function that reads them (e.g., `checkAndRunSetup`, `runEngine`). They persist for the lifetime of the process.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n### Disk / Filesystem\n| Operation | Location | Purpose |\n|-----------|----------|---------|\n| `os.Getwd()` | `executeCommand` | Reads current working directory path |\n| `tools.VerifyGitRepo(repoRoot)` | `executeCommand` | Validates git repository presence (reads `.git` or equivalent markers) |\n| `config.ConfigExists(repoRoot)` | `checkAndRunSetup` | Checks whether config file exists on disk |\n| `RunSetupFlow(repoRoot)` | `checkAndRunSetup` | Writes configuration files and project structure to disk during setup |\n| `engine.IsInitialized(repoRoot, docsDir)` | `checkInitStatus` | Reads filesystem state (likely checks for init marker files in docs directory) |\n\n### Stdout / Stderr\n| Output Channel | Condition | Content |\n|---------------|-----------|---------|\n| stdout (`fmt.Println`) | `ev.Type == engine.EventStatus` OR unknown event type | Status messages and generic events from the runner |\n| stderr (`os.Stderr`) | `ev.Type == engine.EventError` | Error messages prefixed with \"Error: \" |\n\n### Signal Handling\n- Registers `os.Interrupt` (SIGINT) and `syscall.SIGTERM` via `signal.NotifyContext(context.Background(), ...)` to enable graceful cancellation of the runner. The context is stored in a local variable (`ctx`) and deferred stop on exit.\n\n---\n\n## Error Propagation\n\n### Wrapped Errors (preserved chain via `%w`)\n- **Working directory failure**: Wraps `os.Getwd()` error as `\"failed to get current working directory: %w\"` — caller can use `errors.Is`/`errors.As`.\n- **Setup flow failure**: Returns error from `RunSetupFlow(repoRoot)` directly without wrapping — caller sees original error.\n- **Runner failure**: Wraps any error from `runner.Run(...)` as `\"documentation run failed: %w\"`.\n\n### Pass-Through Errors (no wrapping)\n- **Git verification**: Error returned by `tools.VerifyGitRepo` is propagated verbatim to the parent call site.\n- **Config resolution**: Error from `config.ResolveConfig` is passed through unchanged.\n- **Init status checks**: All errors are *new* `fmt.Errorf(...)` messages — no wrapping of previous errors, so error chain terminates at this point.\n\n### User-Facing Error Messages (created inline)\nAll generated in `checkAndRunSetup` and `checkInitStatus`:\n- Config file missing on non-TTY: `\"configuration file %s does not exist... Please run 'code-reducer setup'\"`\n- Init already done: `\"the project has already been initialized. Please use 'code-reducer update'\"`\n- Not yet initialized: `\"the project has not been initialized yet. Please run 'code-reducer init' first\"`\n\n### No Panic / Crash Path\nNo `panic()` calls or unhandled recover blocks are present in this file. All errors are handled via return values and propagated upward to the CLI entry point."
    },
    "cmd/setup.go": {
      "sha256": "b93775150eb0ded835e35052f399dfd840a79469a35e3ba2741b3c1fd3149038",
      "facts": "#### [API_SIGNATURES]\n* `RunSetupFlow(repoRoot string) error`\n\n#### [BUSINESS_LOGIC]\n### Primary Domain Problem\nThe code facilitates interactive configuration management for a tool that interfaces with Large Language Models (LLMs) to process codebases. It provides a mechanism to define and persist environment-specific settings, including LLM connection details, processing constraints, file exclusion rules, and directory paths.\n\n### High-level Algorithm Steps\n1.  **Identify Environment:** Determine the current working directory to locate configuration files.\n2.  **Initialize Configuration:** Load existing configuration settings from the project root and merge them with predefined default values where settings are missing or empty.\n3.  **Interactive Input Collection:** Iteratively prompt the user for specific parameters, allowing the user to accept current values by providing no input:\n    *   LLM Model ID.\n    *   Ollama Base URL.\n    *   Ollama Context Size.\n    *   File/directory ignore patterns.\n    *   Documentation directory path.\n4.  **Consolidate Settings:** Assemble a complete configuration object using both the user-provided inputs and the existing/default values for fields not modified by the user.\n5.  **Persist Configuration:** Save the final configuration object to a file in the repository root.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State**\n* `setupCmd` (package-level `*cobra.Command`)\n* `RootCmd` (modified via `AddCommand` in `init`)\n\n**Synchronization Mechanisms**\n* None\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O**\n* **Disk**:\n    * `os.Getwd`: Retrieves the current working directory.\n    * `config.LoadConfig`: Loads configuration from `repoRoot`.\n    * `config.SaveConfig`: Writes configuration to `repoRoot`.\n* **Standard Input**:\n    * `os.Stdin` (via `bufio.Reader`): Used to capture user input for Model ID, Ollama Base URL, Context Size, ignore patterns (files/directories), and the documentation directory.\n* **Standard Output**:\n    * `fmt.Println` and `fmt.Printf`: Used for status messages, interactive prompts, and success notifications.\n\n**Error Propagation**\n* **`os.Getwd` error**: Wrapped with context and returned.\n* **`config.LoadConfig` error**: Swallowed; returns a default `config.Config` if an error occurs or if the returned config is `nil`.\n* **`strconv.Atoi` error (in `promptContextSize`)**: Swallowed; returns the `defaultVal`.\n* **`reader.ReadString` error**: Wrapped and returned via `promptString`, `promptStringList`, `promptContextSize`, and `promptIgnores`.\n* **`config.SaveConfig` error**: Wrapped and returned."
    },
    "cmd/update.go": {
      "sha256": "56faface0ab0f0c994482e1c88b145dffec088858bd445dc56a63df977f4b638",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface — `cmd/update.go`\n\n**Note:** This file contains no exported (uppercase) identifiers. All public-facing functionality is wired through the unexported `updateCmd` variable and auto-called `init()` function, which are implementation details of the private `cmd` package.\n\n### No exported elements found\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis code implements the `update` subcommand for a CLI tool that manages project documentation. It triggers an incremental update of wiki pages by scanning files changed since the last documented commit and applying those changes to existing wiki content.\n\n## High-Level Algorithm Steps\n\n1. Register a new cobra command named `update` under the root command\n2. On execution, delegate to the internal engine with mode `ModeUpdate`\n3. The engine scans for modified files compared against the last committed documentation baseline\n4. Updated content is merged back into existing wiki pages\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External Side Effects:**\n\n- The RunE handler in `updateCmd` is a pure delegation wrapper. It calls `executeCommand(engine.ModeUpdate)` and returns whatever error that call produces. All actual I/O (network, disk, database) happens inside the unshown `engine` package — this file itself performs none.\n- No explicit network calls, no file reads/writes, no database queries are visible in this file's source.\n\n**Error Propagation:**\n\n- Errors propagate upward through the Cobra command chain via the standard `RunE func(cmd *cobra.Command, args []string) error` signature:\n  - If `executeCommand(engine.ModeUpdate)` returns an `error`, it is returned directly to the caller (the CLI invocation path).\n  - If it returns `nil`, the function returns `nil` — Cobra treats this as a successful execution.\n- No panic, no silent swallow. Any error from the engine's update logic surfaces at the top level of the CLI.\n\n**Summary:** This file is a thin command-definition shell; all external behavior and error handling live in the unreviewed `internal/engine` package."
    },
    "internal/config/config.go": {
      "sha256": "a89a8e4d3499605f8b070f9169c91543bbdc7ad7cf8e60e1fbd46b036da09917",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface of `internal/config` Package\n\n## Constants\n\n| Name | Type | Value / Description |\n|---|---|---|\n| `CodeReducerModelIDEnvKey` | `string` | `\"CODE_REDUCER_MODEL_ID\"` — env key for model ID override. |\n| `OllamaBaseURLEnvKey` | `string` | `\"OLLAMA_BASE_URL\"` — env key for Ollama URL override. |\n| `OllamaNumCtxEnvKey` | `string` | `\"OLLAMA_NUM_CTX\"` — env key for context size override. |\n| `OllamaDefaultBaseURL` | `string` | `\"http://localhost:11434\"` — default URL for local Ollama API. |\n| `OllamaDefaultModelID` | `string` | `\"ornith:9b\"` — default LLM model ID. |\n| `OllamaDefaultNumCtx` | `int` | `8192` — default context size. |\n| `DefaultDocsDir` | `string` | `\"wiki\"` — default documentation folder name. |\n| `ConfigFileName` | `string` | `\".code-reducer.yaml\"` — configuration filename. |\n\n## Types\n\n### `ExtractionStep`\n\n```go\ntype ExtractionStep struct {\n    Name   string // yaml:\"name\"\n    Prompt string // yaml:\"prompt\"\n}\n```\n\n### `Config`\n\n```go\ntype Config struct {\n    ModelID                     string           // yaml:\"model_id\"\n    OllamaBaseURL               string           // yaml:\"ollama_base_url\"\n    OllamaNumCtx                int              // yaml:\"ollama_num_ctx\"\n    DocsDir                     string           // yaml:\"docs_dir\"\n    SystemPrompt                string           // yaml:\"system_prompt\"\n    ModuleSynthesisPrompt       string           // yaml:\"module_synthesis_prompt\"\n    ArchitecturePrompt          string           // yaml:\"architecture_prompt\"\n    FileFactConsolidationPrompt string           // yaml:\"file_fact_consolidation_prompt\"\n    ExtractionSteps             []ExtractionStep // yaml:\"extraction_steps\"\n    Ignore                      []string         // yaml:\"ignore\"\n}\n```\n\n## Variables\n\n### `DefaultExtractionSteps`\n\n- **Type:** `[]ExtractionStep`\n- **Value:** Pre-populated slice containing four default extraction steps:\n  - `{Name: \"API_SIGNATURES\", ...}` — extracts public API surface.\n  - `{Name: \"BUSINESS_LOGIC\", ...}` — explains primary domain problem and high-level algorithm steps.\n  - `{Name: \"STATE_AND_CONCURRENCY\", ...}` — identifies mutable state, shared states, synchronization mechanisms; outputs `\"No mutable state\"` if entirely stateless.\n  - `{Name: \"ERRORS_AND_SIDE_EFFECTS\", ...}` — details external I/O interactions and error propagation; outputs `\"No external side effects\"` if no I/O exists.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\n\nThis code defines the configuration schema for an AI-powered static analysis tool that produces technical documentation from source code. It configures how a local LLM (via Ollama) parses source files through multiple extraction phases to synthesize structured docs.\n\n**High-Level Algorithm Steps:**\n\n1. **API Surface Extraction** — Extract public types, functions, methods, and their signatures without explaining internal logic.\n2. **Business Logic Synthesis** — Explain the primary domain problem solved by the code and list high-level algorithm steps, ignoring implementation syntax.\n3. **State \u0026 Concurrency Mapping** — Identify mutable global/state variables and synchronization mechanisms; report \"No mutable state\" if none exist.\n4. **Error \u0026 Side Effect Analysis** — Detail interactions with external systems (network, disk, databases) and how errors propagate or are swallowed.\n\nThese four extraction steps feed into a multi-step LLM pipeline that ultimately produces module-level documentation and architecture-wide synthesis documents.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** `DefaultExtractionSteps` — declared as a package-level `var` initialized with a slice of four `ExtractionStep` structs. No synchronization mechanism (mutex, atomic type, etc.) protects it in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects. This file contains only type definitions (`ExtractionStep`, `Config`), constants, default values, and environment variable key names. No functions perform reads, writes, network calls, or any system interactions.\n\n**Error Propagation:** N/A — no error-returning functions exist in this file."
    },
    "internal/config/config_test.go": {
      "sha256": "9373e2c7629b6b302ed73c20dac13bd8a79b979c4d71bf7df4854ed3a8a5b804",
      "facts": "#### [API_SIGNATURES]\n* `LoadConfig(dir string) (*Config, error)`\n* `SaveConfig(dir string, cfg *Config) error`\n* `ResolveConfig(dir string, modelID string, numCtx string) (*Config, error)`\n* `Config` (struct)\n    * `ModelID string`\n    * `OllamaNumCtx int`\n    * `OllamaBaseURL string`\n* `ConfigFileName`\n* `OllamaNumCtxEnvKey`\n* `CodeReducerModelIDEnvKey`\n* `OllamaBaseURLEnvKey`\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nManaging application configuration through a hierarchical resolution process that combines settings from local files, environment variables, and command-line flags.\n\n**High-Level Algorithm Steps**\n1. Load configuration data from a specified directory.\n2. Overlay configuration values sourced from environment variables.\n3. Overlay configuration values sourced from command-line flags.\n4. Validate resolved numerical parameters to ensure they are valid and meet specific non-zero requirements.\n\n#### [STATE_AND_CONCURRENCY]\nMutable state:\n- Environment variables (modified via `t.Setenv` using `OllamaNumCtxEnvKey`, `CodeReducerModelIDEnvKey`, and `OllamaBaseURLEnvKey`)\n- Filesystem (modified via `t.TempDir` and `os.WriteFile`)\n\nSynchronization mechanisms:\n- None\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O**\n* **Disk**: `SaveConfig` writes configuration data to the filesystem. `LoadConfig` reads configuration data from the filesystem. `os.WriteFile` writes data to the filesystem.\n* **Environment**: `ResolveConfig` accesses environment variables to retrieve configuration values.\n\n**Error Propagation**\n* **Return Values**: `LoadConfig`, `SaveConfig`, and `ResolveConfig` propagate errors to the caller via return values.\n* **Error Conditions**:\n    * `LoadConfig` returns an error if a file is not found or contains invalid YAML.\n    * `SaveConfig` returns an error during write operations.\n    * `ResolveConfig` returns an error if environment variables or provided arguments contain invalid or non-numeric values."
    },
    "internal/config/io.go": {
      "sha256": "d979275e88e5ef59442f2ecf44417479d4fc3b4c0d4a75632afd0ddb306b5057",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `internal/config/io.go`\n\n## Functions\n\n### ConfigExists\n- **Package**: `config`\n- **Signature**: `func ConfigExists(cwd string) bool`\n- **Parameters**:\n  - `cwd` — directory path (string)\n- **Returns**:\n  - `bool` — true if `.code-reducer.yaml` exists in the specified directory\n\n### LoadConfig\n- **Package**: `config`\n- **Signature**: `func LoadConfig(cwd string) (*Config, error)`\n- **Parameters**:\n  - `cwd` — directory path (string)\n- **Returns**:\n  - `*Config` — parsed configuration pointer\n  - `error` — non-nil if file I/O or YAML parsing fails\n\n### SaveConfig\n- **Package**: `config`\n- **Signature**: `func SaveConfig(cwd string, cfg *Config) error`\n- **Parameters**:\n  - `cwd` — directory path (string)\n  - `cfg` — configuration pointer to persist\n- **Returns**:\n  - `error` — non-nil if write/rename/chmod operations fail\n\n---\n\n## Non-Exported Functions (for reference only; not part of public API)\n\n| Function | Signature |\n|----------|-----------|\n| `getConfigPath` | `func getConfigPath(cwd string) string` |\n| `formatYAML` | `func formatYAML(data []byte) string` |\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nConfiguration management for the **Code-Reducer** system — an AI-driven code synthesis tool that loads, persists, and validates user-configurable parameters (prompts, extraction steps, ignore lists) stored in `.code-reducer.yaml`.\n\n## High-Level Algorithm Steps\n\n1. **Resolve config path** — Build absolute filesystem path from current working directory + configured filename (`ConfigFileName`).\n2. **Check existence** — Stat the resolved path to determine whether a prior config exists at that location.\n3. **Load config** — Read file bytes, unmarshal YAML into a typed `Config` struct; return error if read or parse fails.\n4. **Format/configure output** — Normalize serialized YAML by inserting double newlines before specific prompt keys (`system_prompt`, `module_synthesis_prompt`, etc.) for consistent formatting.\n5. **Save config (atomic write)**:\n   - Marshal the typed struct to YAML bytes.\n   - Apply format normalization.\n   - Write to a temporary file in the same directory.\n   - Sync, close, chmod the temp file.\n   - Atomically rename the temp file over the target path via `os.Rename`.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Disk Operations (File System)\n\n| Function | Operation | Description |\n|---|---|---|\n| `ConfigExists` | `os.Stat()` | Reads directory metadata to check if `.code-reducer.yaml` exists. Returns `bool`, not error — caller must infer absence from `false`. |\n| `LoadConfig` | `os.ReadFile()` | Reads the entire config file into memory for parsing. |\n| `SaveConfig` | `os.CreateTemp()` + `os.Write()` + `os.Sync()` + `os.Chmod()` + `os.Rename()` | Writes config via atomic rename: creates temp file, writes content, syncs to disk, closes file descriptor, sets permissions (`fileMode`), then atomically renames temp over the target path. This is a standard write-through pattern. |\n\n### Library API Calls (Non-Standard)\n\n| Function | Dependency | Description |\n|---|---|---|\n| `LoadConfig` | `gopkg.in/yaml.v3` (`yaml.Unmarshal`) | Parses YAML config from byte slice into struct. Wraps parse errors with a custom prefix string. |\n| `SaveConfig` | `gopkg.in/yaml.v3` (`yaml.Marshal`) | Serializes struct back to YAML bytes before writing to disk. |\n\n### Error Propagation\n\n- **`LoadConfig`**: Returns `(nil, err)` on any failure — either from file read or YAML parse. The parsed error is wrapped via `fmt.Errorf(\"failed to parse yaml config: %w\", err)`.\n- **`SaveConfig`**: Returns `error` after each step fails. Errors are wrapped similarly with descriptive prefixes (`\"failed to create temp file:\"`, `\"failed to write config to temp file:\"`, etc.). The final operation is `os.Rename()`; if that fails, it returns the raw error without wrapping.\n- **`ConfigExists`**: Swallows all errors — returns `false` whether the file doesn't exist, the path is inaccessible due to permissions, or any other OS-level failure occurs. This is a silent swallow with no way for the caller to distinguish causes.\n\n### Potential Panic Points (Unhandled)\n\nThe deferred function in `SaveConfig`:\n```go\ndefer func() {\n    tmpFile.Close()\n    os.Remove(tmpName)\n}()\n```\nIf any error path triggers a panic during execution, or if the deferred cleanup itself panics, it is **not** recovered. No `recover()` wrapping exists anywhere in this file.\n\n### No External Side Effects (Confirmed Absent)\n\n- No network calls, HTTP clients, database drivers, or external API interactions.\n- All I/O targets are local filesystem paths (`cwd` + `.code-reducer.yaml`)."
    },
    "internal/config/resolve.go": {
      "sha256": "f560e11dfcac8b454621ecd0a57970fe788ed1b469a78bcdc99073f0d135c7a6",
      "facts": "#### [API_SIGNATURES]\n* `ResolveConfig(repoRoot string, modelIDFlag string, numCtxFlag string) (*Config, error)`\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code manages hierarchical configuration resolution, merging settings from multiple sources (CLI flags, environment variables, configuration files, and system defaults) into a single, validated configuration object.\n\n**High-Level Algorithm Steps**\n1.  **Load Base Configuration:** Attempt to load the configuration file from the specified repository root.\n2.  **Resolve Numerical Context:** Determine the context size by evaluating settings in this priority order: CLI flag \u003e Environment variable (with validation) \u003e Configuration file value (if $\u003e 0$) \u003e System default.\n3.  **Resolve Model Identity:** Determine the model ID by evaluating settings in this priority order: CLI flag \u003e Environment variable \u003e Configuration file value \u003e System default.\n4.  **Resolve Service Endpoints:** Determine the base URL by evaluating settings in this priority order: Environment variable \u003e Configuration file value \u003e System default.\n5.  **Resolve Text Settings:** Select values for prompts and directory paths by preferring non-empty values from the configuration file over system defaults.\n6.  **Finalize Lists and Defaults:** \n    *   Deduplicate the list of ignored items.\n    *   Use predefined default extraction steps if the configuration file provides an empty list.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O\n* **Disk**: `loadBaseConfig` calls `LoadConfig` using `repoRoot` to attempt to read a configuration file.\n* **Environment Variables**: \n    * `CodeReducerModelIDEnvKey` is read via `os.Getenv`.\n    * `OllamaBaseURLEnvKey` is read via `os.Getenv`.\n    * `OllamaNumCtxEnvKey` is read via `os.Getenv`.\n\n### Error Propagation\n* **`loadBaseConfig`**:\n    * If `LoadConfig` returns an error that is not `os.ErrNotExist`, the error is wrapped and returned.\n    * If `LoadConfig` returns `os.ErrNotExist`, the error is swallowed and an empty `Config` struct is returned.\n* **`resolveNumCtx`**:\n    * If the environment variable `OllamaNumCtxEnvKey` is non-empty but fails `strconv.Atoi` or the resulting integer is $\\le 0$, an error is returned.\n    * If `flagVal` is non-empty but fails `strconv.Atoi` or the resulting integer is $\\le 0$, an error is returned.\n* **`ResolveConfig`**:\n    * Returns errors propagated from `loadBaseConfig` or `resolveNumCtx`."
    },
    "internal/engine/cache.go": {
      "sha256": "3caca4ede2968bdc9e9c51c3cae28229856b1f08e4c2679c5957f5fdd146293d",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — cache.go (engine package)\n\n## Types\n\n| Element | Description | Parameters | Return Type |\n|---|---|---|---|\n| `FileCacheEntry` | Struct for file-level cache entries | — | — |\n| `MetadataCache` | Struct for metadata cache state | — | — |\n\n## Functions\n\n| Element | Description | Parameters | Return Type |\n|---|---|---|---|\n| `loadMetadataCache` | Loads or creates the metadata cache from disk | `repoRoot string`, `docsDir string` | `(*MetadataCache, error)` |\n| `IsInitialized` | Checks if the metadata cache file exists | `repoRoot string`, `docsDir string` | `bool` |\n\n---\n\n**Note:** Private functions (`computeStepsHash`, `newEmptyCache`, `saveMetadataCache`, `computeSHA256`) and private fields/methods are intentionally excluded from this list per Go naming conventions.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code implements a **metadata cache for tracking extracted documentation facts** from source files in a repository. The engine persists per-file extraction results (\"facts\") along with file integrity hashes, enabling change detection and incremental reprocessing.\n\n---\n\n## High-Level Algorithm Steps\n\n1. **Initialize an empty cache structure** — creates versioned metadata container with maps ready to grow (files, modules).\n\n2. **Load existing cache from disk** — reads the serialized metadata file; if missing, returns a fresh empty cache. Handles incompatible cache versions by returning a clean slate rather than failing.\n\n3. **Compute per-file SHA256 hash** — hashes raw file content at its virtual path to produce an integrity fingerprint for change detection.\n\n4. **Persist cache state back to disk** — serializes the current cache (versioned, indented) and writes it atomically to a known metadata path.\n\n5. **Track extraction step history** — marshals the list of extraction steps into JSON and hashes them; this hash is stored in the top-level cache as `steps_hash`, enabling detection when the processing pipeline itself changed between runs.\n\n---\n\n## Domain Rules\n\n- Cache versioning: only cache versions matching `currentCacheVersion` (1) are accepted; mismatches yield a clean cache.\n- Graceful degradation: missing files and read failures return an empty cache instead of errors.\n- Nil safety: maps initialized to nil on load are replaced with fresh empty maps before use.\n- File hash is computed from raw content at the repository root using virtual paths (not relative).\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n- `MetadataCache.Version` — mutated in `saveMetadataCache()` via `cache.Version = currentCacheVersion`. This is a pointer receiver parameter; the caller's reference to the same struct value could be affected if they hold the returned pointer from `loadMetadataCache()`.\n\n- `MetadataCache.Files` and `MetadataCache.Modules` — mutated in `loadMetadataCache()` when either map is nil (reassigned to an empty initialized map). This mutates the local variable; the caller receives a new pointer value via return, so this is not shared mutable state from another goroutine's perspective unless the caller also holds a reference.\n\n- `currentCacheVersion` — package-level constant (`const`). Immutable by design, never modified in this file.\n\n**Synchronization Mechanisms:**\n\nNone detected. No locks (sync.Mutex, sync.RWMutex), no atomic types (`sync/atomic`), no async/await patterns, and no channel-based coordination visible in this file. All functions are synchronous with respect to shared data access.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\nAll I/O operations funnel through the `tools.ReadFileSafely` and `tools.WriteFileSafely` helpers, with no network or database calls visible in this file.\n\n**Disk reads:**\n- **`loadMetadataCache`**: Reads a metadata cache JSON file from `\u003cdocsDir\u003e/\u003cmetadataFileName\u003e` relative to `repoRoot`. Returns an empty cache if the file does not exist (`os.ErrNotExist`). Other read errors are wrapped as \"failed to read metadata cache\" and returned.\n- **`IsInitialized`**: Calls `tools.ReadFileSafely(repoRoot, metadataPath)` to check existence of the metadata file. Returns `true` only when no error occurs (file exists). No explicit error return; uses boolean pattern.\n- **`computeSHA256`**: Reads a target file at `virtualPath` relative to `repoRoot`. If read fails, propagates the error as the second return value. Otherwise computes SHA256 of the content and returns hex string + nil error.\n\n**Disk writes:**\n- **`saveMetadataCache`**: Marshals cache struct to indented JSON, then calls `tools.WriteFileSafely(repoRoot, metadataPath, data)`. Returns marshaling errors directly; write errors propagate through `WriteFileSafely`.\n\n**No external side effects detected beyond disk I/O.**\n\n## Error Propagation\n\n| Function | Failure Mode | Handling Strategy |\n|---|---|---|\n| `computeStepsHash` | JSON marshal failure | Swallowed; returns `\"\"` silently. No error returned to caller. |\n| `loadMetadataCache` | File read failure (other than `os.ErrNotExist`) | Wrapped with \"failed to read metadata cache: %w\" and returned as error. |\n| `loadMetadataCache` | JSON unmarshal failure | Wrapped with \"failed to unmarshal metadata cache: %w\" and returned as error. |\n| `loadMetadataCache` | Version mismatch (`cache.Version != currentCacheVersion`) | Silently recovered; returns empty cache + nil error (treated as fresh start). |\n| `saveMetadataCache` | JSON marshal failure | Returned directly to caller. |\n| `saveMetadataCache` | File write failure | Propagated through `WriteFileSafely` to caller. No recovery logic here. |\n| `computeSHA256` | File read failure | Propagated as the second return value (error type). Caller handles via standard error check. |\n| `IsInitialized` | Any file issue | Returns `false`; no explicit error returned since function signature is bool only. |\n\n**Patterns observed:**\n- **Explicit recovery**: Only in `loadMetadataCache` for version mismatch and missing cache. Other errors propagate up to caller.\n- **Silent swallowing**: `computeStepsHash` discards marshal errors entirely; no indication of failure downstream."
    },
    "internal/engine/chunking.go": {
      "sha256": "5170da5122537172eb0f79f6b32523fc212ca7fc02cff826b2faea34504e12de",
      "facts": "#### [API_SIGNATURES]\nNo public elements were found in the provided file.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code addresses the limitation of Large Language Model (LLM) context windows when processing large volumes of text. It provides a mechanism to iteratively condense, synthesize, or deduplicate extensive datasets (such as architecture descriptions or file facts) by recursively applying reduction operations until the information fits within the model's capacity.\n\n**High-Level Algorithm Steps**\n1.  **Expansion**: Split individual items that exceed the maximum character limit into smaller, overlapping segments.\n2.  **Batching**: Group the items into batches such that the combined length of items in a single batch does not exceed the maximum character limit.\n3.  **Recursive Reduction**:\n    *   If the items fit into a single batch, perform the LLM reduction operation (e.g., synthesis or consolidation) directly.\n    *   If multiple batches exist, recursively apply the reduction process to each batch to generate intermediate results.\n4.  **Consolidation**: Combine the results from all reduced batches into a single set of text.\n5.  **Convergence Check**: Compare the size of the consolidated intermediate results against the size of the input. If the reduction failed to significantly decrease the size (defined as being $\\ge$ 95% of the original size), stop the recursion and return the concatenated text to prevent infinite loops.\n6.  **Iteration**: If the reduction was successful (size significantly decreased), treat the consolidated results as the new input and repeat the process from Step 3.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O\n* **LLM API**: The `llmCaller.CallLLM` method is invoked to perform text synthesis and consolidation via an external service.\n* **Logging/Event Reporting**: The `LogEventFunc` is called to report status updates and progress during the reduction process.\n\n### Error Propagation\n* **Context Cancellation**: Errors from `ctx.Err()` are checked at the beginning of `reduceItems` and during iteration, propagating upward if the context is canceled or reaches its deadline.\n* **LLM Errors**: Errors returned by `llmCaller.CallLLM` are wrapped with a configuration-defined error message (`cfg.errMsg`) and propagated through the call stack.\n* **Validation Errors**: The `chunkTextWithOverlap` function returns errors if `maxRunes` is non-positive or if `overlapRunes` is greater than or equal to `maxRunes`.\n* **Recursive Propagation**: Errors encountered within the recursive `reduceItems` calls (including errors from `expandOversizedItems` and `reduceFn`) are returned through the stack to the original caller."
    },
    "internal/engine/chunking_test.go": {
      "sha256": "514b3558a78e579ed8549c97f00484f57ad106befdd3ff98516bd6a833ac7c12",
      "facts": "#### [API_SIGNATURES]\nNo public API elements found in the provided source.\n\n#### [BUSINESS_LOGIC]\n**Core Purpose**\nThe domain problem involves partitioning text and sequences of items into bounded segments, specifically handling content continuity via overlapping segments and decomposing elements that exceed size constraints.\n\n**High-level Algorithm Steps**\n*   Partition text into segments of a maximum length with a specified overlap between consecutive segments.\n*   Decompose items that exceed a size threshold into smaller components.\n*   Group items into batches subject to a size limit.\n*   Aggregate items within a batch using a provided reduction function.\n*   Calculate a size limit based on an input value that ensures a positive minimum.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O\nNo external side effects.\n\n### Error Propagation\n* **`chunkTextWithOverlap`**: Returns an error as a second return value if `maxRunes \u003c= 0` or if `overlapRunes \u003e= maxRunes`.\n* **`reduceItems`**: Returns an error as a second return value.\n* **`expandOversizedItems`**: Returns an error as a second return value."
    },
    "internal/engine/client.go": {
      "sha256": "eae96ebc417be9d8e74557c74763ef4bdd5e7449a082a8952c6eb39f60700309",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/engine/client.go`\n\n## Types (Exported)\n\n### `Message`\n\n```go\ntype Message struct {\n    Role    string   // json:\"role\"\n    Content string   // json:\"content\"\n}\n```\n\n---\n\n## Unexported Elements (Internal Use Only)\n\n| Element | Type | Notes |\n|---------|------|-------|\n| `llmCaller` | interface | Has methods: `CallLLM`, `NumCtx` |\n| `llmClient` | struct | Embeds `*http.Client`; has method `NumCtx()` |\n| `newLLMClient(modelID, baseURL string, numCtx int)` | function | Returns `*llmClient` |\n| `ollamaRequest` | struct | Fields: `Model`, `Messages`, `Stream`, `Format`, `Options` |\n| `ollamaOptions` | struct | Field: `NumCtx` |\n| `ollamaResponse` | struct | Field: `Message` (type `Message`) |\n| `prepareOllamaRequest(ctx context.Context, systemPrompt string, messages []Message, jsonFormat bool)` | method on `*llmClient` | Returns `(*http.Request, error)` |\n| `CallLLM(ctx context.Context, systemPrompt string, messages []Message, jsonFormat bool)` | method on `*llmClient` | Returns `(string, error)` |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nThis code implements a client for invoking a large language model (LLM) through a remote HTTP endpoint, specifically targeting Ollama's `api/chat` protocol. It abstracts LLM interaction behind an interface (`llmCaller`) so the engine layer can depend on it without tying to a specific transport.\n\n**High-Level Algorithm Steps:**\n\n1. **Construct request payload** — Build an LLM call by prepending any system prompt to the user-provided message list, then serialize into Ollama's expected JSON schema (model ID, messages, stream flag, optional format/options).\n\n2. **Execute synchronous HTTP POST** — Send the serialized body to `baseURL/api/chat` using a context-aware client with a fixed timeout; no retries are attempted.\n\n3. **Parse response** — On 200 OK, deserialize the JSON into an Ollama-style envelope and return only the model's reply content as the result string.\n\n4. **Report non-OK status** — For any other HTTP status code, read a bounded slice of the error body and surface it as a formatted \"ollama api error\" with the status and response text.\n\n#### [STATE_AND_CONCURRENCY]\n**No mutable state**.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**Network (HTTP → Ollama API)** — This module is a direct HTTP client targeting an external LLM provider. The call path is `POST {baseURL}/api/chat` with a JSON body constructed from the model ID, system prompt, and user messages. The response is read back as JSON and parsed into an `ollamaResponse`.\n\n**Timeout behavior** — The `http.Client` is initialized with `defaultHTTPTimeout`, referenced but not defined in this file. If that constant lives elsewhere or is missing, it will fail to compile; if present, requests exceeding the threshold are aborted by Go's `http.Client` transport layer.\n\n## Error Propagation\n\n| Failure point | Behavior |\n|---|---|\n| Request construction (`prepareOllamaRequest`) | Marshaling errors are wrapped with `\"failed to marshal request: %w\"`. New-request errors propagate as-is. Returned as `(string, error)` — caller decides. |\n| HTTP transport failure (`CallLLM` → `c.httpClient.Do(req)`) | Transport-level error (network down, DNS fail, timeout, etc.) is returned directly. No retry logic exists; the call fails fast per the inline comment. |\n| Successful status + parse error | Response body is fully read into memory, then unmarshaled. If JSON doesn't match `ollamaResponse`, a wrapped error `\"failed to parse response: %w\"` is returned. |\n| Non-200 status code | The body is read up to `maxErrorBodyBytes` (referenced but not defined in this file) and the content is embedded in an error string like `\"ollama api error: status {code}, response: {text}\"`. The actual `io.ReadAll` error from that branch is discarded (`body, _ := io.ReadAll(...)`). |\n\n## Notable Observations\n\n- **Error swallowed**: In the non-200 branch, if reading the response body fails (e.g., connection reset mid-stream), only the status code is reported; the underlying read failure is lost.\n- **`maxErrorBodyBytes` undefined in this file** — If this constant does not live elsewhere and is expected to be `int`, compilation will fail. The current branch reads an unbounded stream if the constant is missing, which could leak memory on large error responses from a hostile server.\n- **No retry / no circuit breaker** — Failures are returned immediately. Transient network errors are not retried; callers must handle their own backoff or resilience logic upstream."
    },
    "internal/engine/constants.go": {
      "sha256": "85ec25e43f84ebf8a1ce483e8efd9df8eb8fcf20fa2e8bbf3e243022a0730ae7",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface of `internal/engine/constants.go`\n\n## Result: No public elements found.\n\n- All 9 named constants (`defaultHTTPTimeout`, `maxErrorBodyBytes`, `defaultChunkOverlap`, `minNumCtxFloor`, `contextWindowAllocRatio`, `maxCharsMultiplier`, `metadataFileName`, `agentsFileName`, `defaultDirPerm`) are **unexported** (lowercase first letter).\n- No exported functions, methods, or types are defined in this file.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis file defines operational configuration constants for an AI inference/agent engine runtime. It establishes fixed parameters that govern timeout behavior, error handling limits, context window management, and filesystem conventions used during agent execution.\n\n## High-Level Algorithm Steps\n\n1. **Set HTTP communication timeouts** — `defaultHTTPTimeout` (10 minutes) controls the maximum duration for remote API calls before they are aborted.\n\n2. **Cap error response payloads** — `maxErrorBodyBytes` (1 KB) limits how much error body data can be captured from failed requests.\n\n3. **Configure chunk overlap** — `defaultChunkOverlap` (800 tokens) defines the overlap between consecutive text chunks during streaming or processing.\n\n4. **Define context window floor** — `minNumCtxFloor` (512) sets a minimum threshold for context length calculations.\n\n5. **Allocate context windows proportionally** — `contextWindowAllocRatio` (0.75) reserves 75% of available space for primary content, leaving the remainder for metadata and overhead.\n\n6. **Scale character limits** — `maxCharsMultiplier` (3x) multiplies a base character limit to determine maximum allowed output length.\n\n7. **Establish filesystem conventions** — `metadataFileName`, `agentsFileName`, and `defaultDirPerm` define how state is persisted on disk during agent runs.\n\n## Domain Rules\n\n- All timeouts are absolute durations; no retry or backoff logic is implied by this file alone.\n- Error bodies are truncated at a fixed byte ceiling regardless of content type.\n- Chunk processing uses a fixed overlap value with no dynamic adjustment mentioned here.\n- Context window allocation follows a static 75/25 split ratio.\n- The `LogEventFunc` type is declared as an event callback signature but no events or logging system are implemented in this file.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects in this file. This file contains only constant declarations and a single function-type alias (`LogEventFunc`). It does not perform any disk, network, database, or API operations itself.\n\n**Error Propagation:** No error propagation mechanisms present in this file. There are no error types defined, no exception handling, and no error return values."
    },
    "internal/engine/markdown.go": {
      "sha256": "81bea4827161eb8978cd6f288e2276c2616da7832e8687400b6872d8d9c3749c",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface of `internal/engine/markdown.go`\n\nThis file contains **no exported (public) symbols**. All identifiers (`stripOuterMarkdownFence`, `markdownFenceRe`) begin with lowercase letters, which makes them unexported in Go.\n\n---\n\n### Internal Elements (for reference only — not part of the public API)\n\n- `var markdownFenceRe regexp.Regexp` — compiled regex for matching fenced code blocks\n- `func stripOuterMarkdownFence(content string) string` — strips surrounding markdown/json fences from input strings\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\n**Stripping surrounding code fence markers from markdown or JSON content.** This function cleans input that wraps code blocks in triple-backtick fences (with optional `markdown` or `json` language tags), exposing the raw interior content without the wrapping syntax.\n\n## High-Level Algorithm Steps\n\n1. **Trim whitespace** — Remove leading/trailing spaces and newlines from the input string to normalize boundaries before fence detection.\n2. **Match fenced pattern** — Check whether the trimmed string begins with three or more backticks (optionally followed by `markdown`/`json`) and ends with a matching set of backticks, capturing everything between them.\n3. **Extract inner content** — If the fence pattern matches, return the captured interior text after trimming whitespace around it.\n4. **Fallback pass-through** — If no fenced structure is detected, return the original input unchanged.\n\n#### [STATE_AND_CONCURRENCY]\n**Global/Package-Level Variables:**\n- `markdownFenceRe` — initialized once at package scope; never reassigned or modified anywhere in this file. Effectively read-only after compilation.\n\n**Synchronization Mechanisms:** None present (no mutexes, channels, async/await, or atomic types).\n\n**Conclusion:** No mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\nExternal side effects: None. The function performs no I/O operations (no file, network, database, or API interactions). It relies solely on standard library packages (`regexp`, `strings`).\n\nError propagation: No error return values are used. If the regex fails to match the input, the function returns the trimmed input string unchanged rather than signaling an error or panicking."
    },
    "internal/engine/orchestrator.go": {
      "sha256": "25b084a5d219794d3fe74159248a9584d50b319ead6ec6f7f152340f536e3dbb",
      "facts": "#### [API_SIGNATURES]\n* `EventType` (type)\n* `EventStatus` (constant)\n* `EventError` (constant)\n* `Event` (type)\n    * `Type` (EventType)\n    * `Message` (string)\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code orchestrates an automated pipeline for generating and maintaining hierarchical documentation for code repositories using Large Language Models (LLMs). It employs a Map-Reduce approach to transform source code into structured documentation, including architecture overviews, quickstart guides, and module-level summaries. The system manages incremental updates by tracking file changes through metadata caching and SHA256 hashing to avoid full regenerations when only specific subdirectories are modified.\n\n**High-Level Algorithm Steps**\n\n**Initialization (RunInit)**\n1.  **Setup:** Load ignore patterns and the existing metadata cache.\n2.  **Discovery:** Identify code files and compute their SHA256 hashes.\n3.  **Tree Construction:** Build a hierarchical directory tree from the discovered files.\n4.  **Hierarchical Synthesis:** Execute a Map-Reduce process to synthesize summaries across the tree.\n5.  **Global Documentation:** Use an LLM to generate architecture and quickstart documentation based on the synthesized root summary.\n6.  **Guidance Update:** Append AI agent guidelines to the repository to facilitate agent-based codebase navigation.\n7.  **Teardown:** Persist the updated metadata cache.\n\n**Incremental Update (RunUpdate)**\n1.  **Setup \u0026 Validation:** Initialize environment and verify if the metadata cache is invalidated by changes in the extraction configuration.\n2.  **Change Detection:** Identify added, modified, or deleted files by comparing current hashes against the cache.\n3.  **Cache Pruning:** Remove entries from the cache and the filesystem for directories no longer present in the current code tree.\n4.  **Impact Analysis:** Determine which directories are affected by the detected changes and propagate these changes through the tree.\n5.  **Selective Synthesis:** Perform hierarchical synthesis specifically for the affected directory branches.\n6.  **Conditional Global Update:** Regenerate the root-level architecture and quickstart files only if the root directory is affected or if the files are missing.\n7.  **Teardown:** Persist the updated metadata cache.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State**\n* `cache (*MetadataCache)`: `StepsHash`, `Files`, `Modules`\n\n**Synchronization Mechanisms**\n* None\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O\n* **LLM API**: Communicates with an LLM via `client.CallLLM` to receive text content for architecture and quickstart documentation.\n* **Disk/File System**:\n    * **Read**: Loads `.gitignore` files via `tools.LoadGitignore`, the metadata cache via `loadMetadataCache`, agent guidelines via `tools.ReadFileSafely`, and code files via `computeSHA256`. It also performs existence checks via `os.Stat`.\n    * **Write**: Generates documentation files (`architecture.md`, `quickstart.md`, module files, and agent guidelines) via `tools.WriteFileSafely`, creates the `modules` directory via `os.MkdirAll`, and saves the metadata cache via `saveMetadataCache`.\n    * **Delete**: Removes stale module files via `os.Remove`.\n\n### Error Propagation\n* **Returned (Wrapped)**:\n    * Errors from `client.CallLLM`.\n    * Errors from `tools.WriteFileSafely`.\n    * Errors from `tools.LoadGitignore` (except where the error is `os.IsNotExist`).\n    * Errors from `loadMetadataCache`.\n    * Errors from `os.MkdirAll`.\n    * Errors from `tools.DiscoverCodeFiles`.\n    * Errors from `synthesizeNode`.\n    * Errors from `tools.ReadFileSafely` (if the subsequent attempt to write the file fails).\n* **Swallowed**:\n    * `os.IsNotExist` errors when loading `.gitignore`.\n    * Errors from `computeSHA256` (logged via `logEvent` and execution continues).\n    * Errors from `saveMetadataCache` (logged via `logEvent`).\n    * Errors from `os.Remove`."
    },
    "internal/engine/orchestrator_test.go": {
      "sha256": "dd30631b0853b00de0afec5a317e1d9b84c80471496c791a572500480a7e6cad",
      "facts": "#### [API_SIGNATURES]\n* `DirNode` (struct)\n    * `Path`\n    * `Children` (map[string]*`DirNode`)\n* `EventType` (type)\n* `Event` (type)\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code implements an orchestration engine designed to manage and update agent-specific guidelines or context based on file system structures and file content. It facilitates tracking changes in a directory tree and preparing documentation/guidelines for use by an LLM agent.\n\n**High-level Algorithm Steps**\n1.  **Tree Traversal:** Iterate through a hierarchical directory structure to identify and mark all nodes as affected.\n2.  **Content Hashing:** Scan a directory for specific files and compute unique identifiers (hashes) for existing files while ignoring missing ones.\n3.  **Guideline Management:** Update or create agent guideline files in a specified directory, ensuring the operation is idempotent.\n4.  **Orchestration Initialization:** Initialize the engine's lifecycle using a provided configuration, a target repository path, and an execution context.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O**\n* **Disk**:\n    * `t.TempDir()` is used to create temporary directories.\n    * `os.WriteFile` is used to create `file1.txt` and `main.go`.\n    * `updateAgentGuidelines` performs file creation and updates.\n    * `os.ReadFile` is used to read file content.\n\n**Error Propagation**\n* **Error Return Codes**:\n    * Errors returned by `os.WriteFile`, `updateAgentGuidelines`, `os.ReadFile`, and `o.RunInit` are intercepted by tests and result in `t.Fatalf` calls."
    },
    "internal/engine/runner.go": {
      "sha256": "78f573864a5dfacabfba38c9570672beaf629579334d25024f9662db3a2082ba",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `runner.go` (Module: `internal/engine`)\n\n## Types\n\n### `Mode`\n- **Kind:** Type alias (`string`)\n\n---\n\n## Constants\n\n### `ModeInit`\n- **Kind:** Constant of type `Mode`\n- **Value:** `\"init\"`\n\n### `ModeUpdate`\n- **Kind:** Constant of type `Mode`\n- **Value:** `\"update\"`\n\n---\n\n## Structs\n\n### `Runner`\n| Field | Type | Visibility |\n|-------|------|------------|\n| `cfg` | `*config.Config` | Private |\n\n---\n\n## Functions\n\n### `NewRunner(cfg *config.Config) *Runner`\n- **Kind:** Constructor / Factory function\n- **Parameters:**\n  - `cfg *config.Config` — configuration instance\n- **Returns:**\n  - `*Runner`\n\n---\n\n## Methods on `Runner`\n\n### `Run(ctx context.Context, repoRoot string, mode Mode, onEvent func(Event)) error`\n- **Kind:** Operation method\n- **Parameters:**\n  - `ctx context.Context` — cancellation/context scope\n  - `repoRoot string` — root directory of the repository\n  - `mode Mode` — pipeline operation mode (`ModeInit` or `ModeUpdate`)\n  - `onEvent func(Event)` — callback invoked for each event\n- **Returns:**\n  - `error`\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**: This code manages and orchestrates an automated documentation generation pipeline for software projects, supporting both initial documentation creation (init mode) and incremental updates to existing documentation (update mode). It serves as the main entry point that coordinates AI-powered document processing across a repository.\n\n**High-Level Algorithm Steps**:\n1. Ensure project lockfile is added to `.gitignore` for version control safety\n2. Acquire an exclusive repository lock to prevent concurrent documentation operations  \n3. Initialize LLM client and orchestrator based on provided configuration (model, base URL, context settings)\n4. Execute the appropriate pipeline mode: either run full initialization or incremental update using the configured AI model\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State Analysis for `runner.go`\n\n### Global Variables\n**None.** All state is scoped to struct instances or function parameters.\n\n### Shared States / Modified Properties\n- **`Runner.cfg *config.Config`**: Assigned once in `NewRunner()` and never modified after that. Not mutable post-initialization within this file.\n- No other struct fields are written to; no shared state is mutated by this code.\n\n### Synchronization Mechanisms\n- **Lock acquired via `security.AcquireLock(repoRoot)`** — a repository-level lock is acquired, then released via `defer lock.Unlock()`. This protects repository state during pipeline execution. The exact lock type (file-based, in-memory, etc.) and its thread-safety guarantees depend on the implementation in `internal/security`, which is outside this file's scope.\n\n### Summary\nThis code has **no mutable shared state** within its own boundary. It does introduce a synchronization primitive (`security.AcquireLock`) that wraps the execution of the documentation pipeline to guard against concurrent access to repository resources, but no internal fields are modified after initialization.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Filesystem Operations (Disk)\n\n| Operation | Location in Code | Purpose |\n|-----------|------------------|---------|\n| `tools.EnsureGitignoreHasLockfile(repoRoot)` | Line 53 | Writes/updates `.gitignore` to include the lockfile path. This is a **write** operation targeting disk. |\n| `security.AcquireLock(repoRoot)` → `lock.Unlock()` | Lines 60–65 | Acquires and releases a file-based lock (likely via `os.Flock` or similar). Involves both **read** (checking existing locks) and **write** (creating/removing the lockfile). |\n| Orchestrator pipeline (`orch.RunInit` / `orch.RunUpdate`) | Lines 71–84 | The orchestrator likely writes documentation files to disk during its execution. Specific I/O details are in other modules not provided here, but the runner delegates this entirely. |\n\n### Network Operations (API)\n\n| Operation | Location in Code | Purpose |\n|-----------|------------------|---------|\n| `newLLMClient(r.cfg.ModelID, r.cfg.OllamaBaseURL, r.cfg.OllamaNumCtx)` | Line 68 | Instantiates an LLM client configured with a model ID and Ollama base URL. The actual network I/O occurs downstream within the orchestrator's pipeline calls (`RunInit`/`RunUpdate`), not in this file directly. |\n\n### No External Side Effects\n\n- This file itself does **not** perform network calls, database operations, or API requests directly.\n- All external interactions are delegated to other modules: `config`, `security`, `tools`, and the orchestrator (which wraps an LLM client).\n\n---\n\n## Error Propagation Analysis\n\n### Returned Errors (Explicit)\n\n| Path | Behavior |\n|------|----------|\n| `EnsureGitignoreHasLockfile` fails → logs warning as event, **no return** | Swallowed; caller does not see the error. |\n| `AcquireLock` fails → wraps with `fmt.Errorf(\"failed to acquire repository lock: %w\", err)` and **returns immediately**. | Caller receives wrapped error. |\n| Orchestrator pipeline (`RunInit`/`RunUpdate`) returns non-nil → returned as-is to caller. | Propagated directly. |\n| Unsupported mode detected → wraps with `fmt.Errorf(\"unsupported mode: %s\", mode)`. | Caller receives descriptive error. |\n\n### Swallowed Errors (Silent)\n\n- **Gitignore warning** at line 53 is logged via the event callback but never returned or panicked. The runner continues execution regardless of this failure.\n- Any errors from `lock.Unlock()` are not checked (deferred), meaning lock cleanup errors are silently ignored."
    },
    "internal/engine/synthesize.go": {
      "sha256": "addf50cde81573976ea40816fabc5e2518d4e7db0787a017b53c9df4812963e9",
      "facts": "#### [API_SIGNATURES]\nNo exported elements were found in the provided source code.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code implements a hierarchical documentation synthesis engine. It automates the generation of high-level technical summaries for a codebase by recursively traversing a directory tree and using a Large Language Model (LLM) to interpret file contents and consolidate them into structured, multi-level documentation (files $\\rightarrow$ subsystems $\\rightarrow$ modules).\n\n**High-Level Algorithm Steps**\n1.  **Recursive Traversal (Bottom-Up):** Traverse the directory tree, processing child nodes before parent nodes to enable hierarchical summarization.\n2.  **Cache \u0026 Change Detection:** Determine if a directory or file requires re-processing by checking if the path is marked as \"affected\" or if the file's SHA256 hash has changed.\n3.  **File Fact Extraction:**\n    *   Split large files into overlapping chunks based on the LLM's context window.\n    *   Invoke the LLM for each configured extraction step (e.g., logic, types, etc.) across all chunks.\n    *   Consolidate chunked LLM responses into a single set of structured facts for the file.\n4.  **Component Aggregation:** Collect the summarized facts from all files within a directory and the synthesized summaries from all immediate subdirectories.\n5.  **Hierarchical Reduction:** Pass the aggregated list of file and subsystem summaries to the LLM to synthesize a single, cohesive summary for the current directory.\n6.  **Persistence:** Write the synthesized summary to a Markdown file in the designated documentation directory and update the local cache.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State**\n* `p.cache.Files` (map within `MetadataCache`)\n* `p.cache.Modules` (map within `MetadataCache`)\n\n**Synchronization Mechanisms**\n* None\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O\n* **Disk**:\n    * `tools.ReadFileSafely` is used to read file content from `p.repoRoot`.\n    * `tools.WriteFileSafely` is used to write module documentation to a path constructed from `p.cfg.DocsDir` and `node.Path`.\n* **API**:\n    * `p.client.CallLLM` is used to interact with an LLM.\n\n### Error Propagation\n* **Swallowed Errors**:\n    * In `extractFileFacts`, errors returned by `tools.ReadFileSafely` are logged via `p.logEvent` and the function returns `nil` for the error.\n* **Propagated Errors**:\n    * In `extractFileFacts`, errors from `chunkTextWithOverlap`, `p.client.CallLLM`, and `reduceFileFacts` are returned to the caller.\n    * In `collectComponents`, errors from `extractFileFacts` are returned to the caller.\n    * In `synthesizeNode`, errors from `p.ctx.Err()`, `synthesizeChildren`, `collectComponents`, `reduceInChunks`, and `tools.WriteFileSafely` are returned to the caller."
    },
    "internal/engine/synthesize_test.go": {
      "sha256": "d02ccd059de42bd048a4705e8f9517c422734aee699133be321c601e78835d1e",
      "facts": "#### [API_SIGNATURES]\n* `TestSynthesizeNode(t *testing.T)`\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nGenerating hierarchical summaries of directory structures using Large Language Models (LLMs), utilizing caching and change detection to optimize performance during incremental updates.\n\n**High-level Algorithm Steps**\n1. Evaluate if a node is empty.\n2. Determine if the node has a valid cache entry and is not within the set of affected directories.\n3. Retrieve the cached summary if a valid, unaffected entry exists.\n4. Synthesize a new summary for nodes containing files and/or child nodes.\n\n#### [STATE_AND_CONCURRENCY]\nMutable state: `cache.Modules`\nSynchronization mechanisms: None\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O**\n* **Disk**: \n    * `t.TempDir()` creates a temporary directory.\n    * `os.MkdirAll` creates a directory path (`modulesDir`) on the filesystem.\n    * `os.WriteFile` writes a sample file (`main.go`) to the filesystem.\n* **Network/API**: No actual network or API calls occur; `mockLLMCaller.CallLLM` returns a hardcoded string and a `nil` error.\n\n**Error Propagation**\n* **Filesystem Errors**: Errors from `os.MkdirAll` and `os.WriteFile` result in a call to `t.Fatalf`, which terminates the test execution.\n* **Function Return Values**: `synthesizeNode` returns an `error` as its second return value, which is checked by the caller using `t.Fatalf` if non-nil."
    },
    "internal/engine/tree.go": {
      "sha256": "8c1f570071d9444c7a768beb8d5afc19b827310cc889c30024cc1d6594b485cf",
      "facts": "#### [API_SIGNATURES]\n* `ChangeStatus` (type, `string`)\n* `StatusAdded` (constant, `ChangeStatus`)\n* `StatusModified` (constant, `ChangeStatus`)\n* `StatusDeleted` (constant, `ChangeStatus`)\n* `FileChange` (type)\n    * `Path` (string)\n    * `Status` (ChangeStatus)\n* `DirNode` (type)\n    * `Path` (string)\n    * `Files` ([]string)\n    * `Children` (map[string]*DirNode)\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code identifies which directories in a file tree are \"affected\" by file system changes. This is used to determine which module-related documentation or metadata needs to be recalculated or updated based on file modifications, deletions, or missing documentation files.\n\n**High-level Algorithm Steps**\n1. **Tree Construction**: Convert a list of file paths into a hierarchical tree structure of directory nodes.\n2. **Change Mapping**: Identify specific changed file paths and mark the parent directories of any deleted files as affected.\n3. **Node Impact Detection**: Traverse the tree to identify nodes that are affected due to:\n    * A missing entry in a metadata cache.\n    * The presence of a changed file within the node.\n    * The absence of a corresponding documentation file on the filesystem.\n4. **Upward Propagation**: Recursively propagate the \"affected\" status from child nodes up to their ancestor directories.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O**\n* **Disk:** The `isNodeAffected` function performs a file system check using `os.Stat` to determine if a specific file path exists on disk.\n\n**Error Propagation**\n* **`security.SafeResolve`:** Errors returned by this function are caught; if an error is non-nil, the function skips the subsequent disk check and returns `false`.\n* **`os.Stat`:** \n    * If `os.IsNotExist(err)` evaluates to `true`, the function returns `true`.\n    * If `os.Stat` returns a non-nil error that is not a \"not exist\" error, the error is swallowed and the function returns `false`."
    },
    "internal/engine/tree_test.go": {
      "sha256": "8343fd8c2fc3747b2c5b0562ce72cc41e72e27e8ee1d3b43699c6370eb1b9270",
      "facts": "#### [API_SIGNATURES]\n```markdown\n# Public API Surface (inferred from test references)\n\n## Functions\n\n### buildTree\n- **Parameters**: `files []string` (file paths to index)\n- **Returns**: `*DirNode` — root tree node containing `.Path`, `.Children map[string]*DirNode`, and `.Files []string` fields\n\n### determineAffected  \n- **Parameters**: \n  - `tree *DirNode`\n  - `tempDir string`\n  - `docs string` (repository/docs directory path)\n  - `cache Cache` — interface with `.Modules map[string]string` field\n  - `changes []FileChange` — slice of struct containing `.Path string` and `.Status StatusModified` fields\n- **Returns**: `map[string]bool` — affected module paths\n\n### propagateAffected\n- **Parameters**: \n  - `tree *DirNode`\n  - `affected map[string]bool`\n- **Returns**: `map[string]bool` — propagated affected status per path\n\n## Types (inferred from usage)\n\n| Type | Fields Observed |\n|------|------------------|\n| `*DirNode` | `.Path string`, `.Children map[string]*DirNode`, `.Files []string` |\n| `Cache` | `.Modules map[string]string` |\n| `FileChange` | `.Path string`, `.Status StatusModified` (enum-like) |\n```\n\n---\n\n⚠️ **Note**: This surface was extracted from a test file (`tree_test.go`). Actual definitions, signatures, and error handling reside in the corresponding source files (e.g., `tree.go`).\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code solves **module dependency impact analysis** for a project's source tree. It answers two questions: (1) \"Given these files, what is the hierarchical directory structure?\" and (2) \"When one file changes, which modules are affected—and does that affect their parent directories too?\"\n\n## High-Level Algorithm Steps\n\n### Step 1 — Build Tree\n- Take a flat list of project file paths.\n- Group them into nested `DirNode` structures: files at the same depth share a directory node; subdirectories become children of their parent.\n- Each `DirNode` stores its own path and the files contained directly within it.\n\n### Step 2 — Determine Affected Modules\n- Walk the tree while tracking file changes (e.g., modifications).\n- Mark any module whose source file changed as \"affected.\"\n- Unrelated branches remain unaffected.\n\n### Step 3 — Propagate Affected Status Upward\n- After marking individual modules, propagate the affected status to parent directories.\n- If a child is affected, its parent becomes affected too (transitive propagation up the tree).\n- Sibling branches that did not change stay clean—only ancestors of changed files are marked.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n**TestBuildTree:**\n- **No external side effects.** This test only constructs a tree structure in memory and validates it with assertions. No file system, network, or database interactions occur.\n\n**TestDetermineAndPropagateAffected:**\n- **File system I/O via standard library (`os`, `path/filepath`).** The test creates a temporary directory using `t.TempDir()`, then writes dummy files to simulate module existence so that the production code's `os.Stat` calls succeed:\n  - `os.MkdirAll(...)` — creates nested directories under the temp dir.\n  - `os.WriteFile(...)` — writes `\"test\"` bytes into each module path.\n\n## Error Propagation\n\n- **No error propagation observed.** All test functions invoke production methods (`buildTree`, `determineAffected`, `propagateAffected`) without checking their return values or handling errors. Any errors they produce are swallowed (unhandled/ignored).\n- Assertions use `t.Errorf` and `assert.True` patterns; failures do not propagate to callers because these are leaf-level tests with no caller chain visible here."
    },
    "internal/engine/utils.go": {
      "sha256": "9b8cd943c9fdbb73451b01584d7d76b73d92194f133ec07c04517ac3ca7f713b",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface of `internal/engine/utils.go`\n\n### Functions\n\n| Function | Parameters | Return Type |\n|---|---|---|\n| **`toSafeMarkdownFilename`** | `modulePath string` | `string` |\n| **`makeLogEvent`** | `onEvent func(Event)` | `LogEventFunc` |\n\n\u003e *Note: Types `LogEventFunc`, `EventType`, and `Event` are referenced but not defined within this file. Their definitions must be resolved from the importing package.*\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis file provides lightweight utility helpers for the engine module: generating safe markdown filenames from directory paths, and creating adapter-style log event callbacks that support optional listeners.\n\n## High-Level Steps\n\n1. **Safe filename generation**: Given a module path string, return `\"README.md\"` if the path is empty or `.`; otherwise join the path with `\"README.md\"`.\n2. **Log event factory**: Return a function that, given an `EventType` and message string, forwards to a supplied callback only if it's non-nil (nil-safe adapter pattern).\n\n## Domain Rules\n\n- Empty/`.` module paths map to `\"README.md\"` by default.\n- Unknown or invalid callbacks are silently swallowed — no panic on nil listeners.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects. The only filesystem-related function (`toSafeMarkdownFilename`) constructs path strings via `filepath.Join` but performs no actual file operations (no open/read/write). Pure string manipulation.\n\n**Error Propagation:** None observed. Both functions return values without error types, and neither panics or returns errors."
    },
    "internal/security/errors.go": {
      "sha256": "ccabad7427e45855c7277be338dfd613a25148de1d42d9c284ba887432a5bacd",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface — `internal/security/errors.go`\n\n| Element | Type | Parameters | Returns |\n|---------|------|------------|---------|\n| `ErrPathTraversal` | `error` (variable) | — | — |\n| `ErrLockHeld` | `error` (variable) | — | — |\n\n#### [BUSINESS_LOGIC]\n## Domain Problem\n\nThis code provides error definitions for security operations that handle:\n- **Path traversal prevention** — detecting when file paths resolve outside an allowed root directory\n- **Cross-process lock management** — reporting when a shared resource is already locked by another process\n\n## High-Level Algorithm Steps\n\n1. A path-traversal check validates whether a given file or directory path resolves within the repository root boundary; if it does not, `ErrPathTraversal` is returned.\n\n2. A lock acquisition attempt detects contention from another process holding the same lock; if detected, `ErrLockHeld` is returned.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\nNo external side effects. This file contains only sentinel error declarations; no functions, imports beyond `errors`, or runtime operations exist here to perform disk, network, database, or API interactions.\n\nError propagation: None in this file. Both errors are pre-allocated sentinels awaiting use by other code in the module. Propagation of these errors (e.g., wrapping via `fmt.Errorf` or returning from a function) would occur elsewhere, not here."
    },
    "internal/security/security.go": {
      "sha256": "c0d705dd9e8931f4ec613dd5d7fb8e54dfb3eb794891138f4715c4b15175235a",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/security` package\n\n## Types\n\n### `SafeResolve`\n- **Signature**: `func SafeResolve(repoRoot, inputPath string) (string, error)`\n- **Description**: Cleans the input path and ensures it lies strictly inside the repository. Resolves symlinks on existing ancestor parts to prevent path traversal via symlinks.\n\n---\n\n### `SimpleLock` struct\n- **Fields** (unexported):\n  - `lockPath string`\n  - `file *os.File`\n  - `mu sync.Mutex`\n  - `closed bool`\n\n## Functions\n\n### `AcquireLock`\n- **Signature**: `func AcquireLock(repoRoot string) (*SimpleLock, error)`\n- **Description**: Acquires a simple file lock in the repoRoot. Uses O_EXCL to ensure atomicity.\n\n---\n\n### `Unlock` (method on `*SimpleLock`)\n- **Signature**: `func (l *SimpleLock) Unlock() error`\n- **Description**: Releases the lock by closing the file and removing it. Idempotent and thread-safe.\n\n---\n\n## Unresolved References (not defined within this file)\n- `ErrPathTraversal` — referenced in `SafeResolve` signature path validation but not declared here.\n- `ErrLockHeld` — referenced in `AcquireLock` error wrapping but not declared here.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code solves two distinct problems within a repository context: preventing path traversal attacks and managing process-level locking to avoid stale locks blocking legitimate operations.\n\n## High-Level Algorithm Steps\n\n**SafeResolve (Path Traversal Prevention):**\n1. Compute the absolute root directory, resolving any symlinks on it first\n2. Join the resolved root with an input path candidate\n3. Walk up from that joined path until a physically existing ancestor is found — recording each skipped component along the way\n4. Resolve symlinks on that existing ancestor\n5. Reconstruct the full target path from the resolved ancestor plus all previously-skipped components\n6. Verify the reconstructed path remains inside the resolved root; reject if it escapes\n\n**SimpleLock (File-Based Process Locking):**\n1. Use `O_EXCL` flag to atomically create a lock file — failure means another process holds the lock or a stale lock exists, which is treated as an error condition requiring manual cleanup\n2. Write the current PID into the lockfile for identification purposes\n3. Release via idempotent unlock: close the file descriptor and remove the lockfile; always clean up even on error paths\n\n#### [STATE_AND_CONCURRENCY]\n### Mutable State Identified\n\n**Class-Level Properties (Struct Fields of `SimpleLock`)**\n\n| Field | Type | Mutability | Notes |\n|-------|------|------------|-------|\n| `lockPath` | `string` | **Modified once** in `AcquireLock`, then read-only thereafter. Not mutated by any other method. |\n| `file` | `*os.File` | **Modified once** in `AcquireLock`. Closed during `Unlock()`. No concurrent mutation observed. |\n| `mu` | `sync.Mutex` | Read via lock/unlock primitives only. Field itself is never assigned after struct initialization. |\n| `closed` | `bool` | Modified by `Unlock()` (set to `true`). Also read inside `Unlock()` for the idempotency check (`if l.closed { return nil }`). Concurrent calls serialize through `mu`. |\n\n**Synchronization Mechanisms**\n\n- **`sync.Mutex` (`l.mu`)**: Used in `SimpleLock.Unlock()`. Acquired at entry and released via `defer`. Protects the close-and-remove sequence, making `Unlock()` atomic with respect to itself.\n- **No package-level lock/mutex**: The struct owns its own mutex; there is no shared global lock across instances of `SimpleLock`.\n\n### Thread Safety Assessment\n\n- `Unlock()` is self-synchronizing via `mu.Lock()/defer mu.Unlock()`, making it safe for concurrent calls on the same instance.\n- Concurrent calls to `AcquireLock` on different instances each create their own independent `SimpleLock` with separate mutexes—no contention between instances is modeled or guaranteed by this code.\n- The file-level lock (the `.code-reducer.lock` file itself) provides OS-level mutual exclusion, which complements the in-memory `mu`.\n\n### No Package-Level Global Mutable State\n\nNo package-level variables are declared. All mutable state is encapsulated within individual `SimpleLock` instances.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\nAll interactions are with the **local filesystem** (disk). No network, database, or API calls exist.\n\n### `SafeResolve` — Path Validation\n| Operation | External System | Purpose |\n|---|---|---|\n| `filepath.Abs(repoRoot)` | Disk (filesystem) | Resolve absolute path from relative input |\n| `filepath.EvalSymlinks(absRoot)` | Disk (filesystem) | Resolve symlinks on the root directory before joining |\n| `os.Lstat(current)` loop | Disk (filesystem) | Walk up ancestor chain, stopping at first physically existing parent |\n| `filepath.EvalSymlinks(current)` | Disk (filesystem) | Resolve symlinks on the found ancestor path |\n\n### `AcquireLock` — Lock Acquisition\n| Operation | External System | Purpose |\n|---|---|---|\n| Calls `SafeResolve(repoRoot, LockFileName)` | Disk | Resolve canonical lock file path inside repo root |\n| `os.OpenFile(lockPath, O_WRONLY\\|O_CREATE\\|O_EXCL, 0644)` | Disk | Atomic create-with-exclusion; returns error if another writer holds the file (OS-level atomicity) |\n| `f.Write([]byte(fmt.Sprintf(\"%d\\n\", os.Getpid())))` | Disk | Writes current process PID to lockfile for debugging/inspection |\n\n### `SimpleLock.Unlock()` — Lock Release\n| Operation | External System | Purpose |\n|---|---|---|\n| `l.file.Close()` | Disk (OS file descriptor) | Close the open file handle |\n| `os.Remove(l.lockPath)` | Disk | Remove the lockfile from disk |\n\n---\n\n## Error Propagation Analysis\n\n### SafeResolve\n- **Wraps** with `%w` on `filepath.Abs` and `EvalSymlinks(absRoot)` errors.\n- **Wraps** with `%w` on each `Lstat` failure that is not a \"not exist\" error.\n- **Wraps** with `%w` on the final `EvalSymlinks(current)`.\n- On path-traversal detection, returns a wrapped error using a custom sentinel (`ErrPathTraversal`) with `inputPath` as context — this **swallows** the original traversal signal and replaces it with a formatted message.\n\n### AcquireLock\n- **Propagates directly** (no wrapping) when `SafeResolve` fails.\n- On `OpenFile` failure: if error is `os.IsExist`, wraps with `%w` using custom sentinel (`ErrLockHeld`) plus context string describing stale lockfile; otherwise wraps the raw error with path info.\n- On **Write** failure (after successful OpenFile): closes file and removes it, then returns a wrapped error that **swallows** any close/remove errors into a single formatted message — only the original write error is surfaced, even if cleanup also failed.\n\n### SimpleLock.Unlock()\n- Returns `nil` if already closed (`l.closed == true`).\n- If not yet closed: closes file first; then attempts to remove lockfile. **Swallows** close errors when reporting removal errors (only surfaces the remove error if it fails)."
    },
    "internal/security/security_test.go": {
      "sha256": "c60ccd832bd3368a58529148b43c15672d32aa5aa6563beff3cf61ec3eb32a16",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: internal/security\n\n## Functions\n\n### SafeResolve\n- **Parameters:**\n  - `repoRoot` (string) – repository root path\n  - `inputPath` (string) – input file/directory path to resolve\n- **Return values:**\n  - resolved path (unverified type, inferred from usage pattern as string)\n  - error\n\n### AcquireLock\n- **Parameters:**\n  - `repoRoot` (string) – repository root path\n- **Return values:**\n  - lock object (type: `Lock`, inferred from method calls in tests)\n  - error\n\n## Types\n\n### Lock\n- **Method:**\n  - `Unlock()` (no parameters, returns unverified type and error based on test usage pattern)\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThe code solves two concurrent concerns in a repository-aware context:\n\n1. **Secure path resolution** — resolving user-supplied paths against a known root while preventing directory traversal and symlink escape attacks.\n2. **Exclusive access control** — ensuring only one goroutine/process can operate on the protected resource at a time via file-based locking.\n\n## High-Level Algorithm Steps\n\n### SafeResolve\n1. Receive a candidate path string and an anchor (repository root) directory.\n2. Resolve the candidate to an absolute filesystem path, using the repository root as the base for relative inputs.\n3. Compare the resolved absolute path against the canonicalized repository root. If they differ — meaning the resolved path escapes outside the repository boundary — reject with an error.\n4. Return success when the resolved path remains within (or equals) the repository root.\n\n### AcquireLock / Unlock\n1. Create a lock file inside the protected resource directory and attempt to open it exclusively (`O_CREATE | O_EXCL | O_RDWR`).\n2. If the exclusive open fails, reject — another holder is already active.\n3. On success, return an unlock handle that releases the file when closed (via `syscall.Flock` with `LOCK_UN`).\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n- `AcquireLock(repoRoot)` — acquires a lock that tracks whether it is held (mutable internal state). Confirmed by subsequent call failing and requiring explicit `Unlock()`.\n- `lock1.Unlock()` / `lock2.Unlock()` — releases the acquired lock, modifying its internal held-state.\n\n**Thread Safety:**\n- Lock acquisition (`AcquireLock`) + unlock pattern suggests mutex-like synchronization is implemented (or simulated) by the security package.\n- No locks/mutexes are used within the test file itself; each `Test*` function runs independently in a single goroutine.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O and Error Propagation Analysis\n\n### External Systems Interacted With\n\nAll interactions in this file target the **local filesystem** via the standard library's `os` package:\n\n| Operation | Function | Purpose |\n|-----------|----------|---------|\n| Directory creation | `os.MkdirAll()` | Sets up test fixtures (`src/sub/`) |\n| File writing | `os.WriteFile()` | Creates `file.txt`, `..config` for traversal tests |\n| Symlink creation | `os.Symlink()` | Creates a symlink pointing outside the repo root (conditional) |\n\nThese are all **test-side** operations. The actual external I/O performed by the *production* code (`security.SafeResolve()`, `security.AcquireLock()`) is inferred from test behavior, not directly shown in this file.\n\n### Error Propagation Patterns\n\n**Test fixture setup errors**:\n- `os.MkdirAll()` / `os.WriteFile()` errors are propagated **up to the test framework** via `t.Fatal(err)`. This terminates the test immediately—no recovery or retry logic exists at the test level.\n\n**Symlink creation error**:\n- `os.Symlink()` error is swallowed and replaced with `t.Skip(\"Symlinks not supported on this OS/filesystem\")`. The test continues only if the operation fails, but no further I/O is attempted.\n\n**Production function errors (inferred from test assertions)**:\n- `security.SafeResolve(repoRoot, inputPath)` returns an error to its caller; tests verify both success and failure paths via `wantErr` flag comparison (`t.Errorf(...)`).\n- `security.AcquireLock(repoRoot)` returns `(lock, err)`. Test asserts that a second concurrent call fails with `t.Fatal(\"AcquireLock() should have failed when lock is already held\")` — indicating the production code signals contention via error return.\n\n**No panic/crash observed**. All errors follow Go conventions: errors are returned to callers; tests assert expected outcomes rather than relying on panics."
    },
    "internal/tools/file_tools.go": {
      "sha256": "77f21deb5529b9a10f13c99937ea63c0828a5db0e5295e6a9b8c8b1fa190d4e1",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/tools/file_tools.go`\n\n## Functions\n\n### ReadFileSafely\n```go\nfunc ReadFileSafely(repoRoot string, virtualPath string) ([]byte, error)\n```\nReads a file by resolving its virtual path relative to the repository root. Returns the file content as bytes and any error encountered.\n\n---\n\n### WriteFileAtomic\n```go\nfunc WriteFileAtomic(targetPath string, data []byte, perm os.FileMode) error\n```\nWrites binary data atomically to `targetPath` using a temp-file-and-rename pattern. The directory is created with mode `0755`. Returns an error on failure; returns `nil` only after successful rename.\n\n---\n\n### WriteFileSafely\n```go\nfunc WriteFileSafely(repoRoot string, virtualPath string, content []byte) error\n```\nWrites file content by resolving the virtual path and delegating to `WriteFileAtomic`. Returns an error if path resolution or atomic write fails.\n\n---\n\n### EnsureGitignoreHasLockfile\n```go\nfunc EnsureGitignoreHasLockfile(repoRoot string) error\n```\nEnsures that `.gitignore` contains a line matching `security.LockFileName`. Reads the existing file, appends the lock entry if absent (adding a newline before it when needed), and writes back atomically. Returns an error on read/write failures.\n\n---\n\n### LoadGitignore\n```go\nfunc LoadGitignore(repoRoot string) ([]string, error)\n```\nReads `.gitignore` from `repoRoot`, strips comments (`#`) and blank lines, and returns the remaining patterns as a `[]string`. Returns `nil, nil` when no `.gitignore` exists; returns an error on other read failures.\n\n---\n\n### ShouldIgnoreFile\n```go\nfunc ShouldIgnoreFile(relPath string, gitIgnore *ignore.GitIgnore) bool\n```\nReturns `true` if the given relative path should be ignored: matches via the provided GitIgnore object (if non-nil), contains a dot-prefixed component, or ends with `.egg-info`.\n\n---\n\n### DiscoverCodeFiles\n```go\nfunc DiscoverCodeFiles(repoRoot string, ignores []string) ([]string, error)\n```\nRecursively walks `repoRoot` to collect relative file paths that pass ignore filtering. Skips directories whose names start with `.`, end with `.egg-info`, or match the compiled GitIgnore rules. Returns a list of valid relative paths and any walk error.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThe code provides safe file I/O utilities for analyzing a code repository's structure. It solves two problems: (1) reliably reading/writing files within a known repo root with TOCTOU protection, and (2) discovering which files in the tree are \"code\" versus build artifacts, dependencies, or lockfiles.\n\n## High-Level Algorithm Steps\n\n**Safe file operations:**\n- Resolve any virtual path against a fixed `repoRoot` before touching the filesystem\n- Use temp-file + rename pattern for writes to prevent partial/corrupt state if interrupted\n- Sync and close before finalizing to guarantee data reaches storage\n\n**Lockfile handling:**\n- Ensure `.code-reducer.lock` is recorded in `.gitignore` so it never appears as a source file during discovery\n\n**Gitignore loading:**\n- Parse `.gitignore`, strip comments and blank lines, return the active pattern list\n\n**Ignore detection (two layers):**\n1. Apply user-supplied ignore patterns plus compiled `.gitignore` rules to any given path\n2. Independently reject paths whose components start with `.` or end with `.egg-info`\n\n**Code discovery:**\n- Walk the entire directory tree from `repoRoot`\n- Skip directories matching ignore rules, dot-prefixed names (e.g. `.build`, `.venv`), or `.egg-info` suffixes\n- For files, run both ignore-checks and collect those that pass\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O \u0026 Error Propagation Analysis: `file_tools.go`\n\n---\n\n### 1. Disk Read/Write Operations (POSIX file system)\n\nAll functions in this module interact exclusively with the local filesystem via standard OS calls (`os.ReadFile`, `os.CreateTemp`, `os.MkdirAll`, `os.Rename`, `os.Chmod`, `os.Open`). No network, database, or external API interactions exist.\n\n| Function | I/O Type | External System |\n|---|---|---|\n| `ReadFileSafely` | Disk read (`os.ReadFile`) | Local filesystem — target path resolved through `security.SafeResolve` before reading |\n| `WriteFileAtomic` | Disk write (create temp → sync → chmod → rename) | Local filesystem — writes to a `.tmp.*` intermediate file then renames into place |\n| `WriteFileSafely` | Disk read + disk write | Delegates path resolution and atomic write; no additional I/O beyond what the two sub-functions perform |\n| `EnsureGitignoreHasLockfile` | Disk read (`os.ReadFile`) + disk write (`WriteFileAtomic`) | Reads `.gitignore`, appends lockfile pattern, writes back atomically |\n| `LoadGitignore` | Disk read (`os.Open` + `bufio.Scanner`) | Reads `.gitignore`; returns `nil, nil` when file does not exist (no error) |\n| `DiscoverCodeFiles` | Disk walk (`filepath.WalkDir`) | Recursively walks the repository root; skips directories matching ignore rules or dot-prefixed names |\n\n---\n\n### 2. Error Propagation Patterns\n\nErrors are propagated as Go typed errors in all paths except one notable case:\n\n- **`ReadFileSafely`** — wraps `os.ReadFile` error with context (\"failed to read file content\"). The path resolution error from `security.SafeResolve` is passed through unwrapped.\n- **`WriteFileAtomic`** — each step (mkdir, create temp, write, sync, close, chmod, rename) returns a wrapped error. The deferred cleanup block runs on any failure path but does not propagate; errors are returned directly to the caller.\n- **`WriteFileSafely`** — unwrapped `security.SafeResolve` error propagates straight through. Otherwise defers to `WriteFileAtomic`'s return value.\n- **`EnsureGitignoreHasLockfile`** — `.gitignore` read errors (except `os.IsNotExist`) are wrapped with \"error reading .gitignore\". A non-existent `.gitignore` falls through to the write path, which will then fail if the directory does not exist; no special handling for that intermediate state.\n- **`LoadGitignore`** — returns `nil, nil` when file is absent (treated as success). Any other error from `os.Open` propagates unwrapped. Scanner errors are appended via `scanner.Err()` at the end — if a scan error occurs mid-stream but `lines` has already been populated, it still gets returned to the caller.\n- **`ShouldIgnoreFile`** — no I/O; returns only boolean values. No error path.\n- **`DiscoverCodeFiles`** — walk errors are wrapped with context (path + \"error walking\"). Relative-path computation failures are also wrapped. Directory entries that match ignore rules return `filepath.SkipDir`. Non-matching files append to the slice. If the walk fails at any point, the returned error is passed through as-is after wrapping.\n\n---\n\n### 3. Notable Observations\n\n- **TOCTOU mitigation**: `WriteFileAtomic` uses a temp file + rename pattern, which avoids race conditions on the target path during writes.\n- **Sync before close**: `tmpFile.Sync()` is called before `Close()`, ensuring data reaches stable storage before the handle is released — relevant for crash safety but adds latency per write.\n- **Deferred cleanup in WriteFileAtomic**: On any error, `tmpFile.Close()` and `os.Remove(tmpName)` run via defer. This prevents partial temp file leakage on failure paths. The flag `success` gates whether to trigger cleanup.\n- **No panic/recover anywhere** — all errors are returned as typed values. No `panic`, no bare recovery blocks observed in this module."
    },
    "internal/tools/file_tools_test.go": {
      "sha256": "dc63ebefde774e1f683301b191a1fcbbc7d904dc209eb84af5a9c510bea05889",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/tools` package\n\n## Functions\n\n| Function | Parameters | Returns |\n|---|---|---|\n| **WriteFileSafely** | `(repoRoot string, relPath string, content []byte)` | error |\n| **LoadGitignore** | `(repoRoot string)` | (patterns `[]string`, error) |\n| **ShouldIgnoreFile** | `(relPath string, gitIgnore interface{})` | bool |\n| **DiscoverCodeFiles** | `(repoRoot string, ignores []string)` | ([]string, error) |\n\n---\n\n\u003e **Notes:**  \n\u003e - Function signatures are inferred strictly from the test file's usage patterns. Return types for multi-value returns are deduced from how results are consumed in tests (e.g., `err != nil` checks, slice length comparisons).  \n\u003e - The exact signature of `ShouldIgnoreFile`'s second parameter (`gitIgnore`) is confirmed as a compiled gitignore object per the test's import and usage.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nThis code solves the problem of reliably discovering source-code files within a repository while correctly applying `.gitignore` rules — filtering out build artifacts, hidden files/directories, and anything explicitly ignored by gitignore patterns. It also provides safe file-writing utilities that integrate with this discovery workflow.\n\n**High-Level Algorithm Steps:**\n\n1. **WriteFileSafely** – Atomically writes a target file (likely using temp-file + rename to avoid partial reads).\n2. **LoadGitignore** – Scans for `.gitignore` in the repo root, parses its contents into ignore patterns (skipping blank lines and comments).\n3. **ShouldIgnoreFile** – Given a relative path and an `ignore.IgnoreList`, returns whether the file matches any ignore pattern — used to filter out build outputs, hidden paths, etc.\n4. **DiscoverCodeFiles** – Walks the directory tree, collects files that are Go source code (`.go` extension), excludes anything matched by `.gitignore` patterns or hidden-file rules, and returns the resulting list of discovered source files.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O and Error Propagation Analysis\n\n**All interactions: Local filesystem (disk) only.** No network, API, database, or other external system interactions observed.\n\n---\n\n### Filesystem Operations by Test Function\n\n#### `TestWriteFileSafely`\n| Operation | Source Code | Purpose |\n|-----------|-------------|---------|\n| Write to disk | `tools.WriteFileSafely(repoRoot, \"src/new_file.txt\", []byte(\"hello\"))` | Creates a file under repo root |\n| Read from disk | `os.ReadFile(filepath.Join(repoRoot, \"src\", \"new_file.txt\"))` | Verifies written content |\n\n#### `TestLoadGitignore`\n| Operation | Source Code | Purpose |\n|-----------|-------------|---------|\n| Read .gitignore (missing) | `tools.LoadGitignore(repoRoot)` — first call | Tests graceful handling of missing file |\n| Write to disk | `os.WriteFile(filepath.Join(repoRoot, \".gitignore\"), content, 0644)` | Creates test `.gitignore` fixture |\n| Read .gitignore (exists) | `tools.LoadGitignore(repoRoot)` — second call | Verifies parsed patterns |\n\n#### `TestShouldIgnoreFile`\n**No external I/O.** Uses pre-compiled in-memory gitignore (`ignore.CompileIgnoreLines`).\n\n#### `TestDiscoverCodeFiles`\n| Operation | Source Code | Purpose |\n|-----------|-------------|---------|\n| Create directories | `os.MkdirAll(filepath.Join(repoRoot, d), 0755)` × 3 | Sets up test directory tree (`src`, `src/.hidden`, `build`) |\n| Write files | `os.WriteFile(..., []byte(\"test\"), 0644)` × 5 | Populates fixture files in various locations |\n| Read filesystem | `tools.DiscoverCodeFiles(repoRoot, ignores)` | Discovers code files while filtering ignored paths |\n\n---\n\n### Error Propagation Patterns\n\nAll test functions propagate errors directly to the testing framework via **fatal-level assertions** — no recovery or fallback logic:\n\n| Test Function | Error Path | Handler |\n|---------------|-----------|---------|\n| `TestWriteFileSafely` | Write error → `t.Fatalf(\"WriteFileSafely() failed: %v\", err)` | Aborts test, reports failure with message |\n| `TestWriteFileSafely` | Read error → `t.Fatalf(\"Failed to read created file: %v\", err)` | Aborts test after write succeeded |\n| `TestLoadGitignore` | Load error (missing file) → `t.Fatalf(\"LoadGitignore() failed when file is missing: %v\", err)` | Aborts test |\n| `TestLoadGitignore` | Write fixture error → `t.Fatal(err)` | Aborts test silently |\n| `TestLoadGitignore` | Load error (exists) → `t.Fatalf(\"LoadGitignore() failed: %v\", err)` | Aborts test |\n| `TestDiscoverCodeFiles` | MkdirAll errors → `t.Fatal(err)` × 3 | Aborts test silently |\n| `TestDiscoverCodeFiles` | WriteFile errors → `t.Fatal(err)` × 5 | Aborts test silently |\n| `TestDiscoverCodeFiles` | Discover error → `t.Fatalf(\"DiscoverCodeFiles() failed: %v\", err)` | Aborts test with message |\n\n**Pattern**: Errors are never recovered, deferred, or logged. All failures terminate the test immediately via `t.Fatal`/`t.Fatalf`. No panic recovery observed in this file."
    },
    "internal/tools/git_tools.go": {
      "sha256": "bdf47dd0db3277f300f4775e1dfb68f549dd24b9f78a2a884984bb6c930f8264",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `tools` package (`git_tools.go`)\n\n- **`RunGit(repoRoot string, args ...string) (string, error)`** — Executes a git command in the specified repo directory with `--no-pager`. Returns trimmed stdout and any error.\n\n- **`VerifyGitRepo(repoRoot string) error`** — Checks that `git` is available and that `repoRoot` is inside a git working tree.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code provides an abstraction for executing Git commands within a specific directory and validating whether that directory constitutes a recognized Git repository. It centralizes process execution logic to ensure consistent handling of stdout, stderr, and error reporting when interacting with the Git version control system.\n\n**High-Level Algorithm Steps**\n\n1.  **Initialize Command:** Construct a Git command invocation in the specified root directory, appending `--no-pager` to any user-provided arguments to suppress pager output.\n2.  **Execute and Capture:** Run the constructed process while capturing standard output and error streams into buffers.\n3.  **Handle Success or Failure:**\n    *   If execution succeeds, trim whitespace from the captured standard output and return it as a string result.\n    *   If execution fails, trim whitespace from the captured error stream and return a formatted error message containing the failure details.\n4.  **Validate Repository Status:** Invoke `git rev-parse --is-inside-work-tree` specifically to check if the directory is inside a work tree. If this command returns an error, classify the location as not being a Git repository and return that specific classification.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External Systems Interacted With\n\n| System | Mechanism | Details |\n|--------|-----------|---------|\n| **External process (git)** | `exec.Command(\"git\", ...)` via `os/exec` | Spawns a child `git` subprocess in `repoRoot`. Stdout captured to `bytes.Buffer`, stderr separately buffered. `cmd.Run()` executes the command and returns the exit error. |\n\n### Error Propagation\n\n- **Primary entry point — `RunGit`**:\n  - Returns `(string, error)`. If `cmd.Run()` fails, it wraps the error with:  \n    `fmt.Errorf(\"git command failed: %v, stderr: %s\", err, trimmedErr)`  \n    The original `err` from `exec.Cmd` (e.g., `*exec.Error`, non-zero exit status) is embedded as `%v`; stderr text is appended. No panic occurs on failure.\n\n- **Secondary entry point — `VerifyGitRepo`**:\n  - Calls `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")`. If the returned error is non-nil, it wraps with:  \n    `fmt.Errorf(\"not a git repository (or any of the parent directories): %w\", err)`  \n    Uses `%w` for `wrapError`-compatible wrapping (recoverable by unwrappers).\n\n### No External Side Effects\n\nNo direct disk reads/writes, no network calls, no database interactions. The only external interaction is subprocess execution against the installed `git` binary."
    },
    "main.go": {
      "sha256": "16ab362b1830374f0306a1990dba8d51da887c1ad126f311c03d242933d2a516",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `main.go`\n\n## Functions\n\n- **`main()`** `func() error`  \n  - Entry point of the program. Delegates to `cmd.RootCmd.Execute()`. On error, prints a message to stderr and exits with code 1.\n    - Returns: `error` (via `fmt.Fprintf`/`os.Exit` path — swallowed into exit)\n\n## Referenced External Types \u0026 Methods\n\n- **`RootCmd`** — type from package `github.com/arrase/code-reducer/cmd`.  \n  - Method: **`Execute()`** — invoked as `cmd.RootCmd.Execute()`.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose \u0026 Domain Rules\n\n### Primary Domain Problem Solved\nThis code provides only an **entry-point shell** for a command-line tool. No domain logic is implemented in this file — all processing happens inside the `cmd` package (`github.com/arrase/code-reducer/cmd`). The entry point itself just:\n\n1. Delegates execution to `cmd.RootCmd.Execute()`\n2. Swallows any error by printing it to stderr and exiting with code 1\n3. Returns success (exit code 0) if no error occurred\n\n### High-Level Algorithm Steps\nThere are none visible in this file. The algorithm is entirely externalized into the imported command package.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:**\n- `os.Stderr` — writes formatted error message to standard error stream when execution fails. This is an external side effect (terminal output).\n- `os.Exit(1)` — terminates the process with exit code 1. External system interaction.\n- The third-party package (`github.com/arrase/code-reducer/cmd`) is called via `cmd.RootCmd.Execute()`. No internal source text available to determine what I/O it performs (file writes, network calls, DB queries, etc.). Cannot confirm or deny external effects from this package.\n\n**Error Propagation:**\n- Errors returned by `cmd.RootCmd.Execute()` are propagated as a single-line formatted string (`fmt.Fprintf`) printed to stderr.\n- No retry logic, no fallback paths, no partial recovery — error is terminal.\n- If `Execute()` returns nil (no error), the program exits cleanly with code 0 (default). No explicit call to `os.Exit(0)` but Go's default exit behavior applies."
    },
    "mkdocs.yml": {
      "sha256": "1e3b3dd1b88dd6996bac64233f41ab78427f0fe86a7090fee60182ab4feaaa2f",
      "facts": "#### [API_SIGNATURES]\nNo public API elements (classes, functions, methods, types) were found in the provided source.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nGenerating developer-friendly code wikis from codebases by utilizing local Large Language Models (LLMs) via Ollama.\n\n**High-level Algorithm Steps**\n1. Process codebases using a Map-Reduce engine.\n2. Manage updates through incremental processing and caching.\n3. Execute operations within a security sandbox and using concurrency.\n4. Interface with local LLMs via Ollama.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O\n* **Network**: \n    * Fetches JavaScript from `cdn.jsdelivr.net`.\n    * Fetches CSS from `cdnjs.cloudflare.com`.\n\n### Error Propagation\n* No error propagation mechanisms are defined in the configuration file."
    },
    "sonar-project.properties": {
      "sha256": "cdc6154dcbff4c6c4c342b8e3a3b382e68b4ab596fa44790bae5e83591edd8f7",
      "facts": "#### [API_SIGNATURES]\nNo public API elements (classes, functions, methods, or types) were found in the provided file.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nConfigures static analysis and code coverage parameters for a Sonar scanner to facilitate automated code quality reporting for a software project.\n\n**High-level Algorithm Steps**\n1. Define project identification and character encoding.\n2. Designate root directories for source code and test files.\n3. Filter source code by excluding test files, dependencies, documentation, and auxiliary assets.\n4. Filter the test suite by including specific file patterns and excluding dependencies.\n5. Locate the code coverage report for processing.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O**\n* **Disk:** Reads source files from the current directory (`.`) and coverage reports from `coverage.out`.\n\n**Error Propagation**\n* No error propagation logic is defined in this configuration file."
    }
  },
  "modules": {
    ".": "# Code-Reducer System Architecture Reference\n\n## System Overview\nCode-Reducer is a command-line tool designed to generate structured developer documentation (wikis) from codebases by utilizing local Large Language Models (LLMs) via Ollama. The system employs a Map-Reduce engine to perform hierarchical synthesis, transitioning from individual file analysis to high-level architectural documentation. It supports incremental updates by tracking file state via SHA256 fingerprints.\n\n---\n\n## CLI Orchestration (`cmd` module)\n\n### Responsibility\nThe `cmd` module acts as the orchestration layer, managing the CLI lifecycle, environment validation, and dispatching execution to the underlying engine. It handles user interaction for initial configuration and enforces state transitions between project initialization and incremental updates.\n\n### Data Flow\n1.  **Input Acquisition**: Captures CLI arguments and interactive user input via `os.Stdin`.\n2.  **Environment Validation**: Verifies the existence of a git repository and identifies the working directory.\n3.  **State Assessment**: Evaluates the current project state (e.g., via `engine.IsInitialized`) to enforce valid modes (`ModeInit` vs. `ModeUpdate`).\n4.  **Configuration Resolution**: Loads and merges settings from disk and environmental sources.\n5.  **Execution Dispatch**: Passes the validated `config.Config` and execution `mode` to the `engine` package.\n6.  **Output Stream**: Directs status updates and errors to `stdout` and `stderr`.\n\n### Core Components\n*   **`RootCmd`**: The primary `cobra.Command` entry point.\n*   **`executeCommand`**: The central controller that orchestrates directory resolution, git verification, and configuration loading.\n*   **`RunSetupFlow`**: An interactive process that captures LLM Model IDs, Ollama endpoints, context sizes, and documentation directory paths.\n*   **Subcommands**:\n    *   `init`: Triggers a full repository scan and initial Markdown generation.\n    *   `update`: Triggers incremental analysis by comparing current file states against the last documented git commit.\n\n---\n\n## Configuration Management (`internal/config`)\n\n### Responsibility\nThis module manages the hierarchical resolution, validation, and persistence of system settings required for the LLM pipeline.\n\n### Data Flow\n1.  **Ingestion**: Aggregates data from `.code-reducer.yaml`, environment variables, and CLI flags.\n2.  **Resolution**: Resolves values based on the following precedence: CLI flags $\\rightarrow$ Environment variables $\\rightarrow$ Configuration file $\\rightarrow$ System defaults.\n3.  **Validation**: Ensures numerical parameters (e.g., `OllamaNumCtx`) are integers greater than zero.\n4.  **Persistence**: Serializes the `Config` struct to YAML and performs an atomic write using a temporary file and rename pattern.\n\n### Data Structures\n*   **`Config`**: The primary schema containing `ModelID`, `OllamaBaseURL`, `OllamaNumCtx`, `DocsDir`, and prompt templates for different synthesis stages (Module, Architecture, File Fact Consolidation).\n*   **`ExtractionStep`**: Defines specific phases of the LLM extraction pipeline, mapping a `Name` to a specific `Prompt`.\n\n---\n\n## Map-Reduce Documentation Engine (`internal/engine`)\n\n### Responsibility\nThe engine performs the core logic of transforming source code into structured documentation through a hierarchical reduction process. It manages file discovery, change detection, and recursive LLM-based content reduction.\n\n### Data Flow\n1.  **Acquisition**: The `Runner` secures a repository lock.\n2.  **Discovery**: Files are mapped into a hierarchical `DirNode` tree structure.\n3.  **Change Detection**: The `Orchestrator` compares current file SHA256 hashes against the `MetadataCache`. \"Affected\" status propagates up the `DirNode` tree.\n4.  **Hierarchical Synthesis**:\n    *   **Leaf Processing**: Files are processed via `Chunking`. If content exceeds the context window (using a 0.75 `contextWindowAllocRatio`), recursive reduction is applied.\n    *   **Aggregation**: Extracted information is aggregated from the file level up to the module and directory levels.\n5.  **Persistence**: Synthesized Markdown and `MetadataCache` (JSON) are written to the `docsDir`.\n\n### Core Components\n*   **`Synthesizer`**: Traverses the `DirNode` tree to decide between using cached summaries or triggering new LLM extractions.\n*   **`Chunking`**: Implements recursive reduction for oversized segments. Recursion terminates if the output size is $\\ge$ 95% of the input.\n*   **`MetadataCache`**: Persists SHA256 fingerprints to facilitate incremental updates.\n*   **`LLM Client`**: A synchronous HTTP client communicating via the `api/chat` protocol.\n\n---\n\n## External Tooling Configurations\n\n### `mkdocs.yml`\nDefines the deployment parameters for the generated documentation wiki.\n*   **Domain Logic**: Orchestrates the Map-Reduce engine and local LLM interaction.\n*   **External I/O**: Fetches frontend assets (JS/CSS) from `cdn.jsdelivr.net` and `cdnjs.cloudflare.com`.\n\n### `sonar-project.properties`\nConfigures static analysis and code coverage reporting.\n*   **Filtering Logic**: Excludes test files, dependencies, and auxiliary assets from source analysis.\n*   **I/O**: Reads from the current directory and the `coverage.out` report.",
    "cmd": "# Module: `cmd`\n\n## Module Overview\nThe `cmd` module serves as the command-line interface (CLI) orchestration layer. It manages the lifecycle of project documentation generation by coordinating user input, configuration persistence, git repository validation, and the execution of the underlying documentation engine.\n\n## Data Flow\n1.  **Input Acquisition**: CLI arguments are parsed via `RootCmd`. User-provided flags (`--model-id`, `--num-ctx`) and interactive input (`os.Stdin`) are captured.\n2.  **Environment Validation**: The module verifies the presence of a git repository and determines the current working directory via `os.Getwd`.\n3.  **State Assessment**: The module checks for existing configuration files and determines if the project is in an initialized state to validate the requested operation (`init` vs. `update`).\n4.  **Configuration Resolution**: Configuration is loaded from disk, merged with defaults, and optionally updated via an interactive setup flow.\n5.  **Execution Dispatch**: The validated configuration and mode are passed to the `engine` package to perform documentation generation or incremental updates.\n6.  **Output Stream**: Results, status updates, and errors are streamed to `stdout` and `stderr`.\n\n## Core Components\n\n### Command Orchestration (`root.go`)\nThe central logic for the CLI lifecycle is contained within this file.\n\n*   **`RootCmd`** (`*cobra.Command`): The entry point for the CLI application.\n*   **`executeCommand(mode engine.Mode) error`**: The primary controller. It performs the following sequence:\n    *   Resolves the working directory.\n    *   Verifies the git repository status.\n    *   Triggers `checkAndRunSetup` if configuration is missing and `os.Stdin` is a TTY.\n    *   Resolves the final configuration.\n    *   Validates that the current project state matches the requested `engine.Mode`.\n    *   Dispatches execution to `runEngine`.\n*   **`checkAndRunSetup(repoRoot string) error`**: Evaluates if `RunSetupFlow` is required based on the existence of configuration files and terminal availability.\n*   **`checkInitStatus(repoRoot, docsDir string, mode engine.Mode) error`**: Enforces state transition rules:\n    *   `ModeInit` is prohibited if the project is already initialized.\n    *   `ModeUpdate` is prohibited if the project is not yet initialized.\n*   **`runEngine(repoRoot string, cfg *config.Config, mode engine.Mode) error`**: Orchestrates engine execution with signal handling for `os.Interrupt` and `syscall.SIGTERM`.\n\n### Command Definitions (`init.go`, `update.go`)\nThese files act as thin wrappers that map CLI subcommands to specific engine modes.\n\n*   **`init` subcommand**: Dispatches `engine.ModeInit` to trigger the initial scanning of the repository and generation of wiki markdown files.\n*   **`update` subcommand**: Dispatches `engine.ModeUpdate` to perform incremental updates by comparing current file states against the last documented git commit.\n\n### Configuration Lifecycle (`setup.go`)\nHandles the interactive provisioning of project settings.\n\n*   **`RunSetupFlow(repoRoot string) error`**: Manages a stateful, interactive session to collect:\n    *   LLM Model ID.\n    *   Ollama Base URL.\n    *   Ollama Context Size.\n    *   File/directory ignore patterns.\n    *   Documentation directory paths.\n*   **Persistence**: Finalized settings are consolidated into a `config.Config` object and written to disk via `config.SaveConfig`.\n\n## State and Side Effects\n\n### Mutable State\n*   **Global Flags**: `modelIDFlag` and `numCtxFlag` are modified during the `init()` phase and persist for the lifetime of the process.\n*   **Command State**: `RootCmd` state is modified via `.SetArgs()` and `.Execute()` during testing.\n\n### External I/O\n*   **Filesystem**:\n    *   Directory/file creation via `os.MkdirAll` and `os.WriteFile`.\n    *   Configuration persistence via `config.SaveConfig` and `config.LoadConfig`.\n    *   Git verification via `tools.VerifyGitRepo`.\n    *   State check via `engine.IsInitialized`.\n*   **Standard Streams**:\n    *   `os.Stdin`: Used for interactive parameter collection.\n    *   `os.Stdout`: Used for status messages and engine events.\n    *   `os.Stderr`: Used for error reporting.\n\n### Error Handling\n*   **Propagation**: Errors are passed up the call stack via return values.\n*   **Wrapping**: `os.Getwd` and `runner.Run` errors are wrapped with context using `%w`.\n*   **Swallowed Errors**: \n    *   `config.LoadConfig` errors are swallowed in `RunSetupFlow` (defaults are returned instead).\n    *   `strconv.Atoi` errors in `promptContextSize` are swallowed (default values are returned instead).\n    *   `RootCmd.Execute()` return values are swallowed in `cmd_test.go`.",
    "internal": "# Code-Reducer System Architecture Reference\n\n## Subsystem: `internal/config`\n\n### Module Responsibility and Data Flow\nThe `internal/config` package manages the lifecycle, hierarchical resolution, and persistence of configuration settings. It enables the merging of settings from multiple sources into a unified `Config` struct to direct the LLM-based pipeline.\n\n**Data Flow Pipeline:**\n1.  **Ingestion**: Aggregation of data from the local filesystem (`.code-reducer.yaml`), environment variables, and CLI arguments.\n2.  **Resolution**: Hierarchical merging based on precedence: CLI flags \u003e Environment variables \u003e Configuration file \u003e System defaults.\n3.  **Validation**: Numerical parameters (e.g., `OllamaNumCtx`) are validated to ensure they are non-zero and valid integers.\n4.  **Output**: Returns a validated `*Config` pointer for LLM prompt construction and execution logic.\n5.  **Persistence**: Configuration changes are serialized to YAML and written via an atomic rename pattern.\n\n### Data Structures\n\n#### `Config`\nThe primary operational schema for the system.\n\n| Field | Type | Description |\n|---|---|---|\n| `ModelID` | `string` | Identifier for the LLM model. |\n| `OllamaBaseURL` | `string` | Base endpoint for the local Ollama API. |\n| `OllamaNumCtx` | `int` | Context window size for the LLM. |\n| `DocsDir` | `string` | Target directory for generated documentation. |\n| `SystemPrompt` | `string` | Primary system message for the LLM. |\n| `ModuleSynthesisPrompt` | `string` | Prompt template for module-level synthesis. |\n| `ArchitecturePrompt` | `string` | Prompt template for architectural analysis. |\n| `FileFactConsolidationPrompt` | `string` | Prompt template for consolidating file facts. |\n| `ExtractionSteps` | `[]ExtractionStep` | Sequence of extraction phases for the pipeline. |\n| `Ignore` | `[]string` | Paths or patterns excluded from analysis. |\n\n#### `ExtractionStep`\nDefines a specific phase in the multi-step LLM extraction process.\n\n| Field | Type | Description |\n|---|---|---|\n| `Name` | `string` | Identifier for the extraction phase. |\n| `Prompt` | `string` | Specific prompt utilized for this phase. |\n\n### Hierarchical Resolution and Validation\nThe `ResolveConfig` function determines the final state of the `Config` instance using the following precedence:\n1.  **Command-line Flags**: `modelIDFlag`, `numCtxFlag`.\n2.  **Environment Variables**: `CODE_REDUCER_MODEL_ID`, `OLLAMA_BASE_URL`, `OLLAMA_NUM_CTX`.\n3.  **Configuration File**: Values from `.code-reducer.yaml`.\n4.  **System Defaults**: Hardcoded values.\n\n**Constraints:**\n*   **Numerical Validation**: `OllamaNumCtx` must be a valid integer and greater than zero. Non-zero/non-integer environment variables result in an error.\n*   **Deduplication**: The `Ignore` slice is deduplicated during resolution.\n*   **Default Logic**: If `ExtractionSteps` is empty in the config file, the system defaults to the `DefaultExtractionSteps` sequence.\n\n### I/O and Persistence\n*   **`ConfigExists`**: Employs `os.Stat` to check for the presence of the configuration file. All errors are swallowed; returns `false` on error.\n*   **`LoadConfig`**: Utilizes `os.ReadFile` and `yaml.Unmarshal`. Errors are wrapped and returned.\n*   **`SaveConfig`**: Implements an atomic write pattern:\n    1.  `yaml.Marshal` the struct to bytes.\n    2.  `os.CreateTemp` a temporary file in the target directory.\n    3.  Write data and invoke `os.Sync` for durability.\n    4.  `os.Chmod` to set permissions.\n    5.  `os.Rename` to atomically replace the target file.\n    *   **Note**: The deferred `os.Remove` does not contain a `recover()`.\n\n---\n\n## Subsystem: `internal/engine`\n\n### Module Responsibility and Data Flow\nThe `internal/engine` module is an orchestration engine using a hierarchical Map-Reduce pattern to transform source code into structured documentation via LLMs. It supports `ModeInit` (initial generation) and `ModeUpdate` (incremental updates).\n\n**Data Flow Lifecycle:**\n1.  **Initialization**: The `Runner` acquires an exclusive repository lock via `security.AcquireLock`.\n2.  **Discovery**: The `Tree` logic converts file paths into a hierarchical `DirNode` structure.\n3.  **Change Detection**: The `Orchestrator` compares SHA256 hashes against `MetadataCache`. Changes propagate upward through the `DirNode` tree to mark nodes as \"affected.\"\n4.  **Hierarchical Synthesis**: \n    *   **Leaf Processing**: Files undergo `Chunking` (recursive reduction if context limits are exceeded) followed by LLM extraction.\n    *   **Aggregation**: Results aggregate from file $\\rightarrow$ module $\\rightarrow$ directory levels.\n5.  **Persistence**: Writes synthesized Markdown and the `MetadataCache` (JSON) to the `docsDir`.\n\n### Core Components\n\n#### Orchestration and Execution\n*   **Runner (`runner.go`)**: Entry point; manages execution lifecycle and repository locking.\n*   **Orchestrator (`orchestrator.go`)**: Manages cache invalidation, directory pruning, and global (root-level) documentation regeneration.\n*   **Synthesizer (`synthesize.go`)**: Traverses the `DirNode` tree; decides between cached summaries or new LLM extractions based on \"affected\" status.\n\n#### Data Processing\n*   **Chunking and Reduction (`chunking.go`)**: Implements recursive reduction for oversized items using expansion (overlapping segments), batching, and consolidation. Recursion terminates if output is $\\ge 95\\%$ of input.\n*   **Markdown Processing (`markdown.go`)**: Strips triple-backtick or JSON fences from LLM output to extract raw content.\n*   **Metadata Cache (`cache.go`)**: Persists SHA256 fingerprints. Only version `1` is supported; version mismatches trigger a clean state.\n*   **Tree and Change Analysis (`tree.go`)**: Tracks file status (`StatusAdded`, `StatusModified`, `StatusDeleted`) and propagates impact up the directory hierarchy.\n\n#### Infrastructure\n*   **LLM Client (`client.go`)**: Synchronous HTTP client for the `api/chat` protocol. Performs no retry logic or circuit breaking.\n*   **Constants (`constants.go`)**:\n    *   `defaultHTTPTimeout`: 10 minutes.\n    *   `contextWindowAllocRatio`: 0.75 (75% for content, 25% for overhead).\n    *   `defaultChunkOverlap`: 800 tokens.\n    *   `maxErrorBodyBytes`: 1 KB.\n*   **Utilities (`utils.go",
    "internal/config": "# Package `internal/config`\n\n## Module Responsibility\nThe `internal/config` package manages the lifecycle, hierarchical resolution, and persistence of configuration settings for the Code-Reducer system. It provides the mechanism to merge settings from multiple sources—including command-line flags, environment variables, and configuration files—into a unified `Config` struct. This configuration directs an LLM-based pipeline through specific extraction and synthesis phases.\n\n### Data Flow\n1.  **Ingestion**: The system gathers data from the local filesystem (via `.code-reducer.yaml`), environment variables, and CLI arguments.\n2.  **Resolution**: A hierarchical merge process applies overrides based on a defined priority: CLI flags \u003e Environment variables \u003e Configuration file values \u003e System defaults.\n3.  **Validation**: Numerical parameters, such as context size, undergo validation to ensure they meet non-zero requirements.\n4.  **Output**: A validated `*Config` pointer is returned for use in LLM prompt construction and execution logic.\n5.  **Persistence**: Configuration changes are serialized to YAML and written to the filesystem using an atomic rename pattern to ensure integrity.\n\n## Data Structures\n\n### `Config`\nThe primary schema representing the system's operational parameters.\n\n| Field | Type | Description |\n|---|---|---|\n| `ModelID` | `string` | The identifier for the LLM model. |\n| `OllamaBaseURL` | `string` | The base endpoint for the local Ollama API. |\n| `OllamaNumCtx` | `int` | The context window size for the LLM. |\n| `DocsDir` | `string` | The target directory for generated documentation. |\n| `SystemPrompt` | `string` | The primary system message for the LLM. |\n| `ModuleSynthesisPrompt` | `string` | Prompt template for module-level synthesis. |\n| `ArchitecturePrompt` | `string` | Prompt template for architectural analysis. |\n| `FileFactConsolidationPrompt` | `string` | Prompt template for consolidating file facts. |\n| `ExtractionSteps` | `[]ExtractionStep` | A sequence of extraction phases for the analysis pipeline. |\n| `Ignore` | `[]string` | A list of paths or patterns to be excluded from analysis. |\n\n### `ExtractionStep`\nDefines a single phase in the multi-step LLM extraction process.\n\n| Field | Type | Description |\n|---|---|---|\n| `Name` | `string` | The identifier for the extraction phase. |\n| `Prompt` | `string` | The specific prompt utilized for this phase. |\n\n## Hierarchical Resolution\n\nThe `ResolveConfig` function implements the precedence logic to determine the final state of a `Config` instance.\n\n### Resolution Precedence\nFor each parameter, the first non-default/non-empty value found in the following order is selected:\n1.  **Command-line Flags** (e.g., `modelIDFlag`, `numCtxFlag`).\n2.  **Environment Variables** (defined by `CodeReducerModelIDEnvKey`, `OllamaNumCtxEnvKey`, and `OllamaBaseURLEnvKey`).\n3.  **Configuration File** (values stored in `.code-reducer.yaml`).\n4.  **System Defaults** (hardcoded values).\n\n### Numerical Validation\nThe resolution of `OllamaNumCtx` requires that the value—whether sourced from a flag, environment variable, or file—is a valid integer and greater than zero. If an environment variable is non-empty but fails `strconv.Atoi` or is $\\le 0$, `ResolveConfig` returns an error.\n\n### List and Default Initialization\n*   **Ignore List**: The `Ignore` slice is deduplicated during resolution.\n*   **Extraction Steps**: If the configuration file provides an empty list for `ExtractionSteps`, the system defaults to the `DefaultExtractionSteps` slice.\n\n## I/O and Persistence\n\n### Filesystem Operations\nThe module utilizes a `ConfigFileName` (`.code-reducer.yaml`) within a specified directory to manage persistence.\n\n*   **`ConfigExists`**: Checks for the existence of the configuration file using `os.Stat`. All errors encountered during this operation are swallowed, and the function returns `false`.\n*   **`LoadConfig`**: Reads the configuration file via `os.ReadFile` and unmarshals the content via `yaml.Unmarshal`. Errors from file reading or YAML parsing are wrapped and returned to the caller.\n*   **`SaveConfig`**: Implements an atomic write pattern to prevent file corruption:\n    1.  Serializes the `Config` struct to YAML bytes via `yaml.Marshal`.\n    2.  Creates a temporary file in the target directory using `os.CreateTemp`.\n    3.  Writes the data and ensures durability via `os.Sync`.\n    4.  Sets file permissions via `os.Chmod`.\n    5.  Closes the file and uses `os.Rename` to atomically replace the target file.\n    *   **Note**: The deferred cleanup (via `os.Remove`) in `SaveConfig` does not contain `recover()`; panics during the deferred execution are not handled.\n\n## Constants and Defaults\n\n### Environment Variable Keys\n| Key | Purpose |\n|---|---|\n| `CODE_REDUCER_MODEL_ID` | Overrides the LLM `ModelID`. |\n| `OLLAMA_BASE_URL` | Overrides the `OllamaBaseURL`. |\n| `OLLAMA_NUM_CTX` | Overrides the `OllamaNumCtx`. |\n\n### Default Extraction Pipeline (`DefaultExtractionSteps`)\nWhen no custom extraction steps are provided, the system executes these four stages:\n1.  **`API_SIGNATURES`**: Extracts public types, functions, and methods.\n2.  **`BUSINESS_LOGIC`**: Synthesizes the primary domain problem and high-level algorithmic steps.\n3.  **`STATE_AND_CONCURRENCY`**: Identifies mutable state and synchronization mechanisms. Returns \"No mutable state\" if none are found.\n4.  **`ERRORS_AND_SIDE_EFFECTS`**: Details external I/O interactions and error propagation. Returns \"No external side effects\" if no I/O is detected.",
    "internal/engine": "# Internal Engine Architecture Documentation\n\n## Module Responsibility and Data Flow\n\nThe `internal/engine` module is an automated documentation orchestration engine that employs a hierarchical Map-Reduce pattern to transform repository source code into structured technical documentation using Large Language Models (LLMs). It facilitates both initial documentation generation (`ModeInit`) and incremental updates (`ModeUpdate`) by implementing a change-detection pipeline based on file integrity hashes and directory-level impact analysis.\n\n### Data Flow Lifecycle\n1.  **Initialization \u0026 Locking**: The `Runner` acquires an exclusive repository lock and ensures the `.gitignore` contains the lockfile.\n2.  **Discovery \u0026 Tree Construction**: `Tree` logic converts a flat list of file paths into a hierarchical `DirNode` structure.\n3.  **Change Detection**: The `Orchestrator` compares current file SHA256 hashes against the `MetadataCache`. Changes are mapped to specific `FileChange` objects and propagated upward through the `DirNode` tree to identify \"affected\" directory nodes.\n4.  **Hierarchical Synthesis**:\n    *   The `Synthesizer` performs a bottom-up traversal of the affected tree branches.\n    *   **Leaf Processing**: Files are read, content is processed via `Chunking` (recursive reduction if context limits are exceeded), and LLM extraction is performed.\n    *   **Aggregation**: Results from files and subdirectories are aggregated and passed to the LLM for higher-level synthesis.\n5.  **Persistence**: Synthesized Markdown documentation and the updated `MetadataCache` are written to the filesystem.\n\n---\n\n## Orchestration and Execution Control\n\n### Runner (`runner.go`)\nServes as the primary entry point for the module. It manages the execution lifecycle, handles repository-level synchronization via `security.AcquireLock`, and delegates core logic to the `Orchestrator`. It supports two operational modes: `ModeInit` and `ModeUpdate`.\n\n### Orchestrator (`orchestrator.go`)\nCoordinates the end-to-end pipeline. It manages the transition from file discovery to hierarchical synthesis. It handles the logic for cache invalidation, directory pruning for deleted files, and determines whether global documentation (e.g., `architecture.md`) requires regeneration based on the impact of changes detected in the root directory.\n\n---\n\n## Hierarchical Synthesis and Data Processing\n\n### Synthesizer (`synthesize.go`)\nImplements the bottom-up reduction algorithm. It traverses the `DirNode` tree, determining for each node whether to use cached summaries or perform new LLM-based extractions based on the \"affected\" status. It manages the recursive aggregation of file-level facts into module-level and directory-level summaries.\n\n### Chunking and Reduction (`chunking.go`)\nAddresses LLM context window constraints through a recursive reduction strategy:\n*   **Expansion**: Splits oversized items into overlapping segments.\n*   **Batching**: Groups items to optimize context window utilization.\n*   **Recursive Reduction**: Applies LLM-based consolidation to batches.\n*   **Convergence Check**: Stops recursion if a reduction step fails to decrease the input size by more than 5% (i.e., output $\\ge$ 95% of input).\n\n### Markdown Processing (`markdown.go`)\nProvides utilities for cleaning LLM output, specifically stripping surrounding triple-backtick Markdown or JSON fences to extract raw content.\n\n---\n\n## State, Change Detection, and Cache Management\n\n### Metadata Cache (`cache.go`)\nProvides a persistence layer for tracking extracted documentation facts and file integrity.\n*   **Versioning**: Only version `1` is accepted; version mismatches trigger a clean cache state.\n*   **Integrity**: Uses SHA256 fingerprints of raw file content at the virtual path relative to the repository root.\n*   **Persistence**: Serializes state to a JSON file in the specified `docsDir`.\n\n### Tree and Change Analysis (`tree.go`)\nImplements dependency impact analysis within the directory hierarchy.\n*   **Status Tracking**: Tracks files as `StatusAdded`, `StatusModified`, or `StatusDeleted`.\n*   **Upward Propagation**: If a child node (file or directory) is marked as affected, its status is recursively propagated to all ancestor `DirNode` objects.\n*   **Impact Detection**: Identifies affected nodes based on missing cache entries, modified hashes, or missing documentation files.\n\n---\n\n## Infrastructure and Communication\n\n### LLM Client (`client.go`)\nAn HTTP client targeting the Ollama `api/chat` protocol. \n*   **Interface**: Abstraction via `llmCaller` allows for decoupling the engine from the specific transport.\n*   **Behavior**: Executes synchronous POST requests. It performs no retry logic on transport-level failures and contains no circuit-breaking mechanisms.\n\n### Configuration and Constants (`constants.go`)\nDefines operational parameters, including:\n*   **Timeouts**: `defaultHTTPTimeout` (10 minutes).\n*   **Context Management**: A `contextWindowAllocRatio` of 0.75 reserves 75% of the context window for primary content, leaving 25% for metadata/overhead.\n*   **Chunking**: `defaultChunkOverlap` (800 tokens).\n*   **Limits**: `maxErrorBodyBytes` (1 KB) caps error response parsing.\n\n### Utilities (`utils.go`)\nProvides filesystem path sanitization for generating `README.md` files and a nil-safe event logging adapter.",
    "internal/security": "# Module: `internal/security` — Path Traversal Prevention and Process-Level Locking\n\n## Module Responsibility\n\nThis module provides two security primitives scoped to a repository root:\n\n1. **Path traversal prevention** via `SafeResolve`, which validates that user-supplied paths resolve within the repository boundary, resolving symlinks on existing ancestor parts before comparison.\n2. **Cross-process lock management** via `SimpleLock`/`AcquireLock`/`Unlock`, which ensures only one process holds exclusive access to a protected resource at any given time through an atomic file-based lock mechanism.\n\nBoth primitives operate against the local filesystem; no network, database, or remote API interactions occur within production code. All errors are returned to callers and propagated via Go's `error` interface; panics are not raised by this module.\n\n## Data Flow Overview\n\n1. Callers invoke `SafeResolve(repoRoot, inputPath)` with a candidate path string and an anchor directory; the function returns either a resolved absolute path confined within `repoRoot` or an error (typically `ErrPathTraversal`).\n2. Callers invoke `AcquireLock(repoRoot)` to obtain a lock handle; if contention is detected—either via OS-level atomic failure of `O_EXCL` or presence of a stale lockfile—the function returns an error wrapping the sentinel `ErrLockHeld`.\n3. When release is needed, callers call `Unlock()` on the returned `*SimpleLock`; this method is idempotent and thread-safe with respect to itself, closing the file descriptor and removing the lockfile from disk under a mutex guard.\n\n## Error Definitions — `errors.go`\n\nThe module declares two sentinel error variables used as context markers during error propagation:\n\n| Sentinel | Declaration Scope | Usage Context |\n|---|---|---|\n| `ErrPathTraversal` | package-level in `internal/security/errors.go` | Returned by `SafeResolve` when the resolved path escapes outside `repoRoot`. The original traversal signal is swallowed and replaced with a formatted message carrying `inputPath` as context. |\n| `ErrLockHeld` | package-level in `internal/security/errors.go` | Returned by `AcquireLock` when another process holds the lock or a stale lockfile persists; returned via `%w` wrapping of the underlying OS error with descriptive context. |\n\nNo functions, imports beyond `errors`, or runtime operations exist in this file. Error propagation from these sentinels (e.g., further wrapping via `fmt.Errorf`) occurs elsewhere, not here.\n\n## Path Resolution — `SafeResolve`\n\n### Signature\n\n```go\nfunc SafeResolve(repoRoot, inputPath string) (string, error)\n```\n\n### Responsibility\n\nResolves a candidate path against an anchor root while preventing escape through symlinks and directory traversal. The function returns the cleaned absolute path if it remains strictly inside the resolved repository boundary; otherwise it returns `ErrPathTraversal`.\n\n### Algorithm Steps\n\n1. Compute the absolute root directory from `repoRoot` via `filepath.Abs`, wrapping any resulting error with `%w`.\n2. Resolve symlinks on the absolute root using `filepath.EvalSymlinks(absRoot)`, again wrapping errors with `%w`.\n3. Walk up from the joined path (`absRoot + inputPath`) until a physically existing ancestor is found via repeated `os.Lstat(current)` calls; each Lstat failure that is not \"not exist\" is wrapped and returned immediately.\n4. Resolve symlinks on the first physically existing ancestor via `filepath.EvalSymlinks(current)`, wrapping errors with `%w`.\n5. Reconstruct the full target path from the resolved ancestor plus all previously-skipped components.\n6. Verify that the reconstructed path remains inside the resolved root; reject if it escapes by returning a wrapped error using `ErrPathTraversal` with `inputPath` as context.\n\n### External I/O Operations\n\n| Operation | Function | Purpose |\n|---|---|---|\n| Absolute path resolution from relative input | `filepath.Abs(repoRoot)` | Establishes the canonical root baseline for comparison |\n| Symlink resolution on root | `filepath.EvalSymlinks(absRoot)` | Prevents symlink-based escape at the boundary |\n| Ancestor existence check | `os.Lstat(current)` loop | Walks up the path tree stopping at first physically existing parent |\n| Symlink resolution on ancestor | `filepath.EvalSymlinks(current)` | Prevents symlink-based escape mid-path |\n\n## Lock Acquisition and Release — `SimpleLock`\n\n### Types\n\n#### `SimpleLock` Struct\n\n| Field | Type | Mutability | Notes |\n|---|---|---|---|\n| `lockPath` | `string` | Modified once during acquisition, then read-only thereafter. Not mutated by any other method. | Absolute path of the lockfile within the repository root. |\n| `file` | `*os.File` | Modified once during acquisition. Closed during unlock and removed from disk afterward. | File descriptor holding the exclusive lock; opened with `O_WRONLY\\|O_CREATE\\|O_EXCL`. |\n| `mu` | `sync.Mutex` | Read via lock/unlock primitives only. Field itself is never reassigned after struct initialization. | Protects the close-and-remove sequence in `Unlock()`, making unlock atomic with respect to itself. |\n| `closed` | `bool` | Set to `true` during unlock; read inside unlock for idempotency check (`if l.closed { return nil }`). | Tracks release state so subsequent calls return immediately without further I/O. |\n\n#### Methods\n\n##### `AcquireLock(repoRoot string) (*SimpleLock, error)`\n\n**Responsibility**: Acquires an exclusive file-based lock within the provided repository root. Uses O_EXCL to ensure atomicity; failure implies another process holds the lock or a stale lockfile exists.\n\n**Algorithm Steps**:\n1. Calls `SafeResolve(repoRoot, LockFileName)` to obtain the canonical lock file path inside the repo root. If this fails, the error propagates directly without wrapping.\n2. Opens the lockfile with `os.OpenFile(lockPath, O_WRONLY\\|O_CREATE\\|O_EXCL, 0644)`. The OS-level atomicity of O_EXCL means failure indicates another writer holds the file or a stale lock persists; this is treated as an error condition requiring manual cleanup by the caller.\n3. Writes the current process PID into the lockfile via `f.Write([]byte(fmt.Sprintf(\"%d\\n\", os.Getpid())))` for identification/inspection purposes. If this write fails, the method closes the file and removes it before returning a wrapped error.\n\n**Error Propagation**:\n- Direct propagation when `SafeResolve` fails (no wrapping).\n- Wraps with `%w` using `ErrLockHeld` plus context string describing stale lockfile when the OS reports `os.IsExist`.\n- On write failure after successful OpenFile: closes file and removes it, then returns a wrapped error that swallows close/remove errors into a single formatted message—only the original write error is surfaced.\n\n##### `Unlock() error` (method on `*SimpleLock`)\n\n**Responsibility**: Releases the lock by closing the file descriptor and removing the lockfile from disk. Idempotent and thread-safe with respect to itself.\n\n**Algorithm Steps**:\n1. Acquires `l.mu.Lock()` at entry; releases via `defer mu.Unlock()`.\n2. Checks `if l.closed { return nil }`; if already closed, returns immediately without further I/O.\n3. Closes the file descriptor (`l.file.Close()`). If this fails, the error is swallowed when reporting removal errors—only surfaces the remove error if it subsequently fails.\n4. Attempts to remove the lockfile from disk via `os.Remove(l.lockPath)`.\n\n**Thread Safety**: The struct owns its own mutex; concurrent calls to `Unlock` on the same instance serialize through `mu.Lock()/defer mu.Unlock()`, making the operation atomic with respect to itself. Concurrent acquisition of different instances each creates independent `SimpleLock` objects with separate mutexes—no contention between instances is modeled or guaranteed by this code.\n\n## Test Coverage — `security_test.go`\n\n### Functions Under Test\n\n| Function | Parameters | Return Values |\n|---|---|---|\n| `SafeResolve(repoRoot, inputPath)` | repository root path; input file/directory path to resolve | resolved absolute path (string); error |\n| `AcquireLock(repoRoot)` | repository root path | lock object (`*SimpleLock`); error |\n\n### Test Fixture Operations\n\nAll operations below target the local filesystem via Go's standard library:\n\n| Operation | Function | Purpose | Error Handling |\n|---|---|---|---|\n| Directory creation for test fixtures | `os.MkdirAll()` | Sets up `src/sub/` directories | Propagated to test framework via `t.Fatal(err)` — terminates immediately, no recovery. |\n| File writing for traversal tests | `os.WriteFile()` | Creates `file.txt`, `..config` | Same as above; propagated to `t.Fatal(err)`. |\n| Symlink creation (conditional) | `os.Symlink()` | Creates a symlink pointing outside the repo root | Swallowed and replaced with `t.Skip(\"Symlinks not supported on this OS/filesystem\")`; test continues if operation fails. |\n\n### Test Assertions\n\n- `security.SafeResolve(repoRoot, inputPath)` returns an error to its caller; tests verify both success and failure paths via a `wantErr` flag comparison (`t.Errorf(...)`).\n- `security.AcquireLock(repoRoot)` returns `(lock, err)`. A second concurrent call failing with `AcquireLock() should have failed when lock is already held` confirms the production code signals contention via error return.\n\n## Concurrency and Synchronization Summary\n\n- No package-level variables are declared anywhere in this module; all mutable state is encapsulated within individual `SimpleLock` instances.\n- `Unlock()` self-synchronizes via `mu.Lock()/defer mu.Unlock()`, making it safe for concurrent calls on the same instance.\n- Concurrent calls to `AcquireLock` on different instances each create their own independent `SimpleLock` with separate mutexes—no contention between instances is modeled or guaranteed by this code.\n- The file-level lock (the `.code-reducer.lock` file itself) provides OS-level mutual exclusion, which complements the in-memory `mu`.\n- No locks/mutexes are used within the test file itself; each `Test*` function runs independently in a single goroutine.",
    "internal/tools": "# Module: `internal/tools` — Repository Analysis Utilities\n\n## Responsibility \u0026 Data Flow\n\nThe `internal/tools` package provides two complementary capabilities for analyzing a code repository's structure: (1) safe file I/O operations with TOCTOU protection and atomic writes, and (2) Git command abstraction for process execution. All functions operate against a local filesystem only; no network calls, database interactions, or external API invocations occur.\n\nThe module's entry point is the `repoRoot` string passed to every public function. Data flows from caller → `internal/tools` package functions → OS subsystems (POSIX file system for I/O operations, git binary via subprocess execution). No mutable state exists within any function scope; all operations are stateless and idempotent with respect to repository content.\n\n---\n\n## File Operations (`file_tools.go`)\n\n### Atomic Write — `WriteFileAtomic`\n\n```go\nfunc WriteFileAtomic(targetPath string, data []byte, perm os.FileMode) error\n```\n\nWrites binary data to `targetPath` using a temp-file + rename pattern that prevents partial or corrupt state if interrupted. The directory is created with mode `0755`. Returns an error on any failure; returns `nil` only after the final rename succeeds.\n\n**Sequence:**\n1. Create temporary file in same directory as target (ensures atomic rename semantics)\n2. Write data to temp file, sync before close for crash safety\n3. Close and flush via defer block with cleanup gated by success flag\n4. Apply `perm` mode via `os.Chmod`\n5. Atomic rename from temp path into final location\n\nErrors at any step are wrapped; deferred cleanup runs on failure paths to prevent partial temp file leakage. No panic/recover anywhere in this function.\n\n### Safe Read — `ReadFileSafely`\n\n```go\nfunc ReadFileSafely(repoRoot string, virtualPath string) ([]byte, error)\n```\n\nResolves the virtual path against `repoRoot`, then reads via `os.ReadFile`. Wraps any read failure with context (\"failed to read file content\"). The path resolution error from `security.SafeResolve` propagates unwrapped.\n\n### Safe Write — `WriteFileSafely`\n\n```go\nfunc WriteFileSafely(repoRoot string, virtualPath string, content []byte) error\n```\n\nDelegates to both `ReadFileSafely` (for path resolution) and `WriteFileAtomic`. Unwrapped `security.SafeResolve` error propagates through. Otherwise defers to `WriteFileAtomic`'s return value.\n\n### Gitignore Integration (`file_tools.go`)\n\n#### Lockfile Recording — `EnsureGitignoreHasLockfile`\n\n```go\nfunc EnsureGitignoreHasLockfile(repoRoot string) error\n```\n\nEnsures `.gitignore` contains a line matching `security.LockFileName`. Reads the existing file, appends the lock entry if absent (adding a newline before it when needed), and writes back atomically. Returns an error on read/write failures. Read errors from `.gitignore` (except `os.IsNotExist`) are wrapped with \"error reading .gitignore\". A non-existent `.gitignore` falls through to the write path, which will fail if the directory does not exist; no special handling for that intermediate state.\n\n#### Loading — `LoadGitignore`\n\n```go\nfunc LoadGitignore(repoRoot string) ([]string, error)\n```\n\nReads `.gitignore` from `repoRoot`, strips comments (`#`) and blank lines, returns remaining patterns as `[]string`. Returns `nil, nil` when no `.gitignore` exists (treated as success). Any other read failure propagates unwrapped. Scanner errors appended via `scanner.Err()` at end — if a scan error occurs mid-stream but lines have already been populated, the partial result still gets returned to the caller.\n\n#### Path Filtering — `ShouldIgnoreFile`\n\n```go\nfunc ShouldIgnoreFile(relPath string, gitIgnore *ignore.GitIgnore) bool\n```\n\nReturns `true` when the given relative path should be ignored: matches via the provided GitIgnore object (if non-nil), contains a dot-prefixed component, or ends with `.egg-info`. No I/O; returns only boolean values. No error path.\n\n#### Discovery — `DiscoverCodeFiles`\n\n```go\nfunc DiscoverCodeFiles(repoRoot string, ignores []string) ([]string, error)\n```\n\nRecursively walks `repoRoot` to collect relative file paths that pass ignore filtering. Skips directories whose names start with `.`, end with `.egg-info`, or match the compiled GitIgnore rules. Returns a list of valid relative paths and any walk error. Walk errors are wrapped with context (path + \"error walking\"). Relative-path computation failures are also wrapped. Directory entries matching ignore rules return `filepath.SkipDir`. Non-matching files append to the slice.\n\n**Walk sequence:**\n1. Iterate via `filepath.WalkDir` from `repoRoot`\n2. For each directory entry: skip if name starts with `.`, ends with `.egg-info`, or matches compiled GitIgnore patterns\n3. For each file entry: run both ignore-checks (compiled rules + dot-prefixed / `.egg-info` suffix), collect those that pass\n\n---\n\n## Git Integration (`git_tools.go`)\n\n### Command Execution — `RunGit`\n\n```go\nfunc RunGit(repoRoot string, args ...string) (string, error)\n```\n\nExecutes a git command in the specified repo directory with `--no-pager`. Returns trimmed stdout and any error.\n\n**Sequence:**\n1. Construct invocation: `git --no-pager \u003cargs\u003e` in `repoRoot`\n2. Spawn child process via `exec.Command(\"git\", ...)` — subprocess runs in `repoRoot`\n3. Capture stdout into `bytes.Buffer`, stderr separately buffered\n4. If `cmd.Run()` succeeds: trim whitespace from captured stdout, return as string result\n5. If `cmd.Run()` fails: trim whitespace from captured stderr, wrap with `fmt.Errorf(\"git command failed: %v, stderr: %s\", err, trimmedErr)`\n\nThe original error from `exec.Cmd` (e.g., `*exec.Error`, non-zero exit status) is embedded as `%v`; stderr text appended. No panic occurs on failure.\n\n### Repository Validation — `VerifyGitRepo`\n\n```go\nfunc VerifyGitRepo(repoRoot string) error\n```\n\nChecks that `git` is available and that `repoRoot` is inside a git working tree. Invokes `git rev-parse --is-inside-work-tree`. If the returned error is non-nil, wraps with: `fmt.Errorf(\"not a git repository (or any of the parent directories): %w\", err)`. Uses `%w` for wrapError-compatible wrapping (recoverable by unwrappers).\n\n---\n\n## Error Propagation Patterns\n\n### Disk I/O — File Operations\n\nAll functions interact exclusively with the local filesystem via standard OS calls (`os.ReadFile`, `os.CreateTemp`, `os.MkdirAll`, `os.Rename`, `os.Chmod`, `os.Open`). No network, database, or external API interactions exist.\n\n| Function | I/O Type | External System |\n|---|---|---|\n| `ReadFileSafely` | Disk read (`os.ReadFile`) | Local filesystem — target path resolved through `security.SafeResolve` before reading |\n| `WriteFileAtomic` | Disk write (create temp → sync → chmod → rename) | Local filesystem — writes to a `.tmp.*` intermediate file then renames into place |\n| `WriteFileSafely` | Disk read + disk write | Delegates path resolution and atomic write; no additional I/O beyond what the two sub-functions perform |\n| `EnsureGitignoreHasLockfile` | Disk read (`os.ReadFile`) + disk write (`WriteFileAtomic`) | Reads `.gitignore`, appends lockfile pattern, writes back atomically |\n| `LoadGitignore` | Disk read (`os.Open` + `bufio.Scanner`) | Reads `.gitignore`; returns `nil, nil` when file does not exist (no error) |\n| `DiscoverCodeFiles` | Disk walk (`filepath.WalkDir`) | Recursively walks the repository root; skips directories matching ignore rules or dot-prefixed names |\n\n### Process I/O — Git Operations\n\nThe only external interaction is subprocess execution against the installed `git` binary. Stdout captured to `bytes.Buffer`, stderr separately buffered. `cmd.Run()` executes the command and returns the exit error.\n\n| Function | System | Mechanism |\n|---|---|---|\n| `RunGit` | External process (git) | `exec.Command(\"git\", ...)` via `os/exec` — spawns child git subprocess in `repoRoot`. Stdout captured to `bytes.Buffer`, stderr separately buffered. `cmd.Run()` executes the command and returns the exit error. |\n| `VerifyGitRepo` | External process (git) | `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")` — delegates to `RunGit` for execution; wraps non-nil return value with repository classification error |\n\n---\n\n## Concurrency \u0026 State\n\nNo mutable state exists within any function scope. All operations are stateless and idempotent with respect to repository content. No goroutine spawning or concurrent access patterns observed in this module.\n\n---\n\n## Notable Observations\n\n- **TOCTOU mitigation**: `WriteFileAtomic` uses a temp file + rename pattern, which avoids race conditions on the target path during writes\n- **Sync before close**: `tmpFile.Sync()` is called before `Close()`, ensuring data reaches stable storage before the handle is released — relevant for crash safety but adds latency per write\n- **Deferred cleanup in WriteFileAtomic**: On any error, `tmpFile.Close()` and `os.Remove(tmpName)` run via defer. This prevents partial temp file leakage on failure paths. The flag `success` gates whether to trigger cleanup\n- **No panic/recover anywhere** — all errors are returned as typed values. No `panic`, no bare recovery blocks observed in this module or its tests"
  }
}