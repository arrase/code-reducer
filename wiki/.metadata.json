{
  "version": 1,
  "steps_hash": "61d594e4ff13def120bfbc4dbb8f7556458b5da07bca0af9ff454967f8afc215",
  "files": {
    "cmd/init.go": {
      "sha256": "995bf2f9da724383666ec4f58da72bdea798d316266511b84faae8447fdd4682",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface (cmd/init.go)\n\nThis file defines **no exported or public elements**. All identifiers follow Go's unexported naming conventions:\n\n| Element | Visibility | Notes |\n|---|---|---|\n| `initCmd` | Unexported (`var`) | Cobra command struct literal; lowercase |\n| `init()` | Unexported (`func`) | Registers the cobra command with `RootCmd`; references an external `RootCmd` not defined in this file |\n\n**Conclusion:** No public API surface is exposed by this file.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code establishes a CLI entry point for initializing project documentation. It triggers an engine operation that scans a repository and generates initial wiki markdown pages. The command is wired into a root cobra command tree, meaning the application supports multi-step operations (init being one of them).\n\n## High-Level Algorithm Steps\n\n1. Register an `init` subcommand under the root CLI command\n2. On invocation, delegate to an engine operation identified by `ModeInit`\n3. The engine scans the repository structure and produces a first-pass set of wiki markdown files\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis: `cmd/init.go`\n\n**No direct external side effects visible in this file.** This file is a thin CLI command wrapper. All actual work is delegated to `internal/engine.ExecuteCommand(engine.ModeInit)`. Since that module's source was not provided, its internal interactions (network calls, disk writes, database queries, etc.) cannot be analyzed here — those must be inferred from the engine package separately.\n\n**Dependencies:**\n- `github.com/arrase/code-reducer/internal/engine` — local package; external I/O behavior unknown without its source.\n- `github.com/spf13/cobra` — CLI framework; no external I/O in this file.\n\n## Error Propagation\n\nThe error handling pattern is straightforward and follows cobra conventions:\n\n```go\nRunE: func(cmd *cobra.Command, args []string) error {\n    if err := executeCommand(engine.ModeInit); err != nil {\n        return err  // propagate up to cobra's error handling\n    }\n    return nil  // success path\n},\n```\n\n- If `executeCommand` returns a non-nil error, it is returned directly to the caller (cobra), which will typically print a usage message and exit with status code 1.\n- No wrapping, retry logic, or panic recovery is present — errors pass through as-is."
    },
    "cmd/root.go": {
      "sha256": "a3dd44796f2522c491a523b2077a5c5620eb793d5a503d6db45c5fd7184c76ca",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `cmd` package (`root.go`)\n\n## Functions (unexported / local)\n\n### `executeCommand(mode engine.Mode) error`\n- **Parameters**:\n  - `mode engine.Mode` — LLM execution mode string\n- **Returns**:\n  - `error` — documentation run failure or setup prompt\n\n---\n\n## Variables (unexported / local)\n\n### `RootCmd *cobra.Command`\n- Cobra root command instance for the CLI. Configures usage, completion options, and silence flags.\n\n### `modelIDFlag string`\n- Persistent flag variable bound to `--model-id`. Stores LLM model ID value.\n\n### `numCtxFlag string`\n- Persistent flag variable bound to `--num-ctx`. Stores Ollama context window size value.\n\n---\n\n## Callback Closure (local, defined inline)\n\nThe closure passed to `runner.Run`:\n\n```go\nfunc(ev engine.Event) { ... }\n```\n\n- **Parameters**:\n  - `ev engine.Event` — emitted event from the runner\n- **Behavior**: Routes events by type (`EventStatus`, `EventError`) and prints or errors to stdout/stderr.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:** This code implements the entry point for a CLI tool called \"Code-Reducer\" — a local-first AI agent that writes and maintains project wikis by analyzing source code and generating documentation. It wraps an LLM engine to orchestrate doc generation across a git repository.\n\n**High-Level Algorithm Steps:**\n\n1. **Parse command-line arguments** — Accept `--model-id` (LLM model identifier) and `--num-ctx` (context window size) as persistent flags; resolve operating environment (terminal vs non-terminal).\n\n2. **Verify working directory is a git repository** — Fail fast if not, ensuring the tool operates on version-controlled codebases.\n\n3. **Handle missing configuration via implicit setup flow** — If no config file exists and stdin is a terminal, run an automatic setup routine; otherwise return an error directing the user to `setup` subcommand.\n\n4. **Merge configuration sources** — Combine persisted config (from disk) with CLI-override flags into a single resolved configuration object.\n\n5. **Mode-specific preconditions:**\n   - `init`: Reject if already initialized (force use of `update`).\n   - `update`: Reject if not yet initialized (require prior `init` step).\n\n6. **Register graceful shutdown signals** — Install handlers for `SIGINT` and `SIGTERM` so the runner can terminate cleanly on user interruption.\n\n7. **Execute documentation run flow** — Instantiate an engine runner with the resolved config, invoke it against the target repo root and requested mode, stream events back to stdout/stderr as they occur (status, errors, regular output).\n\n#### [STATE_AND_CONCURRENCY]\n**Global Variables / Mutable State:**\n\n| Variable | Type | Modified? | Notes |\n|---|---|---|---|\n| `modelIDFlag` (package-level) | `string` | Yes — via `StringVar(\u0026modelIDFlag, ...)` in `init()` | Set once at startup; read later by `executeCommand`. No synchronization needed as access is single-threaded. |\n| `numCtxFlag` (package-level) | `string` | Yes — via `StringVar(\u0026numCtxFlag, ...)` in `init()` | Same pattern as above. |\n\n**Synchronization Mechanisms:** None observed in this file (no locks, mutexes, sync primitives).\n\n**Thread Safety Assessment:** The mutable globals are set during `init()` and read from a single goroutine path (`executeCommand`), so no concurrent writes occur. No race conditions can be identified from this file alone.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n| Interaction | Type | Description |\n|---|---|---|\n| `os.Getwd()` | Disk (filesystem read) | Reads current working directory from the filesystem. Wrapped in a formatted error if it fails. |\n| `tools.VerifyGitRepo(repoRoot)` | External (git repo validation) | Likely performs disk I/O to check git repository structure/validity. Error propagated directly without wrapping. |\n| `config.ConfigExists(repoRoot)` | Disk (filesystem read) | Checks whether a configuration file exists on disk. Boolean result only, no error path from this call. |\n| `isatty.IsTerminal(os.Stdin.Fd())` / `isatty.IsCygwinTerminal(os.Stdin.Fd())` | Stdio metadata read | Reads the stdin file descriptor to determine terminal type. Not network or database I/O. |\n| `RunSetupFlow(repoRoot)` | External (likely disk write) | Implicit setup flow; likely writes configuration files to disk. Error propagated directly. |\n| `config.ResolveConfig(repoRoot, modelIDFlag, numCtxFlag)` | Disk (filesystem read) | Reads merged configuration from disk. Error propagated directly. |\n| `engine.IsInitialized(repoRoot, cfg.DocsDir)` | External (disk check) | Likely checks filesystem for initialization markers in the docs directory. No explicit error return observed. |\n| Signal handling (`context.Background()`, `os.Interrupt`, `syscall.SIGTERM`) | OS signal | Registers signal handlers on a background context; does not constitute external I/O. |\n\n## Error Propagation\n\n- **Wrapped errors**: `os.Getwd()` failure is wrapped with `fmt.Errorf(\"failed to get current working directory: %w\", err)`.\n- **Direct pass-through**: Errors from `VerifyGitRepo`, `RunSetupFlow`, and `ResolveConfig` are returned as-is without wrapping.\n- **Contextual wrap**: The engine runner's error (`runner.Run(...)`) is wrapped with `\"documentation run failed: %w\"`.\n- **Silenced errors**: `RootCmd.SilenceErrors: true` means any unhandled errors from Cobra's command execution (e.g., flag parsing failures, subcommand errors) are swallowed and not printed.\n- **Signal context cancellation**: If the context is cancelled via signal before `runner.Run()` returns an error, the resulting error (if any) would be propagated through `err != nil` check as a wrapped \"documentation run failed\" message."
    },
    "cmd/setup.go": {
      "sha256": "0d8eaf91b4a8c5e59c330e7512bbc723ce96d6c622a0c3d71f43f6dd07c1b156",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface for `cmd/setup.go`\n\n## Functions\n\n### RunSetupFlow\n\n```go\nfunc RunSetupFlow(repoRoot string) error\n```\n\n**Parameters:**\n- `repoRoot string` — The root directory of the repository (obtained from current working directory).\n\n**Returns:**\n- `error` — Returns an error if configuration cannot be saved.\n\n---\n\n### promptStringList\n\n```go\nfunc promptStringList(reader *bufio.Reader, promptMsg string, existingList []string) ([]string, bool)\n```\n\n**Parameters:**\n- `reader *bufio.Reader` — The reader for stdin input.\n- `promptMsg string` — The prompt message to display.\n- `existingList []string` — Existing list values to use as defaults.\n\n**Returns:**\n- `[]string` — Updated or existing ignore list.\n- `bool` — Whether the user modified the list (`true`) or kept existing values (`false`).\n\n---\n\n### promptString\n\n```go\nfunc promptString(reader *bufio.Reader, promptMsg string, existingVal string) string\n```\n\n**Parameters:**\n- `reader *bufio.Reader` — The reader for stdin input.\n- `promptMsg string` — The prompt message to display.\n- `existingVal string` — Existing value to use as default if user cancels or clears input.\n\n**Returns:**\n- `string` — User-provided value, or the existing/default value if no valid input is given.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code solves the interactive first-run / re-configuration problem for an AI-assisted code analysis tool. It guides users through entering all required settings so they can be persisted to a local configuration file (`.code-reducer.yaml`). The setup wizard is designed to reuse any previously saved values, allowing partial re-configuration rather than starting from scratch each time.\n\n## High-Level Algorithm Steps\n\n1. **Determine repository root** — resolve the current working directory as the anchor for all subsequent paths and config location.\n\n2. **Load existing configuration** (if present) — read the YAML file; if a prior config exists, extract its values as starting points so the user only needs to change what they want to change.\n\n3. **Prompt for LLM Model ID** — display current value as hint; accept new input or fall back to the default Ollama model identifier when empty.\n\n4. **Prompt for Ollama Base URL** — same pattern: show existing, return existing on empty input.\n\n5. **Prompt for Context Size (numCtx)** — read string input, parse to integer with validation; if invalid or zero, revert to the previously loaded value.\n\n6. **Prompt for Ignore Patterns** — accept comma-separated list of directory/file/pattern strings; support `\"clear\"` / `\"none\"` to reset the ignore list entirely; otherwise keep prior values when no new input is provided.\n\n7. **Prompt for Documentation Directory Path** — show existing path as hint, return previous value on empty response.\n\n8. **Prompt for Extraction Steps** — if a prior config existed with extraction steps defined, reuse them; otherwise start from the built-in defaults.\n\n9. **Prompt for System Prompts** (four separate fields: system prompt, module synthesis prompt, architecture prompt, file fact consolidation prompt) — preserve any previously set values when the user leaves input empty or omits it.\n\n10. **Assemble and persist configuration** — construct a new `Config` struct combining fresh user input with preserved defaults/previous values; write it to disk via the config loader.\n\n#### [STATE_AND_CONCURRENCY]\nGlobal Variables (package-level):\n  - setupCmd (*cobra.Command) — field-only, not read or modified after init(). No mutable state impact.\n\nShared State / Class-Level Properties: None.\n\nSynchronization Mechanisms: None required.\n\nAnalysis: All variables in RunSetupFlow are stack-scoped locals. The existingCfg pointer is copied from a returned config.Config and only referenced within this single function invocation (no concurrent access). The newCfg struct literal is constructed locally and passed to config.SaveConfig, which writes to disk—this is not shared state.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n| Interaction | Direction | Mechanism | Notes |\n|---|---|---|---|\n| **Stdin reading** (user input) | Read | `bufio.NewReader(os.Stdin)` — used by `promptString` and `promptStringList` | Synchronous terminal reads; no buffering to external system |\n| **Current working directory** | Read | `os.Getwd()` in `RunSetupFlow` | Local filesystem query; error is wrapped with `%w` on failure |\n| **Config file load** | Read | `config.LoadConfig(repoRoot)` | Reads `.code-reducer.yaml` from disk at `repoRoot`; result used as fallback values for prompts |\n| **Config file save** | Write | `config.SaveConfig(repoRoot, newCfg)` | Writes updated configuration to disk; error is wrapped with `%w` on failure |\n\nNo network calls, database access, or API interactions are present in this file.\n\n## Error Propagation\n\n- **`os.Getwd()` failure**: Returns a formatted error via `return fmt.Errorf(\"failed to get current working directory: %w\", err)`. Caller receives the wrapped error; no panic/crash.\n- **`config.LoadConfig(repoRoot)` failure**: Treated as \"no existing config.\" Existing values default to their zero-state equivalents (`existingCfg` remains nil). No error is returned; flow continues normally.\n- **Stdin read errors** (both `promptString` and `promptStringList`): Errors are logged to `os.Stderr` with a warning message, then the function returns existing/fallback values instead of propagating upward. No exception escapes these functions.\n- **`strconv.Atoi(ctxInputStr)` failure**: Treated as invalid input; `numCtx` falls back to `existingNumCtx`. No error returned; flow continues normally.\n- **`config.SaveConfig(repoRoot, newCfg)` failure**: Wrapped with `return fmt.Errorf(\"error saving configuration: %w\", err)`. This is the only path that returns a non-nil error from `RunSetupFlow`, so it propagates directly to the cobra command's `RunE` and ultimately to the CLI user.\n- **No panics or crashes** observed in this file."
    },
    "cmd/update.go": {
      "sha256": "56faface0ab0f0c994482e1c88b145dffec088858bd445dc56a63df977f4b638",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface — `cmd/update.go`\n\n**Note:** This file contains no exported (uppercase) identifiers. All public-facing functionality is wired through the unexported `updateCmd` variable and auto-called `init()` function, which are implementation details of the private `cmd` package.\n\n### No exported elements found\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis code implements the `update` subcommand for a CLI tool that manages project documentation. It triggers an incremental update of wiki pages by scanning files changed since the last documented commit and applying those changes to existing wiki content.\n\n## High-Level Algorithm Steps\n\n1. Register a new cobra command named `update` under the root command\n2. On execution, delegate to the internal engine with mode `ModeUpdate`\n3. The engine scans for modified files compared against the last committed documentation baseline\n4. Updated content is merged back into existing wiki pages\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External Side Effects:**\n\n- The RunE handler in `updateCmd` is a pure delegation wrapper. It calls `executeCommand(engine.ModeUpdate)` and returns whatever error that call produces. All actual I/O (network, disk, database) happens inside the unshown `engine` package — this file itself performs none.\n- No explicit network calls, no file reads/writes, no database queries are visible in this file's source.\n\n**Error Propagation:**\n\n- Errors propagate upward through the Cobra command chain via the standard `RunE func(cmd *cobra.Command, args []string) error` signature:\n  - If `executeCommand(engine.ModeUpdate)` returns an `error`, it is returned directly to the caller (the CLI invocation path).\n  - If it returns `nil`, the function returns `nil` — Cobra treats this as a successful execution.\n- No panic, no silent swallow. Any error from the engine's update logic surfaces at the top level of the CLI.\n\n**Summary:** This file is a thin command-definition shell; all external behavior and error handling live in the unreviewed `internal/engine` package."
    },
    "internal/config/config.go": {
      "sha256": "37a32106335c54cf442cac91b4468c05a2352a908e509cdf918d6c7ccc3e9993",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `internal/config`\n\n## Types\n\n### ExtractionStep\n\n```go\ntype ExtractionStep struct {\n    Name   string `yaml:\"name\"`\n    Prompt string `yaml:\"prompt\"`\n}\n```\n\n**Fields:**\n\n| Field | Type | Tags |\n|-------|------|------|\n| `Name` | `string` | `yaml:\"name\"` |\n| `Prompt` | `string` | `yaml:\"prompt\"` |\n\n---\n\n### Config\n\n```go\ntype Config struct {\n    ModelID                     string           `yaml:\"model_id\"`\n    OllamaBaseURL               string           `yaml:\"ollama_base_url\"`\n    OllamaNumCtx                int              `yaml:\"ollama_num_ctx\"`\n    DocsDir                     string           `yaml:\"docs_dir\"`\n    SystemPrompt                string           `yaml:\"system_prompt\"`\n    ModuleSynthesisPrompt       string           `yaml:\"module_synthesis_prompt\"`\n    ArchitecturePrompt          string           `yaml:\"architecture_prompt\"`\n    FileFactConsolidationPrompt string           `yaml:\"file_fact_consolidation_prompt\"`\n    ExtractionSteps             []ExtractionStep `yaml:\"extraction_steps\"`\n    Ignore                      []string         `yaml:\"ignore\"`\n}\n```\n\n**Fields:**\n\n| Field | Type | Tags |\n|-------|------|------|\n| `ModelID` | `string` | `yaml:\"model_id\"` |\n| `OllamaBaseURL` | `string` | `yaml:\"ollama_base_url\"` |\n| `OllamaNumCtx` | `int` | `yaml:\"ollama_num_ctx\"` |\n| `DocsDir` | `string` | `yaml:\"docs_dir\"` |\n| `SystemPrompt` | `string` | `yaml:\"system_prompt\"` |\n| `ModuleSynthesisPrompt` | `string` | `yaml:\"module_synthesis_prompt\"` |\n| `ArchitecturePrompt` | `string` | `yaml:\"architecture_prompt\"` |\n| `FileFactConsolidationPrompt` | `string` | `yaml:\"file_fact_consolidation_prompt\"` |\n| `ExtractionSteps` | `[]ExtractionStep` | `yaml:\"extraction_steps\"` |\n| `Ignore` | `[]string` | `yaml:\"ignore\"` |\n\n---\n\n## Variables\n\n### DefaultExtractionSteps\n\n```go\nvar DefaultExtractionSteps = []ExtractionStep{ ... }\n```\n\n**Type:** `[]ExtractionStep`\n\n---\n\n## Constants\n\n| Constant | Value | Type |\n|----------|-------|------|\n| `CodeReducerModelIDEnvKey` | `\"CODE_REDUCER_MODEL_ID\"` | `string` |\n| `OllamaBaseURLEnvKey` | `\"OLLAMA_BASE_URL\"` | `string` |\n| `OllamaNumCtxEnvKey` | `\"OLLAMA_NUM_CTX\"` | `string` |\n| `OllamaDefaultBaseURL` | `\"http://localhost:11434\"` | `string` |\n| `OllamaDefaultModelID` | `\"ornith:9b\"` | `string` |\n| `OllamaDefaultNumCtx` | `8192` | `int` |\n| `DefaultDocsDir` | `\"wiki\"` | `string` |\n| `ConfigFileName` | `\".code-reducer.yaml\"` | `string` |\n| `DefaultSystemPrompt` | *(multiline string)* | `string` |\n| `DefaultModuleSynthesisPrompt` | *(multiline string)* | `string` |\n| `DefaultArchitecturePrompt` | *(multiline string)* | `string` |\n| `DefaultFileFactConsolidationPrompt` | *(multiline string)* | `string` |\n\n---\n\n## Notes\n\n- `configFilePerm` (`0600`) is **unexported** (lowercase identifier) and not listed above.\n- All constant values are literal; no methods or functions were found in this file.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis module provides the configuration schema and default prompts for **Code-Reducer**, an automated code documentation system. The tool analyzes source code files and generates technical documentation by executing a sequence of fact-extraction phases against each file.\n\nThe core pipeline extracts structured knowledge from source code: API surface → business logic → state \u0026 concurrency → errors \u0026 side effects. These extracted facts are then consolidated into cohesive summaries, which feed into module-level synthesis and architecture-level synthesis stages.\n\n## High-Level Algorithm Steps\n\n1. **File Fact Extraction** — For each file, run four sequential extraction steps (API signatures, business logic, mutable state/concurrency, external I/O \u0026 error propagation) to produce structured facts.\n\n2. **Module Synthesis** — Group related functions and classes under Markdown headings; explain the module's responsibility and data flow using extracted facts.\n\n3. **Architecture Synthesis** — Combine module summaries into a global architecture document or quickstart guide that explains system boundaries and inter-module interactions.\n\n4. **File Fact Consolidation** — Deduplicate and merge overlapping facts from different chunks of the same file into single, cohesive summaries before synthesis consumes them.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** This file is purely declarative: it defines constants, types, and default values. No runtime behavior exists.\n\n- **Network**: None. Constants like `OllamaDefaultBaseURL = \"http://localhost:11434\"` are string literals only; no HTTP client or network calls are made.\n- **Disk**: None. No file operations (`os.Open`, `ioutil.ReadFile`, etc.) occur. The filename `.code-reducer.yaml` is a constant, not used for reading/writing at runtime in this file.\n- **Database/APIs**: None. No database drivers or API clients are referenced.\n\n## Error Propagation Analysis\n\n**No error propagation.** This file contains no function calls, no panic statements, and no recover blocks. Errors cannot be raised or swallowed here because the file performs zero operations that could fail."
    },
    "internal/config/io.go": {
      "sha256": "9af0f95e0eb8f5bac5679053c283565338e4316f1f7065063a26c41df24532cf",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface\n\n### Functions\n\n#### ConfigExists\n\n```go\nfunc ConfigExists(cwd string) bool\n```\n\n**Parameters:**\n\n| Name | Type | Description |\n|------|------|-------------|\n| `cwd` | `string` | The directory path to check for the configuration file. |\n\n**Returns:**\n\n| Name | Type | Description |\n|------|------|-------------|\n| (return) | `bool` | Whether `.code-reducer.yaml` exists in the specified directory. |\n\n---\n\n#### LoadConfig\n\n```go\nfunc LoadConfig(cwd string) (*Config, error)\n```\n\n**Parameters:**\n\n| Name | Type | Description |\n|------|------|-------------|\n| `cwd` | `string` | The directory path containing `.code-reducer.yaml`. |\n\n**Returns:**\n\n| Name | Type | Description |\n|------|------|-------------|\n| (return) | `*Config` | Parsed configuration pointer. |\n| (return) | `error` | Error if reading or parsing fails. |\n\n---\n\n#### SaveConfig\n\n```go\nfunc SaveConfig(cwd string, cfg *Config) error\n```\n\n**Parameters:**\n\n| Name | Type | Description |\n|------|------|-------------|\n| `cwd` | `string` | The directory path to write the configuration file into. |\n| `cfg` | `*Config` | Pointer to the configuration struct to serialize. |\n\n**Returns:**\n\n| Name | Type | Description |\n|------|------|-------------|\n| (return) | `error` | Error if writing or renaming fails. |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\n\nPersistently store and retrieve an application's YAML configuration file (`.code-reducer.yaml`) using an atomic write pattern — preventing partially-written config files if the process is interrupted mid-write.\n\n**High-Level Algorithm Steps**\n\n1. Resolve the absolute path of the config file by joining the working directory with the fixed filename `.code-reducer.yaml`.\n2. Check whether that path exists on disk (returns a boolean).\n3. Read raw bytes from disk and unmarshal them into an in-memory `Config` struct; return any parse error if the YAML is invalid.\n4. Marshal the `Config` back to YAML, then perform targeted string replacements — inserting blank lines after specific top-level keys (`system_prompt`, `module_synthesis_prompt`, `architecture_prompt`, `file_fact_consolidation_prompt`, `extraction_steps`, `ignore`) so each key starts on its own line with a preceding newline.\n5. Open a temporary file in the same directory, write the formatted YAML bytes to it, and close it.\n6. Apply the desired file permissions (`configFilePerm`) to the temp file.\n7. Atomically rename the temp file over the original config path so that readers either see the old or new content — never a half-written state.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O and Error Propagation Analysis — `io.go` (Module: `internal/config`)\n\n### Functions with Disk Read Access\n\n| Function | Operation | Details |\n|----------|-----------|---------|\n| `ConfigExists(cwd string) bool` | `os.Stat(getConfigPath(cwd))` | Single stat call. Returns error only if file does not exist or path is invalid. No error wrapping — caller receives raw `bool`. |\n| `LoadConfig(cwd string) (*Config, error)` | `os.ReadFile(getConfigPath(cwd))` then `yaml.Unmarshal(data, \u0026cfg)` | Two failure points: (1) I/O error from read propagates through as-is via the returned `(nil, err)` tuple. (2) YAML parse error is wrapped with `\"failed to parse yaml config: %w\"`. |\n\n### Functions with Disk Write Access\n\n| Function | Operation | Details |\n|----------|-----------|---------|\n| `SaveConfig(cwd string, cfg *Config) error` | `os.CreateTemp`, `tmpFile.Write`, `tmpFile.Sync`, `os.Chmod`, `os.Rename` | Atomic write via temp file + rename. Failure points: (1) `os.CreateTemp` — wrapped with `\"failed to create temp file\"`. (2) `Write` — wrapped with `\"failed to write config to temp file\"`. (3) `Sync` — wrapped with `\"failed to sync temp file\"`. (4) `Close` — wrapped with `\"failed to close temp file\"`. (5) `Chmod` — wrapped with `\"failed to chmod temp file\"`. (6) `Rename` — wrapped with `\"failed to rename temp config file\"`. |\n\n### Error Propagation Pattern Summary\n\n- All disk-read and disk-write functions return `(error)` or `(*Config, error)` tuples. Errors are returned directly to the caller; no panic/recover is used.\n- YAML parse errors are explicitly re-wrapped with a descriptive prefix using `fmt.Errorf(\"...%w\")`.\n- Raw OS-level read errors (e.g., file not found, permission denied) from `os.ReadFile` and `os.Stat` propagate through unchanged — no additional wrapping occurs.\n- All write-path errors follow the same wrapping convention: `\"failed to \u003caction\u003e temp config file\"` or similar.\n\n### External Systems Interacted With\n\n| System | Interaction Type | Notes |\n|--------|------------------|-------|\n| Local filesystem (disk) | Read (`os.ReadFile`, `os.Stat`) | Configuration file at `\u003ccwd\u003e.code-reducer.yaml` |\n| Local filesystem (disk) | Write via temp + rename (`os.CreateTemp`, `Write`, `Sync`, `Close`, `Chmod`, `Rename`) | Atomic replacement of config file with same name and permissions |\n\n### No External Side Effects?\n\n**No.** This module performs disk I/O exclusively — no network calls, database interactions, or external API usage. All operations are local filesystem reads/writes to a single configuration file path under the caller-supplied directory."
    },
    "internal/config/resolve.go": {
      "sha256": "38eebcda30d161a4a7aaeadf6cd8eeee3ea0497e9e5e86a371277306b67e1633",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `internal/config/resolve.go`\n\n### Functions\n\n| Function | Parameters | Return Types | Description |\n|---|---|---|---|\n| **ResolveConfig** | `repoRoot string`, `modelIDFlag string`, `numCtxFlag string` | `*Config`, `error` | Merges CLI overrides, environment variables, YAML config, and system defaults into a fully resolved Config struct. |\n\n### Inferred Struct (from usage patterns in this file)\n\n| Field | Inferred Type | Notes |\n|---|---|---|\n| `Ignore` | `[]string` | Deduplicated; confirmed by `deduplicate(cfg.Ignore)` call |\n| `ExtractionSteps` | Unknown | Used as default fallback when empty |\n| `ModelID` | `string` | Resolved via: Default \u003e YAML \u003e Env (`CodeReducerModelIDEnvKey`) \u003e Flag |\n| `OllamaBaseURL` | `string` | Resolved via: Default \u003e YAML \u003e Env (`OllamaBaseURLEnvKey`) |\n| `OllamaNumCtx` | Unknown (likely int) | Resolved via: Default \u003e YAML \u003e Env (`OllamaNumCtxEnvKey`) \u003e Flag |\n| `DocsDir` | Unknown (likely string) | Resolved via: Default \u003e YAML |\n| `SystemPrompt` | Unknown (likely string) | Resolved via: Config value \u003e Default |\n| `ModuleSynthesisPrompt` | Unknown (likely string) | Resolved via: Config value \u003e Default |\n| `ArchitecturePrompt` | Unknown (likely string) | Resolved via: Config value \u003e Default |\n| `FileFactConsolidationPrompt` | Unknown (likely string) | Resolved via: Config value \u003e Default |\n\n### Internal Helpers (not exported, but used internally)\n\n| Function | Parameters | Return Types | Description |\n|---|---|---|---|\n| **deduplicate** | `a []string` | `[]string` | Removes duplicates from a string slice. |\n| **resolveString** | `yamlVal string`, `defaultVal string` | `string` | Returns `yamlVal` if non-empty, otherwise returns `defaultVal`. |\n\n### Constants Referenced (not defined in this file)\n\n| Constant | Used In | Notes |\n|---|---|---|\n| `OllamaDefaultModelID` | `ResolveConfig` ModelID resolution | Default model identifier |\n| `CodeReducerModelIDEnvKey` | `ResolveConfig` env var lookup | Environment variable key for model ID |\n| `OllamaBaseURLEnvKey` | `ResolveConfig` env var lookup | Environment variable key for Ollama base URL |\n| `OllamaNumCtxEnvKey` | `ResolveConfig` env var lookup | Environment variable key for context size |\n| `DefaultDocsDir` | `ResolveConfig` DocsDir resolution | Default documentation directory path |\n| `DefaultSystemPrompt` | `ResolveConfig` SystemPrompt fallback | Default system prompt string |\n| `DefaultModuleSynthesisPrompt` | `ResolveConfig` ModuleSynthesisPrompt fallback | Default module synthesis prompt string |\n| `DefaultArchitecturePrompt` | `ResolveConfig` ArchitecturePrompt fallback | Default architecture prompt string |\n| `DefaultFileFactConsolidationPrompt` | `ResolveConfig` FileFactConsolidationPrompt fallback | Default file fact consolidation prompt string |\n\n\u003e **Note**: The `Config` struct definition is not present in this file. All field types and the full struct are inferred from usage patterns.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis module resolves configuration for an AI code-generation pipeline by merging values from multiple sources (CLI flags, environment variables, YAML config files, and hardcoded defaults) into a single unified `Config` struct consumed downstream by the pipeline runner and LLM client.\n\n## Domain Problem\n\nThe system must support flexible configuration across four precedence tiers while handling:\n- **Graceful degradation**: Missing config files are treated as empty rather than errors, allowing the system to run with only flags/env vars.\n- **Type coercion**: Numeric parameters (context size) arrive as strings from env/flags and must be parsed safely.\n- **Deduplication**: The ignore list may contain duplicates that must be collapsed before use.\n- **Prompt inheritance**: Empty prompt fields fall back to built-in defaults rather than leaving them unset.\n\n## High-Level Algorithm Steps\n\n1. **Load YAML config** from the repository root; if absent, initialize an empty `Config` struct instead of failing.\n2. **Deduplicate** the ignore list via hash-set tracking (first occurrence wins).\n3. **Initialize extraction steps**: Use provided YAML steps, or fall back to a built-in default set when the slice is empty.\n4. **Resolve Model ID** with precedence: hardcoded default → YAML value → environment variable (`CodeReducerModelIDEnvKey`) → CLI flag. Skip later sources if already populated from an earlier source.\n5. **Resolve Ollama Base URL** with precedence: hardcoded default → YAML value → environment variable (`OllamaBaseURLEnvKey`).\n6. **Resolve Ollama Context Size** (numeric): parse as integer only when positive; precedence: hardcoded default → YAML value → environment variable → CLI flag. Reject non-positive or unparseable values by keeping the prior tier's value.\n7. **Resolve DocsDir**: Use YAML value if present, otherwise fall back to a built-in default directory path.\n8. **Resolve prompts** via `resolveString` helper: each prompt field (system, module synthesis, architecture, file fact consolidation) uses its YAML value or falls back to the corresponding hardcoded default when empty.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### File I/O\n- **`LoadConfig(repoRoot)`** — called once at entry. Returns `(*Config, error)`. The file is not read directly in this function; only its result is consumed.\n- No other disk operations are performed by this function.\n\n### Environment / Process I/O\nMultiple calls to **`os.Getenv()`**:\n| Variable | Key (referenced as constant) | Usage |\n|---|---|---|\n| Model ID | `CodeReducerModelIDEnvKey` | Resolved after YAML config value |\n| Ollama Base URL | `OllamaBaseURLEnvKey` | Resolved after YAML config value |\n| Ollama Context Size | `OllamaNumCtxEnvKey` | Resolved after YAML config value; parsed with `strconv.Atoi` and validated for `\u003e 0` |\n\n### Network I/O\n- **None.** No HTTP, TCP, or other network calls are made in this file.\n\n---\n\n## Error Propagation Analysis\n\n### Entry Path — `LoadConfig(repoRoot)` Failure\n```go\ncfg, err := LoadConfig(repoRoot)\nif err != nil {\n    if !os.IsNotExist(err) {\n        return nil, fmt.Errorf(\"failed to load configuration file: %w\", err)\n    }\n    cfg = \u0026Config{}\n}\n```\n\n| Condition | Behavior |\n|---|---|\n| `err == nil` | Normal path; `cfg` is used downstream. |\n| `os.IsNotExist(err)` is **true** (missing file) | Silently ignored; a fresh empty `Config{}` replaces the missing-file state. No error is returned to caller. |\n| Any other error | Wrapped with `%w` via `fmt.Errorf(\"failed to load configuration file: %w\", err)` and returned as `(*Config, error)` — `nil, wrappedError`. Caller receives a non-nil error. |\n\n### All Other Paths in This Function\n- **No errors are generated.** Every other branch (deduplication, env lookups, flag parsing) is error-free by design:\n  - `strconv.Atoi` checks for `err == nil` and skips the value if parsing fails — no error propagation.\n  - String comparisons (`!= \"\"`) and length checks (`len(...) \u003e 0`) are non-error paths.\n- The function always returns `(*Config, error)` with a **non-nil config** but **nil error**, except in the single path above where `LoadConfig` fails with a non-not-exist error."
    },
    "internal/engine/cache.go": {
      "sha256": "25d013e4a38fddd9a9d0f3337c6c2f0f7c33c6fd446313a2d8c32c5c2bc14616",
      "facts": "#### [API_SIGNATURES]\n## Types\n\n- **FileCacheEntry** (struct)\n  - `SHA256` string\n  - `Facts` string\n\n- **MetadataCache** (struct)\n  - `Version` int\n  - `StepsHash` string\n  - `Files` map[string]FileCacheEntry\n  - `Modules` map[string]string\n\n## Functions\n\n- **loadMetadataCache** (`repoRoot`, `docsDir` string) (*MetadataCache, error)\n  - Loads or initializes the metadata cache from disk. Returns an empty cache if missing or version-incompatible.\n\n- **IsInitialized** (`repoRoot`, `docsDir` string) bool\n  - Checks whether the metadata cache file exists.\n\n- **saveMetadataCache** (`repoRoot`, `docsDir` string, `cache` *MetadataCache) error\n  - Serializes and writes the metadata cache to disk.\n\n- **computeSHA256** (`repoRoot`, `virtualPath` string) (string, error)\n  - Computes the SHA-256 hash of a file's content.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code solves **persistent extraction state management**. The cache records per-file processing fingerprints (SHA256 + extracted facts) alongside a global steps hash and module registry, enabling the engine to resume from where it left off across invocations—skipping files whose content hasn't changed.\n\n## High-Level Algorithm Steps\n\n1. **Initialize an empty cache structure** with version metadata, file map, and modules map.\n2. **Load or reconstruct cache state** from disk; if missing or incompatible (version mismatch), return a fresh clean cache rather than failing.\n3. **Compute a fingerprint of the extraction steps** by hashing the step configuration—this anchors cache validity to a specific processing pipeline version.\n4. **Persist the cache** atomically back to the docs directory, tagged with the current version and indented for readability.\n5. **Query file-level hashes** on demand by reading raw content from disk, computing SHA256, returning the hex digest—used as the key for the files map during extraction.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** None identified in this file.\n\n- `currentCacheVersion` is declared as a `const`, so it is immutable by definition.\n- All state in `MetadataCache` (e.g., `Files`, `Modules`) resides on local variables or function parameters; no package-level global mutable variables are present in this file alone.\n- The symbol `metadataFileName` used throughout the functions is not declared in this text, so its location and mutability cannot be confirmed from this source.\n\n**Synchronization Mechanisms:** None visible. No locks, mutexes, channels, `sync.Mutex`, atomic types, or async/await patterns are present in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\nAll external interactions occur through disk file operations on the repository's metadata cache:\n\n| Function | Operation | Path | Direction |\n|---|---|---|---|\n| `loadMetadataCache` | Read file | `{docsDir}/{metadataFileName}` (full path = `{repoRoot}/{docsDir}/...`) | Disk read |\n| `IsInitialized` | Read file (existence check) | Same as above | Disk read |\n| `saveMetadataCache` | Write file | Same as above | Disk write |\n| `computeSHA256` | Read file | `{repoRoot}/{virtualPath}` | Disk read |\n\nAll paths are constructed via `filepath.Join`, which normalizes separators for the host OS. The metadata cache is a single JSON file stored inside the docs directory (not in the repo root directly). `computeSHA256` reads arbitrary files under `repoRoot` identified by a virtual path, so its I/O target depends entirely on how the caller constructs that argument.\n\n## Error Propagation\n\n- **`loadMetadataCache`** — If the file does not exist (`os.ErrNotExist`), returns an empty cache with nil error (silent initialization). Any other read error is wrapped into `\"failed to read metadata cache: ...\"` and returned. JSON unmarshal errors are similarly wrapped. Version mismatch or nil map fields do not produce errors; they cause a clean new cache to be returned.\n- **`IsInitialized`** — Returns `true` only when the underlying file read succeeds. A missing file returns `false`. Other I/O errors from `ReadFileSafely` are swallowed (the function does not check or propagate them).\n- **`saveMetadataCache`** — Marshaling errors and write errors are returned directly to the caller without wrapping.\n- **`computeSHA256`** — File read failures are propagated up as-is. On success, returns the hex-encoded SHA-256 hash with no error.\n\n## Standard Library Packages Used\n\n`os`, `path/filepath`, `crypto/sha256`, `encoding/hex`. The custom config package is imported but not used in this file."
    },
    "internal/engine/chunking.go": {
      "sha256": "2c6748ca9de0f6d94a7e2cfd57ed22bd735b85ec392ddd8e5a8d58335b630c33",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/engine/chunking.go`\n\n## Functions\n\n### `reduceWithLLM`\n\n```go\nfunc reduceWithLLM(\n    ctx     context.Context,\n    c       llmCaller,\n    items   []string,\n    sysPrompt string,\n    buildPrompt func(batch []string) string,\n    logMsg  func(batch []string) string,\n    errMsg  string,\n    logEvent LogEventFunc,\n) (string, error)\n```\n\n- **Parameters:**\n  - `ctx` — `context.Context`: the caller's context for cancellation and deadlines.\n  - `c` — `llmCaller`: an interface providing LLM interaction methods (`NumCtx()`, `CallLLM()`).\n  - `items` — `[]string`: input items to reduce/consolidate.\n  - `sysPrompt` — `string`: system prompt sent with the user message.\n  - `buildPrompt` — `func(batch []string) string`: builds a batch-specific user prompt from a slice of strings.\n  - `logMsg` — `func(batch []string) string`: returns a log message for a given batch.\n  - `errMsg` — `string`: error prefix prepended to LLM errors.\n  - `logEvent` — `LogEventFunc`: callback invoked on status events during processing.\n\n- **Returns:** `(string, error)` — the synthesized result string and an optional error.\n\n---\n\n### `reduceInChunks`\n\n```go\nfunc reduceInChunks(\n    ctx     context.Context,\n    c       llmCaller,\n    nodePath string,\n    items   []string,\n    cfg     *config.Config,\n    logEvent LogEventFunc,\n) (string, error)\n```\n\n- **Parameters:**\n  - `ctx` — `context.Context`: the caller's context.\n  - `c` — `llmCaller`: LLM interface.\n  - `nodePath` — `string`: architecture path used in prompt construction.\n  - `items` — `[]string`: input items to synthesize.\n  - `cfg` — `*config.Config`: configuration providing system prompts and synthesis settings.\n  - `logEvent` — `LogEventFunc`: callback for status events.\n\n- **Returns:** `(string, error)` — synthesized architecture string and an optional error.\n\n---\n\n### `reduceFileFacts`\n\n```go\nfunc reduceFileFacts(\n    ctx     context.Context,\n    c       llmCaller,\n    filePath string,\n    stepName string,\n    items   []string,\n    cfg     *config.Config,\n    logEvent LogEventFunc,\n) (string, error)\n```\n\n- **Parameters:**\n  - `ctx` — `context.Context`: the caller's context.\n  - `c` — `llmCaller`: LLM interface.\n  - `filePath` — `string`: file path referenced in prompt construction.\n  - `stepName` — `string`: step name used in prompt construction.\n  - `items` — `[]string`: input facts to consolidate and deduplicate.\n  - `cfg` — `*config.Config`: configuration providing system prompts and consolidation settings.\n  - `logEvent` — `LogEventFunc`: callback for status events.\n\n- **Returns:** `(string, error)` — consolidated result string and an optional error.\n\n---\n\n### `reduceItems`\n\n```go\nfunc reduceItems(\n    ctx     context.Context,\n    items   []string,\n    maxChars int,\n    reduceFn func(batch []string) (string, error),\n) (string, error)\n```\n\n- **Parameters:**\n  - `ctx` — `context.Context`: the caller's context.\n  - `items` — `[]string`: input string items to reduce iteratively.\n  - `maxChars` — `int`: maximum rune count per batch before splitting into sub-batches.\n  - `reduceFn` — `func(batch []string) (string, error)`: function applied to each batch to produce a condensed result.\n\n- **Returns:** `(string, error)` — the final reduced string and an optional error.\n\n---\n\n### `chunkTextWithOverlap`\n\n```go\nfunc chunkTextWithOverlap(text string, maxRunes int, overlapRunes int) []string\n```\n\n- **Parameters:**\n  - `text` — `string`: input text to split into chunks.\n  - `maxRunes` — `int`: target maximum rune count per chunk.\n  - `overlapRunes` — `int`: desired overlap between adjacent chunks (clamped if ≥ `maxRunes`).\n\n- **Returns:** `[]string` — slice of overlapping text chunks.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code implements an **iterative map-reduce reduction pipeline** that progressively compresses multiple text items into a single coherent summary using LLM calls, with safeguards against infinite loops and context window overflow.\n\nIt solves the problem of taking many extracted facts, observations, or architectural descriptions (each potentially large) and repeatedly asking an LLM to condense them until they fit within a target size constraint.\n\n## High-Level Algorithm Steps\n\n### Step 1: Expand oversized items\nIf any individual item exceeds `maxChars`, split it into overlapping sub-chunks so the information is preserved even if later layers can't fully reconstruct it.\n\n### Step 2: Greedy batch packing\nWalk through all items in order, accumulating them into batches that stay within `maxChars`. When adding the next item would exceed the limit, flush the current batch and start a new one with just that oversized item. Everything remaining is flushed at the end.\n\n### Step 3: Reduce via LLM (single or recursive)\n- If there's exactly one batch, send it to the LLM for summarization and return the result.\n- Otherwise, recursively apply this same algorithm to each batch's reduced output — feeding the previous layer's summaries back into another reduction pass.\n\n### Step 4: Loop prevention (termination condition)\nAfter a recursive round, compute total input run count vs total output run count across all intermediate results. If the output is ≥95% of the input, the LLM is no longer condensing — stop iterating and concatenate all summaries with newlines instead of recursing further.\n\n### Step 5: Markdown fence stripping\nAfter each LLM call, remove any surrounding markdown code fences (` ``` `) so the raw text content is returned cleanly.\n\n## Domain Rules (observed from structure)\n\n- **Preserve information over compression**: When a single item exceeds the size limit, it's split with overlap rather than truncated, because later layers will see oversized inputs and chunk them again.\n- **Order matters for batching**: Items are processed in their original order; greedy packing respects that sequence rather than sorting or reordering.\n- **Recursive depth is bounded implicitly** by the 95% output ratio check — this acts as a safety net against LLMs that fail to condense (e.g., returning prompts verbatim).\n- **Context window sizing**: `maxChars` derives from `c.NumCtx() * maxCharsMultiplier`, meaning the batch size adapts to the available context window.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State Analysis: chunking.go\n\n**No mutable state.**\n\nAll variables are local or function parameters passed by value (slices are not mutated in-place). The `cfg *config.Config` pointer is read-only within these functions—no fields on the struct are modified. All intermediate state (`expanded`, `batches`, `intermediate`, etc.) is scoped to individual function calls with no shared references across goroutines or call frames.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**LLM API Interaction:**\nThe code interacts with an external LLM system exclusively through the `llmCaller` interface (`c.CallLLM`). This is called in three entry points:\n- `reduceInChunks` → `reduceWithLLM` → `c.CallLLM(ctx, sysPrompt, messages, false)`\n- `reduceFileFacts` → `reduceWithLLM` → `c.CallLLM(ctx, sysPrompt, messages, false)`\n\nThe `CallLLM` call is the only function that performs external I/O. All other functions are pure in-memory operations (string splitting, batching, recursion).\n\n**No Disk/DB/API Interactions:**\nNo file system reads/writes, database queries, or HTTP calls to non-LLM APIs are present in this file.\n\n## Error Propagation Analysis\n\n**Return Path Errors:**\nAll six functions propagate errors exclusively through the Go `error` return channel:\n- `reduceWithLLM`: Wraps LLM errors with a prefix using `fmt.Errorf(\"%s: %w\", errMsg, err)`. The original error is preserved via `%w` wrapping.\n- `reduceInChunks`, `reduceFileFacts`: Pass through the wrapped error from `reduceWithLLM` unchanged.\n- `reduceItems`: Propagates two error sources — context cancellation (`ctx.Err()`) and recursive call failures. Context errors are returned directly; LLM recursion errors propagate as-is from the recursive `reduceItems` calls.\n\n**Context Cancellation:**\n`reduceItems` checks `ctx.Err()` at entry and before each recursive layer, returning early with the context error if cancelled. This is a clean cancellation path — no cleanup or partial results are returned on cancel.\n\n**Loop Prevention Path:**\nWhen output reaches ≥95% of input (integer division), the function returns an unprocessed concatenation (`strings.Join(intermediate, \"\\n\\n\")`) without error. This is a deliberate design choice to prevent infinite recursion, not an error case.\n\n**No Panics/Crashes:**\nThe code contains no `panic()` calls or nil-pointer dereferences that would cause crashes. All string operations are guarded (empty checks before indexing, bounds-checked slicing in `chunkTextWithOverlap`).\n\n**Edge Case Handling:**\n- `chunkTextWithOverlap`: Returns the full text as a single chunk if `maxRunes \u003c= 0` or `overlapRunes \u003e= maxRunes`, preventing division-by-zero scenarios and infinite loops."
    },
    "internal/engine/client.go": {
      "sha256": "998aa6b162f007326e91cb271e6fc0cc59413378394cd887f85b8a4a832487cb",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: engine (client.go)\n\n## Types\n\n### Message\n```go\ntype Message struct {\n    Role    string `json:\"role\"`\n    Content string `json:\"content\"`\n}\n```\n\n---\n\n**Note:** The following elements are **unexported** (lowercase first letter) and therefore not part of the public API:\n\n| Element | Type | Notes |\n| --- | --- | --- |\n| `llmCaller` | interface | Unexported interface; methods include `CallLLM(ctx context.Context, systemPrompt string, messages []Message, jsonFormat bool) (string, error)` and `NumCtx() int` |\n| `llmClient` | struct | Unexported struct with fields: `modelID string`, `baseURL string`, `numCtx int`, `httpClient *http.Client` |\n| `newLLMClient` | func | Factory function for `*llmClient`; signature: `func newLLMClient(modelID, baseURL string, numCtx int) *llmClient` |\n| `(c *llmClient) NumCtx()` | method | Returns the configured context window size. |\n| `ollamaRequest` | struct | Unexported request struct; fields: `Model`, `Messages []Message`, `Stream bool`, `Format string`, `Options *ollamaOptions` |\n| `ollamaOptions` | struct | Unexported options struct; field: `NumCtx int` |\n| `ollamaResponse` | struct | Unexported response struct; field: `Message Message` |\n| `(c *llmClient) prepareOllamaRequest()` | method | Private helper to build and serialize Ollama chat requests. |\n\n---\n\n**Note:** The following symbols are referenced but not defined within this file, so they cannot be confirmed as part of the public API:\n\n- `defaultHTTPTimeout` (referenced in `newLLMClient`)\n- `maxErrorBodyBytes` (referenced in `CallLLM`)\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis module provides an HTTP-based LLM client that communicates with the Ollama API's `/api/chat` endpoint. It sends chat completions requests and returns textual responses.\n\n## High-Level Algorithm Steps\n\n1. **Construct request** — Build a JSON payload containing:\n   - The configured model identifier\n   - A system prompt prepended to the user messages array\n   - Context window size (`num_ctx`) passed via options\n   - Optional `format` field (e.g., `\"json\"`) when requested\n\n2. **Execute HTTP call** — POST the serialized payload to `\u003cbaseURL\u003e/api/chat`.\n\n3. **Handle response**:\n   - On 200 OK: deserialize and return the model's text output (`message.content`).\n   - On any other status code: read a bounded portion of the error body and return it as an error string with the HTTP status included.\n\n4. **No retries, no caching** — Fail fast on both network errors and non-success responses.\n\n## Domain Rules\n\n- The system prompt is always injected at position 0 in the messages array before any user/assistant turns.\n- Streaming is disabled (`Stream: false`) for all calls.\n- HTTP timeouts are bounded by a default `HTTPTimeout` constant; connection-level failures surface immediately with no retry logic.\n\n#### [STATE_AND_CONCURRENCY]\n**Global Variables / Shared State:**\n\n- `defaultHTTPTimeout` — referenced indirectly via `\u0026http.Client{Timeout: defaultHTTPTimeout}`. Its value is set once at package init and never reassigned, so it acts as immutable state in practice.\n\n**Class-Level Properties (Receiver Fields):**\n\n- `llmClient.modelID`, `llmClient.baseURL`, `llmClient.numCtx` — set only in the constructor (`newLLMClient`). No method reassigns them after creation.\n- `llmClient.httpClient *http.Client` — created once, reused across all calls. This is shared state within a single client instance; no lock protects it, but since it's constructed with a fixed timeout and never reassigned, concurrent reads are safe in practice.\n\n**Synchronization Mechanisms:**\n\n- None detected (no mutex, no atomic types, no channel-based sync).\n\n**Assessment:** The receiver fields are effectively immutable — they are only written once during construction and never modified afterward. No locking is required for the current usage pattern, though `httpClient` reuse under concurrent calls could become an issue if the client were reconfigured at runtime (e.g., changing the base URL or timeout).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n### Network / API\nThe code performs HTTP requests to an external Ollama-compatible chat endpoint:\n\n- **Endpoint:** `{baseURL}/api/chat` (constructed in `prepareOllamaRequest`)\n- **Method:** POST with JSON body (`application/json`)\n- **Client:** An `*http.Client` configured with a timeout of `defaultHTTPTimeout`. This is set at construction time and not configurable per-call.\n- **Payload:** A JSON object containing `model`, `messages` (system prompt prefixed), optional `format`, and `options.num_ctx`.\n- **Response handling:** The response body is read in full only when `StatusCode == http.StatusOK`; otherwise a limited read is performed using `io.LimitReader(resp.Body, maxErrorBodyBytes)`.\n\n### I/O Operations on Local Resources\n- JSON marshaling/unmarshaling via the standard library (`json.Marshal`, `json.Unmarshal`). Failures from these are wrapped into Go errors.\n- No direct disk or database access.\n\n---\n\n## Error Propagation\n\n### Handled and Wrapped Errors\n1. **Request body marshaling** — if `json.Marshal` fails, a new error is returned with the original wrapped via `%w`.\n2. **HTTP request creation** — any failure from `http.NewRequestWithContext` is returned directly (not wrapped).\n3. **Network / transport errors** — failures from `c.httpClient.Do(req)` are returned directly to the caller of `CallLLM`. This includes timeouts, DNS failures, connection resets, etc.\n4. **Response body read on success path** — if `io.ReadAll` fails after a 200 response, the error is returned directly.\n5. **JSON parsing failure** — if `json.Unmarshal` fails on the response payload, a new error wrapping the original is returned to the caller.\n\n### Swallowed / Unhandled Errors\n1. **Non-OK status body read** — in the `else` branch for non-200 responses, the error from `io.ReadAll(io.LimitReader(...))` is discarded (`body, _ := io.ReadAll(...)`). The resulting error string (containing `resp.StatusCode` and truncated response) is returned to the caller, but any I/O failure during that read is silently dropped.\n2. **Unknown symbol references** — `defaultHTTPTimeout` and `maxErrorBodyBytes` are referenced but not defined in this file. These must be package-level constants or variables (possibly declared elsewhere). If they are undefined at runtime, the code will fail to compile.\n\n---\n\n## Summary\n\n| Category | Detail |\n|----------|--------|\n| External system | Ollama-compatible HTTP chat API (`POST {baseURL}/api/chat`) |\n| Transport | `net/http` client with fixed timeout |\n| Error model | Go error values returned from `CallLLM`; some wrapped via `%w`, some propagated raw |\n| Unhandled errors | Swallowed I/O error in non-OK response path |\n| Missing definitions | `defaultHTTPTimeout`, `maxErrorBodyBytes` (not defined in this file) |"
    },
    "internal/engine/constants.go": {
      "sha256": "85ec25e43f84ebf8a1ce483e8efd9df8eb8fcf20fa2e8bbf3e243022a0730ae7",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface of `internal/engine/constants.go`\n\n## Result: No public elements found.\n\n- All 9 named constants (`defaultHTTPTimeout`, `maxErrorBodyBytes`, `defaultChunkOverlap`, `minNumCtxFloor`, `contextWindowAllocRatio`, `maxCharsMultiplier`, `metadataFileName`, `agentsFileName`, `defaultDirPerm`) are **unexported** (lowercase first letter).\n- No exported functions, methods, or types are defined in this file.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis file defines operational configuration constants for an AI inference/agent engine runtime. It establishes fixed parameters that govern timeout behavior, error handling limits, context window management, and filesystem conventions used during agent execution.\n\n## High-Level Algorithm Steps\n\n1. **Set HTTP communication timeouts** — `defaultHTTPTimeout` (10 minutes) controls the maximum duration for remote API calls before they are aborted.\n\n2. **Cap error response payloads** — `maxErrorBodyBytes` (1 KB) limits how much error body data can be captured from failed requests.\n\n3. **Configure chunk overlap** — `defaultChunkOverlap` (800 tokens) defines the overlap between consecutive text chunks during streaming or processing.\n\n4. **Define context window floor** — `minNumCtxFloor` (512) sets a minimum threshold for context length calculations.\n\n5. **Allocate context windows proportionally** — `contextWindowAllocRatio` (0.75) reserves 75% of available space for primary content, leaving the remainder for metadata and overhead.\n\n6. **Scale character limits** — `maxCharsMultiplier` (3x) multiplies a base character limit to determine maximum allowed output length.\n\n7. **Establish filesystem conventions** — `metadataFileName`, `agentsFileName`, and `defaultDirPerm` define how state is persisted on disk during agent runs.\n\n## Domain Rules\n\n- All timeouts are absolute durations; no retry or backoff logic is implied by this file alone.\n- Error bodies are truncated at a fixed byte ceiling regardless of content type.\n- Chunk processing uses a fixed overlap value with no dynamic adjustment mentioned here.\n- Context window allocation follows a static 75/25 split ratio.\n- The `LogEventFunc` type is declared as an event callback signature but no events or logging system are implemented in this file.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects in this file. This file contains only constant declarations and a single function-type alias (`LogEventFunc`). It does not perform any disk, network, database, or API operations itself.\n\n**Error Propagation:** No error propagation mechanisms present in this file. There are no error types defined, no exception handling, and no error return values."
    },
    "internal/engine/markdown.go": {
      "sha256": "81bea4827161eb8978cd6f288e2276c2616da7832e8687400b6872d8d9c3749c",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface of `internal/engine/markdown.go`\n\nThis file contains **no exported (public) symbols**. All identifiers (`stripOuterMarkdownFence`, `markdownFenceRe`) begin with lowercase letters, which makes them unexported in Go.\n\n---\n\n### Internal Elements (for reference only — not part of the public API)\n\n- `var markdownFenceRe regexp.Regexp` — compiled regex for matching fenced code blocks\n- `func stripOuterMarkdownFence(content string) string` — strips surrounding markdown/json fences from input strings\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\n**Stripping surrounding code fence markers from markdown or JSON content.** This function cleans input that wraps code blocks in triple-backtick fences (with optional `markdown` or `json` language tags), exposing the raw interior content without the wrapping syntax.\n\n## High-Level Algorithm Steps\n\n1. **Trim whitespace** — Remove leading/trailing spaces and newlines from the input string to normalize boundaries before fence detection.\n2. **Match fenced pattern** — Check whether the trimmed string begins with three or more backticks (optionally followed by `markdown`/`json`) and ends with a matching set of backticks, capturing everything between them.\n3. **Extract inner content** — If the fence pattern matches, return the captured interior text after trimming whitespace around it.\n4. **Fallback pass-through** — If no fenced structure is detected, return the original input unchanged.\n\n#### [STATE_AND_CONCURRENCY]\n**Global/Package-Level Variables:**\n- `markdownFenceRe` — initialized once at package scope; never reassigned or modified anywhere in this file. Effectively read-only after compilation.\n\n**Synchronization Mechanisms:** None present (no mutexes, channels, async/await, or atomic types).\n\n**Conclusion:** No mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\nExternal side effects: None. The function performs no I/O operations (no file, network, database, or API interactions). It relies solely on standard library packages (`regexp`, `strings`).\n\nError propagation: No error return values are used. If the regex fails to match the input, the function returns the trimmed input string unchanged rather than signaling an error or panicking."
    },
    "internal/engine/orchestrator.go": {
      "sha256": "dc4d987aac76e1235503c7e3cef767b7b9e2615123ea4a487da922f49d35a201",
      "facts": "#### [API_SIGNATURES]\n# Public API — internal/engine (orchestrator.go)\n\n## Types\n\n### EventType\nType: `string`\n\n| Constant | Value |\n|---|---|\n| EventStatus | `\"status\"` |\n| EventError | `\"error\"` |\n\n### Event\n| Field | Type | Description |\n|---|---|---|\n| Type | `EventType` | Category of the event |\n| Message | `string` | Event payload text |\n\n## Methods (on `orchestrator`)\n\n\u003e Note: The receiver type is unexported; these are internal methods.\n\n### GenerateStandardDocs\n```\nfunc (o *orchestrator) GenerateStandardDocs(ctx, repoRoot, docsDir, rootSum string, cfg *config.Config, logEvent LogEventFunc) error\n```\nGenerates the global architecture and quickstart documentation files via LLM synthesis.\n\n---\n\n*The file also references unexported types (`orchestrator`, `pipelineContext`, `DirNode`, `FileChange`) and functions whose full signatures are not visible in this excerpt.*\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code implements an **incremental AI-driven documentation generation pipeline** for software repositories. It automatically produces architecture overviews, quickstart guides, and module-level API descriptions from source code using LLM synthesis, with change detection to avoid unnecessary regeneration.\n\n## High-Level Algorithm Steps\n\n### Initialization (`RunInit`)\n1. **Discover files**: Scan the repository for all code files while respecting `.gitignore` patterns and user-configured ignore lists.\n2. **Hash computation**: Compute SHA-256 hashes for every discovered file; cache them preemptively.\n3. **Build directory tree**: Construct a hierarchical `DirNode` tree from the discovered file paths, organizing by directory structure.\n4. **Mark all directories affected**: Recursively mark every directory in the tree as \"affected\" (since everything is new on first run).\n5. **Synthesize root summary**: Pass the full tree to an LLM call that produces a global architecture summary across the entire codebase.\n6. **Generate standard docs**: Use the root summary to generate `architecture.md` and `quickstart.md` via additional LLM calls.\n7. **Update agent guidelines file**: Append or create AI Agent Guidelines at repository root, referencing the generated documentation paths.\n\n### Update (`RunUpdate`)\n1. **Load cache and detect step changes**: If user-configured extraction steps changed since last run, invalidate the metadata cache to force full regeneration.\n2. **Discover current files and hash them**.\n3. **Diff detection**: Compare current file hashes against cached hashes to classify each file as Added, Modified, or Deleted (including files removed from cache).\n4. **Rebuild tree from allowed files**: Construct a new directory tree only from currently existing code files.\n5. **Prune stale data**: Remove deleted files from the cache; delete module-level docs for directories that no longer exist in the current tree.\n6. **Determine affected directories**: Walk the tree and mark directories containing changed files as affected.\n7. **Early exit check**: If zero directories are affected, skip all regeneration work entirely.\n8. **Regenerate selectively**: Only regenerate architecture/quickstart docs if a top-level directory (root) was affected or standard docs don't exist yet; otherwise leave them intact.\n\n## Domain Rules\n\n- The pipeline is **incremental by design** — it only regenerates documentation for directories impacted by actual file changes, not the entire codebase on every update call.\n- A **steps hash** acts as a global invalidation trigger: if extraction configuration changes, all cached metadata is discarded regardless of per-file change detection.\n- Standard docs (`architecture.md`, `quickstart.md`) are treated as **global artifacts** — they regenerate when any module-level directory is affected, but otherwise persist across updates.\n- Module-level documentation lives under a `modules/` subdirectory and is pruned automatically when its parent code directory no longer exists in the repository.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State Analysis\n\n### Shared/Mutable State Identified\n\n| Variable/Field | Type | Mutability | Notes |\n|---|---|---|---|\n| `cache` (*MetadataCache) | Pointer to cache struct | **Mutable** | Modified in `setupPipeline`, `teardownPipeline`, `RunInit`, and `RunUpdate`. Fields like `.StepsHash`, `.Files`, and `.Modules` are reassigned across calls. |\n| `cfg` (*config.Config) | Pointer to config struct | Potentially mutable | Passed as pointer throughout; while only read fields here (`.SystemPrompt`, `.ArchitecturePrompt`, `.ExtractionSteps`), it remains a shared pointer that external code could mutate. |\n| `client` (llmCaller) | Interface pointer | Read-only in this file | Initialized on struct creation, never reassigned within these methods. Safe from accidental mutation but still a shared reference. |\n| `affectedDirs` (map[string]bool) | Local map | Mutable during execution | Created fresh per call but acts as shared state during the lifetime of `RunInit` and `RunUpdate`. Used by nested functions like `markAllAffected`, `collectDirs`, `determineAffected`, etc. |\n| `precalculatedHashes` (map[string]string) | Local map | Mutable during execution | Created fresh per call, but used as shared state within loop iterations in `RunInit`. |\n\n### Synchronization Mechanisms\n\n- **No locks/mutexes** visible in this file for protecting shared state access.\n- **No atomic types** used.\n- **No explicit concurrency patterns** (goroutines/channels) within this file—operations appear sequential and single-threaded per method invocation.\n\n### Risk Assessment\n\nThe `MetadataCache` pointer (`cache`) is the primary concern: it's mutated across multiple methods without any visible synchronization, making concurrent access to the same cache instance unsafe. If called from multiple goroutines sharing the same context, this would introduce data races.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O and Error Propagation Analysis\n\n### Disk I/O Operations\n\n| Location | Operation | Error Handling |\n|---|---|---|\n| `setupPipeline` → `tools.LoadGitignore(repoRoot)` | Reads `.gitignore` from disk | **Swallowed.** Error logged via `logEvent(EventStatus, fmt.Sprintf(\"Warning: failed to load .gitignore: %v\", err))`. Function continues regardless. |\n| `setupPipeline` → `loadMetadataCache(repoRoot, docsDir)` | Reads metadata cache file(s) from disk | **Swallowed.** Error logged via `logEvent(EventStatus, ...)`. Returns empty/nil values. |\n| `teardownPipeline` → `saveMetadataCache(repoRoot, docsDir, cache)` | Writes metadata cache to disk | **Swallowed.** Error logged via `logEvent(EventStatus, ...)`. Function completes regardless of write success or failure. |\n| `RunInit` / `RunUpdate` → `computeSHA256(repoRoot, f)` (loop over codeFiles) | Reads file bytes and computes SHA-256 hash | **Swallowed in RunInit.** Error logged via `logEvent(EventStatus, fmt.Sprintf(\"Warning: failed to compute hash for %s: %v\", f, err))`. File is skipped from precalculatedHashes map. **Swallowed in RunUpdate.** Error logged similarly; file skipped and loop continues. |\n| `RunInit` → `os.MkdirAll(modulesDir, defaultDirPerm)` | Creates `docsDir/modules/` directory on disk | **Propagated.** Wrapped as `fmt.Errorf(\"failed to create modules directory: %w\", err)`. Returns error up the call stack. |\n| `RunInit` / `RunUpdate` → `tools.ReadFileSafely(repoRoot, agentFilePath)` | Reads existing agents file from disk | **Swallowed in RunInit.** If read fails, falls back to writing a fresh content via `tools.WriteFileSafely(...)`. The write error is propagated (see below). |\n| `RunUpdate` → `security.SafeResolve(...)` + `_ = os.Remove(absModuleFile)` | Resolves module file path and deletes it from disk | **Swallowed.** Error from `SafeResolve` checked (`if err == nil`). If resolve succeeds, `os.Remove` is called with error ignored (`_ = os.Remove(...)`). No cleanup verification. |\n| `RunInit` / `RunUpdate` → `tools.WriteFileSafely(repoRoot, path, bytes)` (architecture.md, quickstart.md, agents file) | Writes documentation files to disk | **Propagated.** Wrapped as `fmt.Errorf(\"failed to write ...: %w\", err)`. Returns error up the call stack. |\n\n### Network / API I/O Operations\n\n| Location | Operation | Error Handling |\n|---|---|---|\n| `GenerateStandardDocs` (called from `RunInit`, `RunUpdate`) → `o.client.CallLLM(ctx, sysPrompt, messages, false)` × 2 | LLM API calls for generating architecture.md and quickstart.md content | **Propagated.** Wrapped as `fmt.Errorf(\"failed to generate global architecture: %w\", err)` or `fmt.Errorf(\"failed to generate quickstart documentation: %w\", err)`. Returns error up the call stack. |\n| `RunInit` → `synthesizeNode(pCtx, tree)` (called once per node traversal) | LLM API calls for synthesizing module summaries (one call per code file in the tree) | **Propagated.** Errors from `CallLLM` inside `synthesizeNode` are returned up through `RunInit`. |\n| `RunUpdate` → `o.GenerateStandardDocs(ctx, ...)` (conditional on affectedDirs[\".\"] or docs missing) | LLM API + disk write chain for architecture.md and quickstart.md | **Propagated.** Same error wrapping as in `GenerateStandardDocs`. Returns error up the call stack. |\n\n### Error Propagation Summary\n\n- **Hard failures** (propagated): Directory creation (`os.MkdirAll`), file writes (`tools.WriteFileSafely`), LLM calls (`o.client.CallLLM`). These terminate the pipeline with a wrapped error.\n- **Soft failures** (swallowed, logged only): Cache loading/writing, hash computation for individual files, `.gitignore` loading, module file cleanup (`os.Remove`). The pipeline continues and returns `nil`.\n- **No crash/panic**: No `panic()` or `recover()` observed in this file. All errors are handled through the error interface (return values).\n- **Context propagation**: `ctx` is threaded through from `RunInit`/`RunUpdate` into `GenerateStandardDocs`, then into `CallLLM`. If context is cancelled, LLM calls will return an error which propagates up.\n- **No database or external service beyond LLM API** observed in this file. All I/O is either local disk or the configured LLM provider via `llmCaller` interface."
    },
    "internal/engine/runner.go": {
      "sha256": "e833cb64ae16b96a2b0ba3ce4fb6d4f9010c49809a5685ba283b6a05b34464ee",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `runner.go` (Module: internal/engine)\n\n### Types\n\n| Element | Description |\n|---------|-------------|\n| **`Mode`** | String-based operation mode for the documentation pipeline |\n| **`Runner`** | Struct with field `cfg *config.Config` |\n\n---\n\n### Constants\n\n| Constant | Type | Value |\n|----------|------|-------|\n| **`ModeInit`** | `Mode` | `\"init\"` |\n| **`ModeUpdate`** | `Mode` | `\"update\"` |\n\n---\n\n### Functions / Methods\n\n| Element | Signature | Description |\n|---------|-----------|-------------|\n| **`NewRunner`** | `func NewRunner(cfg *config.Config) *Runner` | Creates a new `Runner` instance with the given configuration |\n| **`Run`** *(method on `*Runner`) | `func (r *Runner) Run(ctx context.Context, repoRoot string, mode Mode, onEvent func(Event)) error` | Executes the documentation pipeline for the specified mode. Parameters: `ctx` — cancellation context; `repoRoot` — repository root path; `mode` — operation mode (`ModeInit` or `ModeUpdate`); `onEvent` — callback invoked per event. Returns an error if the lock cannot be acquired, the mode is unsupported, or a pipeline step fails |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nOrchestrating a repository documentation generation pipeline that supports both initial creation and incremental updates while enforcing single-process execution via file locking.\n\n**High-Level Algorithm Steps:**\n1. Validate that the lockfile path is included in `.gitignore`; emit a warning if this check fails but continue execution.\n2. Acquire an exclusive lock on the repository root; terminate immediately with an error if the lock cannot be obtained, and ensure the lock is released via defer upon completion or failure.\n3. Instantiate a large language model client and a documentation orchestrator using parameters provided in the configuration (model ID, base URL, context size).\n4. Branch execution based on the requested mode: invoke the initialization pipeline for `ModeInit` or the update pipeline for `ModeUpdate`, returning an error if neither is specified.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n- `Runner.cfg (*config.Config)` — pointer field stored during construction in `NewRunner()`. Not modified after initial assignment within this file; the actual configuration object is external to this code block and its mutability depends on the `config` package.\n\n**Synchronization Mechanisms (observed):**\n\n- Lock acquired via `security.AcquireLock(repoRoot)` — a repository-level lock is obtained at the start of `Run()` and deferred for automatic unlock in `defer lock.Unlock()`. The exact synchronization primitive (file lock, directory lock, etc.) depends on the implementation in the external `security` package.\n- No visible mutexes, atomics, or channel-based primitives within this file.\n\n**Note:** Thread safety of concurrent callers to `Run()` is not fully observable from this file alone — it relies on the external `security.AcquireLock` contract and any internal state changes in `orch.RunInit` / `orch.RunUpdate`, which are defined outside this source block.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n**Filesystem / Repository Operations (via `security` package):**\n- `security.EnsureGitignoreHasLockfile(repoRoot)` — Writes/updates a `.gitignore` file at `repoRoot`. Error is **swallowed and logged as a warning**; execution continues.\n- `security.AcquireLock(repoRoot)` — Acquires a repository-level lock (likely writes to a `.lock` or similar file). Failure returns an error that is **propagated immediately** via `return fmt.Errorf(...)`. The deferred `lock.Unlock()` runs regardless of success/failure path, ensuring cleanup.\n\n**External API / Network:**\n- `newLLMClient(r.cfg.ModelID, r.cfg.OllamaBaseURL, r.cfg.OllamaNumCtx)` — Instantiates an LLM client configured to connect to Ollama at the base URL provided in config. This is **only instantiation**; no network I/O occurs within this file. The actual API calls happen inside `orch.RunInit` / `orch.RunUpdate`, which are not visible here.\n\n**No direct disk reads, database queries, or other external services** are shown in this file beyond the filesystem writes through the security package and the LLM client setup.\n\n## Error Propagation\n\n| Location | Behavior |\n|----------|----------|\n| `security.EnsureGitignoreHasLockfile` error | Swallowed; logged as event warning via `logEvent(EventStatus, ...)`. No return value propagated to caller. |\n| `security.AcquireLock` error | Wrapped with context (`fmt.Errorf(\"failed to acquire repository lock: %w\", err)`), then returned immediately from `Run()`. Caller receives a non-nil error and the pipeline halts. |\n| Orchestrator errors (`orch.RunInit`, `orch.RunUpdate`) | Returned directly from `Run()` via `return err`. Caller receives whatever error (or nil) the orchestrator returns. |\n| Invalid mode string | Returns `fmt.Errorf(\"unsupported mode: %s\", mode)`. Caller receives a non-nil error and pipeline halts. |\n| Successful execution path | Returns `nil` from `Run()`. |\n\n**Key pattern:** This file uses Go's native error return convention exclusively—no panic/recover, no exception-based propagation. The only partially-swallowed error is the gitignore check; all other errors are propagated to the caller with descriptive messages (and one wraps a nested error via `%w`)."
    },
    "internal/engine/synthesize.go": {
      "sha256": "7b229dcedd81d6db642563c4dde4ddec9284084f65ee07b9d5cf4770afac489d",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `internal/engine/synthesize.go`\n\n**No exported elements found.** All types and functions in this file are unexported (lowercase first letter).\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nThis code solves the problem of generating hierarchical documentation summaries for a repository's source tree by recursively extracting facts about individual files, aggregating them per directory, and writing consolidated module-level summaries to disk. It operates on an in-memory tree structure (`DirNode`) representing directories containing files and subdirectories.\n\n**High-Level Algorithm Steps:**\n\n1. **Cache-first lookup with hash verification.** Before any file I/O, check the metadata cache for a stored summary. If a precomputed SHA-256 hash matches, reuse cached facts without re-reading the file. On hash mismatch or miss, read the file and compute its hash to update the cache.\n\n2. **Recursive bottom-up traversal.** For a given directory node:\n   - Check whether this (and all descendants) are \"affected.\" If unaffected, return the cached summary immediately.\n   - Sort child names alphabetically. Recursively synthesize each child first; collect non-empty summaries into a map keyed by child name.\n\n3. **Per-file extraction with chunking and multi-step LLM prompting.** For each file in the directory:\n   - Compute a dynamic content limit based on remaining context window capacity (allocates roughly 75% of available tokens for file content, reserving the rest for prompts).\n   - Split large files into overlapping chunks. Pass each chunk to an LLM with a step-specific prompt derived from `ExtractionSteps`. Strip markdown fences from responses.\n   - Consolidate all chunk outputs per extraction step using a reduction function (`reduceFileFacts`). Concatenate results across steps into a single file fact string.\n\n4. **Assembly of directory summary.** Combine extracted file facts (prefixed with \"### File: basename\") and previously synthesized child summaries (prefixed with \"### Subsystem: name\") into one `components` slice. If nothing exists, clear the cache entry and return empty.\n\n5. **Final reduction and persistence.** Pass all components to a chunked reduction function (`reduceInChunks`) that produces a single consolidated summary for the directory. Store this in the module cache and write it to disk under `\u003cdocsDir\u003e/modules/\u003csafe-filename-of-path\u003e`.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n- **`pipelineContext.cache.Files`** (`map[string]FileCacheEntry`) — mutated inside `extractFileFacts`:\n  ```go\n  p.cache.Files[f] = FileCacheEntry{SHA256: fileHash, Facts: facts}\n  ```\n- **`pipelineContext.cache.Modules`** (`map[string]string`) — mutated inside `synthesizeNode`:\n  ```go\n  p.cache.Modules[node.Path] = \"\"       // when no components found\n  p.cache.Modules[node.Path] = finalSum // after synthesis completes\n  ```\n\n**Shared State:** Both maps are stored as fields on a pointer-referenced struct (`*pipelineContext`). Any number of goroutines sharing the same `pipelineContext` would race on these maps with no protection.\n\n**Synchronization Mechanisms:** None detected in this file. No mutex, atomic type, channel-based locking, or async/await pattern is used to protect cache mutations. If concurrent calls share a `*pipelineContext`, the map writes are unprotected and constitute a data race per Go's memory model.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O and Error Propagation Analysis of `synthesize.go`\n\n### External System Interactions\n\n**1. Filesystem (Read)** — via `tools.ReadFileSafely(p.repoRoot, f)`\n- **Path**: `\u003crepoRoot\u003e/\u003cf\u003e` — arbitrary files under the repository root.\n- **Behavior**: Called once during cache miss; called a second time when initial read returns empty facts. Both failure modes are swallowed: logged as an `EventStatus` warning and returned as `\"\", nil`.\n\n**2. LLM API (Network)** — via `p.client.CallLLM(p.ctx, systemPrompt, []Message{...}, false)`\n- **Path**: Network endpoint determined by the `llmCaller` interface; called once per extraction step per chunk.\n- **Behavior**: Errors propagate fully to the caller wrapped as:\n  ```go\n  return \"\", fmt.Errorf(\"LLM error extracting %s for %s: %w\", step.Name, f, err)\n  ```\n\n**3. Filesystem (Write)** — via `tools.WriteFileSafely(p.repoRoot, modulePath, []byte(finalSum))`\n- **Path**: `\u003crepoRoot\u003e/\u003cdocsDir\u003e/modules/\u003csafe-filename-of-node-path\u003e.md`\n- **Behavior**: Write failures propagate fully wrapped as:\n  ```go\n  return \"\", fmt.Errorf(\"failed to write module documentation for %s: %w\", node.Path, err)\n  ```\n\n**4. No external I/O in this file otherwise.**\n- SHA256 hashing (`crypto/sha256`, `encoding/hex`) is purely in-memory.\n- Sorting, string manipulation are local operations.\n\n---\n\n### Error Propagation Patterns\n\n| Source | Behavior | Returned to Caller |\n|--------|----------|---------------------|\n| First `ReadFileSafely` failure | Logged as warning; returns `\"\", nil` | **Swallowed** — caller sees no error |\n| Second `ReadFileSafely` (inside `if facts == \"\"`) | Same: logged, returns `\"\", nil` | **Swallowed** |\n| LLM call (`CallLLM`) | Wrapped with `%w`, includes step name and file path in message | **Propagated** — caller receives wrapped error |\n| `reduceFileFacts` (consolidation step) | Returned as-is from `synthesizeNode` | **Propagated** |\n| Final `WriteFileSafely` | Wrapped with `%w`, includes node path | **Propagated** |\n\n### Key Observations\n\n- **Read failures are silent.** If a file cannot be read, the pipeline treats it as having no facts and continues. This is an implicit contract: callers must not rely on `\"\", nil` meaning \"file exists but empty\" — they receive that signal only when `facts == \"\"`.\n- **Context cancellation** (`p.ctx.Err()`) is checked at the top of `synthesizeNode`; if cancelled, returns `(\"\", err)` immediately. This propagates cleanly to any caller checking the error.\n- **No panic/recover.** All errors are returned as values; no recovery in this file."
    },
    "internal/engine/tree.go": {
      "sha256": "217d281bc97bb80cc97e68018f4fb3494c9157f59c98839957f7e1e40a92676e",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: engine (tree.go)\n\n## Types\n\n### ChangeStatus\n- **Type**: string\n\n### FileChange\n| Field | Type |\n|-------|------|\n| Path | string |\n| Status | ChangeStatus |\n\n### DirNode\n| Field | Type |\n|-------|------|\n| Path | string |\n| Files | []string |\n| Children | map[string]*DirNode |\n\n## Constants\n\n| Constant | Value |\n|----------|-------|\n| StatusAdded | \"Added\" |\n| StatusModified | \"Modified\" |\n| StatusDeleted | \"Deleted\" |\n\n## Functions (Public/Exported)\n\n### buildTree\n- **Signature**: `func buildTree(files []string) *DirNode`\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\n\nThis code solves the problem of determining which directories in a project structure are \"affected\" by a given set of file changes (adds, modifications, or deletions). It builds a hierarchical tree from file paths and then walks that tree to mark parent directories as affected based on their children's status.\n\n**High-Level Algorithm Steps**\n\n1. **Build directory tree** — Given a list of file paths, construct a nested `DirNode` hierarchy where each node represents a path segment. Files are leaf entries; intermediate segments become child nodes.\n\n2. **Seed changed files** — For each input `FileChange`, record the path in a `changedFiles` map. If the change is a deletion, also mark the parent directory of that file as affected.\n\n3. **Walk tree and determine affected directories** — Recursively traverse every node:\n   - Mark a directory as affected if it contains any of the changed files.\n   - Mark a directory as affected if its corresponding markdown module path (under `docs/modules/\u003csafe-name\u003e`) does not exist on disk at the repo root.\n   - Mark a directory as affected if it is empty in the metadata cache.\n   - Recurse into children to propagate status upward.\n\n4. **Propagate affected flag upward** — After the initial walk, a second recursive pass marks any parent directory as affected whenever *any* child has already been marked affected, regardless of whether that child itself was directly matched.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n### Confirmed (from source text alone):\n\n- **`os.Stat(absModulePath)`** inside `determineAffected()` — reads filesystem state to check whether a module directory exists at the resolved absolute path. This is disk I/O. The error from this call is examined only via `os.IsNotExist(err)`. If the returned error is *not* \"file not found\", the branch simply continues without effect (error swallowed).\n\n- **`security.SafeResolve(repoRoot, modulePath)`** — calls into a dependency package (`internal/security`). Its internal behavior is unknown from this file alone; it may perform path resolution that touches the filesystem. No further assumptions can be made about its side effects or error handling beyond what is visible here.\n\n### Not present:\n\n- `propagateAffected` and `buildTree` contain no I/O — they operate purely on in-memory data structures.\n- No network, database, or API calls are referenced anywhere in this file.\n\n## Error Propagation\n\n| Location | Behavior |\n|---|---|\n| `determineAffected()` → `security.SafeResolve(...)` error | **Swallowed.** The entire `if err == nil { … }` block is skipped; no panic, no return-code signaling to the caller. |\n| `os.Stat(absModulePath)` error (non-NotExist) | **Swallowed.** Only checked via `os.IsNotExist(err)`. Other errors are ignored entirely. |\n| All other functions in this file | No return values or side effects related to external failures. Errors from `buildTree` and `propagateAffected` do not exist at all — they never fail externally. |\n\n**Summary:** The only confirmed external I/O is a single `os.Stat()` call for directory-existence checking, with errors absorbed silently by the caller."
    },
    "internal/engine/utils.go": {
      "sha256": "9b8cd943c9fdbb73451b01584d7d76b73d92194f133ec07c04517ac3ca7f713b",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface of `internal/engine/utils.go`\n\n### Functions\n\n| Function | Parameters | Return Type |\n|---|---|---|\n| **`toSafeMarkdownFilename`** | `modulePath string` | `string` |\n| **`makeLogEvent`** | `onEvent func(Event)` | `LogEventFunc` |\n\n\u003e *Note: Types `LogEventFunc`, `EventType`, and `Event` are referenced but not defined within this file. Their definitions must be resolved from the importing package.*\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis file provides lightweight utility helpers for the engine module: generating safe markdown filenames from directory paths, and creating adapter-style log event callbacks that support optional listeners.\n\n## High-Level Steps\n\n1. **Safe filename generation**: Given a module path string, return `\"README.md\"` if the path is empty or `.`; otherwise join the path with `\"README.md\"`.\n2. **Log event factory**: Return a function that, given an `EventType` and message string, forwards to a supplied callback only if it's non-nil (nil-safe adapter pattern).\n\n## Domain Rules\n\n- Empty/`.` module paths map to `\"README.md\"` by default.\n- Unknown or invalid callbacks are silently swallowed — no panic on nil listeners.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects. The only filesystem-related function (`toSafeMarkdownFilename`) constructs path strings via `filepath.Join` but performs no actual file operations (no open/read/write). Pure string manipulation.\n\n**Error Propagation:** None observed. Both functions return values without error types, and neither panics or returns errors."
    },
    "internal/security/errors.go": {
      "sha256": "eaf4e252d4b8701cb50b75196bf95b853d9b43b8fd8f8902aa2b1d0c86029916",
      "facts": "#### [API_SIGNATURES]\n- `ErrPathTraversal` (type error) — returns `\"security violation: path traversal detected\"` when a path resolves outside the repository root.\n\n- `ErrLockHeld` (type error) — returns `\"lock is already held by another process\"` when another code-reducer process holds the file lock.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nThis module defines sentinel error values used by downstream code to distinguish between two specific failure modes in repository-level security operations: (1) paths that escape the allowed root boundary, and (2) lock contention from concurrent processes. The errors themselves carry no algorithmic logic — they are contract markers for callers to branch on.\n\n**High-Level Steps:**  \nNone applicable at this abstraction level. This file contains only error definitions; any processing logic resides in other files within the same module.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects. This file contains only sentinel error definitions and imports the `errors` package for construction. No network, disk, database, or API interactions occur within this file.\n\n**Error Propagation:** Two sentinel errors are defined:\n\n| Variable | Trigger Condition | Propagation Mechanism |\n|---|---|---|\n| `ErrPathTraversal` (`security violation: path traversal detected`) | Returned when a resolved path falls outside the repository root (detection logic in sibling files) | Standard Go error return value — caller must check via `errors.Is()` or `errors.As()`. Not wrapped, no panic. |\n| `ErrLockHeld` (`lock is already held by another process`) | Returned when another code-reducer instance holds the file lock (detection logic in sibling files) | Standard Go error return value — caller must check via `errors.Is()` or `errors.As()`. Not wrapped, no panic. |\n\nBoth errors are raw `*errors.errorString` values returned from functions elsewhere in the security module. No exception-style wrapping occurs here; propagation is purely through function-level `error` returns per Go convention."
    },
    "internal/security/security.go": {
      "sha256": "9817e26e9cb3a7a964d9416ac811b5f722b47ef4d4e919573ad586ae9540657f",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/security`\n\n## Types\n\n| Element | Description | Fields / Parameters |\n|---|---|---|\n| **`SafeResolve`** | Cleans input path and ensures it lies strictly inside the repository. Resolves symlinks on existing ancestor parts to prevent path traversal via symlinks. | `repoRoot string`, `inputPath string` → `(string, error)` |\n| **`AcquireLock`** | Acquires a simple file lock in the repo root using O_EXCL for atomicity. | `repoRoot string` → `(*SimpleLock, error)` |\n| **`EnsureGitignoreHasLockfile`** | Ensures that `.code-reducer.lock` is present in `.gitignore`. | `repoRoot string` → `error` |\n\n## Structs\n\n| Element | Description | Fields / Parameters |\n|---|---|---|\n| **`SimpleLock`** | File-based lock implementation. | *(unexported: `lockPath`, `file`, `mu`, `closed`) — no public methods other than `Unlock()`* |\n\n## Struct Methods\n\n| Method | Description | Parameters | Return Value |\n|---|---|---|---|\n| **`(*SimpleLock) Unlock`** | Releases the lock by closing the file and removing it. Idempotent and thread-safe. | *(receiver: `l *SimpleLock`, no params)* | `(error)` |\n\n## Constants (Package-level, unexported)\n\nThe following are defined but not exported (lowercase first letter):\n\n- `defaultFilePerm = 0644`\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis module solves two problems for a tool called \"code-reducer\": (1) safely resolving user-supplied filesystem paths without allowing path-traversal escapes, and (2) ensuring only one instance of the process runs at a time via file-based mutual exclusion.\n\n## High-Level Algorithm Steps\n\n**Path sanitization (`SafeResolve`)**\n- Take an input path relative to a repository root.\n- Resolve symlinks on the existing ancestor directory components so that symlink-based traversal is blocked.\n- Walk up from the target until hitting an existing physical ancestor (skipping non-existent intermediate components).\n- Reconstruct the full resolved path from that ancestor plus the skipped suffixes.\n- Verify the final resolved path lies strictly inside the repository root; reject if it does not.\n\n**Process locking (`AcquireLock` / `SimpleLock`)**\n- Acquire an exclusive lock file (`.code-reducer.lock`) using O_EXCL for atomicity, writing the current PID into it.\n- On unlock, close and remove the lockfile; this is idempotent and thread-safe.\n\n**Gitignore integration (`EnsureGitignoreHasLockfile`)**\n- Ensure the lockfile path is listed in `.gitignore` so that stale lockfiles are ignored by version control.\n\n#### [STATE_AND_CONCURRENCY]\nMutable state: None at the package level. The file contains only `const` values and function-local variables; no package-level or class-level properties are modified.\n\nShared/instance state: `SimpleLock` holds per-instance fields (`lockPath`, `file *os.File`, `mu sync.Mutex`, `closed bool`). `Unlock()` modifies `l.closed = true` under its own internal mutex. This is instance-scoped state, not shared global state.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis — `internal/security/security.go`\n\n### Systems Interacted With\n\nAll interactions are **local filesystem only**. No network, database, or external service calls exist in this code. Operations include:\n\n| Operation | Function(s) | Description |\n|---|---|---|\n| `filepath.Abs` / `EvalSymlinks` | `SafeResolve`, `AcquireLock` (via SafeResolve) | Resolve absolute paths and evaluate symlinks on the repository root/ancestor |\n| `os.Lstat` (loop) | `SafeResolve` | Walk up directory hierarchy to find closest physically-existing ancestor during path traversal validation |\n| `filepath.Rel` | `SafeResolve` | Verify resolved target lies strictly inside resolved root |\n| `os.OpenFile(... O_EXCL)` | `AcquireLock` | Atomic create of lock file; failure indicates another process holds the lock or a stale file exists |\n| `f.Write([]byte{...})` | `AcquireLock` | Write current PID to lock file for identification/verification |\n| `os.ReadFile` | `EnsureGitignoreHasLockfile` | Read existing `.gitignore` contents before appending entry |\n| `os.CreateTemp` + `tmpFile.Write` | `EnsureGitignoreHasLockfile` | Atomic write pattern: build new content in temp file, then rename over original |\n| `tmpFile.Sync` | `EnsureGitignoreHasLockfile` | Force flush to disk before close/rename |\n| `os.Chmod` | `EnsureGitignoreHasLockfile` | Set permissions on temp file matching default (0644) |\n| `os.Rename` | `EnsureGitignoreHasLockfile` | Atomically replace `.gitignore` with updated version |\n\n### Error Propagation Patterns\n\n**1. `SafeResolve(repoRoot, inputPath)` → `(string, error)`**\n\n- Errors from `filepath.Abs`, `EvalSymlinks(absRoot)`, and `EvalSymlinks(current)` are wrapped with context and returned directly\n- The `os.Lstat` loop terminates on first success or when reaching the filesystem root (`parent == current`)\n- Non-not-exist errors during stat are wrapped and returned immediately\n- Final check: if `filepath.Rel` returns error OR relative path starts with `\"..\"`, returns a wrapped `ErrPathTraversal` error\n\n**2. `SimpleLock.Unlock()` → `error`**\n\n- Captures close error in `err` variable\n- Attempts `os.Remove`; only replaces `err` if (a) the previous operation succeeded AND (b) remove fails with a non-not-exist error\n- Result: if close succeeds but remove fails, that error is returned; if close fails but remove also fails, only the close error is surfaced\n\n**3. `AcquireLock(repoRoot)` → `(*SimpleLock, error)`**\n\n- SafeResolve errors propagate to caller unchanged\n- On `os.OpenFile` failure: checks `os.IsExist(err)` — if lock file already exists, returns a specific message instructing manual cleanup; otherwise wraps with path context and original error\n- On write failure after successful open: cleans up by calling `f.Close()` then `os.Remove`, then returns the write error\n\n**4. `EnsureGitignoreHasLockfile(repoRoot)` → `error`**\n\n- SafeResolve errors propagate directly\n- Non-not-exist read failures are wrapped with `\"error reading .gitignore\"` context\n- All temp file operations (`CreateTemp`, `Write`, `Sync`, `Close`, `Chmod`) return wrapped errors to caller on failure\n- Final `os.Rename` error is returned if it fails\n\n### Notable Observations\n\n**Swallowed errors in deferred cleanup:**  \nThe deferred block at the end of `EnsureGitignoreHasLockfile`:\n```go\ndefer func() {\n    tmpFile.Close()\n    os.Remove(tmpName)\n}()\n```\nBoth `tmpFile.Close()` and `os.Remove(tmpName)` return values are discarded. If the temp file cannot be closed or removed (e.g., permission denied, interrupted signal), that error information is lost silently. This is a minor leak — not critical for correctness since the rename has already succeeded at that point, but it's worth noting.\n\n**No network/database interactions:** Confirmed by full code inspection. All I/O targets are local paths under `repoRoot`."
    },
    "internal/tools/file_tools.go": {
      "sha256": "148e89ba5fb0c281d7974f4f8eb90b0e067852eb1742503aad3df1404f573cd8",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/tools/file_tools.go`\n\n## Functions\n\n### ReadFileSafely\n```go\nfunc ReadFileSafely(repoRoot, virtualPath string) ([]byte, error)\n```\n\n**Description:** Resolves the virtual path inside the repository and reads the file content. Returns nil for both values when an error occurs.\n\n---\n\n### WriteFileSafely\n```go\nfunc WriteFileSafely(repoRoot, virtualPath string, content []byte) error\n```\n\n**Description:** Resolves the virtual path inside the repository and writes `content`. Creates parent directories if needed and uses a TOCTOU-safe write pattern. Returns nil when successful.\n\n---\n\n### LoadGitignore\n```go\nfunc LoadGitignore(repoRoot string) ([]string, error)\n```\n\n**Description:** Reads `.gitignore` from `repoRoot` and returns its active ignore patterns. Returns `nil` for both values when the file does not exist.\n\n---\n\n### ShouldIgnoreFile\n```go\nfunc ShouldIgnoreFile(relPath string, gitIgnore *ignore.GitIgnore) bool\n```\n\n**Description:** Checks if a file (specified by relative path) is ignored. Returns `true` when the file should be ignored, otherwise returns `false`.\n\n---\n\n### DiscoverCodeFiles\n```go\nfunc DiscoverCodeFiles(repoRoot string, ignores []string) ([]string, error)\n```\n\n**Description:** Recursively walks the codebase to find high-signal source files. Ignores build, dependency, and output files, as well as any paths in the custom ignores list. Returns `nil` for both values when an error occurs.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:** This code provides a repository-aware file I/O and discovery subsystem. It solves the problem of safely reading, writing, and discovering source files within a project root while preventing path traversal attacks and ignoring irrelevant paths (build artifacts, dependencies, dot-files).\n\n**High-Level Algorithm Steps:**\n\n1. **Safe Path Resolution:** All file operations resolve user-supplied virtual paths against the repository root using a security layer that prevents escaping the repo boundary.\n\n2. **TOCTOU-Safe File Writes:** Files are written to a temporary in-directory location first, then atomically renamed into place—preventing race conditions between checking and creating files. Directories are created as needed with default permissions before writing begins.\n\n3. **Gitignore Parsing:** A `.gitignore` file is read from the repo root; non-empty, non-comment lines are collected as active patterns for later filtering.\n\n4. **Ignore Decision Logic:** A path is considered ignored if it matches user-supplied gitignore patterns OR if any path component starts with a dot (hidden files) OR ends with `.egg-info`.\n\n5. **Recursive Code Discovery:** The repository root is walked recursively. Directories starting with a dot or ending with `.egg-info` are skipped entirely. Files are included only if they pass the ignore checks defined in step 4, and their relative paths (normalized to forward slash) are collected as results.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O and Error Propagation Analysis\n\n### 1. `ReadFileSafely` — Disk Read\n\n| Step | System Interaction | Error Handling |\n|------|-------------------|----------------|\n| `security.SafeResolve(repoRoot, virtualPath)` | Path resolution via security layer (external dependency) | Errors propagate directly: returned as-is to caller (`return nil, err`) |\n| `os.ReadFile(safePath)` | Disk read | Wrapped with `fmt.Errorf(\"failed to read file content: %w\", err)` — original error preserved via `%w` for unwrapping |\n\n### 2. `WriteFileSafely` — Disk Write (TOCTOU-safe pattern)\n\n| Step | System Interaction | Error Handling |\n|------|-------------------|----------------|\n| `security.SafeResolve(repoRoot, virtualPath)` | Path resolution via security layer | Errors propagate directly (`return err`) |\n| `os.MkdirAll(dir, defaultDirPerm)` | Disk mkdir for parent directory | Wrapped with `\"failed to create directory: %w\"` — original error preserved |\n| `os.CreateTemp(dir, ...)` | Disk temp file creation in target directory | Wrapped with `\"failed to create temp file: %w\"` — original error preserved |\n| Cleanup deferred block (`tmpFile.Close()`, `os.Remove(tmpName)`) | Resource cleanup (no error propagation — errors silently swallowed) | **No error returned** — if Close/Remove fails, error is discarded. Caller sees no failure signal |\n| `tmpFile.Write(content)` | Disk write to temp file | Wrapped with `\"failed to write to temp file: %w\"` — original error preserved |\n| `tmpFile.Sync()` | fsync/disk sync | Wrapped with `\"failed to sync temp file: %w\"` — original error preserved |\n| `tmpFile.Close()` | Flush/close fd | Wrapped with `\"failed to close temp file: %w\"` — original error preserved |\n| `os.Chmod(tmpName, defaultFilePerm)` | File permission change | Wrapped with `\"failed to chmod temp file: %w\"` — original error preserved |\n| `os.Rename(tmpName, safePath)` | Atomic rename (disk-level) | Wrapped with `\"failed to rename temp file: %w\"` — original error preserved |\n\n### 3. `LoadGitignore` — Disk Read\n\n| Step | System Interaction | Error Handling |\n|------|-------------------|----------------|\n| `os.Open(gitignorePath)` | Disk read of `.gitignore` | Two paths: if `os.IsNotExist(err)` → returns `nil, nil` (no error). Otherwise wraps original error |\n| `bufio.Scanner` reading file | Streaming disk read | Errors from scanner are checked via `scanner.Err()` and returned as-is (`return patterns, scanner.Err()`) |\n\n### 4. `ShouldIgnoreFile` — No External I/O\n\nPure computation on a path string. No disk, network, or external system interactions.\n\n### 5. `DiscoverCodeFiles` — Disk Walk\n\n| Step | System Interaction | Error Handling |\n|------|-------------------|----------------|\n| `filepath.WalkDir(repoRoot, ...)` | Recursive directory walk (disk traversal) | The callback **swallows errors**: when `err != nil`, it logs a warning to `os.Stderr` via `fmt.Fprintf(os.Stderr, ...)` and returns `nil`. Walk continues. Final error is returned as-is from the walk function |\n\n### Error Propagation Summary\n\n- **Preserved errors (via `%w`)**: All disk I/O in `ReadFileSafely`, `WriteFileSafely`, and `LoadGitignore` use `%w` wrapping, preserving the original error chain for unwrapping.\n- **Swallowed errors**: The deferred cleanup block in `WriteFileSafely` discards Close/Remove errors entirely. The walk callback in `DiscoverCodeFiles` logs to stderr and returns `nil`, losing the original error context.\n- **Direct propagation (no wrap)**: Errors from `security.SafeResolve` pass through unchanged — caller sees exactly what happened.\n- **Silent path**: `LoadGitignore` treats a missing `.gitignore` as success, returning empty slice + nil error. This is intentional behavior, not an I/O failure signal."
    },
    "internal/tools/git_tools.go": {
      "sha256": "bdf47dd0db3277f300f4775e1dfb68f549dd24b9f78a2a884984bb6c930f8264",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `tools` package (`git_tools.go`)\n\n- **`RunGit(repoRoot string, args ...string) (string, error)`** — Executes a git command in the specified repo directory with `--no-pager`. Returns trimmed stdout and any error.\n\n- **`VerifyGitRepo(repoRoot string) error`** — Checks that `git` is available and that `repoRoot` is inside a git working tree.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code provides an abstraction for executing Git commands within a specific directory and validating whether that directory constitutes a recognized Git repository. It centralizes process execution logic to ensure consistent handling of stdout, stderr, and error reporting when interacting with the Git version control system.\n\n**High-Level Algorithm Steps**\n\n1.  **Initialize Command:** Construct a Git command invocation in the specified root directory, appending `--no-pager` to any user-provided arguments to suppress pager output.\n2.  **Execute and Capture:** Run the constructed process while capturing standard output and error streams into buffers.\n3.  **Handle Success or Failure:**\n    *   If execution succeeds, trim whitespace from the captured standard output and return it as a string result.\n    *   If execution fails, trim whitespace from the captured error stream and return a formatted error message containing the failure details.\n4.  **Validate Repository Status:** Invoke `git rev-parse --is-inside-work-tree` specifically to check if the directory is inside a work tree. If this command returns an error, classify the location as not being a Git repository and return that specific classification.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External Systems Interacted With\n\n| System | Mechanism | Details |\n|--------|-----------|---------|\n| **External process (git)** | `exec.Command(\"git\", ...)` via `os/exec` | Spawns a child `git` subprocess in `repoRoot`. Stdout captured to `bytes.Buffer`, stderr separately buffered. `cmd.Run()` executes the command and returns the exit error. |\n\n### Error Propagation\n\n- **Primary entry point — `RunGit`**:\n  - Returns `(string, error)`. If `cmd.Run()` fails, it wraps the error with:  \n    `fmt.Errorf(\"git command failed: %v, stderr: %s\", err, trimmedErr)`  \n    The original `err` from `exec.Cmd` (e.g., `*exec.Error`, non-zero exit status) is embedded as `%v`; stderr text is appended. No panic occurs on failure.\n\n- **Secondary entry point — `VerifyGitRepo`**:\n  - Calls `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")`. If the returned error is non-nil, it wraps with:  \n    `fmt.Errorf(\"not a git repository (or any of the parent directories): %w\", err)`  \n    Uses `%w` for `wrapError`-compatible wrapping (recoverable by unwrappers).\n\n### No External Side Effects\n\nNo direct disk reads/writes, no network calls, no database interactions. The only external interaction is subprocess execution against the installed `git` binary."
    },
    "main.go": {
      "sha256": "16ab362b1830374f0306a1990dba8d51da887c1ad126f311c03d242933d2a516",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `main.go`\n\n## Functions\n\n- **`main()`** `func() error`  \n  - Entry point of the program. Delegates to `cmd.RootCmd.Execute()`. On error, prints a message to stderr and exits with code 1.\n    - Returns: `error` (via `fmt.Fprintf`/`os.Exit` path — swallowed into exit)\n\n## Referenced External Types \u0026 Methods\n\n- **`RootCmd`** — type from package `github.com/arrase/code-reducer/cmd`.  \n  - Method: **`Execute()`** — invoked as `cmd.RootCmd.Execute()`.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose \u0026 Domain Rules\n\n### Primary Domain Problem Solved\nThis code provides only an **entry-point shell** for a command-line tool. No domain logic is implemented in this file — all processing happens inside the `cmd` package (`github.com/arrase/code-reducer/cmd`). The entry point itself just:\n\n1. Delegates execution to `cmd.RootCmd.Execute()`\n2. Swallows any error by printing it to stderr and exiting with code 1\n3. Returns success (exit code 0) if no error occurred\n\n### High-Level Algorithm Steps\nThere are none visible in this file. The algorithm is entirely externalized into the imported command package.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:**\n- `os.Stderr` — writes formatted error message to standard error stream when execution fails. This is an external side effect (terminal output).\n- `os.Exit(1)` — terminates the process with exit code 1. External system interaction.\n- The third-party package (`github.com/arrase/code-reducer/cmd`) is called via `cmd.RootCmd.Execute()`. No internal source text available to determine what I/O it performs (file writes, network calls, DB queries, etc.). Cannot confirm or deny external effects from this package.\n\n**Error Propagation:**\n- Errors returned by `cmd.RootCmd.Execute()` are propagated as a single-line formatted string (`fmt.Fprintf`) printed to stderr.\n- No retry logic, no fallback paths, no partial recovery — error is terminal.\n- If `Execute()` returns nil (no error), the program exits cleanly with code 0 (default). No explicit call to `os.Exit(0)` but Go's default exit behavior applies."
    }
  },
  "modules": {
    ".": "# Code-Reducer Architecture Documentation\n\n## Module Responsibility \u0026 Data Flow\n\nCode-Reducer is a local-first AI agent that generates and maintains project wikis by analyzing source code against an LLM backend. The system orchestrates multi-step operations through a Cobra command tree, where each subcommand delegates to an unexported engine via a shared execution path. All configuration resolution, synthesis pipeline orchestration, file I/O, Git validation, security enforcement, and user interaction occur across four internal packages: `internal/config`, `internal/engine`, `internal/security`, and `internal/tools`. The entry point (`main.go`) is a thin shell that delegates to the command package and terminates with exit code 1 on error.\n\n```\nmain.go ──► cmd.RootCmd.Execute()\n    │\n    ├── cmd/init.go          (engine.ModeInit)\n    ├── cmd/update.go        (engine.ModeUpdate)\n    └── cmd/setup.go         (interactive reconfiguration → config.SaveConfig)\n    │\ninternal/config ─────────────► internal/engine ──► LLM client ──► synthesis artifacts\n  │                                                                    │\n  ▼                                                                    ▼\nResolveConfig                  Runner.Run(ctx, repoRoot, mode)        WriteFileSafely\n  │                                                                    │\n  ├── LoadConfig               security.AcquireLock                   │\n  ├── SaveConfig                                                       │\n  └── config.Config pointer                                           │\n                                                                      ▼\ninternal/security ─────────► internal/tools ──► disk I/O / git subprocesses\n```\n\n---\n\n## Entry Point (`main.go`)\n\n### `main()` — Program Initiation\n\nEntry point. Delegates to `cmd.RootCmd.Execute()`. On error, formats the returned value via `fmt.Fprintf` and writes it to `os.Stderr`; exits with code 1. Returns no observable error path (the only return is swallowed into `os.Exit(1)`). Nil returns from `Execute()` result in default Go exit behavior (code 0). No synchronization primitives; single goroutine execution model.\n\n### Referenced External Types\n\n- **`RootCmd`** — type from package `github.com/arrase/code-reducer/cmd`. Method: `Execute() error`. Invoked as the sole root command; all subcommand registration and lifecycle management occurs within this package.\n\n---\n\n## Configuration Subsystem (`internal/config`)\n\n### Module Responsibility\n\nCentralized configuration schema and persistence primitives for the synthesis pipeline. Defines `Config` (runtime parameters: LLM identity, Ollama base URL/context size, docs directory, prompt templates, extraction steps, ignore lists) and `ExtractionStep` (single fact-extraction phase name + system prompt). Exposes three I/O functions (`ConfigExists`, `LoadConfig`, `SaveConfig`) that manage `.code-reducer.yaml`; a fourth function (`ResolveConfig`) merges CLI flags, environment variables, YAML config, and hardcoded defaults into a single resolved `*Config`.\n\n### Types\n\n#### `ExtractionStep`\n\nSingle extraction phase executed during file-fact extraction stage. Carries a human-readable name and an LLM system prompt that guides extraction behavior.\n\n| Field | Type | Tags |\n|-------|------|------|\n| `Name` | `string` | `yaml:\"name\"` |\n| `Prompt` | `string` | `yaml:\"prompt\"` |\n\n#### `Config`\n\nCentral configuration struct. All fields are optional and may be left at zero values; defaults kick in during resolution if the field is empty or unset.\n\n| Field | Type | Tags | Notes |\n|-------|------|------|-------|\n| `ModelID` | `string` | `yaml:\"model_id\"` | LLM model identifier (e.g., `\"ornith:9b\"`). |\n| `OllamaBaseURL` | `string` | `yaml:\"ollama_base_url\"` | Base URL for the Ollama inference server. |\n| `OllamaNumCtx` | `int` | `yaml:\"ollama_num_ctx\"` | Context size parameter forwarded to Ollama. |\n| `DocsDir` | `string` | `yaml:\"docs_dir\"` | Directory path where generated documentation is written. |\n| `SystemPrompt` | `string` | `yaml:\"system_prompt\"` | System prompt injected into the model at synthesis time. |\n| `ModuleSynthesisPrompt` | `string` | `yaml:\"module_synthesis_prompt\"` | Prompt used during module-level synthesis. |\n| `ArchitecturePrompt` | `string` | `yaml:\"architecture_prompt\"` | Prompt used during architecture-level synthesis. |\n| `FileFactConsolidationPrompt` | `string` | `yaml:\"file_fact_consolidation_prompt\"` | Prompt used to consolidate overlapping file facts before synthesis. |\n| `ExtractionSteps` | `[]ExtractionStep` | `yaml:\"extraction_steps\"` | Ordered list of extraction phases; empty slice triggers fallback to `DefaultExtractionSteps`. |\n| `Ignore` | `[]string` | `yaml:\"ignore\"` | File paths or patterns to skip during analysis. Duplicates are removed by the resolver. |\n\n### Constants\n\n#### Environment and CLI Keys\n\n| Constant | Value | Type | Purpose |\n|----------|-------|------|---------|\n| `CodeReducerModelIDEnvKey` | `\"CODE_REDUCER_MODEL_ID\"` | `string` | Env var key for model ID override in resolver. |\n| `OllamaBaseURLEnvKey` | `\"OLLAMA_BASE_URL\"` | `string` | Env var key for Ollama base URL override in resolver. |\n| `OllamaNumCtxEnvKey` | `\"OLLAMA_NUM_CTX\"` | `string` | Env var key for context size override in resolver. |\n\n#### Defaults\n\n| Constant | Value | Type | Purpose |\n|----------|-------|------|---------|\n| `OllamaDefaultBaseURL` | `\"http://localhost:11434\"` | `string` | Default Ollama inference server address. |\n| `OllamaDefaultModelID` | `\"ornith:9b\"` | `string` | Default LLM model identifier. |\n| `OllamaDefaultNumCtx` | `8192` | `int` | Default context size. |\n| `DefaultDocsDir` | `\"wiki\"` | `string` | Default documentation output directory. |\n| `ConfigFileName` | `\".code-reducer.yaml\"` | `string` | Filename used by all three I/O functions and `ResolveConfig`. |\n\n### Public API Surface — Types \u0026 Variables (from config.go)\n\n#### `DefaultExtractionSteps`\n\nPackage-level variable of type `[]ExtractionStep`. Serves as the fallback extraction pipeline when the YAML config contains an empty or nil slice. Each step carries a name and prompt for one of the four fact-extraction phases: *API signatures*, *business logic*, *mutable state \u0026 concurrency*, *external I/O \u0026 error propagation*.\n\n### Public API Surface — I/O Functions (from io.go)\n\n#### `ConfigExists`\n\n```go\nfunc ConfigExists(cwd string) bool\n```\n\nReturns `true` if `.code-reducer.yaml` exists at the path formed by joining `cwd` with `ConfigFileName`. Underlying operation is a single `os.Stat` call; no error wrapping occurs. Returns only a boolean to caller.\n\n#### `LoadConfig`\n\n```go\nfunc LoadConfig(cwd string) (*Config, error)\n```\n\nReads and parses `.code-reducer.yaml` from `cwd`. On success returns the populated `*Config`. Failure modes:\n\n| Condition | Return Value | Notes |\n|-----------|--------------|-------|\n| YAML parse error | `(nil, wrappedError)` | Error is wrapped with `\"failed to parse yaml config: %w\"`. |\n| Raw OS read error (not-not-exist) | `(nil, rawError)` | `os.ReadFile` failure propagates unchanged. |\n\n#### `SaveConfig`\n\n```go\nfunc SaveConfig(cwd string, cfg *Config) error\n```\n\nSerializes `cfg` to `.code-reducer.yaml` using an atomic write pattern: create temp file → write bytes → sync → close → chmod → rename over original path. File permission applied is `0600`. Failure points are wrapped individually with descriptive prefixes (`\"failed to \u003caction\u003e config file\"`). The final state is either the old file or a fully-written new file — never neither, because of the rename semantics.\n\n### Public API Surface — Resolution (from resolve.go)\n\n#### `ResolveConfig`\n\n```go\nfunc ResolveConfig(repoRoot string, modelIDFlag string, numCtxFlag string) (*Config, error)\n```\n\nMerges configuration across four precedence tiers:\n\n1. Hardcoded defaults (`OllamaDefault*`, `DefaultDocsDir`, etc.).\n2. Values parsed from `.code-reducer.yaml` at `repoRoot`.\n3. Environment variables (`CodeReducerModelIDEnvKey`, `OllamaBaseURLEnvKey`, `OllamaNumCtxEnvKey`).\n4. CLI flags (`modelIDFlag`, `numCtxFlag`).\n\nSpecific resolution rules:\n\n- **`Ignore` list**: Deduplicated in-place; first occurrence wins via hash-set tracking.\n- **`ExtractionSteps`**: Falls back to `DefaultExtractionSteps` when the YAML slice is empty or nil.\n- **Numeric fields** (`OllamaNumCtx`): Parsed with `strconv.Atoi`; non-positive values are rejected and the prior tier's value is retained.\n- **String prompts**: Empty YAML values fall back to the corresponding hardcoded default via `resolveString`.\n\n### Internal Helpers (from resolve.go)\n\n| Function | Parameters | Return Types | Description |\n|----------|-----------|--------------|-------------|\n| `deduplicate` | `a []string` | `[]string` | Removes duplicates from a string slice; first occurrence wins. |\n| `resolveString` | `yamlVal string`, `defaultVal string` | `string` | Returns `yamlVal` if non-empty, otherwise returns `defaultVal`. Used for prompt field resolution. |\n\n### Error Propagation Summary — config subsystem\n\n- **config.go**: No runtime operations; no errors can be raised or swallowed here.\n- **io.go**: All disk-read and disk-write functions return error tuples. YAML parse errors are explicitly wrapped with `\"failed to parse yaml config: %w\"`; raw OS-level read/write errors propagate unchanged through the returned tuple. Write-path errors each carry a unique prefix (`\"failed to \u003caction\u003e temp config file\"`).\n- **resolve.go**: `LoadConfig` failure is only surfaced when `os.IsNotExist(err)` returns false — missing files are silently replaced with an empty `Config{}` and no error reaches the caller. All other branches (env lookups, flag parsing, deduplication) produce zero errors by design.\n\n### External I/O Analysis — config subsystem\n\n- **Disk**: All functions interact exclusively with `.code-reducer.yaml` under `cwd`. Writes use temp file + rename for atomicity; permissions are set to `0600`.\n- **Environment / Process**: Three env vars (`CODE_REDUCER_MODEL_ID`, `OLLAMA_BASE_URL`, `OLLAMA_NUM_CTX`) are read by `ResolveConfig`; none are written.\n- **Network**: None. Constants like `OllamaDefaultBaseURL` are string literals; no HTTP client or network calls exist in this module.\n- **Database/APIs**: None referenced.\n\n---\n\n## Engine Subsystem (`internal/engine`)\n\n### Module Responsibility and Data Flow\n\nPersistent, incremental documentation synthesis pipeline for software repositories. Produces three artifact classes — global architecture summaries, quickstart guides, and per-module API descriptions — from source code using an LLM backend, with change detection that limits regeneration to directories impacted by actual file modifications. State persists across invocations via a metadata cache stored as JSON under `docs/`; a global steps hash anchors cache validity to the current extraction pipeline configuration.\n\n### Runner Entry Point (`runner.go`)\n\n#### Mode\n\nString-typed operation modes for the pipeline. Two exported constants:\n\n| Constant | Value |\n|----------|-------|\n| `ModeInit`  | `\"init\"`   |\n| `ModeUpdate` | `\"update\"` |\n\n#### Runner\n\nStruct holding a single pointer field, `cfg *config.Config`, assigned once in the constructor and never reassigned within this file. External mutability of the pointed-to struct is not observable from here alone.\n\n```go\nfunc NewRunner(cfg *config.Config) *Runner\nfunc (r *Runner) Run(ctx context.Context, repoRoot string, mode Mode, onEvent func(Event)) error\n```\n\n`Run()` performs three operations before branching:\n\n1. **Lockfile validation** — `security.EnsureGitignoreHasLockfile(repoRoot)` is invoked; any error is logged as a status event and execution proceeds regardless.\n2. **Exclusive lock acquisition** — `security.AcquireLock(repoRoot)` returns an error that is wrapped with context (`failed to acquire repository lock: %w`) and returned immediately. A deferred unlock runs on every exit path, guaranteeing release even when the pipeline fails partway through.\n3. **Client instantiation** — `newLLMClient(r.cfg.ModelID, r.cfg.OllamaBaseURL, r.cfg.OllamaNumCtx)` constructs an Ollama HTTP client; no network I/O occurs at this point.\n\nAfter lock acquisition, execution branches on the mode value: invalid strings return `fmt.Errorf(\"unsupported mode: %s\", mode)`. Otherwise, either `orch.RunInit` or `orch.RunUpdate` is invoked directly and its error (or nil) is returned to the caller.\n\n### Orchestrator (`orchestrator.go`)\n\n#### Types\n\n```go\ntype EventType string   // \"status\" | \"error\" (unexported)\n\ntype Event struct {\n    Type   EventType `json:\"type\"`\n    Message string    `json:\"message\"`\n}\n```\n\nBoth constants and the type are unexported. The orchestrator itself is also internal; only its public method signatures appear in the package surface.\n\n#### GenerateStandardDocs\n\n```go\nfunc (o *orchestrator) GenerateStandardDocs(ctx, repoRoot, docsDir, rootSum string, cfg *config.Config, logEvent LogEventFunc) error\n```\n\nInvoked from both `RunInit` and `RunUpdate`. Issues two sequential LLM calls: one to generate `architecture.md` and another for `quickstart.md`, each wrapped as a global artifact. Errors propagate fully (wrapped with `%w`). If the top-level directory is marked affected or standard docs are missing, this function runs; otherwise it is skipped entirely on update.\n\n### Pipeline Context State (`orchestrator.go`)\n\nThe orchestrator maintains mutable state across its lifecycle via pointers into shared structs:\n\n| Field | Type | Mutability | Notes |\n|-------|------|------------|-------|\n| `cache` | `*MetadataCache` | **Mutable** | Reassigned in `setupPipeline`, `teardownPipeline`, `RunInit`, `RunUpdate`. Fields `.StepsHash`, `.Files`, `.Modules` are updated across calls. |\n| `cfg` | `*config.Config` | Potentially mutable | Read only here (`SystemPrompt`, `ArchitecturePrompt`, `ExtractionSteps`). External code may mutate the pointed-to struct at runtime. |\n| `client` | `llmCaller` interface | Read-only in this file | Initialized on construction; never reassigned within these methods. |\n\nNo synchronization primitives (mutex, atomic, channel) protect access to any of these fields. Concurrent callers sharing a single context instance would race on the cache maps. All operations are sequential and single-threaded per method invocation.\n\n### Tree Construction and Affected Detection (`tree.go`)\n\n#### Types\n\n```go\ntype ChangeStatus string   // \"Added\" | \"Modified\" | \"Deleted\" (unexported)\n\ntype FileChange struct {\n    Path  string\n    Status ChangeStatus\n}\n\ntype DirNode struct {\n    Path     string\n    Files    []string\n    Children map[string]*DirNode\n}\n```\n\n#### buildTree\n\nConstructs a nested `DirNode` hierarchy from a slice of file paths. Leaf entries are files; intermediate path segments become child nodes. No I/O, no side effects.\n\n### Affected Detection Logic\n\nThe orchestrator walks the tree to mark directories as \"affected\" via four conditions:\n\n1. **Changed-file match** — if any `FileChange` in the map matches a file under this node's subtree.\n2. **Module path non-existence on disk** — `security.SafeResolve(repoRoot, modulePath)` followed by `os.Stat(absModulePath)`. If the stat error is not `os.ErrNotExist`, the branch is skipped silently.\n3. **Cache emptiness** — if the corresponding entry in `cache.Modules` is empty.\n4. **Child propagation** — a second recursive pass marks any parent affected whenever *any* child has already been flagged, regardless of direct match.\n\n### LLM Client (`client.go`)\n\n#### Public Types\n\n```go\ntype Message struct {\n    Role    string `json:\"role\"`\n    Content string `json:\"content\"`\n}\n```\n\n`Message` is the only exported type in this file. All other declarations are unexported and therefore not part of the public API:\n\n| Element | Type | Notes |\n|---|------|-------|\n| `llmCaller` | interface | Unexported; methods include `CallLLM(ctx, systemPrompt, messages, jsonFormat)` returning `(string, error)` and `NumCtx() int`. |\n| `llmClient` | struct | Unexported. Fields: `modelID`, `baseURL`, `numCtx`, `httpClient *http.Client`. |\n| `newLLMClient` | func | Factory for `*llmClient`; referenced but not defined in this file. |\n| `(c *llmClient) NumCtx()` | method | Returns the configured context window size. |\n| `ollamaRequest` / `ollamaOptions` / `ollamaResponse` | structs | Unexported request/response payloads for Ollama's `/api/chat`. |\n| `(c *llmClient) prepareOllamaRequest()` | private method | Builds and serializes the chat payload. |\n\n#### External I/O\n\nAll network interactions target `{baseURL}/api/chat` via HTTP POST with JSON body (`application/json`). The client is constructed once per instance; `httpClient` is reused across calls without locking, which is safe because timeout and base URL are fixed at construction time and never reassigned within this file.\n\n**Response handling:** 200 OK responses have their bodies read fully via `io.ReadAll`; non-OK status codes trigger a bounded read (`io.LimitReader(resp.Body, maxErrorBodyBytes)`) whose error is discarded silently — the resulting string (containing the HTTP status and truncated body) is returned to the caller instead.\n\n**Unresolved references:** `defaultHTTPTimeout` and `maxErrorBodyBytes` are referenced in this file but not defined here; they must be package-level constants or variables declared elsewhere, otherwise compilation fails.\n\n### Chunking and Reduction Pipeline (`chunking.go`)\n\n#### reduceWithLLM\n\n```go\nfunc reduceWithLLM(ctx context.Context, c llmCaller, items []string, sysPrompt string, buildPrompt func(batch []string) string, logMsg func(batch []string) string, errMsg string, logEvent LogEventFunc) (string, error)\n```\n\nEntry point for all LLM-mediated reductions. Calls `c.CallLLM(ctx, sysPrompt, messages, false)` with a system prompt prepended to user messages; errors are wrapped via `fmt.Errorf(\"%s: %w\", errMsg, err)`. The original error is preserved through `%w` wrapping.\n\n#### reduceInChunks\n\n```go\nfunc reduceInChunks(ctx context.Context, c llmCaller, nodePath string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)\n```\n\nInvoked from the synthesis step to consolidate a directory's file facts and child summaries. Calls `reduceWithLLM` with a prompt derived from `cfg.SystemPrompt` and the current extraction steps. Errors propagate through unchanged.\n\n#### reduceFileFacts\n\n```go\nfunc reduceFileFacts(ctx context.Context, c llmCaller, filePath string, stepName string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)\n```\n\nConsolidates per-extraction-step outputs for a single file. Calls `reduceWithLLM` with a prompt built from `cfg.SystemPrompt` and the current extraction steps; errors propagate as-is.\n\n#### reduceItems\n\n```go\nfunc reduceItems(ctx context.Context, items []string, maxChars int, reduceFn func(batch []string) (string, error)) (string, error)\n```\n\nIterative map-reduce loop: accumulates items into batches that fit within `maxChars`, flushing when the next item would exceed the limit. If a single item exceeds the cap at entry, it is split with overlap rather than truncated — information preservation over compression. After each LLM pass, total output run count vs input is computed; if output ≥95% of input (integer division), recursion terminates and results are concatenated with `\\n\\n`. Context cancellation (`ctx.Err()`) returns early without partial results.\n\n#### chunkTextWithOverlap\n\n```go\nfunc chunkTextWithOverlap(text string, maxRunes int, overlapRunes int) []string\n```\n\nSplits text into overlapping chunks of at most `maxRunes` runes with `overlapRunes` rune overlap between adjacent chunks (clamped if ≥ `maxRunes`). Returns the full text as a single chunk when `maxRunes \u003c= 0` or `overlapRunes \u003e= maxRunes`. No panics, no bounds errors.\n\n### Markdown Fence Stripping (`markdown.go`)\n\n#### Internal Implementation\n\n```go\nvar markdownFenceRe regexp.Regexp\nfunc stripOuterMarkdownFence(content string) string\n```\n\nUnexported; both the variable and function begin with lowercase letters. The regex is compiled once at package scope and never reassigned — effectively read-only after compilation.\n\n**Algorithm:** trim whitespace → match triple-backtick fence (with optional `markdown`/`json` language tag) → return captured interior after trimming surrounding whitespace. No error return; if the pattern doesn't match, original input is returned unchanged.\n\n### Constants (`constants.go`)\n\nAll nine named constants in this file are unexported:\n\n| Constant | Purpose |\n|----------|---------|\n| `defaultHTTPTimeout` | 10-minute ceiling for remote API calls (set at package init) |\n| `maxErrorBodyBytes` | 1 KB cap on error response payloads captured from failed requests |\n| `defaultChunkOverlap` | 800-token overlap between consecutive text chunks during streaming/processing |\n| `minNumCtxFloor` | 512 minimum threshold for context length calculations |\n| `contextWindowAllocRatio` | 0.75 reserved proportion of available space for primary content |\n| `maxCharsMultiplier` | 3× multiplier applied to base character limit |\n| `metadataFileName`, `agentsFileName`, `defaultDirPerm` | Filesystem conventions for state persistence |\n\nNo mutable state, no external I/O, no error propagation. The single function-type alias (`LogEventFunc`) is declared but not used within this file.\n\n### Synthesis Engine (`synthesize.go`)\n\nUnexported types and functions only; all identifiers begin with lowercase letters.\n\n#### External I/O Matrix\n\n| Source | Path | Behavior on Failure |\n|--------|------|---------------------|\n| `tools.ReadFileSafely(p.repoRoot, f)` — first call | `\u003crepoRoot\u003e/\u003cf\u003e` | Logged as status warning; returns `\"\", nil`. Swallowed by caller. |\n| `tools.ReadFileSafely` — second call (inside `if facts == \"\"`) | Same | Same swallow pattern. |\n| `p.client.CallLLM(p.ctx, systemPrompt, []Message{...}, false)` | Network endpoint via `llmCaller` | Wrapped with `%w`, includes step name and file path in message. Propagated to caller as `\"LLM error extracting %s for %s: %w\"`. |\n| `tools.WriteFileSafely(p.repoRoot, modulePath, []byte(finalSum))` | `\u003crepoRoot\u003e/\u003cdocsDir\u003e/modules/\u003csafe-filename-of-node-path\u003e.md` | Wrapped with `%w`, includes node path. Propagated to caller as `\"failed to write module documentation for %s: %w\"`. |\n\n#### Synthesis Algorithm (per `synthesizeNode`)\n\n1. **Cache-first lookup** — check `cache.Files[f]` against precomputed SHA-256 hash; on match, reuse cached facts without re-reading the file.\n2. **Recursive bottom-up traversal** — for each directory node: if unaffected and descendants are unaffected, return cached summary immediately. Sort child names alphabetically; recurse into children first; collect non-empty summaries into a map keyed by child name.\n3. **Per-file extraction** — compute dynamic content limit from remaining context window capacity (allocates ~75% for file content). Split large files with `chunkTextWithOverlap`. Pass each chunk to LLM with step-specific prompt derived from `ExtractionSteps`. Strip markdown fences from responses via `stripOuterMarkdownFence`. Consolidate all outputs per extraction step using `reduceFileFacts`. Concatenate across steps into a single file fact string.\n4. **Assembly** — combine extracted file facts (prefixed `\"### File: basename\"`) and child summaries (prefixed `\"### Subsystem: name\"`) into one `components` slice. If nothing exists, clear the cache entry and return empty.\n5. **Final reduction and persistence** — pass all components to chunked reduction (`reduceInChunks`) producing a single consolidated summary for the directory; store in `cache.Modules[node.Path]`; write to disk under `\u003cdocsDir\u003e/modules/\u003csafe-filename-of-path\u003e`.\n\n### Utility Helpers (`utils.go`)\n\n#### toSafeMarkdownFilename\n\n```go\nfunc toSafeMarkdownFilename(modulePath string) string\n```\n\nPure string manipulation; no file operations. Returns `\"README.md\"` when the path is empty or `\".\"`; otherwise joins with `\"README.md\"`. Used by tree traversal and synthesis code for module-level doc paths.\n\n#### makeLogEvent\n\n```go\nfunc makeLogEvent(onEvent func(Event)) LogEventFunc\n```\n\nAdapter factory returning a function that forwards to its callback only when non-nil (nil-safe pattern). No panics, no error returns.\n\n### Error Propagation Summary — engine subsystem\n\n| Category | Handled Pattern | Swallowed Pattern |\n|----------|-----------------|-------------------|\n| LLM calls (`CallLLM`) | Wrapped with `%w`, descriptive message includes step/file context; propagated to caller. | — |\n| File writes (`WriteFileSafely`) | Wrapped with `%w`; propagated to caller. | — |\n| Hash computation per file (`computeSHA256`) | Error logged as status warning; loop continues, file skipped from cache. | Swallowed in both `RunInit` and `RunUpdate`. |\n| Cache load/save (`loadMetadataCache`, `saveMetadataCache`) | — | Logged as status event; function completes regardless of write success/failure. |\n| `.gitignore` loading | — | Logged as warning; execution proceeds. |\n| Module file cleanup (`os.Remove`) | — | Error ignored via `_ = os.Remove(...)`. No verification. |\n| Directory creation (`os.MkdirAll`) | Wrapped with `%w`; propagated to caller. | — |\n| Non-OK HTTP responses | Bounded body read; error string returned (I/O failure during bounded read silently dropped). | Partially swallowed. |\n\n### External I/O Analysis — engine subsystem\n\n- **Disk**: All file operations resolve user-supplied virtual paths against `repoRoot` via a security layer that prevents escaping the repository boundary. Errors from this step propagate directly to the caller without wrapping.\n- **TOCTOU-Safe Writes**: `WriteFileSafely` writes to an in-directory temporary location, then atomically renames into place. Directories are created as needed with default permissions before write begins. Deferred cleanup (`tmpFile.Close`, `os.Remove`) discards errors silently — no return value reflects cleanup failures.\n- **Gitignore Parsing**: Reads `.gitignore` from `repoRoot`. Non-empty, non-comment lines are collected as active patterns. A missing file returns `(nil, nil)` intentionally — not an I/O failure signal. Scanner errors are returned as-is via `scanner.Err()`.\n- **Ignore Decision Logic**: A path is ignored if any component starts with a dot (hidden files) or ends with `.egg-info`, OR if it matches user-supplied gitignore patterns.\n- **Recursive Code Discovery**: Walks the repository root recursively. Directories starting with a dot or ending with `.egg-info` are skipped entirely. Files passing ignore checks have their relative paths normalized to forward slash and collected as results.\n\n---\n\n## Security Subsystem (`internal/security`)\n\n### Module Responsibility and Data Flow\n\nProvides two isolated concerns for repository-level operations: (1) safe resolution of user-supplied filesystem paths to prevent traversal escapes, and (2) file-based mutual exclusion ensuring only one instance of code-reducer holds a lock at any time. A third utility integrates the lockfile into version control so stale state is ignored by `git`. Data flow begins with callers invoking `SafeResolve` or `AcquireLock`, both of which return errors to downstream consumers for branching logic. Sentinel errors from `errors.go` are returned via standard Go `error` returns and must be detected using `errors.Is()` or `errors.As()`. No wrapping occurs; propagation is contract-level, not exception-style.\n\n### Error Contract Markers (`errors.go`)\n\n#### Sentinel Definitions\n\n| Variable | Condition | Mechanism |\n|---|-----------|-----------|\n| `ErrPathTraversal` — `\"security violation: path traversal detected\"` | Returned when a resolved path falls outside the repository root boundary | Standard Go error return value. Not wrapped; no panic. Detected via `errors.Is()` / `errors.As()`. |\n| `ErrLockHeld` — `\"lock is already held by another process\"` | Returned when another code-reducer instance holds the file lock | Standard Go error return value. Not wrapped; no panic. Detected via `errors.Is()` / `errors.As()`. |\n\nBoth errors are raw `*errors.errorString` values constructed using the standard library's `errors.New`. They carry no algorithmic logic and serve solely as contract markers for callers to branch on. No exception-style wrapping occurs in this file; any processing logic resides elsewhere in the module.\n\n### Path Sanitization (`security.go`)\n\n#### Public API: `SafeResolve`\n\n**Signature:** `func SafeResolve(repoRoot string, inputPath string) (string, error)`\n\n**Responsibility:** Cleans an input path and ensures it lies strictly inside the repository root. Resolves symlinks on existing ancestor parts to block symlink-based traversal.\n\n**Algorithm Steps:**\n1. Accept a `repoRoot` absolute reference and a relative or absolute `inputPath`.\n2. Resolve symlinks on existing ancestor directory components so that symlink-based traversal is blocked.\n3. Walk up from the target until hitting an existing physical ancestor (skipping non-existent intermediate components).\n4. Reconstruct the full resolved path from that ancestor plus the skipped suffixes.\n5. Verify the final resolved path lies strictly inside the repository root; reject if it does not.\n\n**Error Propagation:** Errors from `filepath.Abs`, `EvalSymlinks(absRoot)`, and `EvalSymlinks(current)` are wrapped with context and returned directly. The `os.Lstat` loop terminates on first success or when reaching the filesystem root (`parent == current`). Non-not-exist errors during stat are wrapped and returned immediately. If `filepath.Rel` returns an error OR the relative path starts with `\"..\"`, a wrapped `ErrPathTraversal` is returned.\n\n### Process Locking (`security.go`)\n\n#### Public API: `AcquireLock`\n\n**Signature:** `func AcquireLock(repoRoot string) (*SimpleLock, error)`\n\n**Responsibility:** Acquires an exclusive lock file (`.code-reducer.lock`) using O_EXCL for atomicity.\n\n**Algorithm Steps:**\n1. Call `SafeResolve` to sanitize the path; errors propagate to caller unchanged.\n2. Open a new file with O_EXCL; failure indicates another process holds the lock or a stale file exists.\n3. On open failure: check `os.IsExist(err)`. If the lock file already exists, return a specific message instructing manual cleanup; otherwise wrap with path context and original error.\n4. Write the current PID to the lock file for identification/verification.\n5. On write failure after successful open: clean up by calling `f.Close()` then `os.Remove`, then return the write error.\n\n**Return Type:** `*SimpleLock` — a struct holding per-instance state (`lockPath`, `file *os.File`, `mu sync.Mutex`, `closed bool`). No public methods other than `Unlock()`.\n\n#### Struct Method: `(*SimpleLock) Unlock`\n\n**Signature:** `func (l *SimpleLock) Unlock() error`\n\n**Responsibility:** Releases the lock by closing the file and removing it. Idempotent and thread-safe.\n\n**Algorithm Steps:**\n1. Capture close error in an `err` variable.\n2. Attempt `os.Remove`. Only replace `err` if (a) the previous operation succeeded AND (b) remove fails with a non-not-exist error.\n3. Result: if close succeeds but remove fails, that error is returned; if close fails but remove also fails, only the close error is surfaced.\n\n### Gitignore Integration (`security.go`)\n\n#### Public API: `EnsureGitignoreHasLockfile`\n\n**Signature:** `func EnsureGitignoreHasLockfile(repoRoot string) error`\n\n**Responsibility:** Ensures that `.code-reducer.lock` is present in `.gitignore`.\n\n**External I/O Operations:**\n- `filepath.Abs` / `EvalSymlinks`: Resolve absolute paths and evaluate symlinks on the repository root/ancestor (shared with `SafeResolve`)\n- `os.Lstat` (loop): Walk up directory hierarchy to find closest physically-existing ancestor during path traversal validation\n- `filepath.Rel`: Verify resolved target lies strictly inside resolved root\n- `os.OpenFile(... O_EXCL)`: Atomic create of lock file; failure indicates another process holds the lock or a stale file exists\n- `f.Write([]byte{...})`: Write current PID to lock file for identification/verification\n- `os.ReadFile`: Read existing `.gitignore` contents before appending entry\n- `os.CreateTemp` + `tmpFile.Write`: Atomic write pattern — build new content in temp file, then rename over original\n- `tmpFile.Sync`: Force flush to disk before close/rename\n- `os.Chmod`: Set permissions on temp file matching default (0644)\n- `os.Rename`: Atomically replace `.gitignore` with updated version\n\n**Error Propagation:** SafeResolve errors propagate directly. Non-not-exist read failures are wrapped with `\"error reading .gitignore\"` context. All temp file operations (`CreateTemp`, `Write`, `Sync`, `Close`, `Chmod`) return wrapped errors to caller on failure. Final `os.Rename` error is returned if it fails.\n\n### Module-Level Observations — security subsystem\n\n- **No network/database interactions:** Confirmed by full code inspection. All I/O targets are local paths under `repoRoot`.\n- **Mutable state:** None at the package level. The file contains only `const` values and function-local variables; no package-level or class-level properties are modified.\n- **Shared/instance state:** `SimpleLock` holds per-instance fields (`lockPath`, `file *os.File`, `mu sync.Mutex`, `closed bool`). `Unlock()` modifies `l.closed = true` under its own internal mutex. This is instance-scoped state, not shared global state.\n- **Package-level constant:** `defaultFilePerm = 0644` — used by temp file permission setting in the gitignore integration path.\n\n---\n\n## Tools Subsystem (`internal/tools`)\n\n### Module Responsibility\n\nProvides two subsystems: a repository-aware file I/O and discovery layer, and a Git command abstraction. Both operate on a `repoRoot` boundary to prevent path traversal outside the codebase. No mutable state exists in either subsystem; all operations are stateless per-call.\n\n### File Tools (`file_tools.go`)\n\n#### Public API\n\n| Function | Signature |\n|----------|-----------|\n| ReadFileSafely | `func ReadFileSafely(repoRoot, virtualPath string) ([]byte, error)` |\n| WriteFileSafely | `func WriteFileSafely(repoRoot, virtualPath string, content []byte) error` |\n| LoadGitignore | `func LoadGitignore(repoRoot string) ([]string, error)` |\n| ShouldIgnoreFile | `func ShouldIgnoreFile(relPath string, gitIgnore *ignore.GitIgnore) bool` |\n| DiscoverCodeFiles | `func DiscoverCodeFiles(repoRoot string, ignores []string) ([]string, error)` |\n\n#### Data Flow\n\n1. **Safe Path Resolution**: All file operations resolve user-supplied virtual paths against `repoRoot` via a security layer that prevents escaping the repository boundary. Errors from this step propagate directly to the caller without wrapping.\n2. **TOCTOU-Safe Writes**: `WriteFileSafely` writes to an in-directory temporary location, then atomically renames into place. Directories are created as needed with default permissions before write begins. Deferred cleanup (`tmpFile.Close`, `os.Remove`) discards errors silently — no return value reflects cleanup failures.\n3. **Gitignore Parsing**: Reads `.gitignore` from `repoRoot`. Non-empty, non-comment lines are collected as active patterns.",
    "cmd": "# cmd Package Architecture\n\n## Module Responsibility and Data Flow\n\nThe `cmd` package implements the CLI entry point for **Code-Reducer**, a local-first AI agent that generates and maintains project wikis by analyzing source code against an LLM engine. The module orchestrates multi-step operations through a Cobra command tree, where each subcommand delegates to an unexported engine via a shared execution path.\n\nAll subcommands funnel through the internal `executeCommand(mode engine.Mode) error` function in `root.go`, which resolves operating environment state (terminal vs non-terminal), merges persisted and CLI-override configuration, validates preconditions per mode, registers signal handlers for graceful shutdown (`SIGINT`, `SIGTERM`), instantiates an engine runner with the resolved config, invokes it against the target repo root and requested mode, and routes emitted events to stdout/stderr.\n\n## Command Registration Pattern\n\nThe package exposes three subcommands—`init`, `update`, and `setup`—registered via unexported `var` identifiers following Go's lowercase naming convention:\n\n- **`initCmd`** (`cmd/init.go`) — Registers the `init` subcommand under `RootCmd`. On invocation, delegates to the engine with `engine.ModeInit`. The command is a thin wrapper; no public API surface exists beyond this registration.\n- **`setupCmd`** (`cmd/setup.go`) — Implements an interactive first-run / re-configuration wizard. Persists user input to `.code-reducer.yaml` via `config.SaveConfig`. No public exported elements.\n- **`updateCmd`** (`cmd/update.go`) — Registers the `update` subcommand under `RootCmd`. On execution, delegates to the engine with `engine.ModeUpdate`. All actual I/O occurs inside the unshown `internal/engine` package; this file performs none.\n\n## Root Initialization and Configuration Resolution\n\nThe module's primary initialization logic resides in `cmd/root.go`, which wires persistent flags (`--model-id`, `--num-ctx`) to package-level `string` variables via Cobra's `StringVar`. These globals are set once during `init()` and read later by `executeCommand` from a single goroutine path, eliminating the need for synchronization primitives.\n\nOn invocation, the root command performs the following sequence:\n\n1. **Working directory resolution** — `os.Getwd()` reads the filesystem to obtain the current working directory; failure is wrapped with `fmt.Errorf(\"failed to get current working directory: %w\", err)`.\n2. **Git repository validation** — `tools.VerifyGitRepo(repoRoot)` checks whether the resolved path is a valid git repository, performing disk I/O to verify structure/validity. Error propagates directly without wrapping.\n3. **Implicit setup flow** — If no configuration file exists and stdin is a terminal (determined via `isatty.IsTerminal(os.Stdin.Fd())` / `isatty.IsCygwinTerminal(os.Stdin.Fd())`), an automatic setup routine runs; otherwise the user is directed to the `setup` subcommand.\n4. **Configuration merging** — `config.ResolveConfig(repoRoot, modelIDFlag, numCtxFlag)` reads merged configuration from disk, combining persisted config with CLI-override flags into a single resolved object. Error propagates directly without wrapping.\n5. **Mode-specific preconditions** — The engine runner validates that `init` mode is rejected if already initialized (force use of `update`), and `update` mode is rejected if not yet initialized.\n6. **Signal handling** — Handlers for `SIGINT` and `SIGTERM` are installed on a background context so the runner can terminate cleanly on user interruption.\n7. **Engine invocation** — A runner is instantiated with the resolved config, invoked against the target repo root and requested mode, and events are streamed back to stdout/stderr as they occur (status, errors, regular output).\n\n## Interactive Setup Flow\n\nThe setup wizard in `cmd/setup.go` solves the first-run / re-configuration problem by guiding users through entering all required settings so they can be persisted to a local YAML configuration file. The flow is designed to reuse any previously saved values, allowing partial re-configuration rather than starting from scratch.\n\n### External I/O Interactions\n\n| Interaction | Direction | Mechanism | Notes |\n|---|---|---|---|\n| Stdin reading (user input) | Read | `bufio.NewReader(os.Stdin)` — used by `promptString` and `promptStringList` | Synchronous terminal reads; no buffering to external system |\n| Current working directory | Read | `os.Getwd()` in `RunSetupFlow` | Local filesystem query; error is wrapped with `%w` on failure |\n| Config file load | Read | `config.LoadConfig(repoRoot)` | Reads `.code-reducer.yaml` from disk at `repoRoot`; result used as fallback values for prompts. Load failure is treated as \"no existing config\"; existing values default to zero-state equivalents (`existingCfg` remains nil). No error returned; flow continues normally. |\n| Config file save | Write | `config.SaveConfig(repoRoot, newCfg)` | Writes updated configuration to disk; error is wrapped with `%w` on failure. This is the only path that returns a non-nil error from `RunSetupFlow`, propagating directly to the cobra command's `RunE`. |\n\nNo network calls, database access, or API interactions are present in this file.\n\n### Prompt Functions\n\n- **`promptString(reader *bufio.Reader, promptMsg string, existingVal string) string`** — Reads stdin input; if no valid input is given (user cancels or clears), returns the `existingVal`. Stdin read errors are logged to `os.Stderr` with a warning message, then the function returns the existing/fallback value instead of propagating upward. No exception escapes this function.\n- **`promptStringList(reader *bufio.Reader, promptMsg string, existingList []string) ([]string, bool)`** — Reads stdin input; accepts comma-separated list values or supports `\"clear\"` / `\"none\"` to reset the ignore list entirely. Returns updated or existing list and a boolean indicating whether the user modified it (`true`) or kept existing values (`false`). Stdin read errors follow the same pattern as `promptString`.\n\n### Algorithm Steps for RunSetupFlow\n\n1. Determine repository root by resolving the current working directory.\n2. Load existing configuration if present; extract its values as starting points so the user only needs to change what they want to change. If load fails, existing values default to zero-state equivalents (`existingCfg` remains nil).\n3. Prompt for LLM Model ID — display current value as hint; accept new input or fall back to the default Ollama model identifier when empty.\n4. Prompt for Ollama Base URL — same pattern: show existing, return existing on empty input.\n5. Prompt for Context Size (numCtx) — read string input, parse to integer with validation; if invalid or zero, revert to the previously loaded value.\n6. Prompt for Ignore Patterns — accept comma-separated list of directory/file/pattern strings; support `\"clear\"` / `\"none\"` to reset the ignore list entirely; otherwise keep prior values when no new input is provided.\n7. Prompt for Documentation Directory Path — show existing path as hint, return previous value on empty response.\n8. Prompt for Extraction Steps — if a prior config existed with extraction steps defined, reuse them; otherwise start from built-in defaults.\n9. Prompt for System Prompts (four separate fields: system prompt, module synthesis prompt, architecture prompt, file fact consolidation prompt) — preserve any previously set values when the user leaves input empty or omits it.\n10. Assemble and persist configuration — construct a new `Config` struct combining fresh user input with preserved defaults/previous values; write to disk via the config loader.\n\n## Error Propagation Summary\n\n- **Wrapped errors**: `os.Getwd()` failure is wrapped with `%w`; `config.SaveConfig` failure is wrapped with `\"error saving configuration: %w\"`.\n- **Direct pass-through**: Errors from `VerifyGitRepo`, `RunSetupFlow` (excluding save), and `ResolveConfig` are returned as-is without wrapping.\n- **Contextual wrap**: The engine runner's error (`runner.Run(...)`) is wrapped with `\"documentation run failed: %w\"`.\n- **Silenced errors**: `RootCmd.SilenceErrors: true` means any unhandled errors from Cobra's command execution (e.g., flag parsing failures, subcommand errors) are swallowed and not printed.\n- **Signal context cancellation**: If the context is cancelled via signal before `runner.Run()` returns an error, the resulting error would be propagated through the `err != nil` check as a wrapped \"documentation run failed\" message.\n\n## State and Concurrency Assessment\n\nNo mutable state exists in `cmd/init.go`, `cmd/update.go`, or `cmd/setup.go`. The package-level `string` variables (`modelIDFlag`, `numCtxFlag`) are set once during initialization and read from a single goroutine path by `executeCommand`, so no synchronization mechanisms (locks, mutexes, sync primitives) are required. No race conditions can be identified from this code alone.",
    "internal": "# internal — Subsystem Documentation\n\n## 1. Config Module (`internal/config`)\n\n### Responsibility\n\nCentralized configuration schema and persistence primitives for the synthesis pipeline. Defines `Config` (runtime parameters: LLM identity, Ollama base URL/context size, docs directory, prompt templates, extraction steps, ignore lists) and `ExtractionStep` (single fact-extraction phase name + system prompt). Exposes three I/O functions (`ConfigExists`, `LoadConfig`, `SaveConfig`) that manage `.code-reducer.yaml` via atomic write pattern; a fourth function (`ResolveConfig`) merges CLI flags, environment variables, YAML config, and hardcoded defaults into a single resolved `*Config`.\n\n### Data Flow\n\n```\nCLI flags / env vars ──► ResolveConfig (merge) ──► *Config ──► Synthesis pipeline consumer\n                                                              │\nYAML (.code-reducer.yaml) ◄──────────── LoadConfig ────────┘\n                                                              ▲\nSaveConfig ───────────────────────────────────────────────────┘\n```\n\nAll configuration values flow downstream to the LLM client and synthesis stages. No mutable state within this module; all functions are pure or side-effect only through disk I/O.\n\n### Types\n\n#### ExtractionStep\n\nSingle extraction phase executed during file-fact extraction stage. Carries a human-readable name and an LLM system prompt that guides extraction behavior.\n\n| Field | Type | Tags |\n|-------|------|------|\n| `Name` | `string` | `yaml:\"name\"` |\n| `Prompt` | `string` | `yaml:\"prompt\"` |\n\n#### Config\n\nCentral configuration struct. All fields are optional and may be left at zero values; defaults kick in during resolution if the field is empty or unset.\n\n| Field | Type | Tags | Notes |\n|-------|------|------|-------|\n| `ModelID` | `string` | `yaml:\"model_id\"` | LLM model identifier (e.g., `\"ornith:9b\"`). |\n| `OllamaBaseURL` | `string` | `yaml:\"ollama_base_url\"` | Base URL for the Ollama inference server. |\n| `OllamaNumCtx` | `int` | `yaml:\"ollama_num_ctx\"` | Context size parameter forwarded to Ollama. |\n| `DocsDir` | `string` | `yaml:\"docs_dir\"` | Directory path where generated documentation is written. |\n| `SystemPrompt` | `string` | `yaml:\"system_prompt\"` | System prompt injected into the model at synthesis time. |\n| `ModuleSynthesisPrompt` | `string` | `yaml:\"module_synthesis_prompt\"` | Prompt used during module-level synthesis. |\n| `ArchitecturePrompt` | `string` | `yaml:\"architecture_prompt\"` | Prompt used during architecture-level synthesis. |\n| `FileFactConsolidationPrompt` | `string` | `yaml:\"file_fact_consolidation_prompt\"` | Prompt used to consolidate overlapping file facts before synthesis. |\n| `ExtractionSteps` | `[]ExtractionStep` | `yaml:\"extraction_steps\"` | Ordered list of extraction phases; empty slice triggers fallback to `DefaultExtractionSteps`. |\n| `Ignore` | `[]string` | `yaml:\"ignore\"` | File paths or patterns to skip during analysis. Duplicates are removed by the resolver. |\n\n### Constants\n\n#### Environment and CLI Keys\n\n| Constant | Value | Type | Purpose |\n|----------|-------|------|---------|\n| `CodeReducerModelIDEnvKey` | `\"CODE_REDUCER_MODEL_ID\"` | `string` | Env var key for model ID override in resolver. |\n| `OllamaBaseURLEnvKey` | `\"OLLAMA_BASE_URL\"` | `string` | Env var key for Ollama base URL override in resolver. |\n| `OllamaNumCtxEnvKey` | `\"OLLAMA_NUM_CTX\"` | `string` | Env var key for context size override in resolver. |\n\n#### Defaults\n\n| Constant | Value | Type | Purpose |\n|----------|-------|------|---------|\n| `OllamaDefaultBaseURL` | `\"http://localhost:11434\"` | `string` | Default Ollama inference server address. |\n| `OllamaDefaultModelID` | `\"ornith:9b\"` | `string` | Default LLM model identifier. |\n| `OllamaDefaultNumCtx` | `8192` | `int` | Default context size. |\n| `DefaultDocsDir` | `\"wiki\"` | `string` | Default documentation output directory. |\n| `ConfigFileName` | `\".code-reducer.yaml\"` | `string` | Filename used by all three I/O functions and `ResolveConfig`. |\n| `DefaultSystemPrompt`, `DefaultModuleSynthesisPrompt`, `DefaultArchitecturePrompt`, `DefaultFileFactConsolidationPrompt` | *(multiline strings)* | `string` | Hardcoded fallback prompts for the four synthesis stages. |\n\n#### Internal\n\n| Constant | Value | Type | Notes |\n|----------|-------|------|-------|\n| `configFilePerm` | `0600` | *(unexported)* | File permission applied to config file and temp files during write. |\n\n### Public API Surface — Types \u0026 Variables (from config.go)\n\n#### DefaultExtractionSteps\n\nPackage-level variable of type `[]ExtractionStep`. Serves as the fallback extraction pipeline when the YAML config contains an empty or nil slice. Each step carries a name and prompt for one of the four fact-extraction phases: *API signatures*, *business logic*, *mutable state \u0026 concurrency*, *external I/O \u0026 error propagation*.\n\n### Public API Surface — I/O Functions (from io.go)\n\n#### ConfigExists\n\n```go\nfunc ConfigExists(cwd string) bool\n```\n\nReturns `true` if `.code-reducer.yaml` exists at the path formed by joining `cwd` with `ConfigFileName`. Underlying operation is a single `os.Stat` call; no error wrapping occurs. Returns only a boolean to caller.\n\n#### LoadConfig\n\n```go\nfunc LoadConfig(cwd string) (*Config, error)\n```\n\nReads and parses `.code-reducer.yaml` from `cwd`. On success returns the populated `*Config`. Failure modes:\n\n| Condition | Return Value | Notes |\n|-----------|--------------|-------|\n| YAML parse error | `(nil, wrappedError)` | Error is wrapped with `\"failed to parse yaml config: %w\"`. |\n| Raw OS read error (not-not-exist) | `(nil, rawError)` | `os.ReadFile` failure propagates unchanged. |\n\n#### SaveConfig\n\n```go\nfunc SaveConfig(cwd string, cfg *Config) error\n```\n\nSerializes `cfg` to `.code-reducer.yaml` using an atomic write pattern: create temp file → write bytes → sync → close → chmod → rename over original path. Failure points are wrapped individually with descriptive prefixes (`\"failed to \u003caction\u003e config file\"`). The final state is either the old file or a fully-written new file — never neither, because of the rename semantics.\n\n### Public API Surface — Resolution (from resolve.go)\n\n#### ResolveConfig\n\n```go\nfunc ResolveConfig(repoRoot string, modelIDFlag string, numCtxFlag string) (*Config, error)\n```\n\nMerges configuration across four precedence tiers:\n\n1. Hardcoded defaults (`OllamaDefault*`, `DefaultDocsDir`, etc.).\n2. Values parsed from `.code-reducer.yaml` at `repoRoot`.\n3. Environment variables (`CodeReducerModelIDEnvKey`, `OllamaBaseURLEnvKey`, `OllamaNumCtxEnvKey`).\n4. CLI flags (`modelIDFlag`, `numCtxFlag`).\n\nSpecific resolution rules:\n\n- **`Ignore` list**: Deduplicated in-place; first occurrence wins via hash-set tracking.\n- **`ExtractionSteps`**: Falls back to `DefaultExtractionSteps` when the YAML slice is empty or nil.\n- **Numeric fields** (`OllamaNumCtx`): Parsed with `strconv.Atoi`; non-positive values are rejected and the prior tier's value is retained.\n- **String prompts**: Empty YAML values fall back to the corresponding hardcoded default via `resolveString`.\n\n#### Internal Helpers (from resolve.go)\n\n| Function | Parameters | Return Types | Description |\n|----------|-----------|--------------|-------------|\n| `deduplicate` | `a []string` | `[]string` | Removes duplicates from a string slice; first occurrence wins. |\n| `resolveString` | `yamlVal string`, `defaultVal string` | `string` | Returns `yamlVal` if non-empty, otherwise returns `defaultVal`. Used for prompt field resolution. |\n\n### Error Propagation Summary\n\n- **config.go**: No runtime operations; no errors can be raised or swallowed here.\n- **io.go**: All disk-read and disk-write functions return error tuples. YAML parse errors are explicitly wrapped with `\"failed to parse yaml config: %w\"`; raw OS-level read/write errors propagate unchanged through the returned tuple. Write-path errors each carry a unique prefix (`\"failed to \u003caction\u003e temp config file\"`).\n- **resolve.go**: `LoadConfig` failure is only surfaced when `os.IsNotExist(err)` returns false — missing files are silently replaced with an empty `Config{}` and no error reaches the caller. All other branches (env lookups, flag parsing, deduplication) produce zero errors by design.\n\n### External I/O Analysis\n\n- **Disk**: All functions interact exclusively with `.code-reducer.yaml` under `cwd`. Writes use temp file + rename for atomicity; permissions are set to `0600`.\n- **Environment / Process**: Three env vars (`CODE_REDUCER_MODEL_ID`, `OLLAMA_BASE_URL`, `OLLAMA_NUM_CTX`) are read by `ResolveConfig`; none are written.\n- **Network**: None. Constants like `OllamaDefaultBaseURL` are string literals; no HTTP client or network calls exist in this module.\n- **Database/APIs**: None referenced.\n\n---\n\n## 2. Engine Module (`internal/engine`)\n\n### Responsibility\n\nPersistent, incremental documentation synthesis pipeline for software repositories. Produces three artifact classes — global architecture summaries, quickstart guides, and per-module API descriptions — from source code using an LLM backend, with change detection that limits regeneration to directories impacted by actual file modifications. State persists across invocations via a metadata cache stored as JSON under `docs/`; a global steps hash anchors cache validity to the current extraction pipeline configuration.\n\n### Data Flow\n\n```\nRunner.Run(ctx, repoRoot, mode)\n    └─► acquireLock(repoRoot)                    (security.AcquireLock)\n        ├─► ModeInit ──► orchestrator.GenerateStandardDocs ──► architecture.md / quickstart.md\n        └─► ModeUpdate ──► loadMetadataCache      ──► cache.Files, cache.Modules, cache.StepsHash\n                           │\n                           ├─► computeSHA256 × N  ──► precalculatedHashes (cache miss → disk read)\n                           │\n                           ├─► diff detection     ──► FileChange map {Path: Status}\n                           │\n                           ├─► rebuild tree       ──► DirNode hierarchy from current files only\n                           │\n                           ├─► prune stale modules (os.Stat + os.Remove)\n                           │\n                           ├─► determine affected dirs (walk tree + cache empty check + disk existence)\n                           │\n                           └─► selective regeneration ──► synthesizeNode × N ──► WriteFileSafely\n```\n\n### Runner Entry Point (`runner.go`)\n\n#### Mode\n\nString-typed operation modes for the pipeline. Two exported constants:\n\n| Constant | Value |\n|----------|-------|\n| `ModeInit`  | `\"init\"`   |\n| `ModeUpdate` | `\"update\"` |\n\n#### Runner\n\nStruct holding a single pointer field, `cfg *config.Config`, assigned once in the constructor and never reassigned within this file. External mutability of the pointed-to struct is not observable from here alone.\n\n```go\nfunc NewRunner(cfg *config.Config) *Runner\nfunc (r *Runner) Run(ctx context.Context, repoRoot string, mode Mode, onEvent func(Event)) error\n```\n\n`Run()` performs three operations before branching:\n\n1. **Lockfile validation** — `security.EnsureGitignoreHasLockfile(repoRoot)` is invoked; any error is logged as a status event and execution proceeds regardless.\n2. **Exclusive lock acquisition** — `security.AcquireLock(repoRoot)` returns an error that is wrapped with context (`failed to acquire repository lock: %w`) and returned immediately. A deferred unlock runs on every exit path, guaranteeing release even when the pipeline fails partway through.\n3. **Client instantiation** — `newLLMClient(r.cfg.ModelID, r.cfg.OllamaBaseURL, r.cfg.OllamaNumCtx)` constructs an Ollama HTTP client; no network I/O occurs at this point.\n\nAfter lock acquisition, execution branches on the mode value: invalid strings return `fmt.Errorf(\"unsupported mode: %s\", mode)`. Otherwise, either `orch.RunInit` or `orch.RunUpdate` is invoked directly and its error (or nil) is returned to the caller.\n\n### Orchestrator (`orchestrator.go`)\n\n#### Types\n\n```go\ntype EventType string   // \"status\" | \"error\" (unexported)\n\ntype Event struct {\n    Type   EventType `json:\"type\"`\n    Message string    `json:\"message\"`\n}\n```\n\nBoth constants and the type are unexported. The orchestrator itself is also internal; only its public method signatures appear in the package surface.\n\n#### GenerateStandardDocs\n\n```go\nfunc (o *orchestrator) GenerateStandardDocs(ctx, repoRoot, docsDir, rootSum string, cfg *config.Config, logEvent LogEventFunc) error\n```\n\nInvoked from both `RunInit` and `RunUpdate`. Issues two sequential LLM calls: one to generate `architecture.md` and another for `quickstart.md`, each wrapped as a global artifact. Errors propagate fully (wrapped with `%w`). If the top-level directory is marked affected or standard docs are missing, this function runs; otherwise it is skipped entirely on update.\n\n### Pipeline Context State (`orchestrator.go`)\n\nThe orchestrator maintains mutable state across its lifecycle via pointers into shared structs:\n\n| Field | Type | Mutability | Notes |\n|-------|------|------------|-------|\n| `cache` | `*MetadataCache` | **Mutable** | Reassigned in `setupPipeline`, `teardownPipeline`, `RunInit`, `RunUpdate`. Fields `.StepsHash`, `.Files`, `.Modules` are updated across calls. |\n| `cfg` | `*config.Config` | Potentially mutable | Read only here (`SystemPrompt`, `ArchitecturePrompt`, `ExtractionSteps`). External code may mutate the pointed-to struct at runtime. |\n| `client` | `llmCaller` interface | Read-only in this file | Initialized on construction; never reassigned within these methods. |\n\nNo synchronization primitives (mutex, atomic, channel) protect access to any of these fields. Concurrent callers sharing a single context instance would race on the cache maps. All operations are sequential and single-threaded per method invocation.\n\n### Tree Construction and Affected Detection (`tree.go`)\n\n#### Types\n\n```go\ntype ChangeStatus string   // \"Added\" | \"Modified\" | \"Deleted\" (unexported)\n\ntype FileChange struct {\n    Path  string\n    Status ChangeStatus\n}\n\ntype DirNode struct {\n    Path     string\n    Files    []string\n    Children map[string]*DirNode\n}\n```\n\n#### buildTree\n\nConstructs a nested `DirNode` hierarchy from a slice of file paths. Leaf entries are files; intermediate path segments become child nodes. No I/O, no side effects.\n\n### Affected Detection Logic\n\nThe orchestrator walks the tree to mark directories as \"affected\" via four conditions:\n\n1. **Changed-file match** — if any `FileChange` in the map matches a file under this node's subtree.\n2. **Module path non-existence on disk** — `security.SafeResolve(repoRoot, modulePath)` followed by `os.Stat(absModulePath)`. If the stat error is not `os.ErrNotExist`, the branch is skipped silently.\n3. **Cache emptiness** — if the corresponding entry in `cache.Modules` is empty.\n4. **Child propagation** — a second recursive pass marks any parent affected whenever *any* child has already been flagged, regardless of direct match.\n\n### LLM Client (`client.go`)\n\n#### Public Types\n\n```go\ntype Message struct {\n    Role    string `json:\"role\"`\n    Content string `json:\"content\"`\n}\n```\n\n`Message` is the only exported type in this file. All other declarations are unexported and therefore not part of the public API:\n\n| Element | Type | Notes |\n|---|------|-------|\n| `llmCaller` | interface | Unexported; methods include `CallLLM(ctx, systemPrompt, messages, jsonFormat)` returning `(string, error)` and `NumCtx() int`. |\n| `llmClient` | struct | Unexported. Fields: `modelID`, `baseURL`, `numCtx`, `httpClient *http.Client`. |\n| `newLLMClient` | func | Factory for `*llmClient`; referenced but not defined in this file. |\n| `(c *llmClient) NumCtx()` | method | Returns the configured context window size. |\n| `ollamaRequest` / `ollamaOptions` / `ollamaResponse` | structs | Unexported request/response payloads for Ollama's `/api/chat`. |\n| `(c *llmClient) prepareOllamaRequest()` | private method | Builds and serializes the chat payload. |\n\n#### External I/O\n\nAll network interactions target `{baseURL}/api/chat` via HTTP POST with JSON body (`application/json`). The client is constructed once per instance; `httpClient` is reused across calls without locking, which is safe because timeout and base URL are fixed at construction time and never reassigned within this file.\n\n**Response handling:** 200 OK responses have their bodies read fully via `io.ReadAll`; non-OK status codes trigger a bounded read (`io.LimitReader(resp.Body, maxErrorBodyBytes)`) whose error is discarded silently — the resulting string (containing the HTTP status and truncated body) is returned to the caller instead.\n\n**Unresolved references:** `defaultHTTPTimeout` and `maxErrorBodyBytes` are referenced in this file but not defined here; they must be package-level constants or variables declared elsewhere, otherwise compilation fails.\n\n### Chunking and Reduction Pipeline (`chunking.go`)\n\n#### reduceWithLLM\n\n```go\nfunc reduceWithLLM(ctx context.Context, c llmCaller, items []string, sysPrompt string, buildPrompt func(batch []string) string, logMsg func(batch []string) string, errMsg string, logEvent LogEventFunc) (string, error)\n```\n\nEntry point for all LLM-mediated reductions. Calls `c.CallLLM(ctx, sysPrompt, messages, false)` with a system prompt prepended to user messages; errors are wrapped via `fmt.Errorf(\"%s: %w\", errMsg, err)`. The original error is preserved through `%w` wrapping.\n\n#### reduceInChunks\n\n```go\nfunc reduceInChunks(ctx context.Context, c llmCaller, nodePath string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)\n```\n\nInvoked from the synthesis step to consolidate a directory's file facts and child summaries. Calls `reduceWithLLM` with a prompt derived from `cfg.SystemPrompt` and the current extraction steps. Errors propagate through unchanged.\n\n#### reduceFileFacts\n\n```go\nfunc reduceFileFacts(ctx context.Context, c llmCaller, filePath string, stepName string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)\n```\n\nConsolidates per-extraction-step outputs for a single file. Calls `reduceWithLLM` with a prompt built from `cfg.SystemPrompt` and the current extraction steps; errors propagate as-is.\n\n#### reduceItems\n\n```go\nfunc reduceItems(ctx context.Context, items []string, maxChars int, reduceFn func(batch []string) (string, error)) (string, error)\n```\n\nIterative map-reduce loop: accumulates items into batches that fit within `maxChars`, flushing when the next item would exceed the limit. If a single item exceeds the cap at entry, it is split with overlap rather than truncated — information preservation over compression. After each LLM pass, total output run count vs input is computed; if output ≥95% of input (integer division), recursion terminates and results are concatenated with `\\n\\n`. Context cancellation (`ctx.Err()`) returns early without partial results.\n\n#### chunkTextWithOverlap\n\n```go\nfunc chunkTextWithOverlap(text string, maxRunes int, overlapRunes int) []string\n```\n\nSplits text into overlapping chunks of at most `maxRunes` runes with `overlapRunes` rune overlap between adjacent chunks (clamped if ≥ `maxRunes`). Returns the full text as a single chunk when `maxRunes \u003c= 0` or `overlapRunes \u003e= maxRunes`. No panics, no bounds errors.\n\n### Markdown Fence Stripping (`markdown.go`)\n\n#### Internal Implementation\n\n```go\nvar markdownFenceRe regexp.Regexp\nfunc stripOuterMarkdownFence(content string) string\n```\n\nUnexported; both the variable and function begin with lowercase letters. The regex is compiled once at package scope and never reassigned — effectively read-only after compilation.\n\n**Algorithm:** trim whitespace → match triple-backtick fence (with optional `markdown`/`json` language tag) → return captured interior after trimming surrounding whitespace. No error return; if the pattern doesn't match, original input is returned unchanged.\n\n### Constants (`constants.go`)\n\nAll nine named constants in this file are unexported:\n\n| Constant | Purpose |\n|----------|---------|\n| `defaultHTTPTimeout` | 10-minute ceiling for remote API calls (set at package init) |\n| `maxErrorBodyBytes` | 1 KB cap on error response payloads captured from failed requests |\n| `defaultChunkOverlap` | 800-token overlap between consecutive text chunks during streaming/processing |\n| `minNumCtxFloor` | 512 minimum threshold for context length calculations |\n| `contextWindowAllocRatio` | 0.75 reserved proportion of available space for primary content |\n| `maxCharsMultiplier` | 3× multiplier applied to base character limit |\n| `metadataFileName`, `agentsFileName`, `defaultDirPerm` | Filesystem conventions for state persistence |\n\nNo mutable state, no external I/O, no error propagation. The single function-type alias (`LogEventFunc`) is declared but not used within this file.\n\n### Synthesis Engine (`synthesize.go`)\n\nUnexported types and functions only; all identifiers begin with lowercase letters.\n\n#### External I/O Matrix\n\n| Source | Path | Behavior on Failure |\n|--------|------|---------------------|\n| `tools.ReadFileSafely(p.repoRoot, f)` — first call | `\u003crepoRoot\u003e/\u003cf\u003e` | Logged as status warning; returns `\"\", nil`. Swallowed by caller. |\n| `tools.ReadFileSafely` — second call (inside `if facts == \"\"`) | Same | Same swallow pattern. |\n| `p.client.CallLLM(p.ctx, systemPrompt, []Message{...}, false)` | Network endpoint via `llmCaller` | Wrapped with `%w`, includes step name and file path in message. Propagated to caller as `\"LLM error extracting %s for %s: %w\"`. |\n| `tools.WriteFileSafely(p.repoRoot, modulePath, []byte(finalSum))` | `\u003crepoRoot\u003e/\u003cdocsDir\u003e/modules/\u003csafe-filename-of-node-path\u003e.md` | Wrapped with `%w`, includes node path. Propagated to caller as `\"failed to write module documentation for %s: %w\"`. |\n\n#### Synthesis Algorithm (per `synthesizeNode`)\n\n1. **Cache-first lookup** — check `cache.Files[f]` against precomputed SHA-256 hash; on match, reuse cached facts without re-reading the file.\n2. **Recursive bottom-up traversal** — for each directory node: if unaffected and descendants are unaffected, return cached summary immediately. Sort child names alphabetically; recurse into children first; collect non-empty summaries into a map keyed by child name.\n3. **Per-file extraction** — compute dynamic content limit from remaining context window capacity (allocates ~75% for file content). Split large files with `chunkTextWithOverlap`. Pass each chunk to LLM with step-specific prompt derived from `ExtractionSteps`. Strip markdown fences from responses via `stripOuterMarkdownFence`. Consolidate all outputs per extraction step using `reduceFileFacts`. Concatenate across steps into a single file fact string.\n4. **Assembly** — combine extracted file facts (prefixed `\"### File: basename\"`) and child summaries (prefixed `\"### Subsystem: name\"`) into one `components` slice. If nothing exists, clear the cache entry and return empty.\n5. **Final reduction and persistence** — pass all components to chunked reduction (`reduceInChunks`) producing a single consolidated summary for the directory; store in `cache.Modules[node.Path]`; write to disk under `\u003cdocsDir\u003e/modules/\u003csafe-filename-of-path\u003e`.\n\n### Utility Helpers (`utils.go`)\n\n#### toSafeMarkdownFilename\n\n```go\nfunc toSafeMarkdownFilename(modulePath string) string\n```\n\nPure string manipulation; no file operations. Returns `\"README.md\"` when the path is empty or `\".\"`; otherwise joins with `\"README.md\"`. Used by tree traversal and synthesis code for module-level doc paths.\n\n#### makeLogEvent\n\n```go\nfunc makeLogEvent(onEvent func(Event)) LogEventFunc\n```\n\nAdapter factory returning a function that forwards to its callback only when non-nil (nil-safe pattern). No panics, no error returns.\n\n### Error Propagation Summary\n\n| Category | Handled Pattern | Swallowed Pattern |\n|----------|-----------------|-------------------|\n| LLM calls (`CallLLM`) | Wrapped with `%w`, descriptive message includes step/file context; propagated to caller. | — |\n| File writes (`WriteFileSafely`) | Wrapped with `%w`; propagated to caller. | — |\n| Hash computation per file (`computeSHA256`) | Error logged as status warning; loop continues, file skipped from cache. | Swallowed in both `RunInit` and `RunUpdate`. |\n| Cache load/save (`loadMetadataCache`, `saveMetadataCache`) | — | Logged as status event; function completes regardless of write success/failure. |\n| `.gitignore` loading | — | Logged as warning; execution proceeds. |\n| Module file cleanup (`os.Remove`) | — | Error ignored via `_ = os.Remove(...)`. No verification. |\n| Directory creation (`os.MkdirAll`) | Wrapped with `%w`; propagated to caller. | — |\n| Non-OK HTTP responses | Bounded body read; error string returned (I/O failure during bounded read silently dropped). | Partially swallowed. |\n\n### External I/O Analysis\n\n- **Disk**: All file operations resolve user-supplied virtual paths against `repoRoot` via a security layer that prevents escaping the repository boundary. Errors from this step propagate directly to the caller without wrapping.\n- **TOCTOU-Safe Writes**: `WriteFileSafely` writes to an in-directory temporary location, then atomically renames into place. Directories are created as needed with default permissions before write begins. Deferred cleanup (`tmpFile.Close`, `os.Remove`) discards errors silently — no return value reflects cleanup failures.\n- **Gitignore Parsing**: Reads `.gitignore` from `repoRoot`. Non-empty, non-comment lines are collected as active patterns. A missing file returns `(nil, nil)` intentionally — not an I/O failure signal. Scanner errors are returned as-is via `scanner.Err()`.\n- **Ignore Decision Logic**: A path is ignored if any component starts with a dot (hidden files) or ends with `.egg-info`, OR if it matches user-supplied gitignore patterns.\n- **Recursive Code Discovery**: Walks the repository root recursively. Directories starting with a dot or ending with `.egg-info` are skipped entirely. Files passing ignore checks have their relative paths normalized to forward slash and collected as results.\n\n---\n\n## 3. Security Module (`internal/security`)\n\n### Responsibility and Data Flow\n\nProvides two isolated concerns for repository-level operations: (1) safe resolution of user-supplied filesystem paths to prevent traversal escapes, and (2) file-based mutual exclusion ensuring only one instance of code-reducer holds a lock at any time. A third utility integrates the lockfile into version control so stale state is ignored by `git`.\n\nData flow begins with callers invoking `SafeResolve` or `AcquireLock`, both of which return errors to downstream consumers for branching logic. Sentinel errors from `errors.go` are returned via standard Go `error` returns and must be detected using `errors.Is()` or `errors.As()`. No wrapping occurs; propagation is contract-level, not exception-style.\n\n### Error Contract Markers (`errors.go`)\n\n#### Sentinel Definitions\n\n| Variable | Condition | Mechanism |\n|---|---|---|\n| `ErrPathTraversal` — `\"security violation: path traversal detected\"` | Returned when a resolved path falls outside the repository root boundary | Standard Go error return value. Not wrapped; no panic. Detected via `errors.Is()` / `errors.As()`. |\n| `ErrLockHeld` — `\"lock is already held by another process\"` | Returned when another code-reducer instance holds the file lock | Standard Go error return value. Not wrapped; no panic. Detected via `errors.Is()` / `errors.As()`. |\n\nBoth errors are raw `*errors.errorString` values constructed using the standard library's `errors.New`. They carry no algorithmic logic and serve solely as contract markers for callers to branch on. No exception-style wrapping occurs in this file; any processing logic resides elsewhere in the module.\n\n### Path Sanitization (`security.go`)\n\n#### Public API: `SafeResolve`\n\n**Signature:** `func SafeResolve(repoRoot string, inputPath string) (string, error)`\n\n**Responsibility:** Cleans an input path and ensures it lies strictly inside the repository root. Resolves symlinks on existing ancestor parts to block symlink-based traversal.\n\n**Algorithm Steps:**\n1. Accept a `repoRoot` absolute reference and a relative or absolute `inputPath`.\n2. Resolve symlinks on existing ancestor directory components so that symlink-based traversal is blocked.\n3. Walk up from the target until hitting an existing physical ancestor (skipping non-existent intermediate components).\n4. Reconstruct the full resolved path from that ancestor plus the skipped suffixes.\n5. Verify the final resolved path lies strictly inside the repository root; reject if it does not.\n\n**Error Propagation:** Errors from `filepath.Abs`, `EvalSymlinks(absRoot)`, and `EvalSymlinks(current)` are wrapped with context and returned directly. The `os.Lstat` loop terminates on first success or when reaching the filesystem root (`parent == current`). Non-not-exist errors during stat are wrapped and returned immediately. If `filepath.Rel` returns an error OR the relative path starts with `\"..\"`, a wrapped `ErrPathTraversal` is returned.\n\n### Process Locking (`security.go`)\n\n#### Public API: `AcquireLock`\n\n**Signature:** `func AcquireLock(repoRoot string) (*SimpleLock, error)`\n\n**Responsibility:** Acquires an exclusive lock file (`.code-reducer.lock`) using O_EXCL for atomicity.\n\n**Algorithm Steps:**\n1. Call `SafeResolve` to sanitize the path; errors propagate to caller unchanged.\n2. Open a new file with O_EXCL; failure indicates another process holds the lock or a stale file exists.\n3. On open failure: check `os.IsExist(err)`. If the lock file already exists, return a specific message instructing manual cleanup; otherwise wrap with path context and original error.\n4. Write the current PID to the lock file for identification/verification.\n5. On write failure after successful open: clean up by calling `f.Close()` then `os.Remove`, then return the write error.\n\n**Return Type:** `*SimpleLock` — a struct holding per-instance state (`lockPath`, `file *os.File`, `mu sync.Mutex`, `closed bool`). No public methods other than `Unlock()`.\n\n#### Struct Method: `(*SimpleLock) Unlock`\n\n**Signature:** `func (l *SimpleLock) Unlock() error`\n\n**Responsibility:** Releases the lock by closing the file and removing it. Idempotent and thread-safe.\n\n**Algorithm Steps:**\n1. Capture close error in an `err` variable.\n2. Attempt `os.Remove`. Only replace `err` if (a) the previous operation succeeded AND (b) remove fails with a non-not-exist error.\n3. Result: if close succeeds but remove fails, that error is returned; if close fails but remove also fails, only the close error is surfaced.\n\n**Swallowed Error:** The deferred block at the end of `EnsureGitignoreHasLockfile` discards both `tmpFile.Close()` and `os.Remove(tmpName)` return values. If the temp file cannot be closed or removed (e.g., permission denied, interrupted signal), that error information is lost silently. This is a minor leak — not critical for correctness since the rename has already succeeded at that point, but it's worth noting.\n\n### Gitignore Integration (`security.go`)\n\n#### Public API: `EnsureGitignoreHasLockfile`\n\n**Signature:** `func EnsureGitignoreHasLockfile(repoRoot string) error`\n\n**Responsibility:** Ensures that `.code-reducer.lock` is present in `.gitignore`.\n\n**External I/O Operations:**\n- `filepath.Abs` / `EvalSymlinks`: Resolve absolute paths and evaluate symlinks on the repository root/ancestor (shared with `SafeResolve`)\n- `os.Lstat` (loop): Walk up directory hierarchy to find closest physically-existing ancestor during path traversal validation\n- `filepath.Rel`: Verify resolved target lies strictly inside resolved root\n- `os.OpenFile(... O_EXCL)`: Atomic create of lock file; failure indicates another process holds the lock or a stale file exists\n- `f.Write([]byte{...})`: Write current PID to lock file for identification/verification\n- `os.ReadFile`: Read existing `.gitignore` contents before appending entry\n- `os.CreateTemp` + `tmpFile.Write`: Atomic write pattern — build new content in temp file, then rename over original\n- `tmpFile.Sync`: Force flush to disk before close/rename\n- `os.Chmod`: Set permissions on temp file matching default (0644)\n- `os.Rename`: Atomically replace `.gitignore` with updated version\n\n**Error Propagation:** SafeResolve errors propagate directly. Non-not-exist read failures are wrapped with `\"error reading .gitignore\"` context. All temp file operations (`CreateTemp`, `Write`, `Sync`, `Close`, `Chmod`) return wrapped errors to caller on failure. Final `os.Rename` error is returned if it fails.\n\n### Module-Level Observations\n\n- **No network/database interactions:** Confirmed by full code inspection. All I/O targets are local paths under `repoRoot`.\n- **Mutable state:** None at the package level. The file contains only `const` values and function-local variables; no package-level or class-level properties are modified.\n- **Shared/instance state:** `SimpleLock` holds per-instance fields (`lockPath`, `file *os.File`, `mu sync.Mutex`, `closed bool`). `Unlock()` modifies `l.closed = true` under its own internal mutex. This is instance-scoped state, not shared global state.\n- **Package-level constant:** `defaultFilePerm = 0644` — used by temp file permission setting in the gitignore integration path.\n\n---\n\n## 4. Tools Module (`internal/tools`)\n\n### Responsibility\n\nProvides two subsystems: a repository-aware file I/O and discovery layer, and a Git command abstraction. Both operate on a `repoRoot` boundary to prevent path traversal outside the codebase. No mutable state exists in either subsystem; all operations are stateless per-call.\n\n### File Tools (`file_tools.go`)\n\n#### Public API\n\n| Function | Signature |\n|----------|-----------|\n| ReadFileSafely | `func ReadFileSafely(repoRoot, virtualPath string) ([]byte, error)` |\n| WriteFileSafely | `func WriteFileSafely(repoRoot, virtualPath string, content []byte) error` |\n| LoadGitignore | `func LoadGitignore(repoRoot string) ([]string, error)` |\n| ShouldIgnoreFile | `func ShouldIgnoreFile(relPath string, gitIgnore *ignore.GitIgnore) bool` |\n| DiscoverCodeFiles | `func DiscoverCodeFiles(repoRoot string, ignores []string) ([]string, error)` |\n\n#### Data Flow\n\n1. **Safe Path Resolution**: All file operations resolve user-supplied virtual paths against `repoRoot` via a security layer that prevents escaping the repository boundary. Errors from this step propagate directly to the caller without wrapping.\n2. **TOCTOU-Safe Writes**: `WriteFileSafely` writes to an in-directory temporary location, then atomically renames into place. Directories are created as needed with default permissions before write begins. Deferred cleanup (`tmpFile.Close`, `os.Remove`) discards errors silently — no return value reflects cleanup failures.\n3. **Gitignore Parsing**: Reads `.gitignore` from `repoRoot`. Non-empty, non-comment lines are collected as active patterns. A missing file returns `(nil, nil)` intentionally — not an I/O failure signal. Scanner errors are returned as-is via `scanner.Err()`.\n4. **Ignore Decision Logic**: A path is ignored if any component starts with a dot (hidden files) or ends with `.egg-info`, OR if it matches user-supplied gitignore patterns.\n5. **Recursive Code Discovery**: Walks the repository root recursively. Directories starting with a dot or ending with `.egg-info` are skipped entirely. Files passing ignore checks have their relative paths normalized to forward slash and collected as results.\n\n#### Error Handling\n\n- **Preserved via `%w`**: All disk I/O in `ReadFileSafely`, `WriteFileSafely`, and `LoadGitignore` uses wrapping that preserves the original error chain for unwrapping.\n- **Swallowed**: The deferred cleanup block in `WriteFileSafely` discards Close/Remove errors entirely. The walk callback in `DiscoverCodeFiles` logs to stderr and returns `nil`, losing original error context.\n- **Direct propagation**: Errors from the security layer pass through unchanged — caller sees exactly what happened.\n\n### Git Tools (`git_tools.go`)\n\n#### Public API\n\n| Function | Signature |\n|----------|-----------|\n| RunGit | `func RunGit(repoRoot string, args ...string) (string, error)` |\n| VerifyGitRepo | `func VerifyGitRepo(repoRoot string) error` |\n\n#### Data Flow\n\n1. **Initialize Command**: Constructs a Git command invocation in the specified root directory, appending `--no-pager` to suppress pager output.\n2. **Execute and Capture**: Runs the constructed process while capturing stdout into `bytes.Buffer` and stderr separately buffered.\n3. **Handle Success or Failure**: On success, trims whitespace from captured stdout and returns it as a string result. On failure, trims stderr and returns a formatted error containing the failure details.\n4. **Validate Repository Status**: Invokes `git rev-parse --is-inside-work-tree` specifically to check if the directory is inside a work tree. If this command fails, classifies the location as not being a Git repository and returns that specific classification.\n\n#### External Systems Interacted With\n\n| System | Mechanism | Details |\n|--------|-----------|---------|\n| `git` binary | `exec.Command(\"git\", ...)` via `os/exec` | Spawns a child subprocess in `repoRoot`. Stdout captured to `bytes.Buffer`, stderr separately buffered. `cmd.Run()` executes the command and returns the exit error. |\n\n#### Error Propagation\n\n- **RunGit**: Wraps the error with:\n  ```\n  fmt.Errorf(\"git command failed: %v, stderr: %s\", err, trimmedErr)\n  ```\n  The original `err` from `exec.Cmd` (e.g., `*exec.Error`, non-zero exit status) is embedded as `%v`; stderr text is appended. No panic occurs on failure.\n\n- **VerifyGitRepo**: Calls `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")`. If the returned error is non-nil, wraps with:\n  ```\n  fmt.Errorf(\"not a git repository (or any of the parent directories): %w\", err)\n  ```\n  Uses `%w` for wrapError-compatible wrapping.\n\n### External I/O Analysis (tools module)\n\n- **Disk**: All operations target paths under `repoRoot`. Writes use temp file + rename for atomicity; permissions are set to `0644`.\n- **Environment / Process**: No environment variables written or read by this subsystem.\n- **Network**: None.\n- **Database/APIs**: None referenced.\n\n---\n\n## Cross-Subsystem Interaction Summary\n\n| Source | Target | Mechanism | Notes |\n|--------|--------|-----------|-------|\n| `engine` → `security` | `AcquireLock`, `EnsureGitignoreHasLockfile`, `SafeResolve` | Direct import; lock acquisition gates pipeline execution. | Lock errors are wrapped with context and returned to caller. |\n| `engine` → `config` | `*config.Config` pointer | Constructor assigns once; never reassigned within engine file. | External code may mutate pointed-to struct at runtime; no synchronization protects access. |\n| `engine` → `tools` | `ReadFileSafely`, `WriteFileSafely` | Called during synthesis for per-file extraction and result persistence. | Errors wrapped with `%w`; cleanup errors silently swallowed in deferred blocks. |\n| `config` → `engine` | `*config.Config` pointer | Passed to `NewRunner`. | Resolution produces final merged config; engine reads from it without re-resolution. |\n| `tools` → `security` | Implicit via path resolution | All file tools resolve paths against `repoRoot` before I/O. | Security layer prevents traversal escapes; errors propagate unchanged. |",
    "internal/config": "# internal/config — Configuration Module\n\n## Module Responsibility\n\nThis module provides the declarative configuration schema, default values, and persistence primitives for Code-Reducer. It defines:\n\n*   **`Config`** — Centralized struct carrying all runtime parameters consumed by the synthesis pipeline (LLM model identity, Ollama base URL/context size, document directory, prompt templates, extraction step definitions, ignore lists).\n*   **`ExtractionStep`** — Struct holding a single extraction phase name and its system prompt.\n\nThe module exposes three I/O functions (`ConfigExists`, `LoadConfig`, `SaveConfig`) that manage the `.code-reducer.yaml` file using an atomic write pattern to prevent readers from observing partially-written state. A fourth function, `ResolveConfig`, merges CLI flags, environment variables, YAML config, and hardcoded defaults into a single resolved `*Config`.\n\n## Data Flow\n\n```\nCLI flags / env vars ──► ResolveConfig (merge) ──► *Config ──► Synthesis pipeline consumer\n                                                              │\nYAML (.code-reducer.yaml) ◄──────────── LoadConfig ────────┘\n                                                              ▲\nSaveConfig ───────────────────────────────────────────────────┘\n```\n\nAll configuration values flow downstream to the LLM client and synthesis stages. No mutable state exists within this module; all functions are pure or side-effect only through disk I/O.\n\n---\n\n## Types\n\n### ExtractionStep\n\nRepresents a single fact-extraction phase executed against source files during the file-fact extraction stage. Each step carries a human-readable name and an LLM system prompt that guides extraction behavior.\n\n| Field | Type | Tags |\n|-------|------|------|\n| `Name` | `string` | `yaml:\"name\"` |\n| `Prompt` | `string` | `yaml:\"prompt\"` |\n\n### Config\n\nCentral configuration struct. All fields are optional and may be left at zero values; defaults kick in during resolution if the field is empty or unset.\n\n| Field | Type | Tags | Notes |\n|-------|------|------|-------|\n| `ModelID` | `string` | `yaml:\"model_id\"` | LLM model identifier (e.g., `\"ornith:9b\"`). |\n| `OllamaBaseURL` | `string` | `yaml:\"ollama_base_url\"` | Base URL for the Ollama inference server. |\n| `OllamaNumCtx` | `int` | `yaml:\"ollama_num_ctx\"` | Context size parameter forwarded to Ollama. |\n| `DocsDir` | `string` | `yaml:\"docs_dir\"` | Directory path where generated documentation is written. |\n| `SystemPrompt` | `string` | `yaml:\"system_prompt\"` | System prompt injected into the model at synthesis time. |\n| `ModuleSynthesisPrompt` | `string` | `yaml:\"module_synthesis_prompt\"` | Prompt used during module-level synthesis. |\n| `ArchitecturePrompt` | `string` | `yaml:\"architecture_prompt\"` | Prompt used during architecture-level synthesis. |\n| `FileFactConsolidationPrompt` | `string` | `yaml:\"file_fact_consolidation_prompt\"` | Prompt used to consolidate overlapping file facts before synthesis. |\n| `ExtractionSteps` | `[]ExtractionStep` | `yaml:\"extraction_steps\"` | Ordered list of extraction phases; empty slice triggers fallback to `DefaultExtractionSteps`. |\n| `Ignore` | `[]string` | `yaml:\"ignore\"` | File paths or patterns to skip during analysis. Duplicates are removed by the resolver. |\n\n---\n\n## Constants\n\n### Environment and CLI Keys\n\n| Constant | Value | Type | Purpose |\n|----------|-------|------|---------|\n| `CodeReducerModelIDEnvKey` | `\"CODE_REDUCER_MODEL_ID\"` | `string` | Env var key for model ID override in resolver. |\n| `OllamaBaseURLEnvKey` | `\"OLLAMA_BASE_URL\"` | `string` | Env var key for Ollama base URL override in resolver. |\n| `OllamaNumCtxEnvKey` | `\"OLLAMA_NUM_CTX\"` | `string` | Env var key for context size override in resolver. |\n\n### Defaults\n\n| Constant | Value | Type | Purpose |\n|----------|-------|------|---------|\n| `OllamaDefaultBaseURL` | `\"http://localhost:11434\"` | `string` | Default Ollama inference server address. |\n| `OllamaDefaultModelID` | `\"ornith:9b\"` | `string` | Default LLM model identifier. |\n| `OllamaDefaultNumCtx` | `8192` | `int` | Default context size. |\n| `DefaultDocsDir` | `\"wiki\"` | `string` | Default documentation output directory. |\n| `ConfigFileName` | `\".code-reducer.yaml\"` | `string` | Filename used by all three I/O functions and `ResolveConfig`. |\n| `DefaultSystemPrompt`, `DefaultModuleSynthesisPrompt`, `DefaultArchitecturePrompt`, `DefaultFileFactConsolidationPrompt` | *(multiline strings)* | `string` | Hardcoded fallback prompts for the four synthesis stages. |\n\n### Internal\n\n| Constant | Value | Type | Notes |\n|----------|-------|------|-------|\n| `configFilePerm` | `0600` | *(unexported)* | File permission applied to config file and temp files during write. |\n\n---\n\n## Public API Surface — Types \u0026 Variables (from config.go)\n\n### DefaultExtractionSteps\n\nPackage-level variable of type `[]ExtractionStep`. Serves as the fallback extraction pipeline when the YAML config contains an empty or nil slice. Each step carries a name and prompt for one of the four fact-extraction phases: *API signatures*, *business logic*, *mutable state \u0026 concurrency*, *external I/O \u0026 error propagation*.\n\n---\n\n## Public API Surface — I/O Functions (from io.go)\n\n### ConfigExists\n\n```go\nfunc ConfigExists(cwd string) bool\n```\n\nReturns `true` if `.code-reducer.yaml` exists at the path formed by joining `cwd` with `ConfigFileName`. The underlying operation is a single `os.Stat` call; no error wrapping occurs. Returns only a boolean to caller.\n\n### LoadConfig\n\n```go\nfunc LoadConfig(cwd string) (*Config, error)\n```\n\nReads and parses `.code-reducer.yaml` from `cwd`. On success returns the populated `*Config`. Failure modes:\n\n| Condition | Return Value | Notes |\n|-----------|--------------|-------|\n| YAML parse error | `(nil, wrappedError)` | Error is wrapped with `\"failed to parse yaml config: %w\"`. |\n| Raw OS read error (not-not-exist) | `(nil, rawError)` | `os.ReadFile` failure propagates unchanged. |\n\n### SaveConfig\n\n```go\nfunc SaveConfig(cwd string, cfg *Config) error\n```\n\nSerializes `cfg` to `.code-reducer.yaml` using an atomic write pattern: create temp file → write bytes → sync → close → chmod → rename over original path. Failure points are wrapped individually with descriptive prefixes (`\"failed to \u003caction\u003e config file\"`). The final state is either the old file or a fully-written new file — never neither, because of the rename semantics.\n\n---\n\n## Public API Surface — Resolution (from resolve.go)\n\n### ResolveConfig\n\n```go\nfunc ResolveConfig(repoRoot string, modelIDFlag string, numCtxFlag string) (*Config, error)\n```\n\nMerges configuration across four precedence tiers:\n\n1.  Hardcoded defaults (`OllamaDefault*`, `DefaultDocsDir`, etc.).\n2.  Values parsed from `.code-reducer.yaml` at `repoRoot`.\n3.  Environment variables (`CodeReducerModelIDEnvKey`, `OllamaBaseURLEnvKey`, `OllamaNumCtxEnvKey`).\n4.  CLI flags (`modelIDFlag`, `numCtxFlag`).\n\nSpecific resolution rules:\n\n*   **`Ignore` list**: Deduplicated in-place; first occurrence wins via hash-set tracking.\n*   **`ExtractionSteps`**: Falls back to `DefaultExtractionSteps` when the YAML slice is empty or nil.\n*   **Numeric fields** (`OllamaNumCtx`): Parsed with `strconv.Atoi`; non-positive values are rejected and the prior tier's value is retained.\n*   **String prompts**: Empty YAML values fall back to the corresponding hardcoded default via `resolveString`.\n\n### Internal Helpers (from resolve.go)\n\n| Function | Parameters | Return Types | Description |\n|----------|-----------|--------------|-------------|\n| `deduplicate` | `a []string` | `[]string` | Removes duplicates from a string slice; first occurrence wins. |\n| `resolveString` | `yamlVal string`, `defaultVal string` | `string` | Returns `yamlVal` if non-empty, otherwise returns `defaultVal`. Used for prompt field resolution. |\n\n---\n\n## Error Propagation Summary\n\n*   **config.go**: No runtime operations; no errors can be raised or swallowed here.\n*   **io.go**: All disk-read and disk-write functions return error tuples. YAML parse errors are explicitly wrapped with `\"failed to parse yaml config: %w\"`; raw OS-level read/write errors propagate unchanged through the returned tuple. Write-path errors each carry a unique prefix (`\"failed to \u003caction\u003e temp config file\"`).\n*   **resolve.go**: `LoadConfig` failure is only surfaced when `os.IsNotExist(err)` returns false — missing files are silently replaced with an empty `Config{}` and no error reaches the caller. All other branches (env lookups, flag parsing, deduplication) produce zero errors by design.\n\n---\n\n## External I/O Analysis\n\n*   **Disk**: All functions interact exclusively with `.code-reducer.yaml` under `cwd`. Writes use temp file + rename for atomicity; permissions are set to `0600`.\n*   **Environment / Process**: Three env vars (`CODE_REDUCER_MODEL_ID`, `OLLAMA_BASE_URL`, `OLLAMA_NUM_CTX`) are read by `ResolveConfig`; none are written.\n*   **Network**: None. Constants like `OllamaDefaultBaseURL` are string literals; no HTTP client or network calls exist in this module.\n*   **Database/APIs**: None referenced.",
    "internal/engine": "# internal/engine — AI-Driven Incremental Documentation Generation Pipeline\n\n## Module Responsibility\n\nThe `internal/engine` package implements a persistent, incremental documentation synthesis pipeline for software repositories. It produces three artifact classes—global architecture summaries, quickstart guides, and per-module API descriptions—from source code using an LLM backend, with change detection that limits regeneration to directories impacted by actual file modifications. State persists across invocations via a metadata cache stored as a JSON file under the repository's `docs/` directory; a global steps hash anchors cache validity to the current extraction pipeline configuration.\n\n## Data Flow\n\n```\nRunner.Run(ctx, repoRoot, mode) \n    └─► acquireLock(repoRoot)                    (security.AcquireLock)\n        ├─► ModeInit ──► orchestrator.GenerateStandardDocs ──► architecture.md / quickstart.md\n        └─► ModeUpdate ──► loadMetadataCache      ──► cache.Files, cache.Modules, cache.StepsHash\n                           │\n                           ├─► computeSHA256 × N  ──► precalculatedHashes (cache miss → disk read)\n                           │\n                           ├─► diff detection     ──► FileChange map {Path: Status}\n                           │\n                           ├─► rebuild tree       ──► DirNode hierarchy from current files only\n                           │\n                           ├─► prune stale modules (os.Stat + os.Remove)\n                           │\n                           ├─► determine affected dirs (walk tree + cache empty check + disk existence)\n                           │\n                           └─► selective regeneration ──► synthesizeNode × N ──► WriteFileSafely\n```\n\n---\n\n## Runner Entry Point (`runner.go`)\n\n### `Mode`\n\nString-typed operation modes for the pipeline. Two exported constants exist:\n\n| Constant | Value |\n|----------|-------|\n| `ModeInit`  | `\"init\"`   |\n| `ModeUpdate` | `\"update\"` |\n\n### `Runner`\n\nStruct holding a single pointer field, `cfg *config.Config`, assigned once in the constructor and never reassigned within this file. External mutability of the pointed-to struct is not observable from here alone.\n\n```go\nfunc NewRunner(cfg *config.Config) *Runner\nfunc (r *Runner) Run(ctx context.Context, repoRoot string, mode Mode, onEvent func(Event)) error\n```\n\n`Run()` performs three operations before branching:\n\n1. **Lockfile validation** — `security.EnsureGitignoreHasLockfile(repoRoot)` is invoked; any error is logged as a status event and execution proceeds regardless.\n2. **Exclusive lock acquisition** — `security.AcquireLock(repoRoot)` returns an error that is wrapped with context (`failed to acquire repository lock: %w`) and returned immediately. A deferred unlock runs on every exit path, guaranteeing release even when the pipeline fails partway through.\n3. **Client instantiation** — `newLLMClient(r.cfg.ModelID, r.cfg.OllamaBaseURL, r.cfg.OllamaNumCtx)` constructs an Ollama HTTP client; no network I/O occurs at this point.\n\nAfter lock acquisition, execution branches on the mode value: invalid strings return `fmt.Errorf(\"unsupported mode: %s\", mode)`. Otherwise, either `orch.RunInit` or `orch.RunUpdate` is invoked directly and its error (or nil) is returned to the caller.\n\n---\n\n## Orchestrator (`orchestrator.go`)\n\n### Types\n\n```go\ntype EventType string\nconst EventStatus  EventType = \"status\"\nconst EventError   EventType = \"error\"\n\ntype Event struct {\n    Type   EventType `json:\"type\"`\n    Message string    `json:\"message\"`\n}\n```\n\nBoth constants and the type are unexported. The orchestrator itself is also an internal, unexported type; only its public method signatures appear in the package surface.\n\n### `GenerateStandardDocs`\n\n```go\nfunc (o *orchestrator) GenerateStandardDocs(ctx, repoRoot, docsDir, rootSum string, cfg *config.Config, logEvent LogEventFunc) error\n```\n\nInvoked from both `RunInit` and `RunUpdate`. It issues two sequential LLM calls: one to generate `architecture.md` and another for `quickstart.md`, each wrapped as a global artifact. Errors propagate fully (wrapped with `%w`). If the top-level directory is marked affected or standard docs are missing, this function runs; otherwise it is skipped entirely on update.\n\n---\n\n## Pipeline Context State (`orchestrator.go`)\n\nThe orchestrator maintains mutable state across its lifecycle via pointers into shared structs:\n\n| Field | Type | Mutability | Notes |\n|-------|------|------------|-------|\n| `cache` | `*MetadataCache` | **Mutable** | Reassigned in `setupPipeline`, `teardownPipeline`, `RunInit`, `RunUpdate`. Fields `.StepsHash`, `.Files`, `.Modules` are updated across calls. |\n| `cfg` | `*config.Config` | Potentially mutable | Read only here (`SystemPrompt`, `ArchitecturePrompt`, `ExtractionSteps`). External code may mutate the pointed-to struct at runtime. |\n| `client` | `llmCaller` interface | Read-only in this file | Initialized on construction; never reassigned within these methods. |\n\nNo synchronization primitives (mutex, atomic, channel) protect access to any of these fields. Concurrent callers sharing a single context instance would race on the cache maps. All operations are sequential and single-threaded per method invocation.\n\n---\n\n## Tree Construction and Affected Detection (`tree.go`)\n\n### Types\n\n```go\ntype ChangeStatus string   // \"Added\" | \"Modified\" | \"Deleted\" (unexported)\n\ntype FileChange struct {\n    Path  string\n    Status ChangeStatus\n}\n\ntype DirNode struct {\n    Path     string\n    Files    []string\n    Children map[string]*DirNode\n}\n```\n\n### `buildTree`\n\nConstructs a nested `DirNode` hierarchy from a slice of file paths. Leaf entries are files; intermediate path segments become child nodes. No I/O, no side effects.\n\n### Affected Detection Logic\n\nThe orchestrator walks the tree to mark directories as \"affected\" via four conditions:\n\n1. **Changed-file match** — if any `FileChange` in the map matches a file under this node's subtree.\n2. **Module path non-existence on disk** — `security.SafeResolve(repoRoot, modulePath)` followed by `os.Stat(absModulePath)`. If the stat error is not `os.ErrNotExist`, the branch is skipped silently.\n3. **Cache emptiness** — if the corresponding entry in `cache.Modules` is empty.\n4. **Child propagation** — a second recursive pass marks any parent affected whenever *any* child has already been flagged, regardless of direct match.\n\n---\n\n## LLM Client (`client.go`)\n\n### Public Types\n\n```go\ntype Message struct {\n    Role    string `json:\"role\"`\n    Content string `json:\"content\"`\n}\n```\n\n`Message` is the only exported type in this file. All other declarations are unexported and therefore not part of the public API:\n\n| Element | Type | Notes |\n|---|------|-------|\n| `llmCaller` | interface | Unexported; methods include `CallLLM(ctx, systemPrompt, messages, jsonFormat)` returning `(string, error)` and `NumCtx() int`. |\n| `llmClient` | struct | Unexported. Fields: `modelID`, `baseURL`, `numCtx`, `httpClient *http.Client`. |\n| `newLLMClient` | func | Factory for `*llmClient`; referenced but not defined in this file. |\n| `(c *llmClient) NumCtx()` | method | Returns the configured context window size. |\n| `ollamaRequest` / `ollamaOptions` / `ollamaResponse` | structs | Unexported request/response payloads for Ollama's `/api/chat`. |\n| `(c *llmClient) prepareOllamaRequest()` | private method | Builds and serializes the chat payload. |\n\n### External I/O\n\nAll network interactions target `{baseURL}/api/chat` via HTTP POST with JSON body (`application/json`). The client is constructed once per instance; `httpClient` is reused across calls without locking, which is safe because timeout and base URL are fixed at construction time and never reassigned within this file.\n\n**Response handling:** 200 OK responses have their bodies read fully via `io.ReadAll`; non-OK status codes trigger a bounded read (`io.LimitReader(resp.Body, maxErrorBodyBytes)`) whose error is discarded silently—the resulting string (containing the HTTP status and truncated body) is returned to the caller instead.\n\n**Unresolved references:** `defaultHTTPTimeout` and `maxErrorBodyBytes` are referenced in this file but not defined here; they must be package-level constants or variables declared elsewhere, otherwise compilation fails.\n\n---\n\n## Chunking and Reduction Pipeline (`chunking.go`)\n\n### `reduceWithLLM`\n\n```go\nfunc reduceWithLLM(ctx context.Context, c llmCaller, items []string, sysPrompt string, buildPrompt func(batch []string) string, logMsg func(batch []string) string, errMsg string, logEvent LogEventFunc) (string, error)\n```\n\nEntry point for all LLM-mediated reductions. It calls `c.CallLLM(ctx, sysPrompt, messages, false)` with a system prompt prepended to user messages; errors are wrapped via `fmt.Errorf(\"%s: %w\", errMsg, err)`. The original error is preserved through `%w` wrapping.\n\n### `reduceInChunks`\n\n```go\nfunc reduceInChunks(ctx context.Context, c llmCaller, nodePath string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)\n```\n\nInvoked from the synthesis step to consolidate a directory's file facts and child summaries. Calls `reduceWithLLM` with a prompt derived from `cfg.SystemPrompt` and the current extraction steps. Errors propagate through unchanged.\n\n### `reduceFileFacts`\n\n```go\nfunc reduceFileFacts(ctx context.Context, c llmCaller, filePath string, stepName string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)\n```\n\nConsolidates per-extraction-step outputs for a single file. Calls `reduceWithLLM` with a prompt built from `cfg.SystemPrompt` and the current extraction steps; errors propagate as-is.\n\n### `reduceItems`\n\n```go\nfunc reduceItems(ctx context.Context, items []string, maxChars int, reduceFn func(batch []string) (string, error)) (string, error)\n```\n\nIterative map-reduce loop: accumulates items into batches that fit within `maxChars`, flushing when the next item would exceed the limit. If a single item exceeds the cap at entry, it is split with overlap rather than truncated—information preservation over compression. After each LLM pass, total output run count vs input is computed; if output ≥95% of input (integer division), recursion terminates and results are concatenated with `\\n\\n`. Context cancellation (`ctx.Err()`) returns early without partial results.\n\n### `chunkTextWithOverlap`\n\n```go\nfunc chunkTextWithOverlap(text string, maxRunes int, overlapRunes int) []string\n```\n\nSplits text into overlapping chunks of at most `maxRunes` runes with `overlapRunes` rune overlap between adjacent chunks (clamped if ≥ `maxRunes`). Returns the full text as a single chunk when `maxRunes \u003c= 0` or `overlapRunes \u003e= maxRunes`. No panics, no bounds errors.\n\n---\n\n## Markdown Fence Stripping (`markdown.go`)\n\n### Internal Implementation\n\n```go\nvar markdownFenceRe regexp.Regexp\nfunc stripOuterMarkdownFence(content string) string\n```\n\nUnexported; both the variable and function begin with lowercase letters. The regex is compiled once at package scope and never reassigned—effectively read-only after compilation.\n\n**Algorithm:** trim whitespace → match triple-backtick fence (with optional `markdown`/`json` language tag) → return captured interior after trimming surrounding whitespace. No error return; if the pattern doesn't match, original input is returned unchanged.\n\n---\n\n## Constants (`constants.go`)\n\nAll nine named constants in this file are unexported:\n\n| Constant | Purpose |\n|----------|---------|\n| `defaultHTTPTimeout` | 10-minute ceiling for remote API calls (set at package init) |\n| `maxErrorBodyBytes` | 1 KB cap on error response payloads captured from failed requests |\n| `defaultChunkOverlap` | 800-token overlap between consecutive text chunks during streaming/processing |\n| `minNumCtxFloor` | 512 minimum threshold for context length calculations |\n| `contextWindowAllocRatio` | 0.75 reserved proportion of available space for primary content |\n| `maxCharsMultiplier` | 3× multiplier applied to base character limit |\n| `metadataFileName`, `agentsFileName`, `defaultDirPerm` | Filesystem conventions for state persistence |\n\nNo mutable state, no external I/O, no error propagation. The single function-type alias (`LogEventFunc`) is declared but not used within this file.\n\n---\n\n## Synthesis Engine (`synthesize.go`)\n\nUnexported types and functions only; all identifiers begin with lowercase letters.\n\n### External I/O Matrix\n\n| Source | Path | Behavior on Failure |\n|--------|------|---------------------|\n| `tools.ReadFileSafely(p.repoRoot, f)` — first call | `\u003crepoRoot\u003e/\u003cf\u003e` | Logged as status warning; returns `\"\", nil`. Swallowed by caller. |\n| `tools.ReadFileSafely` — second call (inside `if facts == \"\"`) | Same | Same swallow pattern. |\n| `p.client.CallLLM(p.ctx, systemPrompt, []Message{...}, false)` | Network endpoint via `llmCaller` | Wrapped with `%w`, includes step name and file path in message. Propagated to caller as `\"LLM error extracting %s for %s: %w\"`. |\n| `tools.WriteFileSafely(p.repoRoot, modulePath, []byte(finalSum))` | `\u003crepoRoot\u003e/\u003cdocsDir\u003e/modules/\u003csafe-filename-of-node-path\u003e.md` | Wrapped with `%w`, includes node path. Propagated to caller as `\"failed to write module documentation for %s: %w\"`. |\n\n### Synthesis Algorithm (per `synthesizeNode`)\n\n1. **Cache-first lookup** — check `cache.Files[f]` against precomputed SHA-256 hash; on match, reuse cached facts without re-reading the file.\n2. **Recursive bottom-up traversal** — for each directory node: if unaffected and descendants are unaffected, return cached summary immediately. Sort child names alphabetically; recurse into children first; collect non-empty summaries into a map keyed by child name.\n3. **Per-file extraction** — compute dynamic content limit from remaining context window capacity (allocates ~75% for file content). Split large files with `chunkTextWithOverlap`. Pass each chunk to LLM with step-specific prompt derived from `ExtractionSteps`. Strip markdown fences from responses via `stripOuterMarkdownFence`. Consolidate all outputs per extraction step using `reduceFileFacts`. Concatenate across steps into a single file fact string.\n4. **Assembly** — combine extracted file facts (prefixed `\"### File: basename\"`) and child summaries (prefixed `\"### Subsystem: name\"`) into one `components` slice. If nothing exists, clear the cache entry and return empty.\n5. **Final reduction and persistence** — pass all components to `chunked reduction` (`reduceInChunks`) producing a single consolidated summary for the directory; store in `cache.Modules[node.Path]`; write to disk under `\u003cdocsDir\u003e/modules/\u003csafe-filename-of-path\u003e`.\n\n---\n\n## Utility Helpers (`utils.go`)\n\n### `toSafeMarkdownFilename`\n\n```go\nfunc toSafeMarkdownFilename(modulePath string) string\n```\n\nPure string manipulation; no file operations. Returns `\"README.md\"` when the path is empty or `\".\"`; otherwise joins with `\"README.md\"`. Used by tree traversal and synthesis code for module-level doc paths.\n\n### `makeLogEvent`\n\n```go\nfunc makeLogEvent(onEvent func(Event)) LogEventFunc\n```\n\nAdapter factory returning a function that forwards to its callback only when non-nil (nil-safe pattern). No panics, no error returns.\n\n---\n\n## Error Propagation Summary\n\n| Category | Handled Pattern | Swallowed Pattern |\n|----------|-----------------|-------------------|\n| LLM calls (`CallLLM`) | Wrapped with `%w`, descriptive message includes step/file context; propagated to caller. | — |\n| File writes (`WriteFileSafely`) | Wrapped with `%w`; propagated to caller. | — |\n| Hash computation per file (`computeSHA256`) | Error logged as status warning; loop continues, file skipped from cache. | Swallowed in both `RunInit` and `RunUpdate`. |\n| Cache load/save (`loadMetadataCache`, `saveMetadataCache`) | — | Logged as status event; function completes regardless of write success/failure. |\n| `.gitignore` loading | — | Logged as warning; execution proceeds. |\n| Module file cleanup (`os.Remove`) | — | Error ignored via `_ = os.Remove(...)`. No verification. |\n| Directory creation (`os.MkdirAll`) | Wrapped with `%w`; propagated to caller. | — |\n| Non-OK HTTP responses | Bounded body read; error string returned (I/O failure during bounded read silently dropped). | Partially swallowed. |",
    "internal/security": "# internal/security Module\n\n## Responsibility and Data Flow\n\nThe `internal/security` package provides two isolated concerns for repository-level operations: (1) safe resolution of user-supplied filesystem paths to prevent traversal escapes, and (2) file-based mutual exclusion ensuring only one instance of code-reducer holds a lock at any time. A third utility integrates the lockfile into version control so stale state is ignored by `git`.\n\nData flow begins with callers invoking `SafeResolve` or `AcquireLock`, both of which return errors to downstream consumers for branching logic. Sentinel errors from `errors.go` are returned via standard Go `error` returns and must be detected using `errors.Is()` or `errors.As()`. No wrapping occurs; propagation is contract-level, not exception-style.\n\n---\n\n## Error Contract Markers (`errors.go`)\n\n### Sentinel Definitions\n\n| Variable | Condition | Mechanism |\n|---|---|---|\n| `ErrPathTraversal` — `\"security violation: path traversal detected\"` | Returned when a resolved path falls outside the repository root boundary | Standard Go error return value. Not wrapped; no panic. Detected via `errors.Is()` / `errors.As()`. |\n| `ErrLockHeld` — `\"lock is already held by another process\"` | Returned when another code-reducer instance holds the file lock | Standard Go error return value. Not wrapped; no panic. Detected via `errors.Is()` / `errors.As()`. |\n\nBoth errors are raw `*errors.errorString` values constructed using the standard library's `errors.New`. They carry no algorithmic logic and serve solely as contract markers for callers to branch on. No exception-style wrapping occurs in this file; any processing logic resides elsewhere in the module.\n\n---\n\n## Path Sanitization (`security.go`)\n\n### Public API: `SafeResolve`\n\n**Signature:** `func SafeResolve(repoRoot string, inputPath string) (string, error)`\n\n**Responsibility:** Cleans an input path and ensures it lies strictly inside the repository root. Resolves symlinks on existing ancestor parts to block symlink-based traversal.\n\n**Algorithm Steps:**\n1. Accept a `repoRoot` absolute reference and a relative or absolute `inputPath`.\n2. Resolve symlinks on existing ancestor directory components so that symlink-based traversal is blocked.\n3. Walk up from the target until hitting an existing physical ancestor (skipping non-existent intermediate components).\n4. Reconstruct the full resolved path from that ancestor plus the skipped suffixes.\n5. Verify the final resolved path lies strictly inside the repository root; reject if it does not.\n\n**Error Propagation:** Errors from `filepath.Abs`, `EvalSymlinks(absRoot)`, and `EvalSymlinks(current)` are wrapped with context and returned directly. The `os.Lstat` loop terminates on first success or when reaching the filesystem root (`parent == current`). Non-not-exist errors during stat are wrapped and returned immediately. If `filepath.Rel` returns an error OR the relative path starts with `\"..\"`, a wrapped `ErrPathTraversal` is returned.\n\n---\n\n## Process Locking (`security.go`)\n\n### Public API: `AcquireLock`\n\n**Signature:** `func AcquireLock(repoRoot string) (*SimpleLock, error)`\n\n**Responsibility:** Acquires an exclusive lock file (`.code-reducer.lock`) using O_EXCL for atomicity.\n\n**Algorithm Steps:**\n1. Call `SafeResolve` to sanitize the path; errors propagate to caller unchanged.\n2. Open a new file with O_EXCL; failure indicates another process holds the lock or a stale file exists.\n3. On open failure: check `os.IsExist(err)`. If the lock file already exists, return a specific message instructing manual cleanup; otherwise wrap with path context and original error.\n4. Write the current PID to the lock file for identification/verification.\n5. On write failure after successful open: clean up by calling `f.Close()` then `os.Remove`, then return the write error.\n\n**Return Type:** `*SimpleLock` — a struct holding per-instance state (`lockPath`, `file *os.File`, `mu sync.Mutex`, `closed bool`). No public methods other than `Unlock()`.\n\n---\n\n### Struct Method: `(*SimpleLock) Unlock`\n\n**Signature:** `func (l *SimpleLock) Unlock() error`\n\n**Responsibility:** Releases the lock by closing the file and removing it. Idempotent and thread-safe.\n\n**Algorithm Steps:**\n1. Capture close error in an `err` variable.\n2. Attempt `os.Remove`. Only replace `err` if (a) the previous operation succeeded AND (b) remove fails with a non-not-exist error.\n3. Result: if close succeeds but remove fails, that error is returned; if close fails but remove also fails, only the close error is surfaced.\n\n**Swallowed Error:** The deferred block at the end of `EnsureGitignoreHasLockfile` discards both `tmpFile.Close()` and `os.Remove(tmpName)` return values. If the temp file cannot be closed or removed (e.g., permission denied, interrupted signal), that error information is lost silently. This is a minor leak — not critical for correctness since the rename has already succeeded at that point, but it's worth noting.\n\n---\n\n## Gitignore Integration (`security.go`)\n\n### Public API: `EnsureGitignoreHasLockfile`\n\n**Signature:** `func EnsureGitignoreHasLockfile(repoRoot string) error`\n\n**Responsibility:** Ensures that `.code-reducer.lock` is present in `.gitignore`.\n\n**External I/O Operations:**\n- `filepath.Abs` / `EvalSymlinks`: Resolve absolute paths and evaluate symlinks on the repository root/ancestor (shared with `SafeResolve`)\n- `os.Lstat` (loop): Walk up directory hierarchy to find closest physically-existing ancestor during path traversal validation\n- `filepath.Rel`: Verify resolved target lies strictly inside resolved root\n- `os.OpenFile(... O_EXCL)`: Atomic create of lock file; failure indicates another process holds the lock or a stale file exists\n- `f.Write([]byte{...})`: Write current PID to lock file for identification/verification\n- `os.ReadFile`: Read existing `.gitignore` contents before appending entry\n- `os.CreateTemp` + `tmpFile.Write`: Atomic write pattern — build new content in temp file, then rename over original\n- `tmpFile.Sync`: Force flush to disk before close/rename\n- `os.Chmod`: Set permissions on temp file matching default (0644)\n- `os.Rename`: Atomically replace `.gitignore` with updated version\n\n**Error Propagation:** SafeResolve errors propagate directly. Non-not-exist read failures are wrapped with `\"error reading .gitignore\"` context. All temp file operations (`CreateTemp`, `Write`, `Sync`, `Close`, `Chmod`) return wrapped errors to caller on failure. Final `os.Rename` error is returned if it fails.\n\n---\n\n## Module-Level Observations\n\n- **No network/database interactions:** Confirmed by full code inspection. All I/O targets are local paths under `repoRoot`.\n- **Mutable state:** None at the package level. The file contains only `const` values and function-local variables; no package-level or class-level properties are modified.\n- **Shared/instance state:** `SimpleLock` holds per-instance fields (`lockPath`, `file *os.File`, `mu sync.Mutex`, `closed bool`). `Unlock()` modifies `l.closed = true` under its own internal mutex. This is instance-scoped state, not shared global state.\n- **Package-level constant:** `defaultFilePerm = 0644` — used by temp file permission setting in the gitignore integration path.",
    "internal/tools": "# `internal/tools` — Repository-Aware File I/O and Git Abstraction\n\n## Module Responsibility\n\nThis module provides two subsystems: a repository-aware file I/O and discovery layer, and a Git command abstraction. Both operate on a `repoRoot` boundary to prevent path traversal outside the codebase. No mutable state exists in either subsystem; all operations are stateless per-call.\n\n---\n\n## File Tools (`file_tools.go`)\n\n### Public API\n\n| Function | Signature |\n|----------|-----------|\n| ReadFileSafely | `func ReadFileSafely(repoRoot, virtualPath string) ([]byte, error)` |\n| WriteFileSafely | `func WriteFileSafely(repoRoot, virtualPath string, content []byte) error` |\n| LoadGitignore | `func LoadGitignore(repoRoot string) ([]string, error)` |\n| ShouldIgnoreFile | `func ShouldIgnoreFile(relPath string, gitIgnore *ignore.GitIgnore) bool` |\n| DiscoverCodeFiles | `func DiscoverCodeFiles(repoRoot string, ignores []string) ([]string, error)` |\n\n### Data Flow\n\n1. **Safe Path Resolution**: All file operations resolve user-supplied virtual paths against `repoRoot` via a security layer that prevents escaping the repository boundary. Errors from this step propagate directly to the caller without wrapping.\n2. **TOCTOU-Safe Writes**: `WriteFileSafely` writes to an in-directory temporary location, then atomically renames into place. Directories are created as needed with default permissions before write begins. Deferred cleanup (`tmpFile.Close`, `os.Remove`) discards errors silently — no return value reflects cleanup failures.\n3. **Gitignore Parsing**: Reads `.gitignore` from `repoRoot`. Non-empty, non-comment lines are collected as active patterns. A missing file returns `(nil, nil)` intentionally — not an I/O failure signal. Scanner errors are returned as-is via `scanner.Err()`.\n4. **Ignore Decision Logic**: A path is ignored if any component starts with a dot (hidden files) or ends with `.egg-info`, OR if it matches user-supplied gitignore patterns.\n5. **Recursive Code Discovery**: Walks the repository root recursively. Directories starting with a dot or ending with `.egg-info` are skipped entirely. Files passing ignore checks have their relative paths normalized to forward slash and collected as results.\n\n### Error Handling\n\n- **Preserved via `%w`**: All disk I/O in `ReadFileSafely`, `WriteFileSafely`, and `LoadGitignore` uses wrapping that preserves the original error chain for unwrapping.\n- **Swallowed**: The deferred cleanup block in `WriteFileSafely` discards Close/Remove errors entirely. The walk callback in `DiscoverCodeFiles` logs to stderr and returns `nil`, losing original error context.\n- **Direct propagation**: Errors from the security layer pass through unchanged — caller sees exactly what happened.\n\n---\n\n## Git Tools (`git_tools.go`)\n\n### Public API\n\n| Function | Signature |\n|----------|-----------|\n| RunGit | `func RunGit(repoRoot string, args ...string) (string, error)` |\n| VerifyGitRepo | `func VerifyGitRepo(repoRoot string) error` |\n\n### Data Flow\n\n1. **Initialize Command**: Constructs a Git command invocation in the specified root directory, appending `--no-pager` to suppress pager output.\n2. **Execute and Capture**: Runs the constructed process while capturing stdout into `bytes.Buffer` and stderr separately buffered.\n3. **Handle Success or Failure**: On success, trims whitespace from captured stdout and returns it as a string result. On failure, trims stderr and returns a formatted error containing the failure details.\n4. **Validate Repository Status**: Invokes `git rev-parse --is-inside-work-tree` specifically to check if the directory is inside a work tree. If this command fails, classifies the location as not being a Git repository and returns that specific classification.\n\n### External Systems Interacted With\n\n| System | Mechanism | Details |\n|--------|-----------|---------|\n| `git` binary | `exec.Command(\"git\", ...)` via `os/exec` | Spawns a child subprocess in `repoRoot`. Stdout captured to `bytes.Buffer`, stderr separately buffered. `cmd.Run()` executes the command and returns the exit error. |\n\n### Error Propagation\n\n- **RunGit**: Wraps the error with:\n  ```\n  fmt.Errorf(\"git command failed: %v, stderr: %s\", err, trimmedErr)\n  ```\n  The original `err` from `exec.Cmd` (e.g., `*exec.Error`, non-zero exit status) is embedded as `%v`; stderr text is appended. No panic occurs on failure.\n\n- **VerifyGitRepo**: Calls `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")`. If the returned error is non-nil, wraps with:\n  ```\n  fmt.Errorf(\"not a git repository (or any of the parent directories): %w\", err)\n  ```\n  Uses `%w` for wrapError-compatible wrapping."
  }
}