{
  "version": 1,
  "steps_hash": "61d594e4ff13def120bfbc4dbb8f7556458b5da07bca0af9ff454967f8afc215",
  "files": {
    "AGENTS.md": {
      "sha256": "730753023d7cd5de555778f5202cbf73002c717ce346c93db11f741bda0a546e",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: AGENTS.md\n\nNo public API elements found. This file contains AI agent guidelines and documentation references, not a code module with exported functions, classes, methods, or types.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nThis file provides procedural guidance for AI coding agents navigating an existing codebase. It solves the problem of ensuring agents understand system architecture before modifying code—preventing misaligned changes that violate design intent.\n\n**High-Level Steps (as documented):**  \n1. Read `wiki/architecture.md` to get a high-level system overview, module relationships, and boundary definitions.  \n2. Read `wiki/quickstart.md` for onboarding steps, coding patterns, and configuration settings.  \n3. Explore `wiki/modules/` for directory-level summaries and API descriptions of internal packages.  \n\n**Domain Rules:**  \n- Before making any changes, the agent must analyze these wiki files first to align with existing design choices and code structures.\n\n**Note:** This file contains no computational logic or data-processing algorithm. It is purely procedural documentation directing how agents should approach the repository before writing code.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n{\n  \"external_io\": [\n    {\n      \"type\": \"No external side effects\",\n      \"description\": \"This is a static Markdown documentation file with no executable code, runtime interactions, or system calls.\"\n    }\n  ],\n  \"error_propagation\": [\n    {\n      \"type\": \"N/A\",\n      \"description\": \"The file contains only declarative documentation text. There is no error handling, exception management, return codes, or panic/crash logic to analyze.\"\n    }\n  ]\n}"
    },
    "cmd/init.go": {
      "sha256": "87cb2d0246334e1b067c69acf67d42eddd7bae533c7cea9b0cf156ef69a3fcc2",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `cmd/init.go`\n\n**No exported or public elements found.** All identifiers in this file use lowercase first letters, making them unexported (hidden) within the Go module:\n\n- `initCmd` → unexported `*cobra.Command` variable\n- `init()` → unexported package init function\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis code registers a CLI command `init` for initializing project documentation by scanning the repository and generating initial wiki markdown pages.\n\n## High-Level Algorithm Steps\n\n1. Define an `init` cobra subcommand with usage string \"Initialize project documentation\"\n2. On execution, delegate to an external function `executeCommand(\"init\")` — the actual implementation is not contained in this file\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects** are directly observable in this file. No network calls, disk reads/writes, database queries, or API interactions occur within the code shown. The actual work is delegated to `executeCommand(\"init\")`, which resides outside this module/file and cannot be analyzed here.\n\n## Error Propagation\n\n- Errors flow through cobra's command interface via the `RunE` function signature (`func(cmd *cobra.Command, args []string) error`).\n- When `executeCommand(\"init\")` returns a non-nil error, cobra propagates it as a CLI exit code (non-zero process exit). No panic or crash is introduced by this file.\n- The import of `github.com/spf13/cobra` is purely local to the command framework—no external system dependencies are pulled in by this snippet."
    },
    "cmd/root.go": {
      "sha256": "8f73bc460485f005d0153d7fb5be7fb0d1b40d364cd3bc768f5547ef529ea510",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `cmd` Package (root.go)\n\n## Variables\n\n| Name | Type | Description |\n|------|------|-------------|\n| **`RootCmd`** | `*cobra.Command` | The root CLI command for the code-reducer application. |\n\n---\n\n\u003e **Note:** All other package-level declarations (`modelIDFlag`, `numCtxFlag`, `executeCommand`) are unexported (lowercase) and therefore not part of the public API.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code implements the CLI entry point for **Code-Reducer**, a tool that generates and maintains project documentation (wiki). It orchestrates setup, initialization, and execution of documentation runs against an LLM engine.\n\n## High-Level Algorithm Steps\n\n1. **Environment Validation** — Confirm the current working directory is a valid Git repository; abort with error if not.\n\n2. **Configuration Management** — Check whether a config file exists in the project root:\n   - If missing and stdin is a terminal, run an implicit setup flow to create one.\n   - Otherwise, resolve the final configuration by merging flags (`--model-id`, `--num-ctx`) with on-disk settings.\n\n3. **Mode Precondition Checks** — Validate that the requested operation is legal:\n   - `init` mode fails if documentation has already been initialized (suggests using `update` instead).\n   - `update` mode fails if initialization has not occurred yet (requires running `init` first).\n\n4. **Execution Loop** — Set up signal handlers for graceful shutdown (`SIGINT`, `SIGTERM`), instantiate the engine runner with resolved configuration, and execute the documentation run while streaming events:\n   - Status events print to stdout.\n   - Error events print to stderr.\n   - Other events print to stdout.\n\n5. **Error Propagation** — If the execution loop returns an error, wrap it with a descriptive prefix before returning to the caller.\n\n#### [STATE_AND_CONCURRENCY]\n**Global variables modified after init:** None (other than flag vars set once during package initialization, which are effectively read-only).\n\n**Shared/class-level mutable state:** `RootCmd` is initialized inline and never reassigned — no post-init mutation.\n\n**Synchronization mechanisms:** No locks, mutexes, atomic types, or goroutines observed in this file.\n\n**Async/await patterns:** None (no concurrency primitives used).\n\n---\n\n**Mutable State Summary:**\n- `modelIDFlag`, `numCtxFlag` — package-level string vars set once during `init()`; never modified after that point. Effectively immutable for program lifetime.\n- `RootCmd` — pointer to a cobra.Command initialized inline at package scope; no post-init mutation.\n\n**Verdict:** No runtime mutable state (only one-time initialization of global flags).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Disk I/O\n- `os.Getwd()` — reads the current working directory from `/proc/self/cwd` (Linux) or equivalent. Returns a filesystem path string.\n- `tools.VerifyGitRepo(repoRoot)` — calls into an external module; likely performs disk checks for `.git` existence and possibly runs `git status`. The return value is wrapped as-is (`return err`).\n- `config.ConfigExists(repoRoot)` — reads from the local filesystem to check whether a configuration file exists. Returns boolean.\n- `config.ResolveConfig(repoRoot, modelIDFlag, numCtxFlag)` — reads the configuration file (likely JSON/YAML/TOML) and environment variables. Errors are returned directly to the caller.\n\n### Network / API I/O\n- **No direct network calls** in this file. The LLM model ID (`modelIDFlag`) is accepted as a CLI flag but not used here; it flows into `config.ResolveConfig` which likely stores it for later use by `engine`.\n- No HTTP requests, no gRPC connections, no database queries originate from `root.go`.\n\n### Terminal / Stdio I/O\n- `os.Stdin.Fd()` — reads the standard input file descriptor (used only to detect terminal type).\n- `fmt.Println(ev.Message)` — writes to **stdout**. Used for all non-error engine events.\n- `fmt.Fprintf(os.Stderr, \"Error: %s\\n\", ev.Message)` — writes to **stderr** when an error event is emitted by the engine.\n\n### Signal Handling (Process-level)\n- `signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)` — registers interrupt and termination signals on a background context. The handler calls `stop()` via deferred execution. This enables graceful shutdown propagation to the engine runner.\n\n---\n\n## Error Propagation Analysis\n\n| Location | Mechanism | Notes |\n|---|---|---|\n| `tools.VerifyGitRepo` → `executeCommand` | **Return error** (wrapped with `%w`) | Caller returns the wrapped error directly. |\n| `config.ConfigExists` → `isTTY` check | **Implicit failure path**: if not TTY, returns a formatted error string — no return value from this conditional; the error is written via `fmt.Errorf`. |\n| `RunSetupFlow(repoRoot)` | **Return error** (propagated as-is) | Wrapped by the enclosing `if err := ...` block. |\n| `config.ResolveConfig(...)` | **Return error** (returned directly) | No wrapping — raw error from config package propagates up. |\n| `engine.IsInitialized(...)` | Returns bool; no error propagation here. |\n| Engine runner: `runner.Run(ctx, repoRoot, engine.Mode(mode), ...)` | **Return error** — wrapped with `\"documentation run failed: %w\"` | Errors emitted via the event callback are printed to stderr and swallowed (no further propagation). The actual error from `Run` is re-wrapped before returning. |\n| Event callback (`ev.Type == engine.EventError`) | Swallowed — only prints to stderr, returns nothing. | No exception-style crash; errors are logged and execution continues unless the underlying runner returns an error. |\n| All paths in `executeCommand` | **Return error** or **return nil** | The function always terminates via a return value — no panic/crash path visible in this file. |\n\n### Summary of Error Handling Strategy\n- Errors from external systems (`VerifyGitRepo`, `RunSetupFlow`, `ResolveConfig`) are propagated as wrapped Go errors up to the CLI entry point.\n- Engine runtime errors are captured and re-wrapped with a descriptive prefix before returning to the caller.\n- Event-emitted errors (via callback) are **not** returned — they are printed to stderr and execution continues, unless the runner itself reports an error."
    },
    "cmd/setup.go": {
      "sha256": "0d8eaf91b4a8c5e59c330e7512bbc723ce96d6c622a0c3d71f43f6dd07c1b156",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface for cmd/setup.go\n\n## Functions\n\n### RunSetupFlow\n\n**Signature:**\n```go\nfunc RunSetupFlow(repoRoot string) error\n```\n\n**Parameters:**\n- `repoRoot` (string) — the root directory of the repository where configuration is saved.\n\n**Returns:**\n- `error`\n\n---\n\n### promptStringList\n\n**Signature:**\n```go\nfunc promptStringList(reader *bufio.Reader, promptMsg string, existingList []string) ([]string, bool)\n```\n\n**Parameters:**\n- `reader` (*bufio.Reader) — reader for stdin.\n- `promptMsg` (string) — message to display before reading input.\n- `existingList` ([]string) — list of pre-existing values to show or preserve on error/empty input.\n\n**Returns:**\n- `[]string` — the resulting string slice from user input (or existing values).\n- `bool` — whether the user modified the existing values (`true`) or kept them unchanged (`false`).\n\n---\n\n### promptString\n\n**Signature:**\n```go\nfunc promptString(reader *bufio.Reader, promptMsg string, existingVal string) string\n```\n\n**Parameters:**\n- `reader` (*bufio.Reader) — reader for stdin.\n- `promptMsg` (string) — message to display before reading input.\n- `existingVal` (string) — default value shown in brackets and returned if input is empty or an error occurs.\n\n**Returns:**\n- `string` — the trimmed user input, or `existingVal` if no valid input was provided.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nInteractive CLI setup wizard that guides users through configuring a code analysis/reduction tool. It collects LLM model settings, context parameters, ignore rules, documentation paths, and system prompts, then persists them to a local YAML configuration file.\n\n## High-Level Algorithm Steps\n\n1. **Resolve repo root** — capture the current working directory as the base path for config storage\n2. **Load existing state** — attempt to read previously saved configuration; extract any non-empty values (model ID, URL, context size, ignore list, docs dir) so they can be pre-populated in prompts\n3. **Prompt: LLM Model ID** — accept user input with the previous value as default fallback\n4. **Prompt: Ollama Base URL** — same pattern; preserve prior setting if empty\n5. **Prompt: Context Size** — accept comma-separated or \"clear\"/\"none\" for an ignore list of directories, files, and patterns; validate numeric conversion on a separate prompt\n6. **Prompt: Documentation Directory** — collect the path to documentation sources\n7. **Preserve system prompts** — carry forward existing values for extraction, synthesis, architecture, and file fact consolidation prompts when present\n8. **Assemble configuration object** — merge all user inputs with defaults/previous state into a single `Config` struct\n9. **Persist configuration** — write the assembled config to disk; return any errors\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O\n\n- **File system**: `os.Getwd()` reads the current working directory at start of `RunSetupFlow`. This is a synchronous file-system read with no retry or fallback.\n- **Terminal input (stdin)**: A `bufio.NewReader(os.Stdin)` is created once and reused by `promptString` / `promptStringList`. Each prompt calls `reader.ReadString('\\n')`, which blocks until the user presses Enter.\n- **Configuration file**: `config.LoadConfig(repoRoot)` reads a config file from disk in `repoRoot`. If it fails, existing values (or zero defaults) are used.\n- **Configuration write**: `config.SaveConfig(repoRoot, newCfg)` writes the resulting config to disk in `repoRoot`. This is not wrapped with atomic semantics or retries; on failure it returns an error that bubbles up.\n\n## Error Propagation\n\n| Path | Behavior |\n|---|---|\n| `RunSetupFlow` → caller | Returns non-nil error only for: (a) `os.Getwd()` failure, (b) `config.SaveConfig` failure. All other paths return `nil`. |\n| `promptString` | Swallows `ReadString` errors entirely — logs a warning to stderr and returns the existing value. No exception propagates beyond this function. |\n| `promptStringList` | Same pattern: swallows `ReadString` errors, prints to stderr, and returns existing values with `modified=false`. The caller uses that signal to keep old input. |\n| `strconv.Atoi(ctxInputStr)` | Swallowed — if parsing fails or result ≤ 0, falls back to `existingNumCtx`. No error propagates. |\n\nErrors from user input (stdin) are **never** treated as fatal; they degrade gracefully with a stderr notice and existing-value fallback. Only filesystem errors (`Getwd`, `SaveConfig`) propagate up the call stack."
    },
    "cmd/update.go": {
      "sha256": "62e9aa11268d32732195d0dbf3d38f5f817afe9a2a276aa7a1effac06566f8ea",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: cmd/update.go\n\n**No exported or public identifiers found.** All identifiers in this file (`updateCmd`, `init`) begin with lowercase letters and are therefore unexported. The `RootCmd` reference is also not defined within this file.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\n\nThis code defines a CLI command that triggers an incremental documentation update workflow — specifically, scanning for files changed since the last documented commit and updating corresponding wiki pages.\n\n**High-Level Algorithm Steps (as described by this file's intent):**\n\n1. Parse user input into a `cmd *cobra.Command` context\n2. Forward execution to `executeCommand(\"update\")`, which is resolved outside this file\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects observable in this file. All logic is delegated to an undefined `executeCommand(\"update\")`, whose implementation is not present in the provided source text, so no disk, network, database, or API interactions can be confirmed here.\n\n**Error Propagation:** The `RunE` closure returns the error produced by `executeCommand`. This propagates through cobra's command framework to the calling CLI context. No panic or crash recovery is handled within this file."
    },
    "internal/config/config.go": {
      "sha256": "37a32106335c54cf442cac91b4468c05a2352a908e509cdf918d6c7ccc3e9993",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `internal/config` package\n\n## Constants\n\n| Constant | Value / Description |\n|---|---|\n| `CodeReducerModelIDEnvKey` | `\"CODE_REDUCER_MODEL_ID\"` — env key for model ID override |\n| `OllamaBaseURLEnvKey` | `\"OLLAMA_BASE_URL\"` — env key for Ollama URL override |\n| `OllamaNumCtxEnvKey` | `\"OLLAMA_NUM_CTX\"` — env key for context size override |\n| `OllamaDefaultBaseURL` | `\"http://localhost:11434\"` — default URL for local Ollama api |\n| `OllamaDefaultModelID` | `\"ornith:9b\"` — default LLM model |\n| `OllamaDefaultNumCtx` | `8192` — default context size |\n| `DefaultDocsDir` | `\"wiki\"` — default documentation folder name |\n| `ConfigFileName` | `\".code-reducer.yaml\"` — configuration filename |\n| `DefaultSystemPrompt` | Default system instructions for the LLM |\n| `DefaultModuleSynthesisPrompt` | Default prompt for module/folder summaries |\n| `DefaultArchitecturePrompt` | Default prompt for architecture synthesis |\n| `DefaultFileFactConsolidationPrompt` | Default prompt for fact consolidation |\n\n## Types\n\n### `ExtractionStep`\n\n```go\ntype ExtractionStep struct {\n    Name   string  // yaml:\"name\"\n    Prompt string  // yaml:\"prompt\"\n}\n```\n\n### `Config`\n\n| Field | Type | YAML Key |\n|---|---|---|\n| `ModelID` | `string` | `\"model_id\"` |\n| `OllamaBaseURL` | `string` | `\"ollama_base_url\"` |\n| `OllamaNumCtx` | `int` | `\"ollama_num_ctx\"` |\n| `DocsDir` | `string` | `\"docs_dir\"` |\n| `SystemPrompt` | `string` | `\"system_prompt\"` |\n| `ModuleSynthesisPrompt` | `string` | `\"module_synthesis_prompt\"` |\n| `ArchitecturePrompt` | `string` | `\"architecture_prompt\"` |\n| `FileFactConsolidationPrompt` | `string` | `\"file_fact_consolidation_prompt\"` |\n| `ExtractionSteps` | `[]ExtractionStep` | `\"extraction_steps\"` |\n| `Ignore` | `[]string` | `\"ignore\"` |\n\n## Variables\n\n### `DefaultExtractionSteps`\n\n```go\nvar DefaultExtractionSteps []ExtractionStep\n// Pre-populated with: API_SIGNATURES, BUSINESS_LOGIC, STATE_AND_CONCURRENCY, ERRORS_AND_SIDE_EFFECTS\n```\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nConfigures and defines the operational parameters for a **LLM-driven automated code analysis system** that extracts structured technical knowledge from source files and produces synthesized documentation.\n\n## Domain Rules (High-Level Algorithm Steps)\n\n1. **Environment \u0026 LLM Configuration** — Resolve model identity, API endpoint, and context window size from environment variables with sensible defaults (local Ollama + `ornith:9b`, 8192 ctx).\n\n2. **Prompt Injection** — Load system prompt (defensive analytical rules) plus task-specific prompts for architecture synthesis, module summary generation, and file-level fact consolidation.\n\n3. **Multi-Phase Extraction Pipeline** — Execute four sequential analysis steps per source file:\n   - `API_SIGNATURES` — enumerate public surface (classes, functions, methods, types + parameters/return types).\n   - `BUSINESS_LOGIC` — extract the domain problem and high-level algorithm steps; ignore syntax/stdlib.\n   - `STATE_AND_CONCURRENCY` — identify mutable state and synchronization mechanisms.\n   - `ERRORS_AND_SIDE_EFFECTS` — catalog external I/O interactions and error propagation patterns.\n\n4. **Configuration Persistence** — Allow per-field overrides (model, URL, context size, docs dir, all prompts) via `.code-reducer.yaml`, with a default extraction step set that is also overridable.\n\n#### [STATE_AND_CONCURRENCY]\n- **Mutable State**:\n  - `DefaultExtractionSteps` — package-level variable of type `[]ExtractionStep`, holds a static list of extraction step definitions. While initialized at declaration and not mutated within this file, it is technically mutable state accessible to the entire package.\n\n- **Synchronization Mechanisms**: None (no locks, mutexes, channels, async/await, or atomic types present).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O Analysis: `internal/config/config.go`\n\n**No external side effects.**\n\nThis file contains only constants, struct definitions, and default values. There are no function calls, imports of runtime packages, or any code paths that interact with external systems (network, disk, databases, APIs). The environment variable names (`CODE_REDUCER_MODEL_ID`, `OLLAMA_BASE_URL`, `OLLAMA_NUM_CTX`) and the default URL (`http://localhost:11434`) are static strings; they do not perform I/O themselves.\n\nThe prompt text in `DefaultExtractionSteps[\"ERRORS_AND_SIDE_EFFECTS\"]` instructs an LLM to analyze external I/O, but that analysis is performed by a downstream system—it does not occur within this file."
    },
    "internal/config/io.go": {
      "sha256": "9af0f95e0eb8f5bac5679053c283565338e4316f1f7065063a26c41df24532cf",
      "facts": "#### [API_SIGNATURES]\n# io.go — Public API Surface (internal/config)\n\n## Functions\n\n| Function | Parameters | Return Values |\n|----------|-----------|---------------|\n| `getConfigPath` | `cwd string` | `string` |\n| `ConfigExists` | `cwd string` | `bool` |\n| `LoadConfig` | `cwd string` | `(*Config, error)` |\n| `SaveConfig` | `cwd string`, `cfg *Config` | `error` |\n\n## Types\n\n| Type | Description |\n|------|-------------|\n| `Config` | *(referenced as pointer in signatures; full definition not shown in this excerpt)* |\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis module provides configuration file management for a code-reduction tool. It handles reading, writing, and validating a YAML-based config file stored in the project root directory.\n\n## Domain Rules \u0026 High-Level Steps\n\n1. **Config Path Resolution** — Derives the absolute path to the config file by combining the working directory with a fixed filename (`.code-reducer.yaml`).\n\n2. **Existence Check** — Returns true if the config file is present in the specified directory; false otherwise, using stat semantics.\n\n3. **Loading** — Reads the raw bytes from disk, unmarshals them into a `Config` struct via YAML parsing. Any parse failure is wrapped and returned as an error.\n\n4. **Saving (Atomic Write)**:\n   - Marshals the in-memory config back to YAML.\n   - Normalizes key prefixes by inserting extra blank lines before specific top-level keys (`system_prompt`, `module_synthesis_prompt`, `architecture_prompt`, `file_fact_consolidation_prompt`, `extraction_steps`, `ignore`).\n   - Writes the normalized output to a temporary file in the same directory.\n   - Sets restrictive file permissions on the temp file via chmod.\n   - Atomically renames the temp file over the original config path, replacing any existing version.\n\n5. **Error Handling** — Every step (read, parse, write, sync, close, chmod, rename) returns a descriptive error if it fails.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Disk Operations\n\nAll interactions are filesystem-based. No network, database, or API calls exist in this file.\n\n| Function | Operation | Direction | Notes |\n|---|---|---|---|\n| `ConfigExists` | `os.Stat()` on config path | Read (metadata) | Returns bool; no error propagation |\n| `LoadConfig` | `os.ReadFile()` then `yaml.Unmarshal()` | Read + parse | Errors wrapped with descriptive prefix (`failed to parse yaml config:`) |\n| `SaveConfig` | `os.CreateTemp()` → write → `Sync()` → `Close()` → `Chmod()` → `os.Rename()` | Write (atomic rename pattern) | Writes to temp file first, renames atomically. Errors wrapped with descriptive prefix (`failed to create temp file:`, etc.) |\n\n### Error Propagation Pattern\n\nThree consistent error-handling strategies are used across all functions that perform I/O:\n\n1. **Check and propagate immediately** — `if err != nil { return ... }` at every external call site\n2. **Wrap with context** — errors from third-party calls (`yaml.Unmarshal`, `yaml.Marshal`) are wrapped using `%w` format verbs, producing descriptive messages like `\"failed to parse yaml config: %w\"` and `\"failed to marshal yaml: %w\"`\n3. **Defer cleanup on failure** — `tmpFile.Close()` and `os.Remove(tmpName)` run in a defer block so temp files are cleaned up even when errors occur mid-save\n\n### Notable Observations\n\n- **`configFilePerm` is referenced but not defined locally.** It must be declared elsewhere in the module (likely a package-level constant).\n- **Double-close pattern:** `tmpFile.Close()` is called both inside the defer and explicitly after writing. Closing an already-closed file in Go returns `nil`, so this does not crash or panic, but it is redundant code.\n- **`ConfigExists` uses bool return instead of error.** This means callers cannot distinguish between \"file doesn't exist\" (expected) and other OS-level errors (e.g., permission denied, I/O failure)."
    },
    "internal/config/resolve.go": {
      "sha256": "93ec3fdd3dc601ea2a36ac86c0864cd4ef6e1c6886d6863dd2bb4750dd80c2ad",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `internal/config`\n\n## Types\n\n| Name | Kind | Description |\n|------|------|-------------|\n| `Config` | struct | Configuration container; fields resolved by `ResolveConfig`. |\n\n## Functions\n\n### `ResolveConfig(repoRoot, modelIDFlag, numCtxFlag string) (*Config, error)`\n\nMerges CLI overrides, environment variables, YAML config, and system defaults into a fully resolved `*Config`.\n\n- **Parameters**\n  - `repoRoot` — repository root path.\n  - `modelIDFlag` — explicit model ID from command-line flag.\n  - `numCtxFlag` — explicit context size from command-line flag.\n- **Returns**\n  - `(*Config, error)`\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code solves **configuration resolution** for a pipeline runner / LLM client system. It takes raw inputs from four distinct sources (file config, environment variables, command-line flags, built-in defaults) and produces one canonical `Config` object that downstream components consume without ambiguity about which value won.\n\n## High-Level Algorithm Steps\n\n1. **Load file configuration** — Read YAML config from the repo root; if absent, use an empty struct as the baseline.\n\n2. **Deduplicate ignore list** — Merge any pre-existing ignore entries (no duplicates) for downstream filtering logic.\n\n3. **Set extraction pipeline defaults** — If no extraction steps were configured in the file, substitute a built-in default set of extraction steps.\n\n4. **Resolve Model ID** — Priority chain: built-in default → YAML config → environment variable → CLI flag. Each later source overrides the previous if non-empty.\n\n5. **Resolve Ollama Base URL** — Same priority chain as above (default → file → env), no CLI override allowed for this parameter.\n\n6. **Resolve Ollama Context Size** — Priority chain: default → YAML → env → CLI flag. Env and CLI values are parsed from strings to integers with a minimum-of-1 validation gate.\n\n7. **Resolve DocsDir** — Default if unset, otherwise use the file-specified path. No env/CLI override for this parameter.\n\n8. **Resolve prompts** — For each prompt category (system, synthesis, architecture, fact consolidation): start from built-in default, then allow YAML to override if non-empty. Each prompt has its own independent resolution chain; no CLI/env override is provided in this code.\n\n9. **Return resolved config** — Assemble the final `Config` struct with all fields populated and return it alongside a nil error.\n\n#### [STATE_AND_CONCURRENCY]\n**Global/Shared Mutable State:** None identified within this file. The function creates a new local `*Config` struct on each call (`resolved := \u0026Config{...}`) and returns it. No class-level properties are mutated after initialization.\n\n**Synchronization Mechanisms:** None (no locks, mutexes, atomics, or channel-based coordination visible).\n\n**Note:** Environment variables (`os.Getenv`) and file I/O (`LoadConfig` from another package) introduce external state dependencies, but none of those states are modified by this code—only read.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n| Interaction | Type | Description |\n|---|---|---|\n| `LoadConfig(repoRoot)` | Disk (file read) | Reads a configuration file at `repoRoot`. The exact path is unknown from this file alone; the function returns `(cfg, err)`. |\n| `os.Getenv(CodeReducerModelIDEnvKey)` | OS environment variable | Reads the value of an env var named by `CodeReducerModelIDEnvKey` (constant defined elsewhere). |\n| `os.Getenv(OllamaBaseURLEnvKey)` | OS environment variable | Reads the Ollama base URL from an env var. |\n| `os.Getenv(OllamaNumCtxEnvKey)` | OS environment variable | Reads the Ollama context size from an env var, then parses it via `strconv.Atoi`. |\n\n**No network calls.** No database interactions. No API calls in this file. All external I/O is limited to disk reads and process-environment reads.\n\n## Error Propagation\n\n| Path | Behavior |\n|---|---|\n| `LoadConfig(repoRoot)` returns error + non-not-exist condition | Wrapped with `fmt.Errorf(\"failed to load configuration file: %w\", err)` and returned as the function's second return value alongside a `nil` Config. The caller receives both a nil pointer and an error. |\n| `LoadConfig(repoRoot)` returns error + `os.IsNotExist(err)` is true | Swallowed silently — treated as \"no config exists\" and replaced with an empty `\u0026Config{}`. No error returned to caller. |\n| `strconv.Atoi(envVal)` / `strconv.Atoi(numCtxFlag)` | Errors are checked (`err == nil`). If parsing fails, the value is skipped and the previously-resolved fallback (default or YAML) remains in effect. No error propagated. |\n| All other branches | Always return `(resolved, nil)`. The function never returns a non-nil error except for the LoadConfig failure path described above. |\n\n## Summary\n\n- **Single disk read**: `LoadConfig(repoRoot)` at file-not-found → use empty default; any other error → wrapped and returned.\n- **Three environment reads**: All errors are swallowed (empty string = no override). Only successful parses advance resolution.\n- **No panic, no crash path** — all external I/O is either successfully absorbed or surfaced as a return-error."
    },
    "internal/engine/cache.go": {
      "sha256": "f214e3eaec08768b3140ee69e12e1e436c4fe09a0c0b2671d4c63112f6cfef54",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `internal/engine/cache.go`\n\n### Types\n\n| Element | Kind | Parameters / Fields | Returns |\n|---|---|---|---|\n| `FileCacheEntry` | Struct | `SHA256 string`, `Facts string` | — |\n| `MetadataCache` | Struct | `Version int`, `StepsHash string`, `Files map[string]FileCacheEntry`, `Modules map[string]string` | — |\n\n### Functions\n\n| Function | Parameters | Returns |\n|---|---|---|\n| `IsInitialized(repoRoot, docsDir string)` | `repoRoot string`, `docsDir string` | `bool` |\n| `saveMetadataCache(repoRoot, docsDir string, cache *MetadataCache)` | `repoRoot string`, `docsDir string`, `cache *MetadataCache` | `error` |\n| `computeSHA256(repoRoot, virtualPath string)` | `repoRoot string`, `virtualPath string` | `(string, error)` |\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis code implements a **persistent metadata cache** for an engineering extraction engine. It preserves analysis results (which files were examined, what modules exist) between runs so the engine can resume or skip re-processing unchanged content.\n\n## Domain Rules\n\n- Cache state is versioned; incompatible versions yield a clean slate rather than corrupt data\n- File existence and readability are checked before writing cache artifacts\n- SHA256 hashes identify extraction step configurations as fingerprints for cache invalidation\n\n## High-Level Algorithm Steps\n\n1. **Hash extraction steps** – Serialize the list of extraction steps to JSON, then compute a SHA256 hash that uniquely identifies which analysis configuration is in effect\n2. **Load cached metadata** – Read the on-disk cache file; if missing or version mismatched, return an empty initialized cache structure\n3. **Persist updated state** – When new information arrives (new files processed, modules discovered), serialize and write the metadata back to the same path\n4. **Initialize status check** – A separate function reports whether any cache has been written yet by probing for the file's existence\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State and Thread Safety Analysis — `cache.go` (Module: internal/engine)\n\n### 1. Global Variables / Package-Level Shared State\n\n| Variable | Type | Modified? | Notes |\n|---|---|---|---|\n| `currentCacheVersion` | `const int = 1` | No | Compile-time constant, never modified at runtime. |\n| *(none)* | — | — | No other package-level variables exist in this file. |\n\n\u003e **Note:** `metadataFileName` is referenced but not defined in this file. If it lives in another module/package as a variable (not a const), shared access to the cache would require synchronization. However, based on typical Go conventions and usage here, it is likely a `const string`. Cannot confirm without seeing that definition.\n\n### 2. Struct-Level Properties (Potential Mutable State)\n\n| Structure | Fields | Mutability Assessment |\n|---|---|---|\n| **`FileCacheEntry`** | `SHA256`, `Facts` (`string`) | Value types; mutation only occurs when the caller explicitly reassigns a field. No internal modification path exists in this file. |\n| **`MetadataCache`** | `Version`, `StepsHash`, `Files`, `Modules` | Contains two maps (`Files`, `Modules`). These are created fresh via `make()` inside `loadMetadataCache` and returned as part of the struct value. The caller receives a *copy* of the struct, so map references point to shared backing storage—but no code in this file mutates them concurrently without synchronization. |\n\n### 3. Functions That Modify State (Locally)\n\n| Function | Effect on State |\n|---|---|\n| `loadMetadataCache` | Creates fresh maps (`make(map[string]FileCacheEntry)`, `make(map[string]string)`). These are local allocations; the returned struct carries map references, but no concurrent access is demonstrated. |\n| `saveMetadataCache` | Sets `cache.Version = currentCacheVersion`; marshals and writes to disk. Input cache is modified in place before write. |\n\n### 4. Synchronization Mechanisms\n\n| Mechanism | Present? |\n|---|---|\n| Mutex / `sync.Mutex` | No |\n| Atomic types (`sync/atomic`) | No |\n| Channels (`chan`) | No |\n| `async/await` (goroutines + wait groups) | No |\n| File I/O as implicit serialization | Yes — `saveMetadataCache` writes atomically to a single file, which provides coarse-grained mutual exclusion for the cache on disk. Not sufficient for in-memory concurrency. |\n\n### 5. Thread Safety Verdict\n\n- **In-Memory:** If `loadMetadataCache()` (or any function returning a populated `MetadataCache`) is called from multiple goroutines simultaneously and they share the resulting map references, there is **no synchronization** protecting reads/writes to `Files` or `Modules`. This is an unhandled race condition if concurrent access occurs.\n- **On-Disk:** The cache file is written in a single call (`saveMetadataCache`), so disk state transitions are effectively serialized by the OS write operation. No explicit locking is used, but no concurrent writers are demonstrated either.\n\n### Summary\n\n| Category | Finding |\n|---|---|\n| Global mutable variables | **None** (only constants) |\n| Shared maps with concurrent access paths | **Potential risk** — `Files` and `Modules` in `MetadataCache` are modified without locks, but no code in this file demonstrates concurrent callers. Stateful if reused across goroutines. |\n| Synchronization primitives | **None** present in this file. |\n| Overall thread safety | **Not synchronized.** Safe only under the assumption that access is single-threaded or sequential (e.g., one goroutine at a time calls `loadMetadataCache` followed by `saveMetadataCache`). |\n\n\u003e If you need me to add synchronization, suggest a design approach, or analyze other files in this module, let me know.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Filesystem Interactions\n\nAll external interactions are limited to **local filesystem** operations. No network, database, or API calls exist in this file.\n\n| Function | Operation | Path | Direction |\n|---|---|---|---|\n| `loadMetadataCache` | Read + parse JSON | `{docsDir}/{metadataFileName}` | In → Memory |\n| `IsInitialized` | Existence check | `{docsDir}/{metadataFileName}` | In → Bool |\n| `saveMetadataCache` | Write JSON (indented) | `{docsDir}/{metadataFileName}` | Memory → Out |\n| `computeSHA256` | Read + hash | `{repoRoot}/{virtualPath}` | In → Hash string |\n\nAll paths are constructed via `filepath.Join`. The target file for metadata cache operations is determined by `metadataFileName` (value unknown from this file alone). The SHA256 computation operates entirely in memory after reading the source file.\n\n---\n\n## Error Propagation Analysis\n\n### Pattern: Go errors (`error` interface), no panics\n\nAll functions return `(result, error)` or just `error`. No `panic`, `defer os.Exit`, or crash paths detected.\n\n### Per-function behavior:\n\n| Function | Success Path | Failure Handling |\n|---|---|---|\n| `loadMetadataCache` | Returns parsed cache + nil error if file exists and is valid JSON with matching version | If `os.ErrNotExist`: returns empty default cache + **nil error** (silent recovery). Other read errors: wrapped via `%w`. JSON parse errors: wrapped via `%w`. Version mismatch: treated as non-error, returns clean cache + nil. |\n| `IsInitialized` | Returns `true` if file readable | Returns `false` (error swallowed without propagation) |\n| `saveMetadataCache` | Writes file, returns nil error | JSON marshal errors propagated directly as-is |\n| `computeSHA256` | Returns hex hash + nil error | Read errors from `tools.ReadFileSafely` propagated as-is to caller |\n\n### Error wrapping semantics:\n\nTwo functions use `%w` wrapping (`loadMetadataCache`, `computeSHA256` for cache-related read failures). This preserves the original error chain for potential unwrapping. The other two (`saveMetadataCache`, `computeSHA256` direct I/O errors) propagate raw errors—no wrapping applied.\n\n---\n\n## Summary\n\n- **I/O scope**: Local filesystem only (reads, writes, existence checks against a single metadata cache file and arbitrary source files for hashing).\n- **Error model**: Strict Go error pattern. No panics. Silent recovery in two cases (`loadMetadataCache` missing-file returns empty cache; `IsInitialized` silently returns false on read failure). All other errors propagate to callers, some wrapped with `%w`."
    },
    "internal/engine/chunking.go": {
      "sha256": "5ad84939667740e001438e3dddba54ab179cbae877ebc4a6febf1c791a4af61d",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `internal/engine/chunking.go`\n\n**Result:** No exported (public) elements found in this file.\n\nAll functions begin with lowercase letters, making them **unexported** (private to the package):\n\n| Function | Signature |\n|---|---|\n| `reduceWithLLM` | `(ctx context.Context, c llmCaller, items []string, sysPrompt string, buildPrompt func(batch []string) string, logMsg func(batch []string) string, errMsg string, logEvent LogEventFunc) (string, error)` |\n| `reduceInChunks` | `(ctx context.Context, c llmCaller, nodePath string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)` |\n| `reduceFileFacts` | `(ctx context.Context, c llmCaller, filePath string, stepName string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)` |\n| `reduceItems` | `(ctx context.Context, items []string, maxChars int, reduceFn func(batch []string) (string, error)) (string, error)` |\n| `chunkTextWithOverlap` | `(text string, maxRunes int, overlapRunes int) []string` |\n\nNo exported types, constants, variables, or methods are present in this file.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code solves **recursive hierarchical text synthesis** — breaking large collections of textual items (code facts, file observations, architectural notes) into manageable batches, feeding them to an LLM for consolidation/synthesis, then recursively combining results until a single synthesized output remains. It handles the practical constraints of context windows (max character limits) and Unicode-safe rune counting throughout.\n\n## High-Level Algorithm Steps\n\n1. **Batch Partitioning** — Split input items into groups where each group's total character count stays within `maxChars`. A single oversized item is truncated to fit; if every item exceeds the per-item allowance, all are truncated uniformly.\n\n2. **Recursive Reduction** — If exactly one batch exists after partitioning, call the LLM reduction function on it. Otherwise, recursively reduce each sub-batch independently, then concatenate results and recurse again until a single result remains. This forms a binary tree of reductions.\n\n3. **LLM Synthesis Step** — Feed the current batch to an LLM with a role=user message containing the items joined together, plus a system prompt describing what to do (synthesize architecture for a node path, or consolidate/deduplicate file facts). Strip markdown fences from the response and return it as the reduced result.\n\n4. **Single-Item Passthrough** — When only one item exists in `reduceFileFacts`, skip synthesis entirely and return it unchanged.\n\n5. **Overlapping Chunking (auxiliary)** — A separate utility splits a single text string into overlapping rune-counted chunks at a fixed step size, useful for preparing input sequences that need positional overlap between adjacent pieces.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n- **`reduceItems`:** Modifies `items[0]` in-place via `[]rune(items[0])` followed by `items[0] = string(runes[:maxChars]) + \"\\n...[truncated]\"`. This mutates the underlying array of the caller's slice (since slices are reference types). The mutation is intentional truncation behavior for oversized single items.\n\n**Synchronization:** None. No locks, mutexes, atomic types, or async/await used. Functions use `context.Context` for cancellation but no concurrency primitives for shared state protection.\n\n**Shared State:** All functions receive parameters by value (strings) or pointer (`*config.Config`, `llmCaller`). Local closures (`buildPrompt`, `logMsg`) capture only their own parameters. No class-level properties, globals, or package-level variables are modified in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n| Function | External Interaction | Details |\n|---|---|---|\n| `reduceWithLLM` | **Network / API** (LLM) | Calls `c.CallLLM(ctx, sysPrompt, messages, false)` via the `llmCaller` interface. This is an external LLM API call over the network. |\n| `reduceInChunks` | None | Pure string formatting and delegation to `reduceWithLLM`. No disk or database access. |\n| `reduceFileFacts` | None | Pure string formatting and delegation to `reduceWithLLM`. No disk or database access. |\n| `chunkTextWithOverlap` | None | Pure in-memory rune-level text splitting. No I/O. |\n\n**Summary**: The only external side effect is the LLM API call inside `reduceWithLLM`, which is invoked by `reduceInChunks` and `reduceFileFacts`. All other logic is entirely internal (string manipulation, recursion, context checking).\n\n---\n\n## Error Propagation Analysis\n\n| Pattern | Location | Behavior |\n|---|---|---|\n| **Exception-style return** | `reduceWithLLM` → `c.CallLLM` error | Wrapped as: `fmt.Errorf(\"%s: %w\", errMsg, err)` where `errMsg` is passed in (e.g., `\"LLM error during synthesis\"`). Returns `(string, error)`. |\n| **Context cancellation** | `reduceItems`, `reduceWithLLM` | Checks `ctx.Err()` at entry and inside recursion. On cancel/error: returns `(string, err)` immediately—no panic/crash. |\n| **Recursive propagation** | `reduceItems` (recursive calls to itself) | Errors from child recursive batches propagate up via `return \"\", err`. |\n| **Silently swallowed truncation** | Single-item truncation logic (`items[0] = string(runes[:maxChars]) + \"\\n...[truncated]\"`) | No error returned. Truncation is a silent, in-place mutation with no signal to caller. |\n| **Silently swallowed fallback batching** | `len(batches) == len(items)` branch | When every item fails to fit, it forces truncation of all items and returns them as one batch—no error returned. |\n\n---\n\n## Observations\n\n1. **LLM errors are wrapped but not classified**: The error is re-wrapped with a prefix string (`errMsg`), losing the original error type information for potential classification (e.g., timeout vs. rate limit vs. auth failure).\n2. **Context cancellation propagates cleanly**: `ctx.Err()` checks exist at recursion boundaries and return context errors directly—no panic, no ignored cancelation.\n3. **Silent truncations could mask data loss**: The single-item truncation (lines 60-64) and the fallback truncation loop both silently modify input without returning an error. Callers have no way to know truncation occurred unless they inspect the output string.\n4. **No panic or crash paths**: All errors are returned as values; no `panic` calls exist in this file."
    },
    "internal/engine/client.go": {
      "sha256": "998aa6b162f007326e91cb271e6fc0cc59413378394cd887f85b8a4a832487cb",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `internal/engine/client.go`\n\n## Types\n\n| Name | Kind | Fields / Members |\n|------|------|------------------|\n| `Message` | struct | `Role string \\`json:\"role\"\\``\u003cbr\u003e`Content string \\`json:\"content\"\\`` |\n| `ollamaRequest` | struct (unexported) | — not public |\n| `ollamaOptions` | struct (unexported) | — not public |\n| `ollamaResponse` | struct (unexported) | — not public |\n\n## Functions\n\n| Name | Parameters | Return Values |\n|------|------------|---------------|\n| `newLLMClient(modelID, baseURL string, numCtx int)` | `modelID string`, `baseURL string`, `numCtx int` | `*llmClient` |\n| `(c *llmClient).NumCtx()` | `c *llmClient` (receiver) | `int` |\n| `(c *llmClient).prepareOllamaRequest(ctx context.Context, systemPrompt string, messages []Message, jsonFormat bool)` | `ctx context.Context`, `systemPrompt string`, `messages []Message`, `jsonFormat bool` | `*http.Request`, `error` |\n| `(c *llmClient).CallLLM(ctx context.Context, systemPrompt string, messages []Message, jsonFormat bool)` | `ctx context.Context`, `systemPrompt string`, `messages []Message`, `jsonFormat bool` | `string`, `error` |\n\n## Interfaces\n\n| Name | Method Signature |\n|------|------------------|\n| `llmCaller` (unexported) | `CallLLM(ctx context.Context, systemPrompt string, messages []Message, jsonFormat bool) (string, error)`\u003cbr\u003e`NumCtx() int` |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nThe file implements a client for interacting with an external LLM inference service (Ollama) via HTTP, handling the serialization of chat requests, transmission over the network, and parsing of textual responses.\n\n**High-Level Algorithm Steps:**\n\n1.  Define a message schema containing role and content fields to represent conversational turns.\n2.  Construct the request payload by prepending the system prompt as a new entry to the input messages array, then serializing the model identifier, conversation context, and processing options into JSON format.\n3.  Execute an HTTP POST request against the configured API path with the serialized body.\n4.  If the server returns a success status (200), deserialize the response to extract the generated text content and return it.\n5.  If the server returns any other status, read a bounded portion of the error response body and return a formatted error message including the HTTP status code.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** None. All struct fields (`modelID`, `baseURL`, `numCtx`, `httpClient`) are initialized once in the constructor and never reassigned by any public or private method.\n\n**Synchronization Mechanisms:** None required — there is no shared mutable state to protect.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Network Interactions\n\n| Interaction | Mechanism | Endpoint / Target |\n|---|---|---|\n| **Outbound HTTP POST** | `http.NewRequestWithContext` + `c.httpClient.Do` | `{baseURL}/api/chat` (Ollama-compatible `/api/chat` endpoint) |\n| **HTTP Client Timeout** | Set via `\u0026http.Client{Timeout: defaultHTTPTimeout}` | N/A — timeout is applied to the client itself, not a specific call |\n\n### I/O Paths\n\n1. **Request construction**: JSON body marshaled into memory (`bytes.NewReader`), then sent as HTTP POST with `Content-Type: application/json`.\n2. **Response reading (OK path)**: `io.ReadAll(resp.Body)` — reads the entire response stream until EOF or client timeout.\n3. **Response reading (non-OK path)**: `io.ReadAll(io.LimitReader(resp.Body, maxErrorBodyBytes))` — reads up to a bounded size; the limit constant is referenced but not defined in this file.\n\n### Error Propagation Summary\n\n| Location | Failure Mode | Handling |\n|---|---|---|\n| `prepareOllamaRequest` → JSON marshal failure | Wrapped with `%w`, returned as `(\"\", err)` to caller | **Propagated** |\n| `prepareOllamaRequest` → `http.NewRequestWithContext` failure | Returned as-is (no wrapping) | **Propagated** |\n| `CallLLM` → request preparation error | Returned directly from `prepareOllamaRequest` | **Propagated** |\n| `CallLLM` → HTTP Do failure | Returned directly, no retry logic | **Propagated — fails fast per comment** |\n| `CallLLM` → response body read (OK) failure | Wrapped with `%w`, returned as `(\"\", err)` to caller | **Propagated** |\n| `CallLLM` → JSON unmarshal failure | Wrapped with `%w`, returned as `(\"\", err)` to caller | **Propagated** |\n| `CallLLM` → non-200 status code | Body read (with limit), then formatted error string returned | **Partial — see below** |\n\n### Error Swallowing Detected\n\nIn the non-OK branch of `CallLLM`:\n\n```go\nbody, _ := io.ReadAll(io.LimitReader(resp.Body, maxErrorBodyBytes))\nreturn \"\", fmt.Errorf(\"ollama api error: status %d, response: %s\", resp.StatusCode, string(body))\n```\n\nThe error return value from `io.ReadAll` is discarded (`_`). If the body read itself fails (e.g., connection reset by peer, unexpected EOF), that failure is **silently swallowed**. The caller only sees a formatted error containing the HTTP status code and whatever bytes were successfully read before the failure.\n\n### Unhandled / Undefined References\n\n- `defaultHTTPTimeout` — referenced but not defined in this file; resolved at link time or via another package constant.\n- `maxErrorBodyBytes` — referenced but not defined in this file; if it is zero, `io.LimitReader(resp.Body, 0)` will block indefinitely until EOF rather than applying a size limit."
    },
    "internal/engine/constants.go": {
      "sha256": "85ec25e43f84ebf8a1ce483e8efd9df8eb8fcf20fa2e8bbf3e243022a0730ae7",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `engine/internal/engine/constants.go`\n\n## Types (Function Types)\n\n#### `LogEventFunc`\n\n```go\ntype LogEventFunc func(EventType, string)\n```\n\n**Description:** A function type that takes an `EventType` and a `string`, returning no value.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis code defines the **runtime configuration contract** for an AI/LLM inference engine. It specifies the numerical boundaries (timeouts, memory budgets, processing thresholds) and file-naming conventions that govern agent execution and context-window management throughout the system's lifecycle.\n\n## Domain Rules \u0026 High-Level Algorithm Steps\n\n1. **Network Operation Boundaries** — HTTP calls are allowed up to 10 minutes before being aborted; error responses beyond 1 KB are truncated.\n\n2. **Chunked Text Processing** — Input text is split into overlapping segments of ~800 characters, enabling streaming or incremental inference across large documents without loading them entirely at once.\n\n3. **Context Window Budgeting** — When allocating memory for the model's context window, only 75% of the total budget is reserved; a minimum floor of ~512 tokens is enforced regardless of available space.\n\n4. **Output Scaling Rule** — The maximum generated output is capped at roughly 3× the input length (or some other reference measurement), preventing runaway generation loops.\n\n5. **Persistent State \u0026 Agent Configuration** — The engine writes a `.metadata.json` snapshot and reads agent behavior from `AGENTS.md`, establishing a file-based state/identity layer for long-running processes.\n\n6. **Directory Permissions** — Any directories the engine creates are assigned 0755 permissions by default, controlling access at the filesystem boundary.\n\n7. **Event Logging Contract** — A function type (`LogEventFunc`) is defined to hook external observers into system events (type + message), enabling traceability without hardcoding logging behavior.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects. This file contains only constant declarations and a type alias (`LogEventFunc`). No functions perform disk, network, database, or API operations. The constants themselves (e.g., `defaultHTTPTimeout`, `metadataFileName`) reference external systems but do not interact with them here.\n\n**Error Propagation:** No error propagation mechanisms visible. There are no function signatures to return errors, no panic/recover patterns, and no exception handling logic in this file."
    },
    "internal/engine/json_parser.go": {
      "sha256": "81bea4827161eb8978cd6f288e2276c2616da7832e8687400b6872d8d9c3749c",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface for `internal/engine/json_parser.go`\n\n**Result:** No public (exported) elements found in this file.\n\nAll declared identifiers begin with a lowercase letter, making them package-private:\n\n| Element | Kind | Exported? |\n|---------|------|-----------|\n| `stripOuterMarkdownFence(content string) string` | Function | ❌ Private |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nThis code handles the extraction of raw content from strings wrapped in markdown or JSON code fences (backtick-delimited blocks). It isolates the interior text while discarding the surrounding fence syntax.\n\n**High-Level Algorithm Steps:**\n1.  Remove leading and trailing whitespace from the input string.\n2.  Test if the trimmed string matches a regular expression pattern defined for backtick fences (3+ backticks, optional language identifier like `markdown` or `json`).\n3.  If the pattern matches, extract and return the interior content captured between the opening and closing fences.\n4.  If the pattern does not match, return the trimmed input string unchanged.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** This file contains no interactions with:\n- **Network/APIs**: No HTTP calls, RPC, or network communication.\n- **Filesystem/Disk**: No `os.Open`, `io.Read`, filesystem paths, or file operations.\n- **Databases**: No DB drivers, connection pools, or query execution.\n\nThe only external dependency is the Go standard library packages `regexp` and `strings`, used purely for in-memory string processing (regex matching against a provided input buffer). The regex pattern operates on an already-loaded `content` string passed as a parameter — no disk or network source is involved.\n\n## Error Propagation Analysis\n\n**Errors are silently swallowed.** The function signature returns only `string`, never `error`. Consequences:\n\n1. **Regex mismatch**: If `markdownFenceRe.FindStringSubmatch(trimmed)` returns zero matches, the function returns `trimmed` as-is (the original input with whitespace stripped). No error is signaled to the caller.\n2. **Regex panic risk**: If a malformed regex were supplied at compile time, it would panic during package initialization — not handled here.\n3. **No recovery mechanism**: There is no `recover()` block or custom error type in this file.\n\n**Summary**: This function performs best-effort fence stripping with no contract on success/failure. Callers receive whatever string is returned with zero indication of whether the input was valid, partially stripped, or unchanged."
    },
    "internal/engine/orchestrator.go": {
      "sha256": "0a747df1552aa7a6fb8f024c5ef1cc52284551ef0aeef08d9e4d9bb83b08c7a8",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/engine/orchestrator.go`\n\n## Types\n\n| Name | Kind | Fields / Members |\n|------|------|-------------------|\n| `EventType` | type (`string`) | — |\n| `EventStatus` | constant (`EventType`) | — |\n| `EventError` | constant (`EventType`) | — |\n| `Event` | struct | `Type EventType`, `Message string` |\n\n## Methods on `*orchestrator`\n\n| Method | Parameters | Returns |\n|--------|-----------|---------|\n| `GenerateStandardDocs(ctx, repoRoot, docsDir, rootSum, cfg, logEvent)` | `ctx context.Context`, `repoRoot string`, `docsDir string`, `rootSum string`, `cfg *config.Config`, `logEvent LogEventFunc` | `error` |\n| `RunInit(ctx, repoRoot, cfg, onEvent)` | `ctx context.Context`, `repoRoot string`, `cfg *config.Config`, `onEvent func(Event)` | `error` |\n| `RunUpdate(ctx, repoRoot, cfg, onEvent)` | `ctx context.Context`, `repoRoot string`, `cfg *config.Config`, `onEvent func(Event)` | `error` |\n\n## Functions (unexported — private to package)\n\n| Function | Parameters | Returns |\n|----------|-----------|---------|\n| `setupPipeline(repoRoot, cfg, logEvent)` | `repoRoot string`, `cfg *config.Config`, `logEvent LogEventFunc` | `(string, []string, *MetadataCache)` |\n| `teardownPipeline(repoRoot, docsDir, cache, logEvent, successMsg)` | `repoRoot string`, `docsDir string`, `cache *MetadataCache`, `logEvent LogEventFunc`, `successMsg string` | — (void) |\n| `makeLogEvent(onEvent)` | `onEvent func(Event)` | `LogEventFunc` *(inferred from usage)* |\n\n## Unresolved / Assumed External References\n\nThe following identifiers are used in signatures or code but not defined within this file. Their origin is unknown:\n\n- **`LogEventFunc`** — referenced as a parameter type (`GenerateStandardDocs`, `setupPipeline`, `teardownPipeline`) and returned by `makeLogEvent`. No local declaration found.\n- **`FileChange`** — used in `RunUpdate` (e.g., `filteredChanges []FileChange`). Type definition not present in this file.\n- **`pipelineContext`** — referenced as a struct value (`\u0026pipelineContext{...}`). Definition not present in this file.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis code implements an incremental documentation generation system for software repositories. It automatically produces architecture overviews, quickstart guides, and per-module API summaries by analyzing source code through a hierarchical synthesis pipeline. The system distinguishes between initial full-generation (`RunInit`) and targeted updates (`RunUpdate`), where only changed files trigger local re-synthesis while preserving existing documentation elsewhere.\n\n## High-Level Algorithm Steps\n\n### Common Setup (Both Paths)\n1. Load `.gitignore`, user-configured ignore paths, and a metadata cache from disk to establish what should be excluded from analysis.\n2. Discover code files in the repository respecting those ignores.\n3. Compute SHA256 hashes for each discovered file to enable change detection.\n\n### RunInit (Full Generation)\n4. Build a hierarchical directory tree from all discovered code files.\n5. Mark every directory as \"affected\" since this is a fresh generation.\n6. Traverse the tree bottom-up, synthesizing summaries node-by-node via LLM calls—each call consumes the accumulated context of parent summaries plus its own source code.\n7. Use the root-level synthesis result to generate global `architecture.md` and `quickstart.md`.\n8. Ensure an `AI Agent Guidelines` file exists at the repo root, either created fresh or appended to if it already contains prior content.\n\n### RunUpdate (Incremental)\n4. Detect file changes by comparing current hashes against cached ones—classifying each as Added, Modified, or Deleted.\n5. Build a tree only from currently existing files; prune cache entries for deleted files and remove module documentation for directories that no longer exist.\n6. Determine which directories are affected based on the change set and propagate the \"affected\" status through the directory hierarchy.\n7. If nothing changed (zero affected directories), return immediately with no regeneration.\n8. Otherwise, re-run the hierarchical synthesis only within affected modules; if root-level docs don't exist or the top directory is affected, regenerate `architecture.md` and `quickstart.md` from the new root summary.\n\n### Synthesis Engine\n- The tree-traversal synthesizer accumulates context upward: each node's summary becomes part of its parent's input. This means leaf-node summaries (individual files) flow up through their directories, then into module-level summaries, eventually converging at the repository root.\n- LLM calls receive a system prompt combining configuration prompts with the accumulated child summaries as user content.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n| Identifier | Location | Mutation Details |\n|---|---|---|\n| `orchestrator.client` (type `llmCaller`) | Struct field of `orchestrator`, accessed via `o.client.CallLLM(...)` in `GenerateStandardDocs`, `RunInit`, and `RunUpdate` | Field is set on the struct; no synchronization wraps its use. If multiple goroutines invoke these methods concurrently, they share this state without protection. |\n| `MetadataCache.Files` (type `map[string]FileCacheEntry`) | Pointer field of `MetadataCache`; reassigned in `RunUpdate` via `cache.Files = make(map[string]FileCacheEntry)` and modified through `delete(cache.Files, f)` inside `determineAffected`/tree-walk logic | Map is cleared/rebuilt when extraction steps change. Access pattern depends on whether the same cache pointer is used across goroutines. |\n| `MetadataCache.Modules` (type `map[string]string`) | Pointer field of `MetadataCache`; reassigned in `RunUpdate` via `cache.Modules = make(map[string]string)` and deleted inside tree-walk logic | Map is cleared/rebuilt when extraction steps change; entries are removed during the update pipeline. |\n| `pipelineContext.affectedDirs`, `pipelineContext.precalculatedHashes` | Locally allocated in each method call (`RunInit`, `RunUpdate`) | These maps are populated and mutated within single goroutine scopes. No concurrent access detected from this file alone, but they reference the shared `cache` pointer. |\n\n**Synchronization Mechanisms:**\n\n- **None detected.** The file contains no `sync.Mutex`, `sync.RWMutex`, channels, atomic types (`atomic.Int32/Int64`, etc.), or any other concurrency primitives. There is no async/await construct (Go does not use this terminology).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis for `orchestrator.go`\n\n### 1. LLM API Calls (`o.client.CallLLM`)\n\nCalled in:\n- `GenerateStandardDocs()` — two calls (architecture + quickstart)\n- `RunInit()` → `synthesizeNode()` → recursive `synthesizeModule()` / `synthesizeFile()` paths (via `pipelineContext.client`)\n- `RunUpdate()` → `synthesizeNode()` (same recursion path)\n\n**Error propagation**: Errors from `CallLLM` are wrapped with context (`fmt.Errorf(\"failed to generate ...: %w\", err)`). They propagate as returned errors up the call stack — no swallowing.\n\n---\n\n### 2. File System Writes (`tools.WriteFileSafely`)\n\nCalled in:\n- `GenerateStandardDocs()` — writes `architecture.md` and `quickstart.md`\n- `RunInit()` — writes/updates `.agent-guidelines.md`; if file doesn't exist, creates it; if exists without \"AI Agent Guidelines\", appends to it. If write fails after creation attempt, wraps error with context.\n- `teardownPipeline()` (called from both pipelines) — saves metadata cache\n\n**Error propagation**: Write errors are wrapped and returned as propagated errors in the main paths. In `RunInit()`, a failed write of `.agent-guidelines.md` is wrapped and returned. In `teardownPipeline()`, a failed cache save logs a warning event but does **not** return an error — it is silently swallowed.\n\n---\n\n### 3. File System Reads (`tools.ReadFileSafely`)\n\nCalled in:\n- `RunInit()` — reads `.agent-guidelines.md` to check if \"AI Agent Guidelines\" header exists before deciding append vs replace. If read fails, falls through to write path.\n\n**Error propagation**: Read errors are swallowed; the code proceeds as if the file is empty/missing and writes a fresh version. Not propagated as an error.\n\n---\n\n### 4. Gitignore Loading (`tools.LoadGitignore`)\n\nCalled in:\n- `setupPipeline()` — loads `.gitignore` from `repoRoot`. Error logged as warning event, ignored for return value.\n\n**Error propagation**: Swallowed. Only a status log event is emitted; function returns normally regardless of failure.\n\n---\n\n### 5. File Discovery (`tools.DiscoverCodeFiles`)\n\nCalled in:\n- `RunInit()` and `RunUpdate()` (both via `setupPipeline` → direct call)\n\n**Error propagation**: Errors are propagated directly as returned errors from the orchestrator function. Not swallowed.\n\n---\n\n### 6. SHA256 Hash Computation (`computeSHA256`)\n\nCalled in:\n- `RunInit()` — iterates all discovered code files, computes hash per file. If hash computation fails for a file, logs warning and skips that file (does not include it in `precalculatedHashes`).\n- `RunUpdate()` — same pattern; failed hashes cause the file to be skipped from the current map, effectively treating it as missing/unknown.\n\n**Error propagation**: Per-file errors are logged and the file is excluded from subsequent processing. No error returned or propagated for individual files. This could mean a corrupted/unreadable file silently disappears from the pipeline view.\n\n---\n\n### 7. Directory Creation (`os.MkdirAll`)\n\nCalled in:\n- `RunInit()` — creates `\u003crepoRoot\u003e/\u003cdocsDir\u003e/modules/` with default permissions.\n\n**Error propagation**: Errors are wrapped and returned as propagated errors. Not swallowed.\n\n---\n\n### 8. Path Resolution / File Existence Checks (`security.SafeResolve`)\n\nCalled in:\n- `RunUpdate()` — checks existence of `architecture.md` and `quickstart.md` before deciding whether to regenerate global docs. If resolution fails, treated as non-existent (false).\n\n**Error propagation**: Errors from `SafeResolve` are ignored; the code treats a failed resolution as \"file does not exist\". Swallowed implicitly via control flow.\n\n---\n\n### 9. File Deletion (`os.Remove`)\n\nCalled in:\n- `teardownPipeline()` — deletes module files that were previously cached but no longer active. Only called after `security.SafeResolve` succeeds (resolved to absolute path).\n\n**Error propagation**: Errors from `os.Remove` are swallowed. Not logged, not returned. If deletion fails silently, the stale file remains on disk.\n\n---\n\n### 10. No External Side Effects\n\n- No database interactions\n- No network calls beyond LLM API\n- No environment variable reads/writes detected\n- No stdin/stdout I/O (only via callback function `onEvent`)\n\n---\n\n## Error Propagation Summary Table\n\n| Operation | Failure Handling |\n|---|---|\n| LLM call (`CallLLM`) | Wrapped + propagated as returned error |\n| Write file (`WriteFileSafely`) | Wrapped + propagated in main path; swallowed in teardown |\n| Read file (`ReadFileSafely`) | Swallowed — falls through to write path |\n| Load gitignore (`LoadGitignore`) | Logged warning, swallowed |\n| Discover files (`DiscoverCodeFiles`) | Propagated as returned error |\n| SHA256 hash (`computeSHA256`) | Per-file: logged + skipped from processing |\n| MkdirAll (`os.MkdirAll`) | Wrapped + propagated |\n| SafeResolve (`security.SafeResolve`) | Errors treated as \"not found\", swallowed |\n| os.Remove | Swallowed silently |"
    },
    "internal/engine/runner.go": {
      "sha256": "e833cb64ae16b96a2b0ba3ce4fb6d4f9010c49809a5685ba283b6a05b34464ee",
      "facts": "#### [API_SIGNATURES]\n# Public API: `internal/engine/runner.go`\n\n## Types\n\n```go\n// Mode represents the operation mode of the documentation pipeline.\ntype Mode string\n```\n\n| Element | Description |\n|---------|-------------|\n| `ModeInit` (const) | Initializes project documentation. |\n| `ModeUpdate` (const) | Updates existing project documentation incrementally. |\n| `Runner` (struct) | Orchestrates the execution of the documentation pipeline. |\n\n## Methods\n\n```go\n// NewRunner creates a new Runner instance with the given configuration.\nfunc NewRunner(cfg *config.Config) *Runner\n```\n\n```go\n// Run executes the documentation pipeline for the specified mode.\nfunc (r *Runner) Run(ctx context.Context, repoRoot string, mode Mode, onEvent func(Event)) error\n```\n\n## Summary Table\n\n| Export | Type | Description |\n|--------|------|-------------|\n| `Mode` | type | Operation mode of the documentation pipeline |\n| `ModeInit` | const | Init mode constant |\n| `ModeUpdate` | const | Update mode constant |\n| `Runner` | struct | Pipeline orchestrator |\n| `NewRunner(cfg)` | func | Creates a new Runner instance |\n| `(r *Runner) Run(ctx, repoRoot, mode, onEvent)` | method | Executes the documentation pipeline |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code orchestrates a documentation generation pipeline for software repositories, supporting both initial project setup and incremental updates. It manages concurrency by acquiring an exclusive file lock before executing pipeline steps and uses a configured Large Language Model client to drive the orchestration logic.\n\n**High-Level Algorithm Steps**\n1.  **Initialize Runner**: Create a runner instance holding the configuration (model ID, base URL, context size).\n2.  **Gitignore Check**: Attempt to add the lockfile entry to `.gitignore`; log a warning if this step fails but do not halt execution.\n3.  **Acquire Lock**: Obtain an exclusive repository lock; return a fatal error immediately if acquisition fails.\n4.  **Defer Unlock**: Schedule the release of the repository lock for after the pipeline completes (or errors).\n5.  **Instantiate Engine**: Construct the LLM client and orchestrator based on the stored configuration values.\n6.  **Dispatch Mode**: Execute either `RunInit` or `RunUpdate` depending on whether the provided mode string matches \"init\" or \"update\"; return an error for any unrecognized mode.\n\n#### [STATE_AND_CONCURRENCY]\n**Global Variables:** None\n\n**Class/Struct-Level Properties Modified:** None. The `Runner` struct holds an immutable reference (`cfg *config.Config`) set once via `NewRunner`. No field is reassigned within this file after construction.\n\n**Shared State / Synchronization:** A local lock variable acquired via `security.AcquireLock(repoRoot)` and deferred for release. This is a per-call synchronization primitive, not shared state. Whether the underlying lock mechanism provides atomic guarantees cannot be determined from this file alone.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O and Error Propagation Analysis: `runner.go`\n\n---\n\n**1. Disk / Filesystem Interactions**\n\n| Operation | Function | Description |\n|-----------|----------|-------------|\n| Write to `.gitignore` | `security.EnsureGitignoreHasLockfile(repoRoot)` | Modifies the repository root's `.gitignore` file to include a lockfile entry. Error is swallowed — only logged via callback. |\n| Create/use lock file | `security.AcquireLock(repoRoot)` | Likely creates or acquires an OS-level lock file (e.g., `.lock`) in the repo root for concurrent access protection. |\n\n---\n\n**2. Network / API Interactions**\n\nNo direct network I/O occurs at this code level. However, the LLM client is instantiated:\n\n```go\nclient := newLLMClient(r.cfg.ModelID, r.cfg.OllamaBaseURL, r.cfg.OllamaNumCtx)\n```\n\nThis creates a client that will subsequently call the Ollama API (HTTP requests to `r.cfg.OllamaBaseURL`), but those calls are deferred to later pipeline stages (`orch.RunInit` / `orch.RunUpdate`). No network I/O is observed in this file.\n\n---\n\n**3. Error Propagation**\n\n| Path | Behavior |\n|------|----------|\n| `EnsureGitignoreHasLockfile` error | **Swallowed.** Logged via `logEvent(EventStatus, ...)` callback but not returned to the caller. The pipeline continues regardless of failure. |\n| `AcquireLock` error | **Propagated with wrapping.** Returned as: `fmt.Errorf(\"failed to acquire repository lock: %w\", err)`. Caller receives a wrapped error containing the original cause. |\n| Lock release via `defer lock.Unlock()` | Always executes on return (success or failure), assuming no panic between acquisition and return. No explicit error handling for unlock failures is visible. |\n| `RunInit` / `RunUpdate` errors | **Propagated directly.** Errors are returned as-is to the caller of `Run`. |\n| Unknown mode | Returns a typed error: `fmt.Errorf(\"unsupported mode: %s\", mode)`. No wrapping; caller receives exactly this message. |\n\n---\n\n**Summary:** The function performs two disk operations (gitignore modification, lock acquisition). Error propagation is selective — the gitignore step's failure is silently logged via callback, while lock and pipeline errors are returned to callers with varying degrees of wrapping."
    },
    "internal/engine/synthesize.go": {
      "sha256": "5940958793755d9fc83d3d9643536f9a4ef121346147f41cdbd1294f2b771ff7",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface for `internal/engine/synthesize.go`\n\n**No public (exported) elements found.** All types and functions in this file are unexported (lowercase identifiers), scoped entirely within the `engine` package:\n\n| Element | Kind | Exported? |\n|---|---|---|\n| `pipelineContext` | struct | No |\n| `synthesizeNode` | function | No |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nThis code implements a recursive, context-aware synthesis pipeline for generating structured documentation summaries over a directory tree of source files. It walks a hierarchical `DirNode` structure containing both files and subdirectories, extracting meaningful \"facts\" from each file via LLM calls, then consolidating everything into per-directory module summaries that are cached and persisted to disk.\n\n**High-Level Algorithm Steps:**\n\n1. **Directory recursion** — Process children before files (post-order traversal). For each child directory, recursively call `synthesizeNode` first; if the child is not affected by changes and has a cached summary, reuse it directly.\n\n2. **Per-file processing loop**:\n   - Compute or retrieve a SHA256 hash for the file (from precalculated hashes map, else read-and-hash).\n   - On cache hit (hash matches stored entry), return cached facts without reading the file.\n   - On cache miss, read the file content. If multiple chunks are needed (based on dynamic context window size × 4 chars/token × allocation ratio), split with overlap.\n   - For each chunk, run LLM extraction across all configured `ExtractionSteps`, building a consolidated fact per step via `reduceFileFacts`.\n\n3. **Component assembly** — Build a list of components: file facts (prefixed by filename) followed by child directory summaries (prefixed by subsystem name). If no components exist, cache an empty string and return early.\n\n4. **Final reduction** — Combine all assembled components into a single consolidated summary for the current directory using `reduceInChunks`. Cache this final result under `p.cache.Modules[node.Path]` and write it to disk at `\u003cdocsDir\u003e/modules/\u003csafe-filename-of-path\u003e`.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State Analysis for `synthesize.go` (Module: internal/engine)\n\n### Shared Mutable State Modified in this Function\n\n| Variable/Field | Type | Accessed/Mutated? | Notes |\n|----------------|------|--------------------|-------|\n| `p.cache.Files[f]` | `map[string]FileCacheEntry` | **Written** | Updated with synthesized facts and hash after extraction. Race condition risk if multiple goroutines share the same cache. |\n| `p.cache.Modules[node.Path]` | `map[string]string` | **Read then Written** | Pre-check reads cached value; final write stores synthesized summary. Same race condition concern as above. |\n| `node.Children` / `node.Files` (via iteration) | Part of passed-in structs | **Read-only in this function** | Iterated over but not written to here. If other goroutines concurrently add/remove children, results may be inconsistent with no protection. |\n\n### Synchronization Mechanisms\n\n- **No explicit locks/mutexes visible.** No `sync.Mutex`, `sync.RWMutex`, channels used for signaling, or atomic types in this file.\n- **Context (`p.ctx`)** is checked at boundaries to detect cancellation, but it does not prevent concurrent map writes to the cache.\n\n### Thread Safety Assessment\n\n**Potential race condition**: If multiple goroutines invoke `synthesizeNode` with different `pipelineContext` instances that share the same underlying `*MetadataCache`, both `cache.Files` and `cache.Modules` maps will be concurrently written without any locking visible in this file. This is a data race unless external synchronization exists outside this module boundary.\n\n### Package-Level Constants (Read-Only from this File)\n\n| Constant | Usage | Mutated? |\n|----------|-------|----------|\n| `defaultChunkOverlap` | Used for chunking logic | No — read-only here |\n| `minNumCtxFloor` | Floor value for context size | No — read-only here |\n| `contextWindowAllocRatio` | Ratio multiplier | No — read-only here |\n\n### Conclusion\n\n**Mutable state present.** The cache maps (`Files`, `Modules`) are written during synthesis without visible synchronization within this file. If shared across goroutines, a data race exists unless external locking is provided by callers or other modules not shown here.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O and Error Propagation Analysis\n\n### External System Interactions\n\n**1. Disk Read (File Content)**  \n`tools.ReadFileSafely(p.repoRoot, f)` — reads individual source files from the repository root when cache misses occur. This function is called in two places within `synthesizeNode`:\n- First path: initial cache miss branch.\n- Second path: if `facts == \"\" \u0026\u0026 contentBytes == nil`, re-reads the file.\n\n**2. Disk Write (Module Documentation)**  \n`tools.WriteFileSafely(p.repoRoot, modulePath, []byte(finalSum))` — writes synthesized documentation to disk at `\u003crepoRoot\u003e/\u003ccfg.DocsDir\u003e/modules/\u003csafe-filename\u003e`. This is a write side effect that persists the final summary for each synthesized directory.\n\n**3. Network/API (LLM Calls)**  \n`p.client.CallLLM(p.ctx, systemPrompt, []Message{...}, false)` — makes LLM API calls for every extraction step and chunk combination during file synthesis. Each call processes one chunk of a file with the system prompt + user content containing the code chunk. Multiple sequential calls are made per file (one per `cfg.ExtractionSteps`), and multiple files may be processed before moving to child summary reduction.\n\n### Error Propagation\n\n**1. Context Cancellation**  \nChecked at function entry (`p.ctx.Err()`) and again inside the file loop. If the context is cancelled, returns `\"\", err` immediately — no further processing.\n\n**2. File Read Failure**  \nErrors from `tools.ReadFileSafely` are logged as warnings via `p.logEvent(EventStatus, ...)` but **do not propagate**. The function continues to the next file (`continue`). Facts for that file remain empty string.\n\n**3. LLM Call Failure**  \nWrapped with context using `%w`. Returns `\"\", fmt.Errorf(\"LLM error extracting %s for %s: %w\", step.Name, f, err)`. This propagates up through parent synthesis calls and ultimately to the caller of `synthesizeNode`.\n\n**4. Child Synthesis Error**  \nErrors from recursive `synthesizeNode` calls propagate immediately via `return \"\", err` — no attempt to continue processing siblings or accumulate partial results.\n\n**5. Module Write Failure**  \nWrapped with context: `\"failed to write module documentation for %s: %w\"`. Propagates up through the synthesis stack.\n\n**6. Reduction Errors**  \nErrors from `reduceFileFacts` and `reduceInChunks` propagate directly via `return \"\", err` — no recovery or partial result accumulation."
    },
    "internal/engine/tree.go": {
      "sha256": "21a031a938380004c36613d4e9b002c48fe049dbcff60bd8b9605a7143ecd4c5",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `internal/engine/tree.go`\n\n## Types\n\n### `FileChange` (struct)\n\n| Field | Type | Description |\n|-------|------|-------------|\n| `Path` | `string` | File path |\n| `Status` | `string` | Change status: `\"Added\"`, `\"Modified\"`, `\"Deleted\"` |\n\n---\n\n### `DirNode` (struct)\n\n| Field | Type | Description |\n|-------|------|-------------|\n| `Path` | `string` | Directory path |\n| `Files` | `[]string` | List of file paths in this directory |\n| `Children` | `map[string]*DirNode` | Child directory nodes keyed by name |\n\n---\n\n\u003e **Note:** No exported functions or methods are present. All internal functions (`propagateAffected`, `determineAffected`, `buildTree`) use lowercase identifiers and are unexported (package-private).\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis code solves the problem of **determining affected directories in a document/module file tree** given a set of file changes (additions, modifications, deletions). It builds a hierarchical directory structure from file paths, then propagates \"affected\" status upward so that if any child is affected, its parent becomes affected too.\n\n## High-Level Algorithm Steps\n\n1. **Build a directory tree** from raw file paths — walk each path's directory components to create nested `DirNode` objects with files listed under their parent directories.\n\n2. **Initialize the affected set** by marking directories whose direct children contain changed files, and marking parent directories of deleted files (since deletion affects the containing folder).\n\n3. **Walk the tree recursively** — for each node:\n   - Check if any file in its `Files` list matches a changed file; if so, mark this directory as affected.\n   - Resolve an expected \"module\" path derived from the directory name using safe filename conversion and secure resolution; if that resolved path does not exist on disk, mark the directory as affected (indicating a missing module).\n   - Check an external metadata cache (`cache.Modules`); if no cached value is recorded for this directory's path, mark it as affected.\n   - Recursively process all child directories.\n\n4. **Propagate affected status upward** — after determining leaf-level and intermediate state, walk the tree again; any node whose subtree contains an affected descendant gets itself marked as affected, so parent directories inherit their children's impact.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n| Variable / Field | Type | Modified? | Notes |\n|---|---|---|---|\n| `affectedDirs map[string]bool` (parameter) | Map | Yes | Keys added in both `propagateAffected` and `determineAffected`. No synchronization. |\n| `cache *MetadataCache` (parameter) | Pointer to struct | Potentially yes | `cache.Modules[n.Path]` read; other fields of `MetadataCache` unknown — defined outside this file. |\n| `filteredChanges []FileChange` (parameter) | Slice | No | Read-only in scope, but passed as slice header so callers can mutate underlying elements. |\n| `root.Files`, `curr.Files`, `curr.Children[...]` (via `buildTree`) | Tree struct fields | Yes | Mutated during tree construction only; single-threaded build path. |\n\n**Synchronization Mechanisms:** None observed in this file — no locks, mutexes, channels, atomics, or async/await patterns.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n| Function | Interaction | Details |\n|---|---|---|\n| `determineAffected` | **Disk (file system)** | Calls `os.Stat(absModulePath)` after resolving a module path via `security.SafeResolve`. This checks whether a file/directory exists on disk. |\n| All other functions | No external I/O | `propagateAffected`, `buildTree` are pure in-memory operations. Imports (`path`, `path/filepath`, `strings`) are used only for string manipulation, no actual read/write calls. |\n\n## Error Propagation\n\n### Errors that are **silently swallowed** (no propagation):\n1. **`security.SafeResolve` error** — In `determineAffected`:\n   ```go\n   absModulePath, err := security.SafeResolve(repoRoot, modulePath)\n   if err == nil { ... }\n   ```\n   If `SafeResolve` returns an error (e.g., path resolution fails), the code skips the entire block. The caller never learns about it.\n\n2. **`os.Stat` error** — In `determineAffected`:\n   ```go\n   if _, err := os.Stat(absModulePath); os.IsNotExist(err) { ... }\n   ```\n   Only the \"not exist\" case is handled. If `Stat` returns a different error (e.g., permission denied, I/O fault), it is not checked and never propagated to the caller.\n\n3. **No panic/exception wrapping** — No recovered panics or wrapped errors anywhere in this file. Errors are absorbed at the source.\n\n### Error propagation path:\n- None. All external errors originating from disk operations (`os.Stat`) or security layer (`security.SafeResolve`) are discarded without any signal to upstream callers of `determineAffected`."
    },
    "internal/engine/utils.go": {
      "sha256": "64a5a8d76570f4f398d694c2ecc7f243c8b07bdf459009a061f463606fda9128",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `internal/engine/utils.go`\n\n**Result:** No exported/public functions or types are defined in this file. Both declared functions use lowercase prefixes (`toSafeMarkdownFilename`, `makeLogEvent`), which makes them unexported (package-private) within the `engine` package.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose \u0026 Domain Rules\n\n### Primary Domain Problem\nThis file provides utility helpers for an engine module — it does not solve a domain-specific problem. The two functions serve internal plumbing:\n\n1. **Markdown filename generation** — converts a module path string into a filesystem-safe markdown filename by replacing `/` with `_`, defaulting to `root.md` when the result is empty or just `.`.\n2. **Log event factory** — produces a `LogEventFunc` that forwards an `EventType` and message string to an optional callback, with nil-safety for the callback.\n\n### High-Level Algorithm Steps\nNone. There are no algorithms present in this file. Both functions are single-step transformations:\n\n- Replace path separators → append `.md` suffix\n- Wrap a callback function so it can be invoked safely when non-nil\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:**\n- **No external side effects.** Both functions perform only in-memory string manipulation (`strings.ReplaceAll`) and return a closure. No disk, network, database, or API interactions occur.\n\n**Error Propagation:**\n- Neither function returns an error value. Errors are not propagated; `toSafeMarkdownFilename` silently returns the computed string regardless of input, and `makeLogEvent` returns a ready-to-use callback without error handling."
    },
    "internal/security/errors.go": {
      "sha256": "eaf4e252d4b8701cb50b75196bf95b853d9b43b8fd8f8902aa2b1d0c86029916",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface — `security` package (`internal/security/errors.go`)\n\n### Variables\n\n| Name | Type | Description |\n|------|------|-------------|\n| `ErrPathTraversal` | `error` | Returned when a path resolves outside the repository root. |\n| `ErrLockHeld` | `error` | Returned when another code-reducer process holds the file lock. |\n\n---\n\n**Note:** All elements in this package are unexported at the module level (package name is lowercase). The listed items are exported identifiers within the package and may be referenced internally or by other packages that import `security`.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nEnforcing security boundaries and inter-process exclusivity during code reduction operations. Specifically, preventing directory traversal attacks by constraining resolved paths to a repository root, and ensuring shared resources are not accessed concurrently by multiple processes via file locking.\n\n**High-Level Algorithm Steps:**\n1. Resolve the input path against the repository root boundary; if it resolves outside this scope, return an error indicating a security violation due to path traversal.\n2. Check for conflicts where another process holds an active lock on shared resources; if detected, return an error indicating the resource is unavailable because the lock is held externally.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External Side Effects\n\n**No external side effects.** This file contains only sentinel error definitions; no code performs disk, network, database, or API operations.\n\n## Error Propagation\n\nTwo sentinel errors are defined:\n\n| Symbol | Triggered On | Returned To Caller As |\n|---|---|---|\n| `ErrPathTraversal` | Path resolves outside the repository root | Returned as an error value (`errors.New`) |\n| `ErrLockHeld` | Another Code-Reducer process holds the file lock | Returned as an error value (`errors.New`) |\n\nBoth errors are standard-library `error` interface values. Callers receive them via normal Go error-return patterns (e.g., `func(...) (err error)`). The propagation mechanism is **Go convention**: callers check `if err != nil` and handle accordingly. No panic, no crash, no custom error wrapping visible in this file.\n\n## Notes\n\nThe error names imply filesystem access (`path traversal`) and inter-process locking, but neither of those operations is implemented here—only the errors themselves are defined. The actual I/O logic that would generate these errors lives elsewhere (not in `errors.go`)."
    },
    "internal/security/security.go": {
      "sha256": "5dbfee5adec3701c01c5a4db8f512ef2717579ef9c7a2dbf11f3eb22a63bc1e9",
      "facts": "#### [API_SIGNATURES]\n# security.go — Public API Surface\n\n## Types\n\n### `SimpleLock`\n\n```go\ntype SimpleLock struct {\n    lockPath string\n    file     *os.File\n    mu       sync.Mutex\n    closed   bool\n}\n```\n\n- **Methods**\n  - `Unlock() error`\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis module solves infrastructure concerns for a multi-process build tool (Code-Reducer): preventing directory traversal attacks on user-supplied paths and ensuring exclusive execution across processes via file-based locking. It additionally integrates with version control to keep the lockfile out of tracked content.\n\n## High-Level Algorithm Steps\n\n1. **Safe Path Resolution** — Given a repository root and an arbitrary input path, resolve symlinks on existing ancestor components to defeat symlink-based traversal attacks; walk upward until hitting the first physically-existing directory; rebuild the full resolved path by appending any non-existent suffix components; verify the final result lies strictly inside the repo root (reject if relative path starts with `..`).\n\n2. **Cross-Process Lock Acquisition** — Resolve a lockfile path within the repository root using safe resolution; atomically create the file using O_EXCL to prevent concurrent acquisition from another process; write the current PID into the newly-created file to identify the holder; return a handle that can later release the lock.\n\n3. **Lock Release (Idempotent \u0026 Thread-Safe)** — Close the underlying file descriptor and remove the lockfile atomically, ignoring errors if the file no longer exists; this operation is safe to call multiple times or from different threads without deadlocking.\n\n4. **Gitignore Maintenance** — Read `.gitignore`; check whether it already contains an entry for the lockfile; if not, append a commented entry under a temp file (written atomically via rename); ensure the final `.gitignore` includes the lockfile pattern so future builds don't pull stale state into version control.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n- **`SimpleLock` struct fields** — value-type state on instances only:\n  - `lockPath string` — read-only after construction; never reassigned in this file (assigned via constructor).\n  - `file *os.File` — modified inside `Unlock()` (`l.file = nil`); assigned once in `AcquireLock()`.\n  - `mu sync.Mutex` — value-type mutex embedded on each instance. Acquired and released in `Unlock()`.\n  - `closed bool` — modified inside `Unlock()` (`l.closed = true`).\n\n**Synchronization Mechanisms:**\n\n- **Instance-level `sync.Mutex`** embedded in `SimpleLock`. Used only within the `Unlock()` method: it is locked, state changes occur, then unlocked via `defer`.\n- No package-level mutexes or shared locks.\n- The `.gitignore` modification uses a temp-file + rename pattern (no explicit lock).\n\n**No additional global variables or class-level properties modified across the file.**\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis: `security.go` (Module: internal/security)\n\n### Filesystem Interactions\n\n| Function | Operations | Targets |\n|---|---|---|\n| `SafeResolve` | `filepath.Abs`, `filepath.EvalSymlinks`, `os.Lstat`, `filepath.Dir`, `filepath.Join`, `filepath.Clean`, `filepath.Rel` | Repo root and input path on disk. Resolves symlinks on the existing ancestor to prevent traversal via symlinked directories. |\n| `AcquireLock` | `SafeResolve`, `os.OpenFile` (O_WRONLY\\|O_CREATE\\|O_EXCL), `f.Write`, `f.Close`, `os.Remove` | Creates `.code-reducer.lock` inside repo root using O_EXCL for atomicity. Writes PID to the lockfile. |\n| `SimpleLock.Unlock()` | `l.file.Close()`, `os.Remove(lockPath)` | Closes and removes the lockfile. Idempotent (no-op if already closed or file absent). |\n| `EnsureGitignoreHasLockfile` | `SafeResolve`, `os.ReadFile`, `strings.Split`, `os.CreateTemp`, `tmpFile.Write`, `tmpFile.Sync`, `tmpFile.Close`, `os.Chmod`, `os.Rename` | Reads `.gitignore`, appends lock entry, writes to a temp file in the same directory, then atomically renames over `.gitignore`. Uses `Sync()` before rename for durability. |\n\n### Error Propagation Summary\n\n| Function | Failure Mode | Caller Impact |\n|---|---|---|\n| `SafeResolve` | Returns absolute path error, symlink resolution failure, stat-on-nonexistent-component error (only if not ENOENT), or path-traversal detection via `ErrPathTraversal`. | Any caller of `AcquireLock` or `EnsureGitignoreHasLockfile` receives the wrapped error directly. |\n| `AcquireLock` | If lock path cannot be resolved → returns nil + original error from `SafeResolve`. If O_EXCL open fails with EEXIST → wraps `ErrLockHeld` and includes lockPath in message. If write to lockfile fails → closes file, removes lockfile, then returns wrapped error. | Caller receives a non-nil error; no lock is held. No panic or crash path observed. |\n| `SimpleLock.Unlock()` | If close of underlying file fails → captured as `err`. Removal failure (other than ENOENT) is only considered if the close succeeded and removal also failed — otherwise it is swallowed (`if err == nil { err = removeErr }`). | Returns non-nil error only on double-failure; otherwise returns nil. No panic path. |\n| `EnsureGitignoreHasLockfile` | If `.gitignore` cannot be resolved → propagates upstream error. If read fails (non-ENOENT) → wraps error message. If temp file creation fails → wrapped. If write to temp file fails → wrapped. If sync fails → wrapped and returns early; subsequent close/rename are skipped. If chmod fails → wrapped. If rename fails → wrapped. | Caller receives a descriptive error for each failure point. Sync-before-rename pattern means partial writes are not persisted if sync fails (atomic-ish). |\n\n### Notable Patterns / Risks\n\n- **`SafeResolve` loop**: The `for` loop calling `os.Lstat` walks upward until it finds an existing ancestor or reaches the root (`parent == current`). If a non-existent-component stat returns something other than ENOENT, it is returned immediately — this could mask unexpected errors (e.g., EACCES on a directory) as fatal rather than being handled.\n- **`.gitignore` rename**: The temp-file + rename pattern ensures atomicity but requires write access to the parent directory of `.gitignore`. Failure modes are well-covered, though no retry logic exists for transient I/O errors (e.g., NFS timeout during `Sync`).\n- **Lockfile cleanup on write failure**: In `AcquireLock`, if PID write fails, the lockfile is explicitly removed before returning error — this prevents stale locks from being left behind."
    },
    "internal/tools/file_tools.go": {
      "sha256": "f2d2fe4bea860a28a18424bdf8d7d0af16e935fdda0ba345d8b881f9540e20a0",
      "facts": "#### [API_SIGNATURES]\n# Module: internal/tools — Public API Surface\n\n## Functions\n\n### ReadFileSafely\n```go\nReadFileSafely(repoRoot string, virtualPath string) ([]byte, error)\n```\nResolves the virtual path inside the repository and reads the file content. Implements TOCTOU mitigation similar to WriteFileSafely.\n\n---\n\n### WriteFileSafely\n```go\nWriteFileSafely(repoRoot string, virtualPath string, content []byte) error\n```\nResolves the virtual path inside the repository and writes content. Ensures that directories are created and uses a TOCTOU-safe write pattern.\n\n---\n\n### LoadGitignore\n```go\nLoadGitignore(repoRoot string) ([]string, error)\n```\nReads the `.gitignore` file from `repoRoot` and returns its active ignore patterns. Returns an empty slice if no `.gitignore` exists.\n\n---\n\n### ShouldIgnoreFile\n```go\nShouldIgnoreFile(repoRoot string, relPath string, gitIgnore *ignore.GitIgnore) bool\n```\nChecks if a file (specified by relative path) is ignored. Applies four checks: user-defined ignores via `GitIgnore`, dot-prefixed components or `.egg-info` suffixes, known text file extensions, and binary detection for unknown files.\n\n---\n\n### DiscoverCodeFiles\n```go\nDiscoverCodeFiles(repoRoot string, ignores []string) ([]string, error)\n```\nRecursively walks the codebase to find high-signal source files. Ignores build, dependency, output files, as well as any paths in the custom `ignores` list.\n\n---\n\n### IsBinaryFile\n```go\nIsBinaryFile(path string) bool\n```\nChecks if a file is binary by scanning the first 1024 bytes for null bytes.\n\n---\n\n## Constants (Package-Level — Not Exported)\n\n| Constant | Value | Description |\n| --- | --- | --- |\n| `binaryDetectionBufSize` | `1024` | Buffer size used when detecting binary files |\n| `defaultDirPerm` | `0755` | Default directory permission mode |\n| `defaultFilePerm` | `0644` | Default file permission mode |\n\n---\n\n## Dependencies Referenced (External — Not Part of This Module's API)\n\nThe following external packages are imported and used but **not** part of this module's public surface:\n\n- `github.com/arrase/code-reducer/internal/security` → `security.SafeResolve`, `security.IsBinaryFile`\n- `github.com/sabhiram/go-gitignore` → `ignore.GitIgnore`, `ignore.CompileIgnoreLines`\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis module provides safe file I/O operations and source-code discovery utilities for walking a repository directory tree. It filters out binary files, build outputs, dependency directories, and user-configured ignore patterns to produce a list of high-signal text/source files suitable for downstream processing (e.g., code analysis, reduction, or indexing).\n\n## High-Level Algorithm Steps\n\n1. **Safe file read** — Resolve the virtual path against the repo root, verify no symlink race exists between open-and-stat calls, then return contents.\n2. **Atomic safe write** — Create parent directories, write to a temp file in the same directory, sync/close it, then atomically rename into place.\n3. **Gitignore loading** — Read `.gitignore` from repo root, parse non-comment lines as user-defined ignore patterns.\n4. **File-ignore classification** — For any given path: check gitignore matches first; if not matched, scan path components for dot-prefixed entries or `.egg-info`; if still unresolved, consult a known-text-extension whitelist (go, js, ts, py, md, json, yaml, etc.); fall back to binary detection via null-byte scanning.\n5. **Directory discovery** — Recursively walk the repo root; skip directories matching gitignore or dot-prefixed names; for each file, run the ignore classification; collect relative paths of surviving files into a result list.\n6. **Binary detection helper** — Open a file and read 1024 bytes; return true if any null byte is found (otherwise false).\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O and Error Propagation Analysis — `internal/tools/file_tools.go`\n\n---\n\n### 1. External System Interactions (Disk / OS)\n\nAll interactions are local filesystem operations. No network, database, or external API calls exist.\n\n| Function | Disk/OS Operation | Purpose |\n|----------|------------------|---------|\n| **ReadFileSafely** | `os.Open` → `io.ReadAll` | Read file content into memory |\n| **WriteFileSafely** | `filepath.Dir`, `os.MkdirAll` | Ensure parent directory exists |\n| **WriteFileSafely** | `os.CreateTemp` + `os.Rename` (atomic write) | Write with TOCTOU-safe atomic rename pattern |\n| **LoadGitignore** | `os.Open` → `bufio.Scanner` | Read `.gitignore` file line-by-line |\n| **ShouldIgnoreFile** | `security.SafeResolve` (path resolution), then `IsBinaryFile` via `os.Open` + `f.Read` | Check if a path is binary by scanning first 1024 bytes |\n| **DiscoverCodeFiles** | `filepath.WalkDir`, `filepath.Rel`, `filepath.ToSlash`, `filepath.Base` | Recursive directory walk; errors logged to `os.Stderr`, skipped silently (`return nil`) |\n\n---\n\n### 2. Error Propagation Patterns\n\n#### A. Wrapped and returned (defensive)\n- **ReadFileSafely**: Every OS error is wrapped with a descriptive prefix via `fmt.Errorf(\"...: %w\", err)` before returning `[]byte, error`. The final return is either content+nil or nil+wrapped-error.\n- **WriteFileSafely**: Each step (`MkdirAll`, `CreateTemp`, `Write`, `Sync`, `Close`, `Chmod`, `Rename`) wraps its error with a descriptive prefix before returning. If any step fails, the temp file is cleaned up via the `defer` block (which also closes and removes it).\n- **LoadGitignore**: Non-existence (`os.IsNotExist`) returns `(nil, nil)` — treated as success. Other open errors are returned raw (not wrapped). Scanner error at end of loop is returned directly via `scanner.Err()`.\n\n#### B. Swallowed / logged only (no return)\n- **DiscoverCodeFiles** walk callback: \n  - `err != nil` from WalkDir → written to `os.Stderr`, then `return nil` skips the entry. The final `(files, err)` returned by `filepath.WalkDir` is propagated as-is — callers receive whatever error the walker produced at completion (likely nil if no fatal walk error).\n  - `filepath.Rel` failure → logged + skipped (`return nil`).\n- **ShouldIgnoreFile**: If `SafeResolve` fails, returns `true` (treated as \"ignored\"). This is a design choice, not an error return.\n\n#### C. Implicitly swallowing / assuming safe state\n- **IsBinaryFile**: Any `os.Open` error → returns `true` (assumes binary). Any read other than `io.EOF` → returns `true`. Clean read with no null bytes → returns `false`.\n- **WriteFileSafely** cleanup: The `defer` block always calls `tmpFile.Close()` and `os.Remove(tmpName)` regardless of success/failure. If the final `os.Rename` fails, the temp file is removed but the original destination path may be left in an inconsistent state (race window).\n\n---\n\n### 3. Summary\n\n| Category | Details |\n|----------|---------|\n| **External side effects** | Disk I/O only — read/write/delete/rename/mkdir/stat on local filesystem. No network, DB, or API calls. |\n| **Error handling style** | Most functions wrap OS errors with context before returning. A few paths swallow and log to `stderr` (WalkDir callback), treat non-existence as success, or return a sentinel value (`true`) for \"unsafe\" states. |\n| **Risk areas** | WriteFileSafely cleanup runs unconditionally — if `os.Rename` fails after `Chmod`, the temp file is removed but no rollback occurs. DiscoverCodeFiles treats WalkDir errors as non-fatal and returns them raw at the end, which callers may not anticipate. |"
    },
    "internal/tools/git_tools.go": {
      "sha256": "ee9d244fdd8ca3a1dfcc7df969b02a4d68d65c2fe3aba8203d62819bed4efcd0",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `internal/tools/git_tools.go`\n\n### Functions\n\n| Function | Parameters | Return Types |\n|---|---|---|\n| `RunGit(repoRoot string, args ...string)` | - `repoRoot string`: Repository root directory path\u003cbr\u003e- `args ...string`: Git command arguments (variadic) | - `string`: Trimmed stdout output\u003cbr\u003e- `error`: Error if git command fails |\n| `VerifyGitRepo(repoRoot string)` | - `repoRoot string`: Directory to verify as a git repository | - `error`: Error if not a valid git repository (or nil on success) |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nThis code provides utility functions for interacting with Git repositories programmatically—specifically executing git commands and validating repository state.\n\n**High-Level Algorithm Steps:**\n\n1. **ExecuteGitCommand(repoRoot, args...)** — Runs an arbitrary git command within a specified directory:\n   - Prepends `--no-pager` to disable pager wrapping\n   - Captures stdout and stderr into memory buffers\n   - Returns trimmed stdout on success; returns trimmed error message with stderr on failure\n\n2. **IsGitRepo(repoRoot)** — Validates that the given path is inside a git working tree:\n   - Invokes `git rev-parse --is-inside-work-tree`\n   - If the command fails, reports \"not a git repository\" (or parent chain)\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### 1. Subprocess Execution (Disk/OS)\n**`exec.Command(\"git\", ...)`** — Spawns the `git` binary as a child process. The parent waits for it to complete via `cmd.Run()`. This is the only direct external system interaction in this file. All other I/O (reading `.git` objects, filesystem traversal, etc.) happens inside the git binary itself and is invisible to this package.\n\n### 2. In-Memory Buffers\n**`bytes.Buffer`** — stdout and stderr are captured into memory buffers (`var stdout, stderr bytes.Buffer`). No disk I/O occurs; output is read back via `stdout.String()` after the process exits.\n\n### 3. String Operations (No External Dependency)\n**`strings.TrimSpace`**, **`fmt.Errorf`**, **`append`** — All standard library operations. No external systems involved.\n\n---\n\n## Error Propagation Analysis\n\n| Path | Behavior |\n|------|----------|\n| `cmd.Run()` returns error + non-zero exit code | Wrapped: `return trimmedOut, fmt.Errorf(\"git command failed: %v, stderr: %s\", err, trimmedErr)` — stderr text is preserved in the error message. |\n| `cmd.Run()` succeeds but stdout/stderr have content | Returns `(trimmed string, nil)`. |\n| `VerifyGitRepo` calls `RunGit(...)\"rev-parse\",\"--is-inside-work-tree\")` | If `RunGit` returns an error → wrapped as: `\"not a git repository (or any of the parent directories)\"`. The underlying stderr from git is **discarded** at this layer. |\n| No panic/recover patterns present | Errors propagate up as Go errors (`error` interface). Callers receive them via return values and must handle explicitly. |\n\n### Error Summary\n- All I/O errors are converted to `fmt.Errorf` wraps — no raw OS error is exposed to callers.\n- Stderr from failed git commands **is** captured into the error message in `RunGit`, but **lost** when re-wrapped by `VerifyGitRepo`."
    },
    "main.go": {
      "sha256": "16ab362b1830374f0306a1990dba8d51da887c1ad126f311c03d242933d2a516",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface\n\n**File: `main.go` (Module: `.`)**\n\n```go\npackage main\n```\n\n| Element | Kind | Description |\n|---------|------|-------------|\n| `(none)` | — | This file is a standard Go `main` package executable. No exported identifiers are declared here. The only entry point, `func main()`, calls the external command defined in `cmd.RootCmd.Execute()` and handles errors accordingly. |\n\n**Note:** Public surface of this module depends on its dependencies (`github.com/arrase/code-reducer/cmd`). This file itself contributes no public API — it is an internal driver that invokes a pre-built CLI root command from the imported package.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis is an **entry point for a CLI tool named \"code-reducer\"**. The file itself contains no domain logic — it delegates entirely to `cmd.RootCmd.Execute()` from the repository's own command package. All actual functionality lives in that dependency, not here.\n\n## Domain Rules (from this file alone)\n\n- If execution fails, print the error to stderr and exit with status code 1\n- If execution succeeds (no error), the program exits normally (status 0)\n\nThat is it. The algorithm steps, domain rules, and business logic are all in `cmd/` — not in this file.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:**\n- Calls `cmd.RootCmd.Execute()` from an external dependency (`github.com/arrase/code-reducer/cmd`). This is a deferred function call whose behavior depends entirely on that package's implementation; no direct file, network, or database access is visible in this file alone.\n- No disk, network, API, or DB calls are made directly by `main.go`.\n\n**Error Propagation:**\n- If `cmd.RootCmd.Execute()` returns an error, it is printed to `os.Stderr` via `fmt.Fprintf(os.Stderr, \"Execution failed: %v\\n\", err)` and the process exits with code 1 (`os.Exit(1)`).\n- No panic recovery; a runtime panic from any package would terminate the program without exit.\n- If `Execute()` returns `nil`, execution continues silently (no output, no exit call)."
    }
  },
  "modules": {
    ".": "# Code-Reducer Architecture Documentation\n\n## Module Responsibility\n\nCode-Reducer is a CLI tool that generates and maintains project wiki documentation (`wiki/architecture.md`, `wiki/quickstart.md`, per-module API summaries) via an LLM-driven synthesis pipeline operating on source code repositories. The system runs in two modes—**init** (full generation from scratch) and **update** (incremental regeneration triggered by detected file changes). Execution proceeds through a hierarchical directory tree: leaf files are synthesized first, their summaries flow up through child directories to module-level aggregates, then converge at the repository root where global `architecture.md` and `quickstart.md` are produced.\n\n## Data Flow Overview\n\n```\n┌───────────────┐    ┌──────────────┐    ┌─────────────────┐    ┌──────────────┐\n│ cmd.RootCmd   │──►│ executeCommand│──►│ runner.Run(...)  │──►│ Config       │\n│ (entry point) │   │(\"init\"|\"update\")│   │                  │    │ SaveConfig   │\n└───────────────┘   └──────────────┘   └─────────────────┘   └──────────────┘\n```\n\nErrors flow as wrapped Go errors through cobra's `RunE` interface. Event-emitted errors (via callback) are printed to stderr and swallowed; only the runner's return error is re-wrapped with a descriptive prefix before propagation.\n\n---\n\n## Subsystem: `cmd` — CLI Command Registry \u0026 Execution Engine\n\n### Entry Point (`main.go`)\n\nThe `main` package executable delegates entirely to `github.com/arrase/code-reducer/cmd`. If execution fails, the error is printed to stderr and the process exits with code 1; on success it exits normally (code 0). No direct I/O occurs in this file.\n\n### Root Command \u0026 Execution Loop (`cmd/root.go`)\n\n**Precondition Validation Sequence:**\n\n1. **Git Repository Check** — `os.Getwd()` captures current working directory; `tools.VerifyGitRepo(repoRoot)` checks for `.git` existence and equivalent state. Errors propagate through cobra as wrapped errors via `%w`.\n2. **Configuration Existence** — `config.ConfigExists(repoRoot)` reads the local filesystem to confirm a configuration file is present. If missing *and* stdin is not a terminal (TTY), an implicit setup flow runs: `RunSetupFlow(repoRoot)` creates one and returns any error as-is.\n3. **Mode Guard** — `engine.IsInitialized(...)` validates that the requested operation matches current state: `init` mode fails if documentation has already been initialized → suggests using `update`; `update` mode fails if initialization has not occurred yet → requires running `init` first.\n\n**Configuration Resolution:** `config.ResolveConfig(repoRoot, modelIDFlag, numCtxFlag)` merges CLI flags (`--model-id`, `--num-ctx`) with on-disk settings and environment variables. Returns the resolved config or a raw error from the config package (no wrapping). The LLM model ID flows through but is not consumed in this file; it persists for later use by the engine.\n\n**Signal Handling \u0026 Execution Loop:** `signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)` registers interrupt and termination signals on a background context. The handler calls `stop()` via deferred execution, enabling graceful shutdown propagation to the engine runner. This is the only concurrency primitive in this file; no goroutines or mutexes are used.\n\nThe resolved configuration is passed to `runner.Run(ctx, repoRoot, engine.Mode(mode), ...)`. Status events print to stdout (`fmt.Println(ev.Message)`), error events print to stderr (`fmt.Fprintf(os.Stderr, \"Error: %s\\n\", ev.Message)`), and other events print to stdout. Errors from the runner are wrapped with `\"documentation run failed: %w\"` before returning.\n\n### init Command Registration (`cmd/init.go`)\n\n`initCmd` → unexported `*cobra.Command`, registered under `RootCmd`. The `RunE` closure delegates directly to `executeCommand(\"init\")`. No panic or crash path is introduced here; errors propagate through cobra's command framework and exit via non-zero process code when a non-nil error is returned.\n\n### Update Command (`cmd/update.go`)\n\nAll identifiers begin with lowercase letters and are unexported. The `RunE` closure delegates to `executeCommand(\"update\")`. Actual implementation lives in the dependency; no panic or crash recovery is handled within this file.\n\n### Interactive Setup Wizard (`cmd/setup.go`)\n\n**Public API Surface:**\n\n| Function | Signature | Purpose |\n|---|---|---|\n| `RunSetupFlow` | `func RunSetupFlow(repoRoot string) error` | Orchestrates the full interactive configuration flow; returns non-nil only for filesystem-level failures (`os.Getwd`, `config.SaveConfig`) |\n| `promptStringList` | `func promptStringList(reader *bufio.Reader, promptMsg string, existingList []string) ([]string, bool)` | Collects comma-separated or list-style input with an existing value fallback; returns `(result, modified)` where `modified=false` signals no change |\n| `promptString` | `func promptString(reader *bufio.Reader, promptMsg string, existingVal string) string` | Collects single-string input with a default shown in brackets; returns trimmed input or `existingVal` on empty/error |\n\n**Algorithm Steps:**\n\n1. **Repo Root Resolution** — `os.Getwd()` captures the base path at start of `RunSetupFlow`.\n2. **State Loading** — `config.LoadConfig(repoRoot)` reads previously saved configuration; non-empty values (model ID, URL, context size, ignore list, docs dir) are extracted for pre-population.\n3. **Prompt Sequence** — Four prompts run in order: LLM Model ID, Ollama Base URL, Context Size, Documentation Directory. Each uses the `promptString`/`promptStringList` pattern with existing values as fallbacks.\n4. **System Prompt Preservation** — Existing values for extraction, synthesis, architecture, and file fact consolidation prompts are carried forward when present.\n5. **Configuration Assembly** — All user inputs merge with defaults/previous state into a single `Config` struct.\n6. **Persistence** — `config.SaveConfig(repoRoot, newCfg)` writes the assembled config to disk. Errors bubble up un-wrapped.\n\n**Error Handling Strategy:** `promptString` / `promptStringList`: swallow `ReadString` errors → print warning to stderr → return existing value with no propagation beyond the function. `strconv.Atoi(ctxInputStr)`: swallow parse failure or non-positive result → fall back to `existingNumCtx`; no error propagates. Only filesystem-level failures (`Getwd`, `SaveConfig`) propagate up the call stack.\n\n---\n\n## Subsystem: `internal/config` — Configuration Resolution \u0026 Persistence\n\n### Responsibility\n\nThe `config` package owns the lifecycle of a single configuration object (`*Config`) consumed by every downstream component. It provides three operations: resolve, persist, and query. All resolution is deterministic—given identical inputs, the returned `*Config` is identical. No global mutable state exists within the package; the only shared state is a package-level variable holding default extraction steps (`DefaultExtractionSteps`).\n\n### Data Flow\n\n```\nCLI flags (modelIDFlag, numCtxFlag) ─┐\nEnv vars (CODE_REDUCER_MODEL_ID,     │   ┌─────────────┐\nOLLAMA_BASE_URL, OLLAMA_NUM_CTX)    ─┼──►│ LoadConfig  │─► Config\nYAML file (.code-reducer.yaml)      └──\u003e│ getConfigPath│\n                                        └─────────────┘\n\nResolved *Config ──► SaveConfig() ──► disk: .code-reducer.yaml\n```\n\n### Constants\n\n| Constant | Value / Purpose |\n|---|---|\n| `CodeReducerModelIDEnvKey` | Env key `\"CODE_REDUCER_MODEL_ID\"` consumed by `ResolveConfig`. |\n| `OllamaBaseURLEnvKey` | Env key `\"OLLAMA_BASE_URL\"` consumed by both `LoadConfig` and `ResolveConfig`. |\n| `OllamaNumCtxEnvKey` | Env key `\"OLLAMA_NUM_CTX\"` consumed by `ResolveConfig`; value parsed via `strconv.Atoi`. |\n| `OllamaDefaultBaseURL` | Hard-coded fallback URL `\"http://localhost:11434\"`. |\n| `OllamaDefaultModelID` | Hard-coded fallback model identifier `\"ornith:9b\"`. |\n| `OllamaDefaultNumCtx` | Hard-coded fallback context size `8192`. |\n| `DefaultDocsDir` | Hard-coded fallback docs directory name `\"wiki\"`. |\n| `ConfigFileName` | Config file basename `\".code-reducer.yaml\"`. |\n\n### Types: `ExtractionStep` and `Config`\n\n```go\ntype ExtractionStep struct {\n    Name   string  // yaml:\"name\"\n    Prompt string  // yaml:\"prompt\"\n}\n\ntype Config struct {\n    ModelID                              string\n    OllamaBaseURL                       string\n    OllamaNumCtx                        int\n    DocsDir                             string\n    SystemPrompt                        string\n    ModuleSynthesisPrompt               string\n    ArchitecturePrompt                  string\n    FileFactConsolidationPrompt         string\n    ExtractionSteps                     []ExtractionStep\n    Ignore                              []string\n}\n```\n\n- `Config` is the sole configuration container. Every field maps to a distinct YAML key; all four prompt categories are independent strings with no cross-field dependencies.\n- `ExtractionStep` is an opaque two-string struct used only inside the slice field of `Config`. The package-level variable `DefaultExtractionSteps` holds a static instance initialized at declaration and never mutated within this file.\n\n### Variable: `DefaultExtractionSteps`\n\n```go\nvar DefaultExtractionSteps []ExtractionStep\n```\n\nPackage-level slice pre-populated with four entries covering API signatures, business logic, state and concurrency analysis, and errors/side effects. Read-only within the package boundary; any modification must occur outside this file.\n\n### Functions: Configuration Resolution (`internal/config/resolve.go`)\n\n#### `ResolveConfig(repoRoot string, modelIDFlag string, numCtxFlag string) (*Config, error)`\n\nMerges CLI flags, environment variables, YAML config, and built-in defaults into a single resolved `*Config`. Returns nil error on success; returns both a non-nil Config and an error only when the underlying file read fails with a condition that is not \"file does not exist\".\n\n**Parameters:**\n\n| Parameter | Purpose |\n|---|---|\n| `repoRoot` | Repository root path. Used to derive the config file path for the disk-read step. |\n| `modelIDFlag` | Explicit model ID from CLI; overrides environment and YAML only if non-empty. |\n| `numCtxFlag` | Explicit context size from CLI; parsed as int with a minimum-of-1 validation gate before taking effect. |\n\n**Resolution Priority Chains:** Every field follows an independent priority chain:\n\n- **Model ID** — built-in default → YAML `model_id` → env `CODE_REDUCER_MODEL_ID` → CLI flag (skipped if empty).\n- **Ollama Base URL** — built-in default → YAML `ollama_base_url` → env `OLLAMA_BASE_URL`. No CLI override.\n- **Ollama Context Size** — built-in default → YAML `ollama_num_ctx` → env `OLLAMA_NUM_CTX` → CLI flag (parsed via `strconv.Atoi`, minimum 1). Env and CLI values that fail to parse are silently skipped; the previously-resolved fallback remains in effect.\n- **DocsDir** — built-in default if unset, otherwise uses YAML-specified value. No env or CLI override path exists for this field.\n- **Prompts** (system, synthesis, architecture, fact consolidation) — each has its own independent chain: built-in default → YAML override if non-empty. No CLI or env override is provided in this code.\n\n**Error Handling:**\n\n| Condition | Behavior |\n|---|---|\n| `LoadConfig(repoRoot)` returns error + not-exist | Swallowed; replaced with an empty `\u0026Config{}` and returned alongside a nil error. |\n| `LoadConfig(repoRoot)` returns any other error | Wrapped as `\"failed to load configuration file: %w\"` and returned. |\n| Integer parsing (`strconv.Atoi`) fails | Skipped; previous fallback retained. No error propagated. |\n\n### Functions: Configuration Persistence (`internal/config/io.go`)\n\n#### `getConfigPath(cwd string) string`\n\nDerives the absolute path to `.code-reducer.yaml` by combining the working directory with the fixed filename constant. Return value is a plain string; no error path exists in this signature.\n\n#### `ConfigExists(cwd string) bool`\n\nReturns true if the config file is present at the derived path, false otherwise. Uses stat semantics internally (`os.Stat`). No error propagation—callers cannot distinguish \"file does not exist\" from other OS-level errors (e.g., permission denied).\n\n#### `LoadConfig(cwd string) (*Config, error)`\n\nReads raw bytes from disk at the resolved config path, then unmarshals them into a `*Config` struct via YAML parsing. Any parse failure is wrapped and returned as an error with descriptive prefix `\"failed to parse yaml config: %w\"`.\n\n#### `SaveConfig(cwd string, cfg *Config) error`\n\nPersists `*cfg` back to disk using an atomic rename pattern:\n\n1. Marshals the in-memory config to YAML.\n2. Normalizes key prefixes by inserting extra blank lines before specific top-level keys (`system_prompt`, `module_synthesis_prompt`, `architecture_prompt`, `file_fact_consolidation_prompt`, `extraction_steps`, `ignore`) — purpose of this normalization is not documented within the file boundary.\n3. Creates a temporary file in the same directory via `os.CreateTemp`.\n4. Writes normalized YAML to the temp file, syncs, and closes it.\n5. Sets restrictive permissions on the temp file via `chmod`.\n6. Renames the temp file over the original config path atomically.\n\nAny failure at any step (read, parse, write, sync, close, chmod, rename) is wrapped with a descriptive prefix (`\"failed to create temp file:\"`, etc.) and returned as an error. Cleanup of the temp file runs in a defer block so removal happens even when errors occur mid-save. A redundant explicit `Close()` call follows the deferred one; closing an already-closed file returns nil, so this is non-fatal but unnecessary.\n\n---\n\n## Subsystem: `internal/engine` — Incremental Documentation Synthesis Engine\n\n### Responsibility\n\nThis package orchestrates automatic generation of architecture overviews (`architecture.md`), quickstart guides (`quickstart.md`), and per-module API summaries for software repositories. It operates in two modes—**init** (full generation from scratch) and **update** (incremental regeneration triggered by file changes). The engine consumes source code through a hierarchical synthesis pipeline driven by LLM calls, persists intermediate state to disk for resumption between runs, and uses an exclusive lock file to serialize concurrent invocations.\n\n### Data Flow\n\n```\nRunner.Run(ctx, repoRoot, mode) \n    ↓ acquires lock via security.AcquireLock(repoRoot)\norchestrator.RunInit(ctx, ...) | orchestrator.RunUpdate(ctx, ...)\n    ↓ setupPipeline() loads gitignore + discovers code files + computes SHA256 hashes per file\ntree.buildTree(files) → DirNode hierarchy\n    ↓ synthesizeNode(root) recurses post-order: child dirs first (reusing cached summaries), then each file\nclient.CallLLM(...) — LLM API call for extraction step on chunked content\nreduceInChunks / reduceFileFacts — recursive text synthesis with batch partitioning and overlap handling\ncache.saveMetadataCache(repoRoot, docsDir, cache) — persists Files + Modules maps to disk as JSON\n```\n\n### `cache.go` — Persistent Metadata Cache\n\n#### Types\n\n- **`FileCacheEntry`** — Key-value pair mapping a file's SHA256 hash to its synthesized facts string. Used as the per-file cache entry in `MetadataCache.Files`.\n- **`MetadataCache`** — Package-level cache state containing:\n  - `Version int` — Cache format version; mismatched versions return an empty initialized structure rather than corrupt data.\n  - `StepsHash string` — SHA256 of serialized extraction steps, used to invalidate the entire cache when configuration changes.\n  - `Files map[string]FileCacheEntry` — Per-file hash→facts mapping for incremental updates.\n  - `Modules map[string]string` — Per-directory synthesized summary storage.\n\n#### Public API Surface\n\n- **`IsInitialized(repoRoot, docsDir string) bool`** — Probes `{docsDir}/{metadataFileName}` existence; returns `false` on read failure (error swallowed).\n- **`saveMetadataCache(repoRoot, docsDir string, cache *MetadataCache) error`** — Serializes the metadata cache to indented JSON at `{docsDir}/{metadataFileName}`. Sets `cache.Version = currentCacheVersion` before marshaling. JSON marshal errors propagate; write failures propagate as-is.\n- **`computeSHA256(repoRoot, virtualPath string) (string, error)`** — Reads a source file via `tools.ReadFileSafely`, computes SHA256 hash in memory. Read errors propagated directly.\n\n#### Internal Functions\n\n- **`loadMetadataCache(repoRoot, docsDir string) (*MetadataCache, error)`** — Constructs an empty `MetadataCache` with fresh maps (`make(map[string]FileCacheEntry)`, `make(map[string]string)`). If the cache file exists and parses successfully with matching version, returns parsed state + nil. On `os.ErrNotExist`: returns default structure + **nil error** (silent recovery). Other read errors wrapped via `%w`. Version mismatch: returns clean cache + nil without signaling an error.\n\n#### Thread Safety Assessment\n\nNo synchronization primitives present. The maps in `MetadataCache` are created fresh and returned as struct values, but callers receive map references pointing to shared backing storage. Concurrent access to `Files`/`Modules` from multiple goroutines constitutes a data race unless external locking exists outside this module boundary. On-disk writes via `saveMetadataCache` provide coarse-grained serialization through OS write ordering alone; no explicit mutex is used.\n\n### `chunking.go` — Recursive Hierarchical Text Synthesis\n\n#### Internal Functions (All Unexported)\n\n- **`reduceWithLLM(ctx, c llmCaller, items []string, sysPrompt string, buildPrompt func(batch []string) string, logMsg func(batch []string) string, errMsg string, logEvent LogEventFunc) (string, error)`** — Feeds a batch of items to an LLM via `c.CallLLM(ctx, sysPrompt, messages, false)`. System prompt describes the synthesis task. Response markdown fences are stripped before returning. Errors from `CallLLM` wrapped as `fmt.Errorf(\"%s: %w\", errMsg, err)` where `errMsg` is caller-supplied (e.g., `\"LLM error during synthesis\"`). Original error type information lost for classification purposes.\n\n- **`reduceInChunks(ctx, c llmCaller, nodePath string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)`** — Delegates to `reduceWithLLM`. No external I/O beyond the LLM call.\n\n- **`reduceFileFacts(ctx, c llmCaller, filePath string, stepName string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)`** — Delegates to `reduceWithLLM`. Used per extraction step during file synthesis in `synthesize.go`.\n\n- **`reduceItems(ctx, items []string, maxChars int, reduceFn func(batch []string) (string, error)) (string, error)`** — Core recursive reduction engine:\n  1. Partitions input items into batches where total character count stays within `maxChars`. Oversized single items are truncated in-place via `items[0] = string(runes[:maxChars]) + \"\\n...[truncated]\"` (mutation without signal).\n  2. If exactly one batch exists, calls the reduction function on it directly.\n  3. Otherwise, recursively reduces each sub-batch independently, concatenates results, and recurses until a single result remains — forming a binary tree of reductions.\n\n- **`chunkTextWithOverlap(text string, maxRunes int, overlapRunes int) []string`** — Splits a single text into overlapping rune-counted chunks at fixed step size. Used for preparing input sequences requiring positional overlap between adjacent pieces during file synthesis.\n\n#### State and Side Effects\n\n- `reduceItems` mutates `items[0]` in-place when a single oversized item is encountered. This silent truncation provides no error signal to callers; data loss goes undetected unless output string is inspected.\n- All other functions are pure (string manipulation, recursion) with the sole exception of the LLM API call inside `reduceWithLLM`.\n\n### `client.go` — Ollama HTTP Client\n\n#### Types\n\n- **`Message`** — Struct with `Role string \\`json:\"role\"\\`` and `Content string \\`json:\"content\"\\``. Used as the conversational turn schema for LLM requests.\n- **`ollamaRequest`, `ollamaOptions`, `ollamaResponse`** — Unexported structs holding request/response payloads.\n\n#### Methods on `*llmClient`\n\n- **`newLLMClient(modelID, baseURL string, numCtx int) *llmClient`** — Constructor storing model identifier, base URL, and context size. Field assignments occur once; no subsequent mutation within the file.\n- **`(c *llmClient).NumCtx() int`** — Returns stored `numCtx` value. Read-only accessor.\n- **`(c *llmClient).prepareOllamaRequest(ctx, systemPrompt string, messages []Message, jsonFormat bool) (*http.Request, error)`** — Prepends the system prompt as a new entry to the messages array, then serializes model ID + conversation context + processing options into JSON. Constructs HTTP POST request targeting `{baseURL}/api/chat`. JSON marshal failures wrapped with `%w`; request construction errors propagated as-is.\n- **`(c *llmClient).CallLLM(ctx, systemPrompt string, messages []Message, jsonFormat bool) (string, error)`** — Executes the HTTP POST via `c.httpClient.Do` with timeout applied to the client itself (`\u0026http.Client{Timeout: defaultHTTPTimeout}`). On 200 status: deserializes response body fully and returns generated text. On non-OK status: reads up to `maxErrorBodyBytes` (value undefined in this file; if zero, `io.LimitReader(resp.Body, 0)` blocks indefinitely until EOF rather than applying a size limit), then returns formatted error `\"ollama api error: status %d, response: %s\"` where the body read failure is silently swallowed — only the HTTP status and successfully-read bytes are visible in the returned error.\n\n#### Unresolved References\n\n- `defaultHTTPTimeout` and `maxErrorBodyBytes` referenced but not defined in this file; resolved at link time or via another package constant. If these resolve to zero values, behavior deviates from intended timeout/limit semantics.\n\n### `constants.go` — Runtime Configuration Contract\n\n#### Function Types\n\n- **`LogEventFunc func(EventType, string)`** — Signature for external observer hooks into system events (type + message), enabling traceability without hardcoding logging implementation.\n\n#### Constants and Values\n\nThis file defines the numerical boundaries governing engine behavior:\n- HTTP operation timeout: 10 minutes before aborting network calls.\n- Error response truncation limit: responses beyond 1 KB are truncated for non-critical error paths.\n- Chunked text processing: input split into overlapping segments of ~800 characters, enabling streaming across large documents without full loading.\n- Context window budgeting: only 75% of total memory reserved; minimum floor enforced at ~512 tokens regardless of available space.\n- Output scaling rule: maximum generated output capped at roughly 3× input length to prevent runaway generation loops.\n- Persistent state layer: engine writes `.metadata.json` snapshot and reads agent behavior from `AGENTS.md`, establishing a file-based identity layer for long-running processes.\n- Directory permissions: any directories the engine creates are assigned 0755 by default, controlling filesystem boundary access.\n\n### `json_parser.go` — Markdown Fence Stripping Utility\n\n#### Internal Functions\n\n- **`stripOuterMarkdownFence(content string) string`** — Best-effort extraction of raw content from strings wrapped in backtick-delimited markdown or JSON fences (3+ backticks, optional language identifier like `markdown` or `json`). Algorithm: trim whitespace → test against regex pattern for fence syntax → if match found, extract interior between opening and closing fences; otherwise return trimmed input unchanged. Regex mismatch returns original with whitespace stripped — no error signaled.\n\n#### Side Effects\n\nNo external I/O. Only standard library `regexp` and `strings` used for in-memory processing. If a malformed regex were supplied at compile time it would panic during package initialization, but this is not handled within the function scope.\n\n### `orchestrator.go` — Incremental Documentation Generation Orchestrator\n\n#### Types\n\n- **`EventType`** (`type string`) — Event classification type.\n- **`EventStatus`, `EventError`** (`constant EventType`) — Predefined event status/error constants.\n- **`Event struct`** — `Type EventType`, `Message string`. Carries typed event data with message payload.\n\n#### Methods on `*orchestrator`\n\n- **`GenerateStandardDocs(ctx, repoRoot, docsDir, rootSum string, cfg *config.Config, logEvent LogEventFunc) error`** — Generates `architecture.md` and `quickstart.md` from the synthesized root summary. Writes both files via `tools.WriteFileSafely`. Errors wrapped and propagated.\n\n- **`RunInit(ctx, repoRoot string, cfg *config.Config, onEvent func(Event)) error`** — Full generation path:\n  1. Loads `.gitignore`, user-configured ignore paths, metadata cache from disk.\n  2. Discovers code files respecting ignores; errors propagated directly as returned errors.\n  3. Computes SHA256 hashes per file (per-file failures logged + skipped from processing).\n  4. Builds hierarchical directory tree from all discovered files.\n  5. Marks every directory as \"affected\" for fresh generation.\n  6. Traverses tree bottom-up: each node's summary becomes part of its parent's input; leaf summaries flow up through directories, then into module-level summaries, converging at repository root.\n  7. Uses root-level synthesis result to generate global `architecture.md` and `quickstart.md`.\n  8. Ensures `.agent-guidelines.md` exists: if missing, creates fresh; if contains \"AI Agent Guidelines\", appends; otherwise replaces. Read failures for this file are swallowed — code proceeds as if empty/missing. Write errors wrapped and returned.\n\n- **`RunUpdate(ctx, repoRoot string, cfg *config.Config, onEvent func(Event)) error`** — Incremental path:\n  1. Detects file changes by comparing current hashes against cached ones — classifying each as Added, Modified, or Deleted.\n  2. Builds tree only from currently existing files; prunes cache entries for deleted files and removes module documentation for directories no longer present.\n  3. Determines affected directories based on change set; propagates \"affected\" status through hierarchy.\n  4. If nothing changed (zero affected directories), returns immediately with no regeneration.\n  5. Otherwise re-runs hierarchical synthesis within affected modules only.\n\n- **Private Functions** — `setupPipeline(repoRoot, cfg, logEvent) (string, []string, *MetadataCache)` loads gitignore + discovers files + computes hashes; errors from `LoadGitignore` swallowed after logging warning event. `teardownPipeline(repoRoot, docsDir, cache, logEvent, successMsg)` saves metadata cache — write failures logged as warnings but **not returned** (silently swallowed).\n\n#### Error Propagation Summary\n\n| Operation | Failure Handling |\n|---|---|\n| LLM call (`CallLLM`) | Wrapped + propagated as returned error |\n| Write file (`WriteFileSafely`) | Wrapped + propagated in main path; swallowed in teardown |\n| Read file (`ReadFileSafely` for `.agent-guidelines.md`) | Swallowed — falls through to write path |\n| Load gitignore | Logged warning, swallowed |\n| Discover files | Propagated as returned error |\n| SHA256 hash per file | Per-file: logged + skipped from processing |\n| MkdirAll | Wrapped + propagated |\n| SafeResolve (path existence) | Errors treated as \"not found\", swallowed |\n| os.Remove (cleanup) | Swallowed silently — stale files remain on disk if deletion fails |\n\n### `runner.go` — Pipeline Execution Wrapper with Locking\n\n#### Types and Constants\n\n- **`Mode type string`** — Operation mode of the documentation pipeline.\n- **`ModeInit`, `ModeUpdate`** (`const Mode`) — Init mode constant and update mode constant.\n- **`Runner struct`** — Orchestrates execution of the documentation pipeline. Holds immutable reference to configuration set once via `NewRunner`.\n\n#### Methods on `*Runner`\n\n- **`NewRunner(cfg *config.Config) *Runner`** — Constructor storing configuration values (model ID, base URL, context size). No field reassignment within this file after construction.\n- **`(r *Runner) Run(ctx, repoRoot string, mode Mode, onEvent func(Event)) error`** — Main entry point:\n  1. Attempts to add lockfile entry to `.gitignore` via `security.EnsureGitignoreHasLockfile(repoRoot)`; failure swallowed — only logged via callback.\n  2. Acquires exclusive repository lock via `security.AcquireLock(repoRoot)`. Failure propagated with wrapping: `\"failed to acquire repository lock: %w\"`.\n  3. Defer unlock for after pipeline completes (success or error). No explicit error handling for unlock failures visible in this file.\n  4. Instantiates LLM client and orchestrator based on stored configuration values.\n  5. Dispatches either `RunInit` or `RunUpdate` depending on mode string match; unknown modes return `\"unsupported mode: %s\"` without wrapping.\n\n### `synthesize.go` — Recursive Context-Aware Synthesis Engine\n\n#### Internal Structures and Functions\n\n- **`pipelineContext struct`** — Holds shared state for a synthesis run: LLM client, repository root, docs directory, configuration, extraction steps, affected directories set, precalculated hashes map. Locally allocated per method call in `RunInit`/`RunUpdate`.\n- **`synthesizeNode(p *pipelineContext, node *DirNode) (string, error)`** — Recursive synthesis walking the directory tree:\n  1. Post-order traversal: process children before files. For each child directory, recursively call `synthesizeNode`; if child is not affected and has a cached summary, reuse directly.\n  2. Per-file processing loop: compute or retrieve SHA256 hash (from precalculated hashes map or read-and-hash). On cache hit (hash matches stored entry), return cached facts without reading the file. On cache miss, read file content; if multiple chunks needed based on dynamic context window size × 4 chars/token × allocation ratio, split with overlap. For each chunk, run LLM extraction across all configured `ExtractionSteps`, building consolidated fact per step via `reduceFileFacts`.\n  3. Component assembly: build list of components — file facts (prefixed by filename) followed by child directory summaries (prefixed by subsystem name). If no components exist, cache empty string and return early.\n  4. Final reduction: combine all assembled components into single consolidated summary for current directory using `reduceInChunks`. Cache result under `p.cache.Modules[node.Path]` and write to disk at `\u003cdocsDir\u003e/modules/\u003csafe-filename-of-path\u003e`.\n\n#### External Interactions\n\n- **Disk Read**: `tools.ReadFileSafely(p.repoRoot, f)` reads individual source files when cache misses occur. Errors logged as warnings via callback but do not propagate — facts for that file remain empty string; processing continues to next file.\n- **Disk Write**: `tools.WriteFileSafely(p.repoRoot, modulePath, []byte(finalSum))` writes synthesized documentation at `\u003crepoRoot\u003e/\u003ccfg.DocsDir\u003e/modules/\u003csafe-filename\u003e`. Failures wrapped with context: `\"failed to write module documentation for %s: %w\"`. Propagates up through synthesis stack.\n- **Network/API**: `p.client.CallLLM(p.ctx, systemPrompt, []Message{...}, false)` makes LLM API calls per extraction step and chunk combination during file synthesis. Multiple sequential calls made per file (one per `cfg.ExtractionSteps`), multiple files processed before moving to child summary reduction.\n\n#### Thread Safety Assessment\n\nNo explicit locks/mutexes visible in this file. Context (`p.ctx`) checked at boundaries for cancellation detection but does not prevent concurrent map writes to the cache. If multiple goroutines invoke `synthesizeNode` with different `pipelineContext` instances sharing the same underlying `*MetadataCache`, both `cache.Files` and `cache.Modules` maps will be concurrently written without locking — a data race unless external synchronization exists outside this module boundary.\n\n### `tree.go` — Directory Tree Building and Affected Detection\n\n#### Types\n\n- **`FileChange struct`** — `Path string` (file path), `Status string` (change status: `\"Added\"`, `\"Modified\"`, `\"Deleted\"`).\n- **`DirNode struct`** — `Path string` (directory path), `Files []string` (list of file paths in this directory), `Children map[string]*DirNode` (child directory nodes keyed by name).\n\n#### Internal Functions\n\n- **`buildTree(repoRoot, files []string) *DirNode`** — Walks each path's directory components to create nested `DirNode` objects with files listed under parent directories. Mutated during tree construction only; single-threaded build path.\n- **`determineAffected(p *pipelineContext, node *DirNode) bool`** — For each node: checks if any file in its `Files` list matches a changed file (marks as affected); resolves expected module path via safe filename conversion and secure resolution; if resolved path does not exist on disk, marks as affected (indicating missing module); checks external metadata cache (`cache.Modules`); if no cached value recorded for this directory's path, marks as affected.\n- **`propagateAffected(p *pipelineContext) map[string]bool`** — Walks tree again; any node whose subtree contains an affected descendant gets itself marked as affected. Parent directories inherit children's impact.\n\n#### External I/O and Error Propagation\n\n- `determineAffected`: calls `os.Stat(absModulePath)` after resolving a module path via `security.SafeResolve`. Only the \"not exist\" case is handled; if `SafeResolve` returns an error or `Stat` returns a non-not-exist error (e.g., permission denied, I/O fault), it is not checked and never propagated to callers.\n- All other functions (`propagateAffected`, `buildTree`) are pure in-memory operations using only string manipulation via standard library imports — no actual read/write calls.\n\n### `utils.go` — Internal Utility Helpers\n\n#### Internal Functions (Both Unexported)\n\n- **`toSafeMarkdownFilename(path string) string`** — Converts a module path into a filesystem-safe markdown filename by replacing `/` with `_`, defaulting to `root.md` when the result is empty or just `.`. Single-step transformation using only in-memory string manipulation (`strings.ReplaceAll`). Silently returns computed string regardless of input — no error signaling.\n- **`makeLogEvent(onEvent func(Event)) LogEventFunc`** — Produces a `LogEventFunc` that forwards an `EventType` and message string to an optional callback, with nil-safety for the callback. Returns a ready-to-use closure without error handling.\n\n#### Side Effects\n\nNo external side effects. Both functions perform only in-memory operations: path separator replacement, default value fallback, and closure wrapping. No disk, network, database, or API interactions occur.\n\n---\n\n## Subsystem: `internal/security` — Security Boundaries \u0026 Inter-Process Exclusivity\n\n### Responsibility\n\nEnforces security boundaries and inter-process exclusivity during Code-Reducer operations. The module prevents directory-traversal attacks by constraining resolved paths within the repository root, ensures shared resources are not accessed concurrently via file locking, and integrates with version control to keep lock state out of tracked content. No mutable package-level state exists; all concurrency control is instance-scoped.\n\n### Data Flow\n\nThe two sentinel errors defined in `errors.go` (`ErrPathTraversal`, `ErrLockHeld`) serve as the terminal failure signals for path-validation and lock-contention scenarios. Callers receive these values through standard Go error-return patterns and propagate them via `if err != nil` checks; no panic, crash, or custom wrapping occurs within this package. The actual I/O operations that trigger these errors reside in other files not shown here.\n\n### Type: `SimpleLock`\n\n```go\ntype SimpleLock struct {\n    lockPath string\n    file     *os.File\n    mu       sync.Mutex\n    closed   bool\n}\n```\n\n**State characteristics:**\n\n- **`lockPath string`** — read-only after construction; never reassigned within this package. Assigned exclusively by the constructor.\n- **`file *os.File`** — modified inside `Unlock()` (`l.file = nil`) and assigned once in `AcquireLock()`.\n- **`mu sync.Mutex`** — value-type mutex embedded on each instance. Acquired and released only within `Unlock()` via defer.\n- **`closed bool`** — modified inside `Unlock()` (`l.closed = true`).\n\n#### `Unlock() error`\n\nCloses the underlying file descriptor, removes the lockfile atomically (ignoring errors if the file no longer exists), and sets internal state to closed. Safe to call multiple times or from different goroutines without deadlocking. Returns a non-nil error only when both close and remove fail; otherwise returns nil. No panic path observed.\n\n#### `SafeResolve(repoRoot string, inputPath string) (string, error)`\n\nResolves an arbitrary input path against the repository root boundary. Walks upward via `os.Lstat` until hitting the first physically-existing directory or reaching the root (`parent == current`). Symlinks are evaluated on existing ancestor components to defeat symlink-based traversal attacks. Non-existent suffix components are appended after the walk completes. If the relative path starts",
    "cmd": "# cmd Package — CLI Command Registry \u0026 Execution Engine\n\n## Module Responsibility\n\nThe `cmd` package implements the top-level CLI entry point for Code-Reducer, a tool that generates and maintains project wiki documentation via an LLM engine. It registers cobra commands (`init`, `update`), validates preconditions (Git repo presence, initialization state), resolves configuration from flags + on-disk settings + interactive prompts, and delegates execution to the engine runner while streaming status/error events through stdout/stderr.\n\n---\n\n## Data Flow Overview\n\n```\n┌─────────────┐     ┌──────────────┐     ┌─────────────────┐     ┌──────────────┐\n│ RootCmd      │────▶│ executeCommand│────▶│ engine.Run(...)  │────▶│ Config       │\n│ (entry point) │    │(\"init\"|\"update\")│   │                  │    │ SaveConfig    │\n└─────────────┘     └──────────────┘     └─────────────────┘     └──────────────┘\n      │                    │                     │                      │\n      ▼                    ▼                     ▼                      ▼\n  Git check            mode guard           event callback        file write\n```\n\nErrors flow as wrapped Go errors through cobra's `RunE` interface. Event-emitted errors (via callback) are printed to stderr and swallowed; only the runner's return error is re-wrapped with a descriptive prefix before propagation.\n\n---\n\n## init Command Registration (`cmd/init.go`)\n\n### API Surface\n\n- `initCmd` → unexported `*cobra.Command`, registered under `RootCmd`\n- `init()` → package-level `init` function that binds the command to cobra's registry\n\n### Execution Path\n\nThe `RunE` closure delegates directly to `executeCommand(\"init\")`. The actual implementation is not contained in this file. Errors propagate through cobra's command framework and exit via non-zero process code when a non-nil error is returned. No panic or crash path is introduced here.\n\n---\n\n## Root Command \u0026 Execution Loop (`cmd/root.go`)\n\n### Public API\n\n| Identifier | Type | Scope |\n|---|---|---|\n| `RootCmd` | `*cobra.Command` | package-level; initialized inline, never reassigned |\n\nAll other declarations (`modelIDFlag`, `numCtxFlag`, `executeCommand`) are unexported.\n\n### Precondition Validation Sequence\n\n1. **Git Repository Check** — `os.Getwd()` captures the current working directory; `tools.VerifyGitRepo(repoRoot)` checks for `.git` existence and equivalent state. Errors from this path propagate through cobra as wrapped errors via `%w`.\n2. **Configuration Existence** — `config.ConfigExists(repoRoot)` reads the local filesystem to confirm a configuration file is present. If missing *and* stdin is not a terminal (TTY), an implicit setup flow runs: `RunSetupFlow(repoRoot)` creates one and returns any error as-is.\n3. **Mode Guard** — `engine.IsInitialized(...)` validates that the requested operation matches the current state:\n   - `init` mode fails if documentation has already been initialized → suggests using `update` instead\n   - `update` mode fails if initialization has not occurred yet → requires running `init` first\n\n### Configuration Resolution\n\n`config.ResolveConfig(repoRoot, modelIDFlag, numCtxFlag)` merges CLI flags (`--model-id`, `--num-ctx`) with on-disk settings and environment variables. Returns the resolved config or a raw error from the config package (no wrapping). The LLM model ID flows through but is not consumed in this file; it persists for later use by the engine.\n\n### Signal Handling \u0026 Execution Loop\n\n`signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)` registers interrupt and termination signals on a background context. The handler calls `stop()` via deferred execution, enabling graceful shutdown propagation to the engine runner. This is the only concurrency primitive in this file; no goroutines or mutexes are used.\n\nThe resolved configuration is passed to `runner.Run(ctx, repoRoot, engine.Mode(mode), ...)`. Status events print to stdout (`fmt.Println(ev.Message)`), error events print to stderr (`fmt.Fprintf(os.Stderr, \"Error: %s\\n\", ev.Message)`), and other events print to stdout. Errors from the runner are wrapped with `\"documentation run failed: %w\"` before returning.\n\n---\n\n## Interactive Setup Wizard (`cmd/setup.go`)\n\n### Public API Surface\n\n| Function | Signature | Purpose |\n|---|---|---|\n| `RunSetupFlow` | `func RunSetupFlow(repoRoot string) error` | Orchestrates the full interactive configuration flow; returns non-nil only for filesystem-level failures (`os.Getwd`, `config.SaveConfig`) |\n| `promptStringList` | `func promptStringList(reader *bufio.Reader, promptMsg string, existingList []string) ([]string, bool)` | Collects comma-separated or list-style input with an existing value fallback; returns `(result, modified)` where `modified=false` signals no change |\n| `promptString` | `func promptString(reader *bufio.Reader, promptMsg string, existingVal string) string` | Collects single-string input with a default shown in brackets; returns trimmed input or `existingVal` on empty/error |\n\n### Algorithm Steps\n\n1. **Repo Root Resolution** — `os.Getwd()` captures the base path at start of `RunSetupFlow`.\n2. **State Loading** — `config.LoadConfig(repoRoot)` reads previously saved configuration; non-empty values (model ID, URL, context size, ignore list, docs dir) are extracted for pre-population.\n3. **Prompt Sequence** — Four prompts run in order: LLM Model ID, Ollama Base URL, Context Size, Documentation Directory. Each uses the `promptString`/`promptStringList` pattern with existing values as fallbacks.\n4. **System Prompt Preservation** — Existing values for extraction, synthesis, architecture, and file fact consolidation prompts are carried forward when present.\n5. **Configuration Assembly** — All user inputs merge with defaults/previous state into a single `Config` struct.\n6. **Persistence** — `config.SaveConfig(repoRoot, newCfg)` writes the assembled config to disk. Errors bubble up un-wrapped.\n\n### Error Handling Strategy\n\n- `promptString` / `promptStringList`: swallow `ReadString` errors → print warning to stderr → return existing value with no propagation beyond the function\n- `strconv.Atoi(ctxInputStr)`: swallow parse failure or non-positive result → fall back to `existingNumCtx`; no error propagates\n- Only filesystem-level failures (`Getwd`, `SaveConfig`) propagate up the call stack\n\n---\n\n## Update Command (`cmd/update.go`)\n\n### API Surface\n\nAll identifiers (`updateCmd`, etc.) begin with lowercase letters and are unexported. No public surface exists in this file.\n\n### Execution Path\n\nThe `RunE` closure delegates to `executeCommand(\"update\")`. The actual implementation is not contained in this file; errors propagate through cobra's command framework as returned by the delegate. No panic or crash recovery is handled within this file.",
    "internal": "# Code-Reducer Internal Architecture Documentation\n\n---\n\n## Subsystem: `internal/config` — Configuration Resolution \u0026 Persistence\n\n### Responsibility\n\nThe `config` package owns the lifecycle of a single configuration object (`*Config`) consumed by every downstream component. It provides three operations: resolve, persist, and query. All resolution is deterministic: given identical inputs, the returned `*Config` is identical. No global mutable state exists within the package; the only shared state is a package-level variable holding default extraction steps (`DefaultExtractionSteps`).\n\n### Data Flow\n\n```\nCLI flags (modelIDFlag, numCtxFlag) ─┐\nEnv vars (CODE_REDUCER_MODEL_ID,     │   ┌─────────────┐\nOLLAMA_BASE_URL, OLLAMA_NUM_CTX)    ─┼──►│ LoadConfig  │─► Config\nYAML file (.code-reducer.yaml)      └──\u003e│ getConfigPath│\n                                        └─────────────┘\n\nResolved *Config ──► SaveConfig() ──► disk: .code-reducer.yaml\n```\n\n### Constants\n\n| Constant | Value / Purpose |\n|---|---|\n| `CodeReducerModelIDEnvKey` | Env key `\"CODE_REDUCER_MODEL_ID\"` consumed by `ResolveConfig`. |\n| `OllamaBaseURLEnvKey` | Env key `\"OLLAMA_BASE_URL\"` consumed by both `LoadConfig` and `ResolveConfig`. |\n| `OllamaNumCtxEnvKey` | Env key `\"OLLAMA_NUM_CTX\"` consumed by `ResolveConfig`; value parsed via `strconv.Atoi`. |\n| `OllamaDefaultBaseURL` | Hard-coded fallback URL `\"http://localhost:11434\"`. |\n| `OllamaDefaultModelID` | Hard-coded fallback model identifier `\"ornith:9b\"`. |\n| `OllamaDefaultNumCtx` | Hard-coded fallback context size `8192`. |\n| `DefaultDocsDir` | Hard-coded fallback docs directory name `\"wiki\"`. |\n| `ConfigFileName` | Config file basename `\".code-reducer.yaml\"`. |\n\n### Types: `ExtractionStep` and `Config`\n\n```go\ntype ExtractionStep struct {\n    Name   string  // yaml:\"name\"\n    Prompt string  // yaml:\"prompt\"\n}\n\ntype Config struct {\n    ModelID                              string\n    OllamaBaseURL                       string\n    OllamaNumCtx                        int\n    DocsDir                             string\n    SystemPrompt                        string\n    ModuleSynthesisPrompt               string\n    ArchitecturePrompt                  string\n    FileFactConsolidationPrompt         string\n    ExtractionSteps                     []ExtractionStep\n    Ignore                              []string\n}\n```\n\n- `Config` is the sole configuration container. Every field maps to a distinct YAML key; all four prompt categories are independent strings with no cross-field dependencies.\n- `ExtractionStep` is an opaque two-string struct used only inside the slice field of `Config`. The package-level variable `DefaultExtractionSteps` holds a static instance initialized at declaration and never mutated within this file.\n\n### Variable: `DefaultExtractionSteps`\n\n```go\nvar DefaultExtractionSteps []ExtractionStep\n```\n\nPackage-level slice pre-populated with four entries covering API signatures, business logic, state and concurrency analysis, and errors/side effects. Read-only within the package boundary; any modification must occur outside this file.\n\n### Functions: Configuration Resolution (`internal/config/resolve.go`)\n\n#### `ResolveConfig(repoRoot string, modelIDFlag string, numCtxFlag string) (*Config, error)`\n\nMerges CLI flags, environment variables, YAML config, and built-in defaults into a single resolved `*Config`. Returns nil error on success; returns both a non-nil Config and an error only when the underlying file read fails with a condition that is not \"file does not exist\".\n\n**Parameters:**\n\n| Parameter | Purpose |\n|---|---|\n| `repoRoot` | Repository root path. Used to derive the config file path for the disk-read step. |\n| `modelIDFlag` | Explicit model ID from CLI; overrides environment and YAML only if non-empty. |\n| `numCtxFlag` | Explicit context size from CLI; parsed as int with a minimum-of-1 validation gate before taking effect. |\n\n**Resolution Priority Chains:** Every field follows an independent priority chain:\n\n- **Model ID** — built-in default → YAML `model_id` → env `CODE_REDUCER_MODEL_ID` → CLI flag (skipped if empty).\n- **Ollama Base URL** — built-in default → YAML `ollama_base_url` → env `OLLAMA_BASE_URL`. No CLI override.\n- **Ollama Context Size** — built-in default → YAML `ollama_num_ctx` → env `OLLAMA_NUM_CTX` → CLI flag (parsed via `strconv.Atoi`, minimum 1). Env and CLI values that fail to parse are silently skipped; the previously-resolved fallback remains in effect.\n- **DocsDir** — built-in default if unset, otherwise uses YAML-specified value. No env or CLI override path exists for this field.\n- **Prompts** (system, synthesis, architecture, fact consolidation) — each has its own independent chain: built-in default → YAML override if non-empty. No CLI or env override is provided in this code.\n\n**Error Handling:**\n\n| Condition | Behavior |\n|---|---|\n| `LoadConfig(repoRoot)` returns error + not-exist | Swallowed; replaced with an empty `\u0026Config{}` and returned alongside a nil error. |\n| `LoadConfig(repoRoot)` returns any other error | Wrapped as `\"failed to load configuration file: %w\"` and returned. |\n| Integer parsing (`strconv.Atoi`) fails | Skipped; previous fallback retained. No error propagated. |\n\n### Functions: Configuration Persistence (`internal/config/io.go`)\n\n#### `getConfigPath(cwd string) string`\n\nDerives the absolute path to `.code-reducer.yaml` by combining the working directory with the fixed filename constant. Return value is a plain string; no error path exists in this signature.\n\n#### `ConfigExists(cwd string) bool`\n\nReturns true if the config file is present at the derived path, false otherwise. Uses stat semantics internally (`os.Stat`). No error propagation — callers cannot distinguish \"file does not exist\" from other OS-level errors (e.g., permission denied).\n\n#### `LoadConfig(cwd string) (*Config, error)`\n\nReads raw bytes from disk at the resolved config path, then unmarshals them into a `*Config` struct via YAML parsing. Any parse failure is wrapped and returned as an error with descriptive prefix `\"failed to parse yaml config: %w\"`.\n\n#### `SaveConfig(cwd string, cfg *Config) error`\n\nPersists `*cfg` back to disk using an atomic rename pattern:\n\n1. Marshals the in-memory config to YAML.\n2. Normalizes key prefixes by inserting extra blank lines before specific top-level keys (`system_prompt`, `module_synthesis_prompt`, `architecture_prompt`, `file_fact_consolidation_prompt`, `extraction_steps`, `ignore`) — purpose of this normalization is not documented within the file boundary.\n3. Creates a temporary file in the same directory via `os.CreateTemp`.\n4. Writes normalized YAML to the temp file, syncs, and closes it.\n5. Sets restrictive permissions on the temp file via `chmod`.\n6. Renames the temp file over the original config path atomically.\n\nAny failure at any step (read, parse, write, sync, close, chmod, rename) is wrapped with a descriptive prefix (`\"failed to create temp file:\"`, etc.) and returned as an error. Cleanup of the temp file runs in a defer block so removal happens even when errors occur mid-save. A redundant explicit `Close()` call follows the deferred one; closing an already-closed file returns nil, so this is non-fatal but unnecessary.\n\n---\n\n## Subsystem: `internal/engine` — Incremental Documentation Synthesis Engine\n\n### Responsibility\n\nThis package orchestrates automatic generation of architecture overviews (`architecture.md`), quickstart guides (`quickstart.md`), and per-module API summaries for software repositories. It operates in two modes: **init** (full generation from scratch) and **update** (incremental regeneration triggered by file changes). The engine consumes source code through a hierarchical synthesis pipeline driven by LLM calls, persists intermediate state to disk for resumption between runs, and uses an exclusive lock file to serialize concurrent invocations.\n\n### Data Flow\n\n```\nRunner.Run(ctx, repoRoot, mode) \n    ↓ acquires lock via security.AcquireLock(repoRoot)\norchestrator.RunInit(ctx, ...) | orchestrator.RunUpdate(ctx, ...)\n    ↓ setupPipeline() loads gitignore + discovers code files + computes SHA256 hashes per file\ntree.buildTree(files) → DirNode hierarchy\n    ↓ synthesizeNode(root) recurses post-order: child dirs first (reusing cached summaries), then each file\nclient.CallLLM(...) — LLM API call for extraction step on chunked content\nreduceInChunks / reduceFileFacts — recursive text synthesis with batch partitioning and overlap handling\ncache.saveMetadataCache(repoRoot, docsDir, cache) — persists Files + Modules maps to disk as JSON\n```\n\n---\n\n### `cache.go` — Persistent Metadata Cache\n\n#### Types\n\n- **`FileCacheEntry`** — Key-value pair mapping a file's SHA256 hash to its synthesized facts string. Used as the per-file cache entry in `MetadataCache.Files`.\n- **`MetadataCache`** — Package-level cache state containing:\n  - `Version int` — Cache format version; mismatched versions return an empty initialized structure rather than corrupt data.\n  - `StepsHash string` — SHA256 of serialized extraction steps, used to invalidate the entire cache when configuration changes.\n  - `Files map[string]FileCacheEntry` — Per-file hash→facts mapping for incremental updates.\n  - `Modules map[string]string` — Per-directory synthesized summary storage.\n\n#### Public API Surface\n\n- **`IsInitialized(repoRoot, docsDir string) bool`** — Probes `{docsDir}/{metadataFileName}` existence; returns `false` on read failure (error swallowed).\n- **`saveMetadataCache(repoRoot, docsDir string, cache *MetadataCache) error`** — Serializes the metadata cache to indented JSON at `{docsDir}/{metadataFileName}`. Sets `cache.Version = currentCacheVersion` before marshaling. JSON marshal errors propagate; write failures propagate as-is.\n- **`computeSHA256(repoRoot, virtualPath string) (string, error)`** — Reads a source file via `tools.ReadFileSafely`, computes SHA256 hash in memory. Read errors propagated directly.\n\n#### Internal Functions\n\n- **`loadMetadataCache(repoRoot, docsDir string) (*MetadataCache, error)`** — Constructs an empty `MetadataCache` with fresh maps (`make(map[string]FileCacheEntry)`, `make(map[string]string)`). If the cache file exists and parses successfully with matching version, returns parsed state + nil. On `os.ErrNotExist`: returns default structure + **nil error** (silent recovery). Other read errors wrapped via `%w`. Version mismatch: returns clean cache + nil without signaling an error.\n\n#### Thread Safety Assessment\n\nNo synchronization primitives present. The maps in `MetadataCache` are created fresh and returned as struct values, but callers receive map references pointing to shared backing storage. Concurrent access to `Files`/`Modules` from multiple goroutines constitutes a data race unless external locking exists outside this module boundary. On-disk writes via `saveMetadataCache` provide coarse-grained serialization through OS write ordering alone; no explicit mutex is used.\n\n---\n\n### `chunking.go` — Recursive Hierarchical Text Synthesis\n\n#### Internal Functions (All Unexported)\n\n- **`reduceWithLLM(ctx, c llmCaller, items []string, sysPrompt string, buildPrompt func(batch []string) string, logMsg func(batch []string) string, errMsg string, logEvent LogEventFunc) (string, error)`** — Feeds a batch of items to an LLM via `c.CallLLM(ctx, sysPrompt, messages, false)`. System prompt describes the synthesis task. Response markdown fences are stripped before returning. Errors from `CallLLM` wrapped as `fmt.Errorf(\"%s: %w\", errMsg, err)` where `errMsg` is caller-supplied (e.g., `\"LLM error during synthesis\"`). Original error type information lost for classification purposes.\n\n- **`reduceInChunks(ctx, c llmCaller, nodePath string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)`** — Delegates to `reduceWithLLM`. No external I/O beyond the LLM call.\n\n- **`reduceFileFacts(ctx, c llmCaller, filePath string, stepName string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)`** — Delegates to `reduceWithLLM`. Used per extraction step during file synthesis in `synthesize.go`.\n\n- **`reduceItems(ctx, items []string, maxChars int, reduceFn func(batch []string) (string, error)) (string, error)`** — Core recursive reduction engine:\n  1. Partitions input items into batches where total character count stays within `maxChars`. Oversized single items are truncated in-place via `items[0] = string(runes[:maxChars]) + \"\\n...[truncated]\"` (mutation without signal).\n  2. If exactly one batch exists, calls the reduction function on it directly.\n  3. Otherwise, recursively reduces each sub-batch independently, concatenates results, and recurses until a single result remains — forming a binary tree of reductions.\n\n- **`chunkTextWithOverlap(text string, maxRunes int, overlapRunes int) []string`** — Splits a single text into overlapping rune-counted chunks at fixed step size. Used for preparing input sequences requiring positional overlap between adjacent pieces during file synthesis.\n\n#### State and Side Effects\n\n- `reduceItems` mutates `items[0]` in-place when a single oversized item is encountered. This silent truncation provides no error signal to callers; data loss goes undetected unless output string is inspected.\n- All other functions are pure (string manipulation, recursion) with the sole exception of the LLM API call inside `reduceWithLLM`.\n\n---\n\n### `client.go` — Ollama HTTP Client\n\n#### Types\n\n- **`Message`** — Struct with `Role string \\`json:\"role\"\\`` and `Content string \\`json:\"content\"\\``. Used as the conversational turn schema for LLM requests.\n- **`ollamaRequest`, `ollamaOptions`, `ollamaResponse`** — Unexported structs holding request/response payloads.\n\n#### Methods on `*llmClient`\n\n- **`newLLMClient(modelID, baseURL string, numCtx int) *llmClient`** — Constructor storing model identifier, base URL, and context size. Field assignments occur once; no subsequent mutation within the file.\n- **`(c *llmClient).NumCtx() int`** — Returns stored `numCtx` value. Read-only accessor.\n- **`(c *llmClient).prepareOllamaRequest(ctx, systemPrompt string, messages []Message, jsonFormat bool) (*http.Request, error)`** — Prepends the system prompt as a new entry to the messages array, then serializes model ID + conversation context + processing options into JSON. Constructs HTTP POST request targeting `{baseURL}/api/chat`. JSON marshal failures wrapped with `%w`; request construction errors propagated as-is.\n- **`(c *llmClient).CallLLM(ctx, systemPrompt string, messages []Message, jsonFormat bool) (string, error)`** — Executes the HTTP POST via `c.httpClient.Do` with timeout applied to the client itself (`\u0026http.Client{Timeout: defaultHTTPTimeout}`). On 200 status: deserializes response body fully and returns generated text. On non-OK status: reads up to `maxErrorBodyBytes` (value undefined in this file; if zero, `io.LimitReader(resp.Body, 0)` blocks indefinitely until EOF rather than applying a size limit), then returns formatted error `\"ollama api error: status %d, response: %s\"` where the body read failure is silently swallowed — only the HTTP status and successfully-read bytes are visible in the returned error.\n\n#### Unresolved References\n\n- `defaultHTTPTimeout` and `maxErrorBodyBytes` referenced but not defined in this file; resolved at link time or via another package constant. If these resolve to zero values, behavior deviates from intended timeout/limit semantics.\n\n---\n\n### `constants.go` — Runtime Configuration Contract\n\n#### Function Types\n\n- **`LogEventFunc func(EventType, string)`** — Signature for external observer hooks into system events (type + message), enabling traceability without hardcoding logging implementation.\n\n#### Constants and Values\n\nThis file defines the numerical boundaries governing engine behavior:\n- HTTP operation timeout: 10 minutes before aborting network calls.\n- Error response truncation limit: responses beyond 1 KB are truncated for non-critical error paths.\n- Chunked text processing: input split into overlapping segments of ~800 characters, enabling streaming across large documents without full loading.\n- Context window budgeting: only 75% of total memory reserved; minimum floor enforced at ~512 tokens regardless of available space.\n- Output scaling rule: maximum generated output capped at roughly 3× input length to prevent runaway generation loops.\n- Persistent state layer: engine writes `.metadata.json` snapshot and reads agent behavior from `AGENTS.md`, establishing a file-based identity layer for long-running processes.\n- Directory permissions: any directories the engine creates are assigned 0755 by default, controlling filesystem boundary access.\n\n---\n\n### `json_parser.go` — Markdown Fence Stripping Utility\n\n#### Internal Functions\n\n- **`stripOuterMarkdownFence(content string) string`** — Best-effort extraction of raw content from strings wrapped in backtick-delimited markdown or JSON fences (3+ backticks, optional language identifier like `markdown` or `json`). Algorithm: trim whitespace → test against regex pattern for fence syntax → if match found, extract interior between opening and closing fences; otherwise return trimmed input unchanged. Regex mismatch returns original with whitespace stripped — no error signaled.\n\n#### Side Effects\n\nNo external I/O. Only standard library `regexp` and `strings` used for in-memory processing. If a malformed regex were supplied at compile time it would panic during package initialization, but this is not handled within the function scope.\n\n---\n\n### `orchestrator.go` — Incremental Documentation Generation Orchestrator\n\n#### Types\n\n- **`EventType`** (`type string`) — Event classification type.\n- **`EventStatus`, `EventError`** (`constant EventType`) — Predefined event status/error constants.\n- **`Event struct`** — `Type EventType`, `Message string`. Carries typed event data with message payload.\n\n#### Methods on `*orchestrator`\n\n- **`GenerateStandardDocs(ctx, repoRoot, docsDir, rootSum string, cfg *config.Config, logEvent LogEventFunc) error`** — Generates `architecture.md` and `quickstart.md` from the synthesized root summary. Writes both files via `tools.WriteFileSafely`. Errors wrapped and propagated.\n- **`RunInit(ctx, repoRoot string, cfg *config.Config, onEvent func(Event)) error`** — Full generation path:\n  1. Loads `.gitignore`, user-configured ignore paths, metadata cache from disk.\n  2. Discovers code files respecting ignores; errors propagated directly as returned errors.\n  3. Computes SHA256 hashes per file (per-file failures logged + skipped from processing).\n  4. Builds hierarchical directory tree from all discovered files.\n  5. Marks every directory as \"affected\" for fresh generation.\n  6. Traverses tree bottom-up: each node's summary becomes part of its parent's input; leaf summaries flow up through directories, then into module-level summaries, converging at repository root.\n  7. Uses root-level synthesis result to generate global `architecture.md` and `quickstart.md`.\n  8. Ensures `.agent-guidelines.md` exists: if missing, creates fresh; if contains \"AI Agent Guidelines\", appends; otherwise replaces. Read failures for this file are swallowed — code proceeds as if empty/missing. Write errors wrapped and returned.\n\n- **`RunUpdate(ctx, repoRoot string, cfg *config.Config, onEvent func(Event)) error`** — Incremental path:\n  1. Detects file changes by comparing current hashes against cached ones — classifying each as Added, Modified, or Deleted.\n  2. Builds tree only from currently existing files; prunes cache entries for deleted files and removes module documentation for directories no longer present.\n  3. Determines affected directories based on change set; propagates \"affected\" status through hierarchy.\n  4. If nothing changed (zero affected directories), returns immediately with no regeneration.\n  5. Otherwise re-runs hierarchical synthesis within affected modules only.\n\n- **Private Functions** — `setupPipeline(repoRoot, cfg, logEvent) (string, []string, *MetadataCache)` loads gitignore + discovers files + computes hashes; errors from `LoadGitignore` swallowed after logging warning event. `teardownPipeline(repoRoot, docsDir, cache, logEvent, successMsg)` saves metadata cache — write failures logged as warnings but **not returned** (silently swallowed).\n\n#### Error Propagation Summary\n\n| Operation | Failure Handling |\n|---|---|\n| LLM call (`CallLLM`) | Wrapped + propagated as returned error |\n| Write file (`WriteFileSafely`) | Wrapped + propagated in main path; swallowed in teardown |\n| Read file (`ReadFileSafely` for `.agent-guidelines.md`) | Swallowed — falls through to write path |\n| Load gitignore | Logged warning, swallowed |\n| Discover files | Propagated as returned error |\n| SHA256 hash per file | Per-file: logged + skipped from processing |\n| MkdirAll | Wrapped + propagated |\n| SafeResolve (path existence) | Errors treated as \"not found\", swallowed |\n| os.Remove (cleanup) | Swallowed silently — stale files remain on disk if deletion fails |\n\n---\n\n### `runner.go` — Pipeline Execution Wrapper with Locking\n\n#### Types and Constants\n\n- **`Mode type string`** — Operation mode of the documentation pipeline.\n- **`ModeInit`, `ModeUpdate`** (`const Mode`) — Init mode constant and update mode constant.\n- **`Runner struct`** — Orchestrates execution of the documentation pipeline. Holds immutable reference to configuration set once via `NewRunner`.\n\n#### Methods on `*Runner`\n\n- **`NewRunner(cfg *config.Config) *Runner`** — Constructor storing configuration values (model ID, base URL, context size). No field reassignment within this file after construction.\n- **`(r *Runner) Run(ctx, repoRoot string, mode Mode, onEvent func(Event)) error`** — Main entry point:\n  1. Attempts to add lockfile entry to `.gitignore` via `security.EnsureGitignoreHasLockfile(repoRoot)`; failure swallowed — only logged via callback.\n  2. Acquires exclusive repository lock via `security.AcquireLock(repoRoot)`. Failure propagated with wrapping: `\"failed to acquire repository lock: %w\"`.\n  3. Defer unlock for after pipeline completes (success or error). No explicit error handling for unlock failures visible in this file.\n  4. Instantiates LLM client and orchestrator based on stored configuration values.\n  5. Dispatches either `RunInit` or `RunUpdate` depending on mode string match; unknown modes return `\"unsupported mode: %s\"` without wrapping.\n\n---\n\n### `synthesize.go` — Recursive Context-Aware Synthesis Engine\n\n#### Internal Structures and Functions\n\n- **`pipelineContext struct`** — Holds shared state for a synthesis run: LLM client, repository root, docs directory, configuration, extraction steps, affected directories set, precalculated hashes map. Locally allocated per method call in `RunInit`/`RunUpdate`.\n- **`synthesizeNode(p *pipelineContext, node *DirNode) (string, error)`** — Recursive synthesis walking the directory tree:\n  1. Post-order traversal: process children before files. For each child directory, recursively call `synthesizeNode`; if child is not affected and has a cached summary, reuse directly.\n  2. Per-file processing loop: compute or retrieve SHA256 hash (from precalculated hashes map or read-and-hash). On cache hit (hash matches stored entry), return cached facts without reading the file. On cache miss, read file content; if multiple chunks needed based on dynamic context window size × 4 chars/token × allocation ratio, split with overlap. For each chunk, run LLM extraction across all configured `ExtractionSteps`, building consolidated fact per step via `reduceFileFacts`.\n  3. Component assembly: build list of components — file facts (prefixed by filename) followed by child directory summaries (prefixed by subsystem name). If no components exist, cache empty string and return early.\n  4. Final reduction: combine all assembled components into single consolidated summary for current directory using `reduceInChunks`. Cache result under `p.cache.Modules[node.Path]` and write to disk at `\u003cdocsDir\u003e/modules/\u003csafe-filename-of-path\u003e`.\n\n#### External Interactions\n\n- **Disk Read**: `tools.ReadFileSafely(p.repoRoot, f)` reads individual source files when cache misses occur. Errors logged as warnings via callback but do not propagate — facts for that file remain empty string; processing continues to next file.\n- **Disk Write**: `tools.WriteFileSafely(p.repoRoot, modulePath, []byte(finalSum))` writes synthesized documentation at `\u003crepoRoot\u003e/\u003ccfg.DocsDir\u003e/modules/\u003csafe-filename\u003e`. Failures wrapped with context: `\"failed to write module documentation for %s: %w\"`. Propagates up through synthesis stack.\n- **Network/API**: `p.client.CallLLM(p.ctx, systemPrompt, []Message{...}, false)` makes LLM API calls per extraction step and chunk combination during file synthesis. Multiple sequential calls made per file (one per `cfg.ExtractionSteps`), multiple files processed before moving to child summary reduction.\n\n#### Thread Safety Assessment\n\nNo explicit locks/mutexes visible in this file. Context (`p.ctx`) checked at boundaries for cancellation detection but does not prevent concurrent map writes to the cache. If multiple goroutines invoke `synthesizeNode` with different `pipelineContext` instances sharing the same underlying `*MetadataCache`, both `cache.Files` and `cache.Modules` maps will be concurrently written without locking — a data race unless external synchronization exists outside this module boundary.\n\n---\n\n### `tree.go` — Directory Tree Building and Affected Detection\n\n#### Types\n\n- **`FileChange struct`** — `Path string` (file path), `Status string` (change status: `\"Added\"`, `\"Modified\"`, `\"Deleted\"`).\n- **`DirNode struct`** — `Path string` (directory path), `Files []string` (list of file paths in this directory), `Children map[string]*DirNode` (child directory nodes keyed by name).\n\n#### Internal Functions\n\n- **`buildTree(repoRoot, files []string) *DirNode`** — Walks each path's directory components to create nested `DirNode` objects with files listed under parent directories. Mutated during tree construction only; single-threaded build path.\n- **`determineAffected(p *pipelineContext, node *DirNode) bool`** — For each node: checks if any file in its `Files` list matches a changed file (marks as affected); resolves expected module path via safe filename conversion and secure resolution; if resolved path does not exist on disk, marks as affected (indicating missing module); checks external metadata cache (`cache.Modules`); if no cached value recorded for this directory's path, marks as affected.\n- **`propagateAffected(p *pipelineContext) map[string]bool`** — Walks tree again; any node whose subtree contains an affected descendant gets itself marked as affected. Parent directories inherit children's impact.\n\n#### External I/O and Error Propagation\n\n- `determineAffected`: calls `os.Stat(absModulePath)` after resolving a module path via `security.SafeResolve`. Only the \"not exist\" case is handled; if `SafeResolve` returns an error or `Stat` returns a non-not-exist error (e.g., permission denied, I/O fault), it is not checked and never propagated to callers.\n- All other functions (`propagateAffected`, `buildTree`) are pure in-memory operations using only string manipulation via standard library imports — no actual read/write calls.\n\n---\n\n### `utils.go` — Internal Utility Helpers\n\n#### Internal Functions (Both Unexported)\n\n- **`toSafeMarkdownFilename(path string) string`** — Converts a module path into a filesystem-safe markdown filename by replacing `/` with `_`, defaulting to `root.md` when the result is empty or just `.`. Single-step transformation using only in-memory string manipulation (`strings.ReplaceAll`). Silently returns computed string regardless of input — no error signaling.\n- **`makeLogEvent(onEvent func(Event)) LogEventFunc`** — Produces a `LogEventFunc` that forwards an `EventType` and message string to an optional callback, with nil-safety for the callback. Returns a ready-to-use closure without error handling.\n\n#### Side Effects\n\nNo external side effects. Both functions perform only in-memory operations: path separator replacement, default value fallback, and closure wrapping. No disk, network, database, or API interactions occur.\n\n---\n\n## Subsystem: `internal/security` — Security Boundaries \u0026 Inter-Process Exclusivity\n\n### Responsibility\n\nEnforces security boundaries and inter-process exclusivity during Code-Reducer operations. The module prevents directory-traversal attacks by constraining resolved paths within the repository root, ensures shared resources are not accessed concurrently via file locking, and integrates with version control to keep lock state out of tracked content. No mutable package-level state exists; all concurrency control is instance-scoped.\n\n### Data Flow\n\nThe two sentinel errors defined in `errors.go` (`ErrPathTraversal`, `ErrLockHeld`) serve as the terminal failure signals for path-validation and lock-contention scenarios. Callers receive these values through standard Go error-return patterns and propagate them via `if err != nil` checks; no panic, crash, or custom wrapping occurs within this package. The actual I/O operations that trigger these errors reside in other files not shown here.\n\n### Type: `SimpleLock`\n\n```go\ntype SimpleLock struct {\n    lockPath string\n    file     *os.File\n    mu       sync.Mutex\n    closed   bool\n}\n```\n\n**State characteristics:**\n\n- **`lockPath string`** — read-only after construction; never reassigned within this package. Assigned exclusively by the constructor.\n- **`file *os.File`** — modified inside `Unlock()` (`l.file = nil`) and assigned once in `AcquireLock()`.\n- **`mu sync.Mutex`** — value-type mutex embedded on each instance. Acquired and released only within `Unlock()` via defer.\n- **`closed bool`** — modified inside `Unlock()` (`l.closed = true`).\n\n#### `Unlock() error`\n\nCloses the underlying file descriptor, removes the lockfile atomically (ignoring errors if the file no longer exists), and sets internal state to closed. Safe to call multiple times or from different goroutines without deadlocking. Returns a non-nil error only when both close and remove fail; otherwise returns nil. No panic path observed.\n\n#### `SafeResolve(repoRoot string, inputPath string) (string, error)`\n\nResolves an arbitrary input path against the repository root boundary. Walks upward via `os.Lstat` until hitting the first physically-existing directory or reaching the root (`parent == current`). Symlinks are evaluated on existing ancestor components to defeat symlink-based traversal attacks. Non-existent suffix components are appended after the walk completes. If the relative path starts with `..`, it is rejected as a security violation and returns `ErrPathTraversal`. Any stat returning something other than ENOENT during the upward walk is returned immediately without masking — this could surface unexpected errors (e.g., EACCES) as fatal rather than being handled.\n\n#### `AcquireLock() (*SimpleLock, error)`\n\nResolves a lockfile path within the repository root using `SafeResolve`; atomically creates the file via O_WRONLY|O_CREATE|O_EXCL to prevent concurrent acquisition from another process; writes the current PID into the newly-created file. If lock path cannot be resolved → returns nil plus the original error from SafeResolve. If O_EXCL open fails with EEXIST → wraps `ErrLockHeld` and includes lockPath in message. If write to lockfile fails → closes file, removes lockfile, then returns wrapped error (prevents stale locks). No panic or crash path observed.\n\n#### `EnsureGitignoreHasLockfile(lockPath string) error`\n\nReads `.gitignore`; checks whether it already contains an entry for the lockfile; if not, appends a commented entry under a temp file written atomically via rename. Uses `Sync()` before rename for durability. Failure modes: unreadable `.gitignore` → propagates upstream error; read failure (non-ENOENT) → wrapped; temp-file creation/write/sync/chmod/rename failures → each wrapped with descriptive messages. Partial writes are not persisted if sync fails (atomic-ish via same-directory pattern).\n\n### Error Propagation Summary\n\n| Function | Failure Mode | Caller Impact |\n|---|---|---|\n| `SafeResolve` | Absolute path error, symlink resolution failure, stat-on-nonexistent-component error (only if not ENOENT), or path-traversal detection via `ErrPathTraversal`. | Any caller of `AcquireLock` or `EnsureGitignoreHasLockfile` receives the wrapped error directly. |\n| `AcquireLock` | Lock path cannot be resolved → returns nil + original error from SafeResolve. O_EXCL open fails with EEXIST → wraps `ErrLockHeld`. Write failure → closes file, removes lockfile, then returns wrapped error. | Caller receives non-nil error; no lock is held. No panic or crash path observed. |\n| `SimpleLock.Unlock()` | Close of underlying file fails → captured as err. Removal failure (other than ENOENT) only considered if close succeeded and removal also failed — otherwise swallowed (`if err == nil { err = removeErr }`). | Returns non-nil error only on double-failure; otherwise returns nil. No panic path. |\n| `EnsureGitignoreHasLockfile` | `.gitignore` cannot be resolved → propagates upstream. Read fails (non-ENOENT) → wraps error message. Temp file creation fails → wrapped. Write to temp file fails → wrapped. Sync fails → wrapped and returns early; subsequent close/rename skipped. Chmod fails → wrapped. Rename fails → wrapped. | Caller receives a descriptive error for each failure point. Sync-before-rename pattern means partial writes are not persisted if sync fails (atomic-ish). |\n\n### Notable Patterns / Risks\n\n- **`SafeResolve` loop**: The `for` loop calling `os.Lstat` walks upward until it finds an existing ancestor or reaches the root (`parent == current`). If a non-existent-component stat returns something other than ENOENT, it is returned immediately — this could mask unexpected errors (e.g., EACCES on a directory) as fatal rather than being handled.\n- **`.gitignore` rename**: The temp-file + rename pattern ensures atomicity but requires write access to the parent directory of `.gitignore`. Failure modes are well-covered, though no retry logic exists for transient I/O errors (e.g., NFS timeout during `Sync`).\n- **Lockfile cleanup on write failure**: In `AcquireLock`, if PID write fails, the lockfile is explicitly removed before returning error — this prevents stale locks from being left behind.\n\n---\n\n## Subsystem: `internal/tools` — Repository File Operations \u0026 Git Integration\n\n### Responsibility\n\nThe `internal/tools` package provides safe filesystem I/O primitives and Git command execution utilities for operating on a repository root directory. It is split into two files: file handling (`file_tools.go`) and Git interaction (`git_tools.go`). The module has no mutable state and performs only local disk operations.\n\n### Data Flow\n\n```\ncaller → [ReadFileSafely | WriteFileSafely] → security.SafeResolve (path validation) → OS open/write/rename\n```\n\n```\ncaller → [DiscoverCodeFiles | ShouldIgnoreFile] → gitignore parsing / extension whitelist / binary detection → []string or bool\n```\n\n```\ncaller → RunGit(repoRoot, args...) → exec.Command(\"git\", ...) → bytes.Buffer capture → stdout trimmed string\n```\n\n---\n\n### File I/O: `file_tools.go`\n\n#### Safe Read and Write\n\n**ReadFileSafely** resolves the virtual path against `repoRoot`, performs a TOCTOU-safe open-and-stat sequence to prevent symlink races, reads the file contents via `io.ReadAll`, and returns `(content []byte, error)`. All OS errors are wrapped with a descriptive prefix.\n\n**WriteFileSafely** ensures parent directories exist via `os.MkdirAll` (`defaultDirPerm = 0755`), creates a temporary file in the same directory as the destination using `os.CreateTemp`, writes content, syncs and closes the temp file, applies `Chmod` with `defaultFilePerm = 0644`, then atomically renames into place. Each step wraps its error; on failure, the deferred cleanup block unconditionally calls `tmpFile.Close()` and `os.Remove(tmpName)`. If `os.Rename` fails after `Chmod`, the destination path may be left in an inconsistent state with no rollback.\n\n#### Gitignore Loading and File Classification\n\n**LoadGitignore** reads `.gitignore` from `repoRoot` using `bufio.Scanner`. Non-comment lines are parsed via `ignore.CompileIgnoreLines`; existence errors return `(nil, nil)` (treated as success). Scanner errors at end-of-loop propagate directly.\n\n**ShouldIgnoreFile** evaluates whether a file at relative path is ignored by applying four checks in order:\n1. User-defined patterns from the loaded `GitIgnore` instance via `ignore.GitIgnore`.\n2. Dot-prefixed components or `.egg-info` suffixes in the path.\n3. Known text extension whitelist (go, js, ts, py, md, json, yaml, etc.).\n4. Binary detection: opens the file and scans 1024 bytes for null bytes; returns `true` if any are found.\n\nIf `SafeResolve` fails during classification, returns `true`.\n\n#### Source Discovery and Binary Detection\n\n**DiscoverCodeFiles** recursively walks the repo root via `filepath.WalkDir`, skipping directories matching gitignore patterns or dot-prefixed names. For each file, it runs the ignore classification; surviving relative paths are collected into a result list. Walk callback errors (directory traversal failures) are logged to `os.Stderr` and skipped (`return nil`). The final `(files, err)` from `filepath.WalkDir` is propagated as-is — callers receive whatever error the walker produced at completion.\n\n**IsBinaryFile** opens a file and reads 1024 bytes (constant `binaryDetectionBufSize = 1024`). Any `os.Open` error returns `true` (assumes binary). Reads other than `io.EOF` return `true`. Clean read with no null bytes returns `false`.\n\n#### Constants (`file_tools.go`)\n\n| Constant | Value | Description |\n|---|---|---|\n| `binaryDetectionBufSize` | 1024 | Buffer size for binary file detection |\n| `defaultDirPerm` | 0755 | Default directory permission mode |\n| `defaultFilePerm` | 0644 | Default file permission mode |\n\n#### Error Propagation (`file_tools.go`)\n\n- **Defensive wrapping**: `ReadFileSafely` and `WriteFileSafely` wrap every OS error with a descriptive prefix via `fmt.Errorf(\"...: %w\", err)`.\n- **Swallowed/logged only**: WalkDir errors in `DiscoverCodeFiles` are written to stderr and not returned. LoadGitignore treats non-existence as success. `ShouldIgnoreFile` returns `true` on resolution failure rather than propagating the error.\n- **Implicit assumptions**: `IsBinaryFile` assumes binary state when open or read errors occur.\n\n---\n\n### Git Operations: `git_tools.go`\n\n#### Public API Surface\n\n**RunGit(repoRoot string, args ...string)** executes an arbitrary git command within `repoRoot`. Prepends `--no-pager` to disable pager wrapping. Captures stdout and stderr into memory buffers (`bytes.Buffer`). On success returns `(trimmed string, nil)`. On failure returns `(trimmed string, error)` where the error message includes stderr content.\n\n**VerifyGitRepo(repoRoot string)** validates that `repoRoot` is inside a git working tree by invoking `git rev-parse --is-inside-work-tree`. If `RunGit` fails, wraps as `\"not a git repository (or any of the parent directories)\"`; underlying stderr from git is discarded at this layer.\n\n#### Subprocess Interaction Model\n\nThe only external system interaction in this file is spawning `git` as a child process via `exec.Command(\"git\", ...)` and waiting for completion with `cmd.Run()`. All other I/O (reading `.git` objects, filesystem traversal) occurs inside the git binary itself.\n\n#### Error Propagation (`git_tools.go`)\n\n- `cmd.Run()` error + non-zero exit → wrapped with both stderr text and exit code in a single `fmt.Errorf`.\n- Success with output → `(trimmed string, nil)`.\n- No panic/recover patterns — errors propagate as Go `error` interface values.",
    "internal/config": "# `internal/config` — Configuration Resolution \u0026 Persistence Module\n\n## Responsibility\n\nThe `internal/config` package owns the lifecycle of a single configuration object (`*Config`) consumed by every downstream component in the system. It provides three operations:\n\n1. **Resolve** — collapse four distinct input sources (CLI flags, environment variables, YAML file, built-in defaults) into one canonical `Config`.\n2. **Persist** — read and write a YAML config file at the project root via atomic rename.\n3. **Query** — inspect whether the config file exists on disk without loading its contents.\n\nAll resolution is deterministic: given identical inputs, the returned `*Config` is identical. No global mutable state exists within the package; the only shared state is a package-level variable holding default extraction steps (`DefaultExtractionSteps`).\n\n---\n\n## Constants\n\n| Constant | Value / Purpose |\n|---|---|\n| `CodeReducerModelIDEnvKey` | Env key `\"CODE_REDUCER_MODEL_ID\"` used by `ResolveConfig` to read model ID overrides from the process environment. |\n| `OllamaBaseURLEnvKey` | Env key `\"OLLAMA_BASE_URL\"` consumed by both `LoadConfig` and `ResolveConfig`. |\n| `OllamaNumCtxEnvKey` | Env key `\"OLLAMA_NUM_CTX\"` consumed by `ResolveConfig`; value is parsed via `strconv.Atoi`. |\n| `OllamaDefaultBaseURL` | Hard-coded fallback URL `\"http://localhost:11434\"`. |\n| `OllamaDefaultModelID` | Hard-coded fallback model identifier `\"ornith:9b\"`. |\n| `OllamaDefaultNumCtx` | Hard-coded fallback context size `8192`. |\n| `DefaultDocsDir` | Hard-coded fallback docs directory name `\"wiki\"`. |\n| `ConfigFileName` | Config file basename `\".code-reducer.yaml\"`. |\n| `DefaultSystemPrompt`, `DefaultModuleSynthesisPrompt`, `DefaultArchitecturePrompt`, `DefaultFileFactConsolidationPrompt` | Hard-coded prompt texts used as the zero-order fallback for every prompt category in resolution. |\n\n---\n\n## Types: `Config` and `ExtractionStep`\n\n```go\ntype ExtractionStep struct {\n    Name   string  // yaml:\"name\"\n    Prompt string  // yaml:\"prompt\"\n}\n\ntype Config struct {\n    ModelID                              string\n    OllamaBaseURL                       string\n    OllamaNumCtx                        int\n    DocsDir                             string\n    SystemPrompt                        string\n    ModuleSynthesisPrompt               string\n    ArchitecturePrompt                  string\n    FileFactConsolidationPrompt         string\n    ExtractionSteps                     []ExtractionStep\n    Ignore                              []string\n}\n```\n\n- `Config` is the sole configuration container. Every field maps to a distinct YAML key; all four prompt categories are independent strings with no cross-field dependencies.\n- `ExtractionStep` is an opaque two-string struct used only inside the slice field of `Config`. The package-level variable `DefaultExtractionSteps` holds a static instance initialized at declaration and never mutated within this file; it serves as the zero-order extraction pipeline when the YAML file does not specify one.\n\n---\n\n## Types: `ExtractionStep` (definition)\n\n- **Name** — human-readable label for the step, serialized under `\"name\"` in YAML.\n- **Prompt** — prompt text injected into the LLM during that extraction phase, serialized under `\"prompt\"`.\n\n---\n\n## Variables: `DefaultExtractionSteps`\n\n```go\nvar DefaultExtractionSteps []ExtractionStep\n```\n\nPackage-level slice pre-populated with four entries covering API signatures, business logic, state and concurrency analysis, and errors/side effects. The variable is read-only within the package boundary; any modification must occur outside this file.\n\n---\n\n## Functions: Configuration Resolution (`internal/config/resolve.go`)\n\n### `ResolveConfig(repoRoot string, modelIDFlag string, numCtxFlag string) (*Config, error)`\n\nMerges CLI flags, environment variables, YAML config, and built-in defaults into a single resolved `*Config`. Returns a nil error on success; returns both a non-nil Config and an error only when the underlying file read fails with a condition that is not \"file does not exist\".\n\n#### Parameters\n\n| Parameter | Purpose |\n|---|---|\n| `repoRoot` | Repository root path. Used to derive the config file path for the disk-read step. |\n| `modelIDFlag` | Explicit model ID from CLI; overrides environment and YAML only if non-empty. |\n| `numCtxFlag` | Explicit context size from CLI; parsed as int with a minimum-of-1 validation gate before taking effect. |\n\n#### Resolution Priority Chains\n\nEvery field follows an independent priority chain. The order is the same across all chains except where documented otherwise:\n\n- **Model ID** — built-in default → YAML `model_id` → env `CODE_REDUCER_MODEL_ID` → CLI flag (skipped if empty).\n- **Ollama Base URL** — built-in default → YAML `ollama_base_url` → env `OLLAMA_BASE_URL`. No CLI override.\n- **Ollama Context Size** — built-in default → YAML `ollama_num_ctx` → env `OLLAMA_NUM_CTX` → CLI flag (parsed via `strconv.Atoi`, minimum 1). Env and CLI values that fail to parse are silently skipped; the previously-resolved fallback remains in effect.\n- **DocsDir** — built-in default if unset, otherwise uses YAML-specified value. No env or CLI override path exists for this field.\n- **Prompts** (system, synthesis, architecture, fact consolidation) — each has its own independent chain: built-in default → YAML override if non-empty. No CLI or env override is provided in this code.\n\n#### Error Handling\n\n| Condition | Behavior |\n|---|---|\n| `LoadConfig(repoRoot)` returns error + not-exist | Swallowed; replaced with an empty `\u0026Config{}` and returned alongside a nil error. |\n| `LoadConfig(repoRoot)` returns any other error | Wrapped as `\"failed to load configuration file: %w\"` and returned. |\n| Integer parsing (`strconv.Atoi`) fails | Skipped; previous fallback retained. No error propagated. |\n\n---\n\n## Functions: Configuration Persistence (`internal/config/io.go`)\n\n### `getConfigPath(cwd string) string`\n\nDerives the absolute path to `.code-reducer.yaml` by combining the working directory with the fixed filename constant. Return value is a plain string; no error path exists in this signature.\n\n### `ConfigExists(cwd string) bool`\n\nReturns true if the config file is present at the derived path, false otherwise. Uses stat semantics internally (`os.Stat`). No error propagation — callers cannot distinguish \"file does not exist\" from other OS-level errors (e.g., permission denied).\n\n### `LoadConfig(cwd string) (*Config, error)`\n\nReads raw bytes from disk at the resolved config path, then unmarshals them into a `*Config` struct via YAML parsing. Any parse failure is wrapped and returned as an error with descriptive prefix `\"failed to parse yaml config: %w\"`.\n\n### `SaveConfig(cwd string, cfg *Config) error`\n\nPersists `*cfg` back to disk using an atomic rename pattern:\n\n1. Marshals the in-memory config to YAML.\n2. Normalizes key prefixes by inserting extra blank lines before specific top-level keys (`system_prompt`, `module_synthesis_prompt`, `architecture_prompt`, `file_fact_consolidation_prompt`, `extraction_steps`, `ignore`) — purpose of this normalization is not documented within the file boundary.\n3. Creates a temporary file in the same directory via `os.CreateTemp`.\n4. Writes normalized YAML to the temp file, syncs, and closes it.\n5. Sets restrictive permissions on the temp file via `chmod`.\n6. Renames the temp file over the original config path atomically.\n\nAny failure at any step (read, parse, write, sync, close, chmod, rename) is wrapped with a descriptive prefix (`\"failed to create temp file:\"`, etc.) and returned as an error. Cleanup of the temp file runs in a defer block so removal happens even when errors occur mid-save. A redundant explicit `Close()` call follows the deferred one; closing an already-closed file returns nil, so this is non-fatal but unnecessary.\n\n---\n\n## Data Flow Summary\n\n```\nCLI flags (modelIDFlag, numCtxFlag) ─┐\n                                      ├──\u003e ResolveConfig()\nEnv vars (CODE_REDUCER_MODEL_ID,     │   ┌─────────────┐\nOLLAMA_BASE_URL, OLLAMA_NUM_CTX)    ─┼──►│ LoadConfig  │─► Config\nYAML file (.code-reducer.yaml)      └──\u003e│ getConfigPath│\n                                        └─────────────┘\n\nResolved *Config ──► SaveConfig() ──► disk: .code-reducer.yaml\n```\n\nThe `*Config` returned by `ResolveConfig` is the single source of truth for every downstream component. All other functions in the package either read from or write to this same object — there is no alternative configuration path within the module boundary.",
    "internal/engine": "# internal/engine — Incremental Documentation Synthesis Engine\n\n## Module Responsibility\n\nThis package orchestrates automatic generation of architecture overviews (`architecture.md`), quickstart guides (`quickstart.md`), and per-module API summaries for software repositories. It operates in two modes: **init** (full generation from scratch) and **update** (incremental regeneration triggered by file changes). The engine consumes source code through a hierarchical synthesis pipeline driven by LLM calls, persists intermediate state to disk for resumption between runs, and uses an exclusive lock file to serialize concurrent invocations.\n\n## Data Flow\n\n```\nRunner.Run(ctx, repoRoot, mode) \n    ↓ acquires lock via security.AcquireLock(repoRoot)\norchestrator.RunInit(ctx, ...) | orchestrator.RunUpdate(ctx, ...)\n    ↓ setupPipeline() loads gitignore + discovers code files + computes SHA256 hashes per file\ntree.buildTree(files) → DirNode hierarchy\n    ↓ synthesizeNode(root) recurses post-order: child dirs first (reusing cached summaries), then each file\nclient.CallLLM(...) — LLM API call for extraction step on chunked content\nreduceInChunks / reduceFileFacts — recursive text synthesis with batch partitioning and overlap handling\ncache.saveMetadataCache(repoRoot, docsDir, cache) — persists Files + Modules maps to disk as JSON\n```\n\n## File-Level Documentation\n\n### `cache.go` — Persistent Metadata Cache\n\n#### Types\n\n- **`FileCacheEntry`** — Key-value pair mapping a file's SHA256 hash to its synthesized facts string. Used as the per-file cache entry in `MetadataCache.Files`.\n- **`MetadataCache`** — Package-level cache state containing:\n  - `Version int` — Cache format version; mismatched versions return an empty initialized structure rather than corrupt data.\n  - `StepsHash string` — SHA256 of serialized extraction steps, used to invalidate the entire cache when configuration changes.\n  - `Files map[string]FileCacheEntry` — Per-file hash→facts mapping for incremental updates.\n  - `Modules map[string]string` — Per-directory synthesized summary storage.\n\n#### Public API Surface\n\n- **`IsInitialized(repoRoot, docsDir string) bool`** — Probes `{docsDir}/{metadataFileName}` existence; returns `false` on read failure (error swallowed).\n- **`saveMetadataCache(repoRoot, docsDir string, cache *MetadataCache) error`** — Serializes the metadata cache to indented JSON at `{docsDir}/{metadataFileName}`. Sets `cache.Version = currentCacheVersion` before marshaling. JSON marshal errors propagate; write failures propagate as-is.\n- **`computeSHA256(repoRoot, virtualPath string) (string, error)`** — Reads a source file via `tools.ReadFileSafely`, computes SHA256 hash in memory. Read errors propagated directly.\n\n#### Internal Functions\n\n- **`loadMetadataCache(repoRoot, docsDir string) (*MetadataCache, error)`** — Constructs an empty `MetadataCache` with fresh maps (`make(map[string]FileCacheEntry)`, `make(map[string]string)`). If the cache file exists and parses successfully with matching version, returns parsed state + nil. On `os.ErrNotExist`: returns default structure + **nil error** (silent recovery). Other read errors wrapped via `%w`. Version mismatch: returns clean cache + nil without signaling an error.\n\n#### Thread Safety\n\nNo synchronization primitives present. The maps in `MetadataCache` are created fresh and returned as struct values, but callers receive map references pointing to shared backing storage. Concurrent access to `Files`/`Modules` from multiple goroutines constitutes a data race unless external locking exists outside this module boundary. On-disk writes via `saveMetadataCache` provide coarse-grained serialization through OS write ordering alone; no explicit mutex is used.\n\n---\n\n### `chunking.go` — Recursive Hierarchical Text Synthesis\n\n#### Internal Functions (All Unexported)\n\n- **`reduceWithLLM(ctx, c llmCaller, items []string, sysPrompt string, buildPrompt func(batch []string) string, logMsg func(batch []string) string, errMsg string, logEvent LogEventFunc) (string, error)`** — Feeds a batch of items to an LLM via `c.CallLLM(ctx, sysPrompt, messages, false)`. System prompt describes the synthesis task. Response markdown fences are stripped before returning. Errors from `CallLLM` wrapped as `fmt.Errorf(\"%s: %w\", errMsg, err)` where `errMsg` is caller-supplied (e.g., `\"LLM error during synthesis\"`). Original error type information lost for classification purposes.\n\n- **`reduceInChunks(ctx, c llmCaller, nodePath string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)`** — Delegates to `reduceWithLLM`. No external I/O beyond the LLM call.\n\n- **`reduceFileFacts(ctx, c llmCaller, filePath string, stepName string, items []string, cfg *config.Config, logEvent LogEventFunc) (string, error)`** — Delegates to `reduceWithLLM`. Used per extraction step during file synthesis in `synthesize.go`.\n\n- **`reduceItems(ctx, items []string, maxChars int, reduceFn func(batch []string) (string, error)) (string, error)`** — Core recursive reduction engine:\n  1. Partitions input items into batches where total character count stays within `maxChars`. Oversized single items are truncated in-place via `items[0] = string(runes[:maxChars]) + \"\\n...[truncated]\"` (mutation without signal).\n  2. If exactly one batch exists, calls the reduction function on it directly.\n  3. Otherwise, recursively reduces each sub-batch independently, concatenates results, and recurses until a single result remains — forming a binary tree of reductions.\n\n- **`chunkTextWithOverlap(text string, maxRunes int, overlapRunes int) []string`** — Splits a single text into overlapping rune-counted chunks at fixed step size. Used for preparing input sequences requiring positional overlap between adjacent pieces during file synthesis.\n\n#### State and Side Effects\n\n- `reduceItems` mutates `items[0]` in-place when a single oversized item is encountered. This silent truncation provides no error signal to callers; data loss goes undetected unless output string is inspected.\n- All other functions are pure (string manipulation, recursion) with the sole exception of the LLM API call inside `reduceWithLLM`.\n\n---\n\n### `client.go` — Ollama HTTP Client\n\n#### Types\n\n- **`Message`** — Struct with `Role string \\`json:\"role\"\\`` and `Content string \\`json:\"content\"\\``. Used as the conversational turn schema for LLM requests.\n- **`ollamaRequest`, `ollamaOptions`, `ollamaResponse`** — Unexported structs holding request/response payloads.\n\n#### Methods on `*llmClient`\n\n- **`newLLMClient(modelID, baseURL string, numCtx int) *llmClient`** — Constructor storing model identifier, base URL, and context size. Field assignments occur once; no subsequent mutation within the file.\n- **`(c *llmClient).NumCtx() int`** — Returns stored `numCtx` value. Read-only accessor.\n- **`(c *llmClient).prepareOllamaRequest(ctx, systemPrompt string, messages []Message, jsonFormat bool) (*http.Request, error)`** — Prepends the system prompt as a new entry to the messages array, then serializes model ID + conversation context + processing options into JSON. Constructs HTTP POST request targeting `{baseURL}/api/chat`. JSON marshal failures wrapped with `%w`; request construction errors propagated as-is.\n- **`(c *llmClient).CallLLM(ctx, systemPrompt string, messages []Message, jsonFormat bool) (string, error)`** — Executes the HTTP POST via `c.httpClient.Do` with timeout applied to the client itself (`\u0026http.Client{Timeout: defaultHTTPTimeout}`). On 200 status: deserializes response body fully and returns generated text. On non-OK status: reads up to `maxErrorBodyBytes` (value undefined in this file; if zero, `io.LimitReader(resp.Body, 0)` blocks indefinitely until EOF rather than applying a size limit), then returns formatted error `\"ollama api error: status %d, response: %s\"` where the body read failure is silently swallowed — only the HTTP status and successfully-read bytes are visible in the returned error.\n\n#### Unresolved References\n\n- `defaultHTTPTimeout` and `maxErrorBodyBytes` referenced but not defined in this file; resolved at link time or via another package constant. If these resolve to zero values, behavior deviates from intended timeout/limit semantics.\n\n---\n\n### `constants.go` — Runtime Configuration Contract\n\n#### Function Types\n\n- **`LogEventFunc func(EventType, string)`** — Signature for external observer hooks into system events (type + message), enabling traceability without hardcoding logging implementation.\n\n#### Constants and Values\n\nThis file defines the numerical boundaries governing engine behavior:\n- HTTP operation timeout: 10 minutes before aborting network calls.\n- Error response truncation limit: responses beyond 1 KB are truncated for non-critical error paths.\n- Chunked text processing: input split into overlapping segments of ~800 characters, enabling streaming across large documents without full loading.\n- Context window budgeting: only 75% of total memory reserved; minimum floor enforced at ~512 tokens regardless of available space.\n- Output scaling rule: maximum generated output capped at roughly 3× input length to prevent runaway generation loops.\n- Persistent state layer: engine writes `.metadata.json` snapshot and reads agent behavior from `AGENTS.md`, establishing a file-based identity layer for long-running processes.\n- Directory permissions: any directories the engine creates are assigned 0755 by default, controlling filesystem boundary access.\n\n---\n\n### `json_parser.go` — Markdown Fence Stripping Utility\n\n#### Internal Functions\n\n- **`stripOuterMarkdownFence(content string) string`** — Best-effort extraction of raw content from strings wrapped in backtick-delimited markdown or JSON fences (3+ backticks, optional language identifier like `markdown` or `json`). Algorithm: trim whitespace → test against regex pattern for fence syntax → if match found, extract interior between opening and closing fences; otherwise return trimmed input unchanged. Regex mismatch returns original with whitespace stripped — no error signaled.\n\n#### Side Effects\n\nNo external I/O. Only standard library `regexp` and `strings` used for in-memory processing. If a malformed regex were supplied at compile time it would panic during package initialization, but this is not handled within the function scope.\n\n---\n\n### `orchestrator.go` — Incremental Documentation Generation Orchestrator\n\n#### Types\n\n- **`EventType`** (`type string`) — Event classification type.\n- **`EventStatus`, `EventError`** (`constant EventType`) — Predefined event status/error constants.\n- **`Event struct`** — `Type EventType`, `Message string`. Carries typed event data with message payload.\n\n#### Methods on `*orchestrator`\n\n- **`GenerateStandardDocs(ctx, repoRoot, docsDir, rootSum string, cfg *config.Config, logEvent LogEventFunc) error`** — Generates `architecture.md` and `quickstart.md` from the synthesized root summary. Writes both files via `tools.WriteFileSafely`. Errors wrapped and propagated.\n- **`RunInit(ctx, repoRoot string, cfg *config.Config, onEvent func(Event)) error`** — Full generation path:\n  1. Loads `.gitignore`, user-configured ignore paths, metadata cache from disk.\n  2. Discovers code files respecting ignores; errors propagated directly as returned errors.\n  3. Computes SHA256 hashes per file (per-file failures logged + skipped from processing).\n  4. Builds hierarchical directory tree from all discovered files.\n  5. Marks every directory as \"affected\" for fresh generation.\n  6. Traverses tree bottom-up: each node's summary becomes part of its parent's input; leaf summaries flow up through directories, then into module-level summaries, converging at repository root.\n  7. Uses root-level synthesis result to generate global `architecture.md` and `quickstart.md`.\n  8. Ensures `.agent-guidelines.md` exists: if missing, creates fresh; if contains \"AI Agent Guidelines\", appends; otherwise replaces. Read failures for this file are swallowed — code proceeds as if empty/missing. Write errors wrapped and returned.\n\n- **`RunUpdate(ctx, repoRoot string, cfg *config.Config, onEvent func(Event)) error`** — Incremental path:\n  1. Detects file changes by comparing current hashes against cached ones — classifying each as Added, Modified, or Deleted.\n  2. Builds tree only from currently existing files; prunes cache entries for deleted files and removes module documentation for directories no longer present.\n  3. Determines affected directories based on change set; propagates \"affected\" status through hierarchy.\n  4. If nothing changed (zero affected directories), returns immediately with no regeneration.\n  5. Otherwise re-runs hierarchical synthesis within affected modules only.\n\n- **Private Functions** — `setupPipeline(repoRoot, cfg, logEvent) (string, []string, *MetadataCache)` loads gitignore + discovers files + computes hashes; errors from `LoadGitignore` swallowed after logging warning event. `teardownPipeline(repoRoot, docsDir, cache, logEvent, successMsg)` saves metadata cache — write failures logged as warnings but **not returned** (silently swallowed).\n\n#### Error Propagation Summary\n\n| Operation | Failure Handling |\n|---|---|\n| LLM call (`CallLLM`) | Wrapped + propagated as returned error |\n| Write file (`WriteFileSafely`) | Wrapped + propagated in main path; swallowed in teardown |\n| Read file (`ReadFileSafely` for `.agent-guidelines.md`) | Swallowed — falls through to write path |\n| Load gitignore | Logged warning, swallowed |\n| Discover files | Propagated as returned error |\n| SHA256 hash per file | Per-file: logged + skipped from processing |\n| MkdirAll | Wrapped + propagated |\n| SafeResolve (path existence) | Errors treated as \"not found\", swallowed |\n| os.Remove (cleanup) | Swallowed silently — stale files remain on disk if deletion fails |\n\n---\n\n### `runner.go` — Pipeline Execution Wrapper with Locking\n\n#### Types and Constants\n\n- **`Mode type string`** — Operation mode of the documentation pipeline.\n- **`ModeInit`, `ModeUpdate`** (`const Mode`) — Init mode constant and update mode constant.\n- **`Runner struct`** — Orchestrates execution of the documentation pipeline. Holds immutable reference to configuration set once via `NewRunner`.\n\n#### Methods on `*Runner`\n\n- **`NewRunner(cfg *config.Config) *Runner`** — Constructor storing configuration values (model ID, base URL, context size). No field reassignment within this file after construction.\n- **`(r *Runner) Run(ctx, repoRoot string, mode Mode, onEvent func(Event)) error`** — Main entry point:\n  1. Attempts to add lockfile entry to `.gitignore` via `security.EnsureGitignoreHasLockfile(repoRoot)`; failure swallowed — only logged via callback.\n  2. Acquires exclusive repository lock via `security.AcquireLock(repoRoot)`. Failure propagated with wrapping: `\"failed to acquire repository lock: %w\"`.\n  3. Defer unlock for after pipeline completes (success or error). No explicit error handling for unlock failures visible in this file.\n  4. Instantiates LLM client and orchestrator based on stored configuration values.\n  5. Dispatches either `RunInit` or `RunUpdate` depending on mode string match; unknown modes return `\"unsupported mode: %s\"` without wrapping.\n\n---\n\n### `synthesize.go` — Recursive Context-Aware Synthesis Engine\n\n#### Internal Structures and Functions\n\n- **`pipelineContext struct`** — Holds shared state for a synthesis run: LLM client, repository root, docs directory, configuration, extraction steps, affected directories set, precalculated hashes map. Locally allocated per method call in `RunInit`/`RunUpdate`.\n- **`synthesizeNode(p *pipelineContext, node *DirNode) (string, error)`** — Recursive synthesis walking the directory tree:\n  1. Post-order traversal: process children before files. For each child directory, recursively call `synthesizeNode`; if child is not affected and has a cached summary, reuse directly.\n  2. Per-file processing loop: compute or retrieve SHA256 hash (from precalculated hashes map or read-and-hash). On cache hit (hash matches stored entry), return cached facts without reading the file. On cache miss, read file content; if multiple chunks needed based on dynamic context window size × 4 chars/token × allocation ratio, split with overlap. For each chunk, run LLM extraction across all configured `ExtractionSteps`, building consolidated fact per step via `reduceFileFacts`.\n  3. Component assembly: build list of components — file facts (prefixed by filename) followed by child directory summaries (prefixed by subsystem name). If no components exist, cache empty string and return early.\n  4. Final reduction: combine all assembled components into single consolidated summary for current directory using `reduceInChunks`. Cache result under `p.cache.Modules[node.Path]` and write to disk at `\u003cdocsDir\u003e/modules/\u003csafe-filename-of-path\u003e`.\n\n#### External Interactions\n\n- **Disk Read**: `tools.ReadFileSafely(p.repoRoot, f)` reads individual source files when cache misses occur. Errors logged as warnings via callback but do not propagate — facts for that file remain empty string; processing continues to next file.\n- **Disk Write**: `tools.WriteFileSafely(p.repoRoot, modulePath, []byte(finalSum))` writes synthesized documentation at `\u003crepoRoot\u003e/\u003ccfg.DocsDir\u003e/modules/\u003csafe-filename\u003e`. Failures wrapped with context: `\"failed to write module documentation for %s: %w\"`. Propagates up through synthesis stack.\n- **Network/API**: `p.client.CallLLM(p.ctx, systemPrompt, []Message{...}, false)` makes LLM API calls per extraction step and chunk combination during file synthesis. Multiple sequential calls made per file (one per `cfg.ExtractionSteps`), multiple files processed before moving to child summary reduction.\n\n#### Thread Safety Assessment\n\nNo explicit locks/mutexes visible in this file. Context (`p.ctx`) checked at boundaries for cancellation detection but does not prevent concurrent map writes to the cache. If multiple goroutines invoke `synthesizeNode` with different `pipelineContext` instances sharing the same underlying `*MetadataCache`, both `cache.Files` and `cache.Modules` maps will be concurrently written without locking — a data race unless external synchronization exists outside this module boundary.\n\n---\n\n### `tree.go` — Directory Tree Building and Affected Detection\n\n#### Types\n\n- **`FileChange struct`** — `Path string` (file path), `Status string` (change status: `\"Added\"`, `\"Modified\"`, `\"Deleted\"`).\n- **`DirNode struct`** — `Path string` (directory path), `Files []string` (list of file paths in this directory), `Children map[string]*DirNode` (child directory nodes keyed by name).\n\n#### Internal Functions\n\n- **`buildTree(repoRoot, files []string) *DirNode`** — Walks each path's directory components to create nested `DirNode` objects with files listed under parent directories. Mutated during tree construction only; single-threaded build path.\n- **`determineAffected(p *pipelineContext, node *DirNode) bool`** — For each node: checks if any file in its `Files` list matches a changed file (marks as affected); resolves expected module path via safe filename conversion and secure resolution; if resolved path does not exist on disk, marks as affected (indicating missing module); checks external metadata cache (`cache.Modules`); if no cached value recorded for this directory's path, marks as affected.\n- **`propagateAffected(p *pipelineContext) map[string]bool`** — Walks tree again; any node whose subtree contains an affected descendant gets itself marked as affected. Parent directories inherit children's impact.\n\n#### External I/O and Error Propagation\n\n- `determineAffected`: calls `os.Stat(absModulePath)` after resolving a module path via `security.SafeResolve`. Only the \"not exist\" case is handled; if `SafeResolve` returns an error or `Stat` returns a non-not-exist error (e.g., permission denied, I/O fault), it is not checked and never propagated to callers.\n- All other functions (`propagateAffected`, `buildTree`) are pure in-memory operations using only string manipulation via standard library imports — no actual read/write calls.\n\n---\n\n### `utils.go` — Internal Utility Helpers\n\n#### Internal Functions (Both Unexported)\n\n- **`toSafeMarkdownFilename(path string) string`** — Converts a module path into a filesystem-safe markdown filename by replacing `/` with `_`, defaulting to `root.md` when the result is empty or just `.`. Single-step transformation using only in-memory string manipulation (`strings.ReplaceAll`). Silently returns computed string regardless of input — no error signaling.\n- **`makeLogEvent(onEvent func(Event)) LogEventFunc`** — Produces a `LogEventFunc` that forwards an `EventType` and message string to an optional callback, with nil-safety for the callback. Returns a ready-to-use closure without error handling.\n\n#### Side Effects\n\nNo external side effects. Both functions perform only in-memory operations: path separator replacement, default value fallback, and closure wrapping. No disk, network, database, or API interactions occur.",
    "internal/security": "# Module: internal/security\n\n## Responsibility\n\nEnforces security boundaries and inter-process exclusivity during Code-Reducer operations. The module prevents directory-traversal attacks by constraining resolved paths within the repository root, ensures shared resources are not accessed concurrently via file locking, and integrates with version control to keep lock state out of tracked content. No mutable package-level state exists; all concurrency control is instance-scoped.\n\n## Data Flow\n\nThe two sentinel errors defined in `errors.go` (`ErrPathTraversal`, `ErrLockHeld`) serve as the terminal failure signals for path-validation and lock-contention scenarios. Callers receive these values through standard Go error-return patterns and propagate them via `if err != nil` checks; no panic, crash, or custom wrapping occurs within this package. The actual I/O operations that trigger these errors reside in other files not shown here.\n\n## Public API: `SimpleLock`\n\n### Type Definition\n\n```go\ntype SimpleLock struct {\n    lockPath string\n    file     *os.File\n    mu       sync.Mutex\n    closed   bool\n}\n```\n\n**State characteristics:**\n\n- **`lockPath string`** — read-only after construction; never reassigned within this package. Assigned exclusively by the constructor.\n- **`file *os.File`** — modified inside `Unlock()` (`l.file = nil`) and assigned once in `AcquireLock()`.\n- **`mu sync.Mutex`** — value-type mutex embedded on each instance. Acquired and released only within `Unlock()` via defer.\n- **`closed bool`** — modified inside `Unlock()` (`l.closed = true`).\n\n### Methods\n\n#### `Unlock() error`\n\nCloses the underlying file descriptor, removes the lockfile atomically (ignoring errors if the file no longer exists), and sets internal state to closed. Safe to call multiple times or from different goroutines without deadlocking. Returns a non-nil error only when both close and remove fail; otherwise returns nil. No panic path observed.\n\n#### `SafeResolve(repoRoot string, inputPath string) (string, error)`\n\nResolves an arbitrary input path against the repository root boundary. Walks upward via `os.Lstat` until hitting the first physically-existing directory or reaching the root (`parent == current`). Symlinks are evaluated on existing ancestor components to defeat symlink-based traversal attacks. Non-existent suffix components are appended after the walk completes. If the relative path starts with `..`, it is rejected as a security violation and returns `ErrPathTraversal`. Any stat returning something other than ENOENT during the upward walk is returned immediately without masking — this could surface unexpected errors (e.g., EACCES) as fatal rather than being handled.\n\n#### `AcquireLock() (*SimpleLock, error)`\n\nResolves a lockfile path within the repository root using `SafeResolve`; atomically creates the file via O_WRONLY|O_CREATE|O_EXCL to prevent concurrent acquisition from another process; writes the current PID into the newly-created file. If lock path cannot be resolved → returns nil plus the original error from SafeResolve. If O_EXCL open fails with EEXIST → wraps `ErrLockHeld` and includes lockPath in message. If write to lockfile fails → closes file, removes lockfile, then returns wrapped error (prevents stale locks). No panic or crash path observed.\n\n#### `EnsureGitignoreHasLockfile(lockPath string) error`\n\nReads `.gitignore`; checks whether it already contains an entry for the lockfile; if not, appends a commented entry under a temp file written atomically via rename. Uses `Sync()` before rename for durability. Failure modes: unreadable `.gitignore` → propagates upstream error; read failure (non-ENOENT) → wrapped; temp-file creation/write/sync/chmod/rename failures → each wrapped with descriptive messages. Partial writes are not persisted if sync fails (atomic-ish via same-directory pattern).\n\n## Error Propagation Summary\n\n| Function | Failure Mode | Caller Impact |\n|---|---|---|\n| `SafeResolve` | Absolute path error, symlink resolution failure, stat-on-nonexistent-component error (only if not ENOENT), or path-traversal detection via `ErrPathTraversal`. | Any caller of `AcquireLock` or `EnsureGitignoreHasLockfile` receives the wrapped error directly. |\n| `AcquireLock` | Lock path cannot be resolved → returns nil + original error from SafeResolve. O_EXCL open fails with EEXIST → wraps `ErrLockHeld`. Write failure → closes file, removes lockfile, then returns wrapped error. | Caller receives non-nil error; no lock is held. No panic or crash path observed. |\n| `SimpleLock.Unlock()` | Close of underlying file fails → captured as err. Removal failure (other than ENOENT) only considered if close succeeded and removal also failed — otherwise swallowed (`if err == nil { err = removeErr }`). | Returns non-nil error only on double-failure; otherwise returns nil. No panic path. |\n| `EnsureGitignoreHasLockfile` | `.gitignore` cannot be resolved → propagates upstream. Read fails (non-ENOENT) → wraps error message. Temp file creation fails → wrapped. Write to temp file fails → wrapped. Sync fails → wrapped and returns early; subsequent close/rename skipped. Chmod fails → wrapped. Rename fails → wrapped. | Caller receives a descriptive error for each failure point. Sync-before-rename pattern means partial writes are not persisted if sync fails (atomic-ish). |\n\n## Notable Patterns / Risks\n\n- **`SafeResolve` loop**: The `for` loop calling `os.Lstat` walks upward until it finds an existing ancestor or reaches the root (`parent == current`). If a non-existent-component stat returns something other than ENOENT, it is returned immediately — this could mask unexpected errors (e.g., EACCES on a directory) as fatal rather than being handled.\n- **`.gitignore` rename**: The temp-file + rename pattern ensures atomicity but requires write access to the parent directory of `.gitignore`. Failure modes are well-covered, though no retry logic exists for transient I/O errors (e.g., NFS timeout during `Sync`).\n- **Lockfile cleanup on write failure**: In `AcquireLock`, if PID write fails, the lockfile is explicitly removed before returning error — this prevents stale locks from being left behind.",
    "internal/tools": "# internal/tools — Repository File Operations \u0026 Git Integration\n\n## Module Responsibility\n\nThe `internal/tools` package provides safe filesystem I/O primitives and Git command execution utilities for operating on a repository root directory. It is split into two files: file handling (`file_tools.go`) and Git interaction (`git_tools.go`). The module has no mutable state and performs only local disk operations.\n\n## Data Flow\n\n```\ncaller → [ReadFileSafely | WriteFileSafely] → security.SafeResolve (path validation) → OS open/write/rename\n```\n\n```\ncaller → [DiscoverCodeFiles | ShouldIgnoreFile] → gitignore parsing / extension whitelist / binary detection → []string or bool\n```\n\n```\ncaller → RunGit(repoRoot, args...) → exec.Command(\"git\", ...) → bytes.Buffer capture → stdout trimmed string\n```\n\n## File I/O: `file_tools.go`\n\n### Safe Read and Write\n\n**ReadFileSafely** resolves the virtual path against `repoRoot`, performs a TOCTOU-safe open-and-stat sequence to prevent symlink races, reads the file contents via `io.ReadAll`, and returns `(content []byte, error)`. All OS errors are wrapped with a descriptive prefix.\n\n**WriteFileSafely** ensures parent directories exist via `os.MkdirAll` (`defaultDirPerm = 0755`), creates a temporary file in the same directory as the destination using `os.CreateTemp`, writes content, syncs and closes the temp file, applies `Chmod` with `defaultFilePerm = 0644`, then atomically renames into place. Each step wraps its error; on failure, the deferred cleanup block unconditionally calls `tmpFile.Close()` and `os.Remove(tmpName)`. If `os.Rename` fails after `Chmod`, the destination path may be left in an inconsistent state with no rollback.\n\n### Gitignore Loading and File Classification\n\n**LoadGitignore** reads `.gitignore` from `repoRoot` using `bufio.Scanner`. Non-comment lines are parsed via `ignore.CompileIgnoreLines`; existence errors return `(nil, nil)` (treated as success). Scanner errors at end-of-loop propagate directly.\n\n**ShouldIgnoreFile** evaluates whether a file at relative path is ignored by applying four checks in order:\n1. User-defined patterns from the loaded `GitIgnore` instance via `ignore.GitIgnore`.\n2. Dot-prefixed components or `.egg-info` suffixes in the path.\n3. Known text extension whitelist (go, js, ts, py, md, json, yaml, etc.).\n4. Binary detection: opens the file and scans 1024 bytes for null bytes; returns `true` if any are found.\n\nIf `SafeResolve` fails during classification, returns `true`.\n\n### Source Discovery and Binary Detection\n\n**DiscoverCodeFiles** recursively walks the repo root via `filepath.WalkDir`, skipping directories matching gitignore patterns or dot-prefixed names. For each file, it runs the ignore classification; surviving relative paths are collected into a result list. Walk callback errors (directory traversal failures) are logged to `os.Stderr` and skipped (`return nil`). The final `(files, err)` from `filepath.WalkDir` is propagated as-is — callers receive whatever error the walker produced at completion.\n\n**IsBinaryFile** opens a file and reads 1024 bytes (constant `binaryDetectionBufSize = 1024`). Any `os.Open` error returns `true` (assumes binary). Reads other than `io.EOF` return `true`. Clean read with no null bytes returns `false`.\n\n### Constants (`file_tools.go`)\n\n| Constant | Value | Description |\n|---|---|---|\n| `binaryDetectionBufSize` | 1024 | Buffer size for binary file detection |\n| `defaultDirPerm` | 0755 | Default directory permission mode |\n| `defaultFilePerm` | 0644 | Default file permission mode |\n\n### Error Propagation (`file_tools.go`)\n\n- **Defensive wrapping**: `ReadFileSafely` and `WriteFileSafely` wrap every OS error with a descriptive prefix via `fmt.Errorf(\"...: %w\", err)`.\n- **Swallowed/logged only**: WalkDir errors in `DiscoverCodeFiles` are written to stderr and not returned. LoadGitignore treats non-existence as success. `ShouldIgnoreFile` returns `true` on resolution failure rather than propagating the error.\n- **Implicit assumptions**: `IsBinaryFile` assumes binary state when open or read errors occur.\n\n## Git Operations: `git_tools.go`\n\n### Public API Surface\n\n**RunGit(repoRoot string, args ...string)** executes an arbitrary git command within `repoRoot`. Prepends `--no-pager` to disable pager wrapping. Captures stdout and stderr into memory buffers (`bytes.Buffer`). On success returns `(trimmed string, nil)`. On failure returns `(trimmed string, error)` where the error message includes stderr content.\n\n**VerifyGitRepo(repoRoot string)** validates that `repoRoot` is inside a git working tree by invoking `git rev-parse --is-inside-work-tree`. If `RunGit` fails, wraps as `\"not a git repository (or any of the parent directories)\"`; underlying stderr from git is discarded at this layer.\n\n### Subprocess Interaction Model\n\nThe only external system interaction in this file is spawning `git` as a child process via `exec.Command(\"git\", ...)` and waiting for completion with `cmd.Run()`. All other I/O (reading `.git` objects, filesystem traversal) occurs inside the git binary itself.\n\n**Error Propagation (`git_tools.go`)**:\n- `cmd.Run()` error + non-zero exit → wrapped with both stderr text and exit code in a single `fmt.Errorf`.\n- Success with output → `(trimmed string, nil)`.\n- No panic/recover patterns — errors propagate as Go `error` interface values."
  }
}