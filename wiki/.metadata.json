{
  "last_documented_commit": "local",
  "files": {
    "AGENTS.md": {
      "sha256": "730753023d7cd5de555778f5202cbf73002c717ce346c93db11f741bda0a546e",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area Analysis\n\n## File: AGENTS.md\n\n**File Type:** Markdown documentation (AI Agent Guidelines)\n\n---\n\n## Exported Types: None\n\nThis file contains **zero** programming constructs. It does not export any structs, interfaces, methods, functions, or types of any kind.\n\n### Content Summary:\n- Instructions for AI coding agents on how to navigate the repository\n- References to external documentation files (`wiki/architecture.md`, `wiki/quickstart.md`, `wiki/modules/`)\n- No source code in any language (Go, Python, Rust, TypeScript, etc.)\n\n#### [BUSINESS_LOGIC]\n## Analysis of AGENTS.md\n\n**What it solves:** This file does not contain business logic, algorithms, or domain-specific code. It is an AI agent onboarding and navigation document that provides a checklist for agents to consult before making changes:\n\n1. Read `wiki/architecture.md` — system blueprint (module relationships, boundary definitions)\n2. Read `wiki/quickstart.md` — coding patterns and configuration settings\n3. Explore `wiki/modules/` — directory-level API descriptions\n\n**High-level flow:** The document instructs agents to follow a three-step consultation pattern before any code modification: architecture context → developer conventions → module-specific details.\n\nThat is the only meaningful content in this file. No business rules, domain concepts, or algorithmic logic are expressed here.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** None. This file is a static Markdown document with no variables, structs, or data structures.\n\n**Concurrency Mechanisms:** None. No concurrency primitives are present (no Mutex, channels, goroutines, etc.).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis: AGENTS.md\n\n### External Communication (I/O)\n\n**None.** This file performs no external communication whatsoever. Specifically:\n\n- **Network:** No HTTP requests, DNS lookups, or network calls are made by this code.\n- **Disk:** No read/write operations to disk occur from executing this code. The references (`wiki/architecture.md`, `wiki/quickstart.md`, `wiki/modules/`) are static path strings in markdown — they do not trigger file access unless an external agent reads them separately.\n- **Database:** No database connections or queries are performed.\n\nThis is a pure configuration/documentation artifact. It instructs AI agents to read other files before making changes, but the instructions themselves carry no execution semantics.\n\n### Error Handling / Return Mechanisms\n\n**None.** This file contains:\n\n- No error wrapping logic (`Result`, `Option`, etc.)\n- No panic recovery or unwinding paths\n- No sentinel values for error signaling\n- No try/catch blocks or exception handling constructs\n- No custom error types or codes\n\nThe only \"failure mode\" in this text is a human agent ignoring the instructions — but that is not programmatic behavior and cannot be characterized as an error-handling mechanism."
    },
    "README.md": {
      "sha256": "2d690fa1ff1c0c200f9e5e61583d1a6f5f9141e874d72c69f12c269ec3977d1d",
      "facts": "#### [API_SIGNATURES]\nNo Go source code is present in this file. This is a `README.md` containing project documentation, not compilable Go modules.\n\nTo extract the public surface area (exported structs, interfaces, and methods), I would need the actual `.go` source files from the repository.\n\n#### [BUSINESS_LOGIC]\n## Business Rules \u0026 Domain Concepts\n\n**Core Purpose:** Code-Reducer is a tool that automatically generates and maintains developer wikis for code repositories by analyzing source files through local LLM inference (Ollama).\n\n**Target Users:** Developers working with small local models (~7B–26B parameters) who want comprehensive documentation without cloud API costs.\n\n---\n\n## High-Level Algorithmic Flow\n\n### 1. Initialization (`init`)\n- Scans the repository to discover all code files and non-code assets (images, binaries, lockfiles).\n- Builds a hierarchical directory tree structure from discovered paths.\n- Executes file-level extraction for every source file — extracting API signatures, business logic, state/concurrency patterns, error handling, and side effects via a 5-step LLM pipeline.\n- Performs recursive directory synthesis: merges child summaries upward into parent directories until the root is synthesized.\n- Writes module-level markdown files per directory (e.g., `wiki/modules/internal_engine.md`).\n- Generates global documentation: architecture overview (`architecture.md`) and quickstart guide (`quickstart.md`).\n\n### 2. Incremental Updates (`update`)\n- Computes SHA256 hashes for all workspace source files.\n- Compares current hashes against cached values in `.metadata.json`.\n- Files with hash changes are re-extracted; unchanged files reuse cached facts.\n- Only affected directories (those containing changed files) trigger re-synthesis.\n- Unchanged directories reuse their cached summaries without LLM calls.\n- Deleted files are pruned from the cache; new files enter as additions.\n\n### 3. Configuration Resolution\nImplements a four-tier precedence chain: CLI flags override environment variables, which override YAML config file values, which override system defaults.\n\n---\n\n## Domain Concepts\n\n**Hierarchical Map-Reduce:** The codebase is decomposed into leaf-level file extractions (Map phase), then recursively merged upward through directory levels (Reduce phase) until a global summary emerges. This preserves structural relationships between modules.\n\n**Incremental Hash-Based Change Detection:** Instead of comparing full git diffs, the tool hashes entire files and compares against persisted metadata — classifying each file as added, modified, or deleted without external diff parsing.\n\n**Context Budgeting:** The LLM context window is dynamically calculated (`num_ctx × 3` characters). Directory components are chunked into batches that fit within this limit; if a directory exceeds the budget, it splits recursively until individual chunks fit.\n\n**Security Boundaries:** All filesystem operations validate paths against repository root to prevent traversal attacks. File writes verify symlink integrity to prevent TOCTOU hijacking. Concurrent runs acquire an atomic lock file before proceeding.\n\n#### [STATE_AND_CONCURRENCY]\n## Analysis: Mutable State \u0026 Concurrency Mechanisms\n\n### From This Document Only\n\n**Mutable State:**\n- `.code-reducer.lock` — PID is written into it at startup; updated on each run (process-level lock, not in-memory)\n- `.metadata.json` (`wiki/.metadata.json`) — Maps file paths to SHA256 hashes and generated facts. Updated during `update` command: entries added/removed for files that changed, `last_documented_commit` tracker modified\n- `.code-reducer.yaml` — Read-only at runtime; configuration is resolved from CLI → env → YAML → defaults, no mutation described\n\n**Concurrency Mechanisms:**\n- **Atomic OS-level process lock** (`security.AcquireLock`) — Uses `os.O_WRONLY|os.O_CREATE|os.O_EXCL` flags to create `.code-reducer.lock`. If the file already exists, execution fails fast. This serializes across multiple terminal windows or background jobs.\n- **TOCTOU symlink defense** (`WriteFileSafely`) — Validates open file descriptors via `f.Stat()` + `os.Lstat` before truncating/writing; not a concurrency primitive per se, but prevents race conditions on path replacement\n\n**Not mentioned in this document:**\n- No mention of `sync.Mutex`, `sync.RWMutex`, channels (`chan`), atomic variables, or goroutines. The README does not describe concurrent map operations protected by locks.\n\n### Verdict: Insufficient Information\n\nThis is a **README.md**, not source code. It describes the tool's architecture and behavior but does not expose internal implementation details (e.g., whether directory tree construction uses mutexes, how LLM calls are serialized or parallelized across goroutines). Claims about internal concurrency cannot be verified from this document alone.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects (External Communication)\n\n### Network I/O\n\n| Direction | Target | Protocol/Endpoint | Notes |\n|-----------|--------|-------------------|-------|\n| Outbound | Local Ollama server (or remote if configured) | `POST /api/chat` HTTP, timeout 10 minutes | All LLM calls go here |\n\n### Disk I/O\n\n**Configuration \u0026 State:**\n- `.code-reducer.yaml` — read during setup/initialization; written by interactive wizard (`setup`)\n- `.metadata.json` — metadata cache mapping file paths to SHA256 hashes and generated facts; updated on `update`; state commit tracker set to `\"local\"` after successful runs\n\n**Wiki Output (all writes):**\n- `wiki/modules/\u003cpath_with_underscores\u003e.md` — directory-level module summaries during Map phase\n- `wiki/architecture.md` — global system blueprint (final synthesis step)\n- `wiki/quickstart.md` — developer quickstart guide (final synthesis step)\n- `AGENTS.md` — written or appended to, with AI agent guidelines\n\n**Locking:**\n- `.code-reducer.lock` — created atomically (`O_WRONLY|O_CREATE|O_EXCL`) before execution; PID recorded inside; auto-appended to project's `.gitignore` if not already ignored\n\n**File Discovery (read-only):**\n- Walks repository root via `filepath.WalkDir`; skips default directories (`.git`, `node_modules`, `bower_components`, `dist`, `build`, `cache`, `__pycache__`, `venv`, `.venv`, `*.egg-info`) using `filepath.SkipDir`\n- Reads first 1024 bytes of unlabeled files to check for null byte (`0x00`) to classify as binary\n\n**Git:**\n- Executes `git` commands via wrapper; stdout/stderr isolated separately with `--no-pager`; only stderr isolation is explicitly documented\n\n---\n\n## Error Handling\n\n| Pattern | Mechanism | Behavior |\n|---------|-----------|----------|\n| Fail-fast (LLM) | No retry logic, no exponential backoff on Ollama calls | Any failure returns error immediately to caller; swallowed downstream if not propagated |\n| Path traversal guard (`SafeResolve`) | Computes absolute path from root + input, cleans via `filepath.Clean`, checks relative prefix for `..` | Returns path traversal error immediately when detected or resolution fails |\n| TOCTOU symlink defense (`WriteFileSafely`) | Open without `O_TRUNC`; verify target is not a symlink via `os.Lstat`; compare inodes via `os.SameFile()` on open FD vs Lstat result | Aborts write if inode mismatch detected; truncated and written only after validation passes |\n| Concurrent run guard (`AcquireLock`) | Atomic lockfile creation with `O_EXCL` flag | Fails fast at OS level if `.code-reducer.lock` already exists (prevents concurrent runs); no retry or backoff mentioned |"
    },
    "cmd/init.go": {
      "sha256": "bdb2660da13e2f3054276ea3751c0d90b5222b367b4311c6282d9594baf3829b",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area: `cmd/init.go`\n\n**Package:** `cmd` (private — lowercase identifier)\n\n## Result\n\n**No public surface area.**\n\nAll identifiers in this file are unexported (`initCmd`, `executeCommand`, `RootCmd`) because the package name itself is lowercase. No exported structs, interfaces, or methods exist.\n\n#### [BUSINESS_LOGIC]\n**Business Rule / Domain Concept:**\n\nThis file defines a CLI subcommand entry point (`init`) that triggers repository scanning and wiki markdown page generation. The high-level flow:\n\n1. User invokes the `init` command via the root CLI application\n2. The command handler delegates execution to `executeCommand(\"init\")` — implying a shared dispatcher routes commands by name\n3. The documented outcome is: scan the repository structure, then build initial wiki documentation pages from that scan\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n- `initCmd` — a package-level global variable of type `*cobra.Command`, initialized via an anonymous struct literal. No mutation of its fields occurs within this file; it is assigned once and read-only thereafter in the scope of this file alone.\n\n**Concurrency Mechanisms:**\n- None detected. The file contains no mutexes, channels, or other synchronization primitives.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis: `cmd/init.go`\n\n### I/O Communication with Outside World\n\n**None.** This file contains zero external communication. No network calls, disk access, database operations, logging, or any other form of I/O is present. The only observable behavior is the definition and registration of a CLI command (`init`) via cobra. The actual execution path delegates to `executeCommand(\"init\")`, which is defined elsewhere — this file's side effects end at the command setup.\n\n### Error Handling\n\n**None visible.** This file does not:\n- Wrap or propagate errors from any function call (the `.Run` closure calls `executeCommand(\"init\")` without capturing its return value).\n- Panic or recover.\n- Define sentinel types, error codes, or custom errors.\n- Handle registration failures (`RootCmd.AddCommand`) — if that fails, the error is silently swallowed.\n\nThe only implicit risk is that `.Run` ignores whatever `executeCommand` returns; any failure from downstream logic in other files has no escape path through this file."
    },
    "cmd/root.go": {
      "sha256": "44dff2fde852219cb15afdb0b012d280fc9081057a9583c475b03a8fbb909617",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area of `cmd/root.go`\n\n## Exported Types \u0026 Variables\n\n| Name | Kind | Input | Output / Notes |\n|------|------|--------|-----------------|\n| `RootCmd` | `var` (package-level) | — | Type: `*cobra.Command`. Initialized as a cobra root command. |\n\n## Exported Functions\n\n| Name | Kind | Input | Output |\n|------|------|-------|--------|\n| `NeedsCredentialSetup()` | function | — | Returns `bool`. |\n\n#### [BUSINESS_LOGIC]\n## Domain Concepts \u0026 Business Rules\n\n### Core Concepts\n\n**Documentation Agent Lifecycle.** This file implements the entry point for a CLI tool that writes and maintains project documentation (a \"wiki\"). The system operates in two distinct modes: an initialization phase and an ongoing update phase.\n\n**Configuration-Driven Execution.** All behavior is governed by resolved configuration, which can come from three sources: persistent flags on the command itself, environment or file-based config resolution, and user-provided overrides. These are merged into a single effective configuration before any work begins.\n\n**Repository State Tracking.** The tool tracks project state through a metadata file stored in a designated docs directory. This file records which Git commit was last processed, enabling the system to resume from where it left off rather than re-processing everything.\n\n### Business Rules (State Guards)\n\n1. **Git Repository Requirement.** The current working directory must be a valid Git repository before any command runs. If not, execution aborts with an error message.\n\n2. **Configuration Existence Check.** Before the main flow can proceed, a configuration file must exist in the project root. If it is missing and standard input is not a terminal (i.e., piped or redirected), the tool exits with an instruction to run setup first.\n\n3. **Implicit Setup on Missing Config.** When no config exists but stdin is interactive, the tool automatically triggers a setup flow before attempting to read further configuration. This prevents users from hitting dead ends during normal use.\n\n4. **Init/Update Mode Isolation.** The two operational modes are mutually exclusive:\n   - Running `init` when the project has already been initialized (a prior commit was documented) is rejected, directing the user to use `update` instead.\n   - Running `update` when no initialization has occurred is rejected, requiring the user to run `init` first.\n\n5. **Credential Validation.** A separate check verifies that a model identifier is configured. If it is empty, setup is flagged as incomplete. This acts as a gate for downstream execution.\n\n### Algorithmic Flow\n\n1. Resolve the current working directory and validate it is a Git repository.\n2. Determine whether configuration exists in the project root.\n3. If missing, either auto-trigger setup (interactive) or exit with an error message (non-interactive).\n4. Merge all configuration sources into a single effective config object.\n5. Read and parse the metadata file to determine the last documented commit SHA.\n6. Validate that the requested mode is consistent with the current initialization state.\n7. Set up signal handlers for graceful interruption.\n8. Instantiate the engine runner with the resolved configuration.\n9. Execute the main workflow, during which the runner emits events representing progress updates, errors, and informational messages.\n10. Propagate any final error from the run to the user and exit if one occurred.\n\nThe file itself does not contain the core documentation logic; it serves as the orchestration layer that validates preconditions, resolves configuration, and delegates execution to an engine component.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State\n\n| Variable | Scope | Mutated? | Notes |\n|---|---|---|---|\n| `modelIdFlag` | package-level (`var`) | No | Assigned from flag registration in `init()`. Not modified anywhere else in this file. |\n| `numCtxFlag` | package-level (`var`) | No | Same as above — read-only after init. |\n| `RootCmd` | package-level (`var`) | No | Initialized once; never reassigned or mutated post-init in this file. |\n\n**Local mutable state** (single-threaded, no concurrency concerns):\n- `meta` struct in `executeCommand()` — local to the function scope, not shared across goroutines.\n- `cfg`, `repoRoot`, `lastSHA`, `hasInit` — all local to `executeCommand()`.\n\n## Concurrency Mechanisms\n\n**None.** The global variables (`modelIdFlag`, `numCtxFlag`) are never mutated after their initial assignment in `init()`, so no synchronization is required. All other mutable state is either local to a function scope or read-only globals.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 External Communication\n\n| Resource | Mechanism | Direction | Notes |\n|---|---|---|---|\n| **Stdout** (`os.Stdout`) | `fmt.Println` / `fmt.Printf` | Write (output) | Prints status events and normal messages from the engine runner callback. |\n| **Stderr** (`os.Stderr`) | `fmt.Fprintf(os.Stderr, ...)` | Write (error output) | Used for two sentinel paths: config-missing-without-TTY error, and \"not initialized\" state error. |\n| **Disk — CWD** | `os.Getwd()` | Read | Returns the current working directory; error is printed to stdout and causes `os.Exit(1)`. |\n| **Disk — Git repo check** | `tools.VerifyGitRepo(repoRoot)` | Read (git metadata) | Delegates to another package. Error from this call is formatted and sent to stderr, then exits with code 1. |\n| **Disk — Config file existence** | `config.ConfigExists(repoRoot)` | Read | Boolean; used only for the TTY guard branch. Not directly observable externally beyond triggering a setup prompt. |\n| **Disk — Terminal detection** | `isatty.IsTerminal(os.Stdin.Fd())` / `isatty.IsCygwinTerminal(os.Stdin.Fd())` | Read (stdin fd state) | Determines whether to allow implicit setup flow or error out. No external network/DB call. |\n| **Disk — `.metadata.json`** | `os.ReadFile(metadataPath)` | Read | Reads the merged config and last documented commit from disk. The resulting JSON is unmarshaled; a parse failure here is silently swallowed (no log, no exit). |\n| **Signals (OS)** | `signal.NotifyContext(... os.Interrupt, syscall.SIGTERM)` | System-level I/O | Registers OS interrupt/termination signals on the background context. `defer stop()` cancels the context when the function returns. No explicit cancellation observation beyond this deferral. |\n| **Engine runner** | `engine.NewRunner(cfg).Run(ctx, repoRoot, mode, ...)` | Read + Write (unknown) | Delegates to another package. The callback prints engine events to stdout/stderr. Any error returned from `Run` is printed and causes `os.Exit(1)`. Internal I/O of the runner itself cannot be determined from this file alone. |\n| **Config resolution** | `config.ResolveConfig(repoRoot, ...)` | Read (merged config) | Used for two paths: after setup flow completes, and in `NeedsCredentialSetup()`. No external network/DB call observable here. |\n\n## Error Handling Patterns\n\n| Pattern | Evidence | Notes |\n|---|---|---|\n| **Sentinel exit** (`os.Exit(1)`) | Every explicit error path (CWD failure, git check, config-missing+non-TTY, init-already-ran, update-not-init, runner failure) calls `fmt.Fprintf(os.Stderr, ...)` then `os.Exit(1)`. | No wrapping — errors are formatted as plain strings and the process terminates. |\n| **Swallowed silently** | `if data, err := os.ReadFile(metadataPath); err == nil { ... }` — if read fails, nothing is logged or reported; execution continues with empty variables. Similarly, `json.Unmarshal(data, \u0026meta) == nil` — parse errors are not checked after the initial `err == nil`. | The metadata file is treated as optional. A missing or malformed `.metadata.json` is effectively a no-op for this branch. |\n| **No wrapping** | No `fmt.Errorf`, no `%w` usage, no custom error types defined in this file. All errors are either passed through from another package (which may wrap internally) or formatted as plain strings before exit. | Cannot confirm whether upstream packages wrap — only observable behavior is that this file does not. |\n| **No panic recovery** | No `recover()` calls, no deferred panic handlers visible in this file. | If a downstream function panics and is not caught elsewhere, the process terminates with SIGSEGV/SIGABRT. Not observable from here. |\n| **Context cancellation** | `ctx, stop := signal.NotifyContext(...)` followed by `defer stop()`. | Cancellation propagates to anything receiving the context (`engine.Runner`). No explicit error observation beyond this deferral; any cancellation side effects are swallowed unless the runner surfaces them. |\n| **No custom sentinel types** | No user-defined error structs or interfaces in this file. All error handling is string-based formatting + exit code 1. | Downstream packages may define sentinels — not observable here without inspecting those files. |\n\n## Summary\n\n- **I/O surface**: Stdout/stderr for human-readable output, disk reads for CWD/git/config/metadata, signal registration for OS-level termination. No network or DB calls are visible in this file.\n- **Error strategy**: Almost exclusively `fmt.Fprintf(os.Stderr) + os.Exit(1)` — plain string formatting with immediate process exit. One silent swallow path for metadata parsing. No wrapping, no panic recovery, no structured error types."
    },
    "cmd/setup.go": {
      "sha256": "880ecf5c2d82afc76a63c5c952f4a849351568aae3392ad8ca8229cbed4d7195",
      "facts": "#### [API_SIGNATURES]\n## Public Surface Area of `cmd/setup.go`\n\n### Exported Functions\n\n| Identifier | Signature | Notes |\n|------------|-----------|-------|\n| `RunSetupFlow` | `(repoRoot string)` | Interactive setup flow entry point. Takes a repository root path (string). Returns nothing. |\n\n---\n\n**Not exported:** All other functions in this file (`setupCmd`, `init`, `promptAndAppend`, `promptString`) begin with lowercase letters and are therefore internal to the package.\n\n#### [BUSINESS_LOGIC]\n**Business Rules \u0026 Domain Concepts:**\n\nThis file implements an interactive CLI setup wizard that generates and updates a `.code-reducer.yaml` configuration file. Key concepts:\n\n1. **Configuration persistence with incremental editing.** The system reads the existing config first, then presents each prompt pre-populated with the current value. Only fields the user changes get updated; untouched values persist unchanged. This avoids forcing full re-entry on every run.\n\n2. **LLM model selection.** Default LLM is `gemma4:26b-a4b-it-qat`. Users may override this to any other Ollama-compatible model ID.\n\n3. **Ollama connection parameters.** Two settings are configurable — base URL (defaults to standard Ollama port) and context window size in tokens. Both fall back to defaults if input is invalid or empty.\n\n4. **File/directory exclusion rules.** The app maintains a list of directories and file extensions to skip during processing. On first setup, the user provides these fresh; on subsequent runs, existing values are shown and users may append additional entries via comma-separated input. Empty input leaves the current list intact.\n\n5. **Documentation directory.** Default is `wiki`. User can change this path freely.\n\n6. **Extraction steps.** A fixed sequence of code-extraction operations (defined elsewhere in config) is loaded once for first-time setup; on subsequent runs, existing steps are preserved without prompting. This suggests the extraction pipeline is a static concept that users don't reconfigure.\n\n**High-level algorithmic flow:**\n\n1. Read current working directory.\n2. Load existing config if present (nil otherwise).\n3. For each configuration field — model ID, base URL, context size, docs dir, ignores, ignore extensions — prompt the user with the existing value pre-filled. Empty or invalid input retains the previous value; defaults are applied when no prior state exists.\n4. Construct a new config struct from collected values (extraction steps either preserved or defaulted).\n5. Persist the resulting config to disk and confirm success.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n| Variable | Scope | Mutability Notes |\n|---|---|---|\n| `setupCmd` (line 12) | Package-level (`var`) | Initialized once in `init()`. Not modified after initialization; only referenced. |\n| `reader` (line 39, line 156, line 178) | Local to `RunSetupFlow`, `promptAndAppend`, `promptString` | Re-created per call. Single-threaded use within each function. |\n| `existingModel` / `existingBaseURL` / `existingNumCtx` (line 40–42) | Local to `RunSetupFlow` | Assigned once, never mutated after. Read-only locals. |\n| `cfg` (line 52) | Local to `RunSetupFlow` | Pointer reassigned conditionally (`if err == nil \u0026\u0026 cfg != nil`). Single-threaded. |\n| `newCfg` (line 109) | Local to `RunSetupFlow` | Created fresh per call, then passed to `config.SaveConfig`. Not mutated after creation. |\n\n**Concurrency Mechanisms:**\n\nNone found in this file. No `sync.Mutex`, channels, atomic operations, or other synchronization primitives are used. All state mutations occur within single-threaded function scopes (no goroutines spawned).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis: `cmd/setup.go`\n\n### 1. External Communication (I/O)\n\n| Interaction | Mechanism | Notes |\n|---|---|---|\n| **Stdout** (user-facing output) | `fmt.Printf`, `fmt.Println` | All messages go to stdout directly—no logger, no structured logging. Includes prompts, error messages, and success confirmation. |\n| **Stdin** (user input) | `bufio.NewReader(os.Stdin)` + `ReadString('\\n')` | Reads entire lines from stdin. No buffering/piping support beyond line-based. |\n| **Filesystem: read** | `config.LoadConfig(repoRoot)` | Loads existing config file from disk. Path resolution is delegated to the `config` package (not shown). |\n| **Filesystem: write** | `config.SaveConfig(repoRoot, newCfg)` | Writes new config file to disk. Path resolution delegated to `config` package. |\n\nNo network I/O, no database calls, no external process invocation in this file. All external state changes are limited to local filesystem and terminal.\n\n### 2. Error Handling Patterns\n\n| Location | Pattern | Behavior on error |\n|---|---|---|\n| **`os.Getwd()`** → `setupCmd.Run` | **Fatal exit** (`os.Exit(1)`) | Prints error message, then terminates the process immediately. No return value to caller. |\n| **`config.LoadConfig(...)`** | **Silent swallow** (no handling shown here) | The returned `err` is captured but only checked in `if err == nil \u0026\u0026 cfg != nil`. If it fails, `existingCfg` remains `nil`, and the flow continues with defaults. No user-facing error message for this case. |\n| **`strconv.Atoi(...)`** → `numCtx` | **Silent swallow** (no handling shown) | The `err == nil \u0026\u0026 n \u003e 0` guard means any parse failure falls through to `else`, defaulting to `existingNumCtx`. User never sees an error for invalid context size input. |\n| **`promptAndAppend(...)` with `reader.ReadString('\\n')`** → ignore lists (first call) | **Warning + fallback** | Prints warning message, returns default values instead of propagating the error. Flow continues normally. |\n| **`promptAndAppend(...)` with `reader.ReadString('\\n')`** → ignore lists (existing config path) | **Warning + fallback** | Prints warning message, returns existing list unchanged. Flow continues. |\n| **`promptString(...)`** (all three calls) | **Warning + fallback** | Prints warning message, returns existing value. Flow continues. |\n| **`config.SaveConfig(...)`** | **Fatal exit** (`os.Exit(1)`) | Prints error message, then terminates the process immediately. No return to caller. |\n\n### 3. Summary of Patterns\n\n- **Two fatal exits**: `Getwd()` failure and config save failure both call `os.Exit(1)`. The program never returns normally from `setupCmd.Run` after these points.\n- **Silent swallows (unreported)**: `LoadConfig` errors, `Atoi` parse failures—these degrade silently to defaults without any user notification.\n- **Warning + fallback**: All stdin read errors are caught, a warning is printed, and the function returns a sensible default rather than bubbling up. The user gets feedback but no hard failure.\n- **No error wrapping or propagation**: Errors are either printed-and-exited (fatal) or discarded entirely (non-fatal). No `errors.Is`/`errors.As`, no custom sentinel types, no context propagation for cancellation."
    },
    "cmd/update.go": {
      "sha256": "aa65f071d7373807ecd8df62f6e03a39ff061a5409894107f7057631cfddd127",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area: `cmd/update.go`\n\n**No exported types or methods found.**\n\n| Category | Details |\n|---|---|\n| Exported Structs | None |\n| Exported Interfaces | None |\n| Exported Functions | None |\n| Exported Variables | None |\n\n#### [BUSINESS_LOGIC]\n## Business Rules \u0026 Domain Concepts\n\n**None identifiable from this file alone.**\n\nThis file only registers a cobra CLI subcommand named `update`. The command string states it will \"scan changed files since the last documented commit and update the wiki pages,\" but:\n\n- The actual implementation is in an external function (`executeCommand`), which is not present in this file.\n- No business logic, validation rules, or domain concepts are defined here.\n- No data structures, state machines, or algorithmic steps are implemented.\n\n## High-Level Algorithmic Flow\n\n**Cannot be determined from this file.** The only flow visible is:\n\n1. Register `update` command with RootCmd via `init()`.\n2. On invocation, call `executeCommand(\"update\")`.\n\nWhat happens after that step is outside the scope of this file and cannot be inferred safely.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State\n\n| Variable | Type | Mutated? | Notes |\n|---|---|---|---|\n| `updateCmd` | `*cobra.Command` | No (in this file) | Global variable, assigned once during init. Not modified after initial assignment in this source. |\n\n**Note:** While `executeCommand(\"update\")` is called at runtime, its implementation is not visible here; whether it mutates state cannot be determined from this file alone.\n\n## Concurrency Mechanisms\n\n**None.** No `sync.Mutex`, channels (`chan`), or other concurrency primitives are used in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis: `cmd/update.go`\n\n### Side Effects (I/O)\n\n**None.** This file contains no I/O operations whatsoever. No network, disk, database, or external communication occurs here. It exclusively performs command registration within the CLI framework (`cobra`). All actual work is delegated to an undefined function `executeCommand(\"update\")`, which lives outside this file and is not visible in the provided source.\n\n### Error Handling\n\n**None.** This file contains no error handling mechanisms:\n- No error returns from `Run` — the call to `executeCommand` discards any returned value\n- No panics, no panic recovery\n- No sentinel errors, no wrapping of errors\n- No logging or fallback paths visible in this file\n\n### Summary\n\nThis is a **thin command registration layer**. It wires up a cobra subcommand named `update`, whose actual behavior (and any error handling) is implemented elsewhere. The only observable side effect is registering the command with `RootCmd` at init time."
    },
    "go.mod": {
      "sha256": "360e06dd1314ef70f66d1f9b1c0c4c87ed7e1adbcf3e0c7e042a2056077d894b",
      "facts": "#### [API_SIGNATURES]\nNo public surface area exists in this file. It is a `go.mod` module metadata file containing only:\n\n- Module declaration (`module github.com/arrase/code-reducer`)\n- Go version constraint (`go 1.26`)\n- Dependency requirements (direct and indirect)\n\n#### [BUSINESS_LOGIC]\n## Analysis: go.mod\n\n### File Type\nThis file defines **project dependencies and module metadata**. It contains no business logic, domain concepts, or algorithmic flow.\n\n### Contents\n- **Module name**: `github.com/arrase/code-reducer`\n- **Go version requirement**: 1.26\n- **Direct dependencies** (4):\n  - CLI framework (`spf13/cobra`)\n  - Terminal detection (`mattn/go-isatty`)\n  - Gitignore parsing (`sabhiram/go-gitignore`)\n  - YAML data parsing (`gopkg.in/yaml.v3`)\n- **Indirect dependencies** (7): transitive packages pulled in by direct deps\n\n### Conclusion\nThis file solves the **infrastructure problem** of declaring what external libraries the project needs to build and run. It does not solve any business rules, domain concepts, or algorithmic problems.\n\n#### [STATE_AND_CONCURRENCY]\n# State Mutation \u0026 Concurrency Analysis\n\n## File: go.mod\n\n**File Type:** Module metadata / dependency manifest (not executable code)\n\n### Mutable State\n- **None.** This file contains only module path, Go version, and `require` declarations for dependencies. No variables, structs, or state are defined here.\n\n### Concurrency Mechanisms\n- **None.** No channels, `sync.Mutex`, `atomic.Xxx`, goroutines, or any concurrency primitives appear in this file.\n\n### Conclusion\n**No mutable state.** The only contents of the provided source text are package metadata and third-party dependency declarations; there is no application logic to analyze for state mutation or concurrency protection.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n# Side Effects and Error Handling Analysis\n\n## Scope\nThe provided source text is a Go module definition file (`go.mod`). It defines dependencies for the module `github.com/arrase/code-reducer` targeting Go 1.26.\n\n## External Communication (I/O)\n**No external communication logic is present in this file.**\n\n*   **Network:** No network calls, HTTP requests, or socket operations are defined in this text.\n*   **Disk:** No file read/write operations are defined in this text. The existence of the file itself implies a `go mod` tooling interaction occurred to generate it, but that is not logic present within the source content provided.\n*   **Database:** No database interactions are referenced or implemented here.\n\nWhile direct dependencies such as `github.com/spf13/cobra` (CLI framework) and `gopkg.in/yaml.v3` (serialization) suggest the *project* may perform I/O in other files, their usage is not observable within this specific source text.\n\n## Error Handling\n**No error handling logic is present in this file.**\n\n*   **Wrapping:** No custom error wrapping or `fmt.Errorf` calls are found here.\n*   **Sentinels:** No sentinel errors (e.g., zero-value sentinels) are defined.\n*   **Panic:** No runtime panics, recover blocks, or panic recovery logic is present.\n\nAll listed dependencies (`mattn/go-isatty`, `sabhiram/go-gitignore`, `spf13/cobra`, `yaml.v3`, and their indirects) are declared here as requirements, but no function bodies or logic utilizing them exist in this manifest file."
    },
    "go.sum": {
      "sha256": "332d3e9533ae48f722de495fd08dca261e67e317b5bd7cda6266c6775a3fd64c",
      "facts": "#### [API_SIGNATURES]\n## File: `go.sum`\n\n**Public Surface Area:** None.\n\nThis file is a Go module checksum manifest. It contains only dependency hash entries (e.g., `github.com/spf13/cobra v1.10.2 h1:...`, `gopkg.in/yaml.v3 v3.0.1 h1:...`). There are no exported types, interfaces, methods, structs, or any Go API surface to extract.\n\n#### [BUSINESS_LOGIC]\n## File Analysis: `go.sum`\n\n### What this file is\nThis file contains **checksum metadata** (hash digests and module paths) for Go dependency packages. It serves as an integrity-verification manifest used by `go mod` to validate downloaded modules match their expected hashes.\n\n### Business Logic / Domain Concepts\n- **None.** This file has zero application logic, no domain concepts, no business rules.\n- It exists solely for build-time dependency verification (similar to a checksum database).\n\n### Algorithmic Flow\n- **None.** There is no algorithm or processing flow here — it is passive metadata.\n\n### Summary\nThis file cannot be analyzed for business logic, domain concepts, or high-level algorithms because it contains only checksum records for third-party Go packages (`cobra`, `spf13/pflag`, `spft13/cobra`, `kr/text`, etc.). No source code is present to analyze. If you intended to share a different file (e.g., an `.go` file), please provide it.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state.\n\nThis file (`go.sum`) is a dependency checksum manifest, not executable Go source code. It contains no functions, variables, structs, or runtime logic to analyze.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis\n\n### File: `go.sum`\n\n**Nature of file:** Dependency checksum manifest (hash table for modules in `go.mod`). Not application code.\n\n---\n\n### Side Effects (I/O, Network, Disk, DB)\n\n| Category | Finding |\n|----------|---------|\n| **Network / HTTP** | None. No network calls, no HTTP clients, no servers. |\n| **Disk / Filesystem** | None. No file reads/writes beyond the `go.sum` file itself (which is auto-generated by `go mod tidy`). |\n| **Database** | None. No DB drivers or connections. |\n| **Process / OS** | None. No exec, signal handling, or syscall calls visible. |\n\n---\n\n### Error Handling\n\n| Mechanism | Finding |\n|-----------|---------|\n| **Errors returned from functions** | N/A — no functions defined in this file. |\n| **Error wrapping (fmt.Errorf / errors.Join)** | None. |\n| **Sentinel errors** | None. |\n| **Panic recovery** | None. |\n| **Custom error types** | None. |\n\n---\n\n### Summary\n\nThis file has **zero side effects** and **no error handling logic**. It is purely declarative data used by the Go toolchain to verify dependency integrity during `go build` / `go test`. All external communication (downloading, checksumming) happens at module resolution time before this file exists; that behavior belongs in `go.sum`'s generation process, not in its static content."
    },
    "internal/config/env.go": {
      "sha256": "3b90de66701e6e1824dc59dcd448078e419b41c92324eb2689bf09b2333b82fa",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area of `internal/config/env.go`\n\n## Structs\n\n| Type | Fields |\n|------|--------|\n| **ExtractionStep** | `Name string`, `Prompt string` (yaml: \"name\", \"prompt\") |\n| **Config** | `ModelID string`, `OllamaBaseURL string`, `OllamaNumCtx int`, `DocsDir string`, `ExtractionSteps []ExtractionStep`, `Ignore []string`, `IgnoreExtensions []string` |\n\n## Functions\n\n| Function | Input(s) | Output |\n|----------|----------|--------|\n| **ConfigExists** | `cwd string` | `bool` |\n| **LoadConfig** | `cwd string` | `(*Config, error)` |\n| **SaveConfig** | `cwd string`, `cfg *Config` | `error` |\n| **MergeAndDeduplicate** | `a []T`, `b []T` (`[T comparable]`) | `[]T` |\n| **ResolveConfig** | `repoRoot, modelIdFlag, numCtxFlag string` | `*Config` |\n\n#### [BUSINESS_LOGIC]\n## Domain Concepts \u0026 Business Rules\n\n### Multi-Source Configuration Resolution (Priority Chain)\n\nThe file implements a four-tier configuration resolution strategy where values are sourced from CLI flags → environment variables → YAML config file → hardcoded defaults. Each tier has explicit precedence: if a higher-priority source provides a value, it supersedes lower tiers. This allows users to override settings at runtime without modifying persisted state or the executable.\n\n### Configuration Persistence\n\nConfigurations can be saved to `.code-reducer.yaml` in the current working directory using `SaveConfig`. The save operation writes with restricted file permissions (`0600`). Conversely, `LoadConfig` reads from this same path and returns an error if it cannot be parsed.\n\n### Ignore Path Deduplication\n\nWhen merging ignore directories/paths, duplicates are removed via a `MergeAndDeduplicate` helper using a map-based seen-set pattern. The merge order is: system defaults first, then user-provided config additions. This prevents the same directory (e.g., `.git`, `node_modules`) from being listed twice if both sources specify it.\n\n### Extraction Steps Pipeline Definition\n\nA fixed set of four analysis tasks is defined by default:\n1. **API_SIGNATURES** — Extract exported structs, interfaces, methods and their types\n2. **BUSINESS_LOGIC** — Analyze domain concepts and high-level algorithmic flow\n3. **STATE_AND_CONCURRENCY** — Identify mutable state and concurrency protections (mutexes, channels)\n4. **ERRORS_AND_SIDE_EFFECTS** — Document external communication (I/O) and error handling patterns\n\nThese steps are configurable in the YAML file but fall back to defaults when empty.\n\n### Default Ignore Lists\n\nTwo hardcoded default lists exist: one for directories (`DefaultIgnores`), one for file extensions (`DefaultIgnoredExtensions`). Both include build artifacts, caches, lock files, and runtime-specific directories common across languages (Python venvs, Node modules, Ruby gems). These are merged with user config at resolve time.\n\n### Default Model Fallback\n\nWhen no model ID is provided through any source, a hardcoded default (`gemma4:26b-a4b-it-qat`) is used as the final fallback for Ollama model selection.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State Analysis\n\n**Mutable state:** None identified.\n\n### Details:\n\n- **Package-level variables** (`DefaultIgnores`, `DefaultIgnoredExtensions`, `DefaultExtractionSteps`) are initialized at package scope but never reassigned after their initial definition. They serve as read-only defaults consumed by `ResolveConfig`.\n- **`Config` struct fields** (e.g., `ModelID`, `OllamaBaseURL`, `DocsDir`, etc.) are only assigned via the `ResolveConfig` function, which returns a new pointer. No concurrent access to these fields occurs within this file.\n- **Local variables** in functions (`resolvedIgnore`, `resolvedExtensions`, `numCtx`, etc.) are stack-scoped and not shared across goroutines or threads.\n\n### Concurrency mechanisms: None present.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis\n\n### I/O Operations (Outside-World Communication)\n\n| Function | I/O Path | Description |\n|---|---|---|\n| `LoadConfig` | Disk: reads `.code-reducer.yaml` via `os.ReadFile` | Reads config file from the specified working directory. No network/DB access. |\n| `SaveConfig` | Disk: writes `.code-reducer.yaml` via `os.WriteFile` with mode `0600` | Writes marshaled YAML to disk. Permission is explicitly restricted to owner-read/write (`0600`). |\n| `ResolveConfig` | Disk: reads config file (via `LoadConfig`) | Indirect I/O — delegates to `LoadConfig`. No other external communication in this file. |\n\n### Error Handling Strategy\n\n#### Functions that **wrap** errors with context:\n\n- **`SaveConfig`**: Wraps both YAML marshaling errors and file-write errors using `fmt.Errorf(..., err)` with `%w` verb (preserves chain for unwrapping).\n  ```go\n  return fmt.Errorf(\"failed to marshal yaml: %w\", err)\n  return fmt.Errorf(\"failed to write config file: %w\", err)\n  ```\n\n- **`LoadConfig`**: Passes through the `os.ReadFile` error directly (no wrapping). Wraps YAML parse failures with a descriptive prefix.\n  ```go\n  // Direct pass-through — no wrap\n  return nil, err\n  // Wrapped for clarity\n  return nil, fmt.Errorf(\"failed to parse yaml config: %w\", err)\n  ```\n\n#### Functions that **swallow** errors (no propagation):\n\n- **`ConfigExists`**: Treats any `os.Stat` error as \"file does not exist\" and returns `false`. Permission denial, I/O failures, etc. are silently swallowed.\n  ```go\n  return err == nil\n  // Any non-nil error → false\n  ```\n\n- **`ResolveConfig`**: Swallows the entire `LoadConfig` error and falls back to an empty `\u0026Config{}`. No wrapping or propagation occurs.\n  ```go\n  cfg, err := LoadConfig(repoRoot)\n  if err != nil {\n      cfg = \u0026Config{}\n  }\n  ```\n\n#### Functions with **no meaningful errors** (return types don't permit them):\n\n- **`MergeAndDeduplicate`**: Returns `[]T`. No error return. The caller gets whatever slice is produced; if the map logic had an issue, it would panic at runtime (e.g., non-comparable type passed).\n\n### Summary of Error Patterns\n\n| Pattern | Used by | Direction |\n|---|---|---|\n| **Wrapped with context** (`fmt.Errorf` + `%w`) | `SaveConfig`, `LoadConfig` (YAML parse only) | Upward to caller |\n| **Direct pass-through** (no wrap) | `LoadConfig` (read error), `ResolveConfig` (swallow via empty struct) | Depends on function |\n| **Silently swallowed** (`err == nil` check, no wrap) | `ConfigExists` | Discarded at boundary |\n| **Swallowed via fallback value** | `ResolveConfig` | Discarded; replaced by empty config |"
    },
    "internal/engine/cache.go": {
      "sha256": "b4e2146f2fb0568f554251b96c9ebacb1f21affb7af18c1eb5a93be369988235",
      "facts": "#### [API_SIGNATURES]\n## Public Surface Area: `internal/engine/cache.go`\n\n### Structs\n\n| Type | Fields |\n|------|--------|\n| **`FileCacheEntry`** | `SHA256 string`, `Facts string` |\n| **`MetadataCache`** | `LastDocumentedCommit string`, `Files map[string]FileCacheEntry`, `Modules map[string]string` |\n\n### Methods / Functions\n\nNone. All functions (`loadMetadataCache`, `saveMetadataCache`, `computeSHA256`) are unexported (lowercase first letter).\n\n#### [BUSINESS_LOGIC]\n**Domain Concepts**\n\n1. **Content-addressed file tracking.** Each file is associated with a SHA256 hash of its content plus derived \"facts.\" This implies the engine caches analysis results keyed by what the file actually contains, not just its path.\n\n2. **Persistent cross-session state.** A `.metadata.json` artifact survives between runs, recording:\n   - The last commit that was documented (audit/progress tracking).\n   - Per-file entries (hash + facts) for fast re-lookup.\n   - Registered modules with an associated string value.\n\n3. **Graceful degradation on missing state.** If the metadata file does not exist, a fresh empty cache is returned rather than failing outright—subsequent runs start clean without user intervention.\n\n**High-Level Algorithmic Flow**\n\n1. **Initialize / restore:** Read `.metadata.json` from the docs directory. On first run (file absent), create an empty cache structure. On subsequent runs, deserialize and normalize it.\n2. **Compute file identity:** For any given virtual path, read its content and derive a SHA256 hash as a deterministic identifier.\n3. **Persist updates:** When file facts or module registrations change, marshal the full cache back to `.metadata.json` with indentation for human readability.\n\n**Business Rules**\n\n- A file's cached entry is invalidated by recomputing its content hash—changes to the file automatically produce a new fingerprint.\n- The cache is stored at a fixed location relative to `docsDir`; callers must supply both `repoRoot` and `docsDir` for correct path resolution.\n- Parse errors are surfaced distinctly from \"not-exists\" errors, allowing different recovery strategies per failure mode.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n- `MetadataCache.Files` — `map[string]FileCacheEntry`, initialized to empty map if nil during `loadMetadataCache`. Populated/modified via cache entry operations (not shown in this file).\n- `MetadataCache.Modules` — `map[string]string`, same pattern as above.\n- `MetadataCache.LastDocumentedCommit` — string field, set externally outside the scope of this file.\n\n**Concurrency Mechanisms:** None visible in this file. No `sync.Mutex`, channels, or atomic operations are used to protect any state.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 I/O Communication\n\n| Function | Direction | Path / Target | Mechanism |\n|---|---|---|---|\n| `loadMetadataCache` | **Read** | `\u003cdocsDir\u003e/.metadata.json` | Reads via `tools.ReadFileSafely(repoRoot, metadataPath)` |\n| `saveMetadataCache` | **Write** | `\u003cdocsDir\u003e/.metadata.json` | Writes via `tools.WriteFileSafely(repoRoot, metadataPath)` |\n| `computeSHA256` | **Read** | `\u003crepoRoot\u003e/\u003cvirtualPath\u003e` (any file path) | Reads via `tools.ReadFileSafely(repoRoot, virtualPath)` |\n\nAll disk I/O is funneled through the `tools.*` package. No network calls, no DB access, no external services are invoked by this code. The only filesystem interaction is reading/writing a single JSON metadata file and arbitrary source files for hashing.\n\n## Error Handling Analysis\n\n| Function | Pattern | Details |\n|---|---|---|\n| `loadMetadataCache` | **Sentinel + wrap** | Checks `errors.Is(err, os.ErrNotExist)` — if true, returns a default-initialized `*MetadataCache` with nil error. For all other errors (I/O failure, JSON parse failure), wraps the original error using `%w`. The caller can therefore unwrap and distinguish \"cache missing\" from \"cache corrupted/corruptable\". |\n| `saveMetadataCache` | **Pass-through** | Returns `json.MarshalIndent` error directly; then returns `tools.WriteFileSafely` error directly. No wrapping, no sentinel check. Any failure is returned as-is to the caller. |\n| `computeSHA256` | **Pass-through** | Returns whatever `tools.ReadFileSafely` yields — I/O errors propagate unmodified. The SHA-256 hash computation itself cannot fail (no error return from it). |\n\n## Notable Observations\n\n1. **No panics.** All code paths exit by returning `(result, error)` tuples.\n2. **Error wrapping is inconsistent.** `loadMetadataCache` wraps; the other two functions do not. A caller that uses `%w` to unwrap errors will succeed only for `loadMetadataCache`. The marshal/write and read-hash errors must be inspected directly or via `errors.Is(err, specificStdLibErr)` against known values.\n3. **Default cache is a sentinel pattern.** Returning an empty-but-initialized `*MetadataCache` rather than nil when the file is missing means callers get a non-nil pointer with an empty map, which avoids nil-map panics on subsequent access but carries no error signal."
    },
    "internal/engine/chunking.go": {
      "sha256": "7be4d97d95416073aa815d6effbaddfb0e433a984ca6a14df5382ebce1b1d536",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area: `engine/chunking.go`\n\n## Summary\n\nThis file contains **no exported symbols** — all identifiers use lowercase letters, which makes them private to the `engine` package per Go convention.\n\n---\n\n## Structs | Interfaces\n\n| Symbol | Exported? |\n|--------|-----------|\n| *(none defined in this file)* | ✗ |\n\n---\n\n## Functions\n\n| Function | Exported? | Signature |\n|----------|-----------|-----------|\n| `reduceInChunks` | ✗ | `(ctx context.Context, c *LLMClient, nodePath string, items []string, logEvent func(string, string)) (string, error)` |\n\n#### [BUSINESS_LOGIC]\n## Business Logic \u0026 Domain Concepts\n\n### Core Purpose\nThis file implements a **recursive chunk-reduction algorithm** designed to process collections of code/architecture items through an LLM-based synthesis pipeline, ultimately producing a single consolidated output.\n\n### Key Domain Concepts\n\n- **Context Window Management**: The `maxChunkChars` limit (3× the LLM context window) ensures no intermediate batch exceeds what the model can reasonably handle.\n- **Module Synthesis**: Items are grouped and fed to an LLM with a \"module_synthesis\" system prompt, which presumably instructs the model to consolidate/summarize architectural information.\n- **Recursive Reduction**: The algorithm reduces items in multiple passes — grouping → synthesizing per group → concatenating results → reducing again until one item remains.\n\n### Algorithmic Flow\n\n1. **Empty input** returns immediately (no-op base case).\n\n2. **Single-item truncation**: If exactly one item exceeds the context window, it is truncated with a `...[truncated]` marker rather than being discarded. This preserves partial information.\n\n3. **Batching phase**: Items are greedily grouped into batches where each batch's combined length stays within `maxChunkChars`. Once an item would overflow its current batch, it starts a new batch.\n\n4. **Fallback truncation** (when all items exceed limits individually): If every item is too large to fit even alone in any meaningful combination, each item is proportionally truncated and forced into a single batch. This prevents deadlock on unbatchable inputs.\n\n5. **Single-batch terminal**: When exactly one batch exists after grouping, the entire set is sent directly to the LLM for synthesis and the result is returned (stripped of markdown fences).\n\n6. **Multi-batch recursion** (the core reduction pattern):\n   - Each batch is processed independently by recursively calling `reduceInChunks` on itself.\n   - The intermediate results from all batches are concatenated into a single list.\n   - That combined list is then reduced again via another recursive call to `reduceInChunks`.\n\n### High-Level Reduction Pattern (Visualized)\n\n```\nitems → [batch1, batch2, ...]  (grouping pass)\n       ↓ each batch recursively reduced\n[chunk1, chunk2, ...]           (intermediate results)\n       ↓ single recursive call on all chunks combined\nfinal synthesized output        (terminal synthesis)\n```\n\nThis creates a tree-like reduction where the first level groups items into batches and synthesizes them independently, then a second pass merges those syntheses together for final consolidation.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n- `items[0]` — When `len(items) == 1`, the first element is reassigned (`items[0] = items[0][:maxChunkChars] + \"\\n...[truncated]\"`). Since slices share an underlying array, this mutates caller-visible data if the backing array is shared.\n- All other state changes (`batches`, `currentBatch`, `currentLen`, `intermediate`) are confined to local variables within this function and do not escape.\n\n**Concurrency Mechanisms:** None. No mutexes, channels, or goroutines are used in this file. The `context.Context` parameter is accepted only for cancellation/timeout propagation.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis\n\n### I/O Communication\n\n**Network (LLM API)**: The function makes exactly one or more calls to `c.CallLLM(ctx, ...)`. Each call sends a prompt and receives an LLM response. The number of calls depends on recursion depth — when batches collapse into a single batch, it recurses again until reaching the base case where all items fit in one context window.\n\n**No Disk/DB/File I/O**: This code contains no file system or database operations. All interactions are through the `LLMClient` interface and the `logEvent` callback.\n\n**Logging/Metrics (via callback)**: The function writes status events to an external system via the injected `logEvent func(string, string)` parameter. Called on three paths:\n- When a single item exceeds `maxChunkChars` (truncation warning)\n- When items cannot be batched together (warning + forced truncation)\n- When synthesizing a chunk (`\"LLM Synthesizing chunk for %s (%d items)\"`)\n\n### Error Handling Strategy\n\n**Error propagation**: Errors from recursive calls and the LLM call are propagated directly. The function returns `\"\"` with an error on failure — no wrapping beyond what's explicit:\n- Recursive call errors: returned as-is (`return \"\", err`)\n- LLM synthesis errors: wrapped with context string (`\"LLM error during synthesis: %w\"`), then returned\n\n**No panic/sentinel recovery**: Failures are never swallowed or recovered. The function always returns the first non-nil error it encounters up the call stack.\n\n**Graceful degradation (no error)**: Truncation is a silent side effect, not an error path. When a single item exceeds `maxChunkChars`, the slice element is mutated in-place (`items[0] = items[0][:maxChunkChars] + \"\\n...[truncated]\"`) and processing continues normally — no warning logged unless it's the only item.\n\n**Empty input**: Returns `(\"\", nil)` immediately when `len(items) == 0`."
    },
    "internal/engine/client.go": {
      "sha256": "d5fbb0381361a37bbfb4ed19d88717148932382acd07a5517cc26a260d22c883",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area: `internal/engine/client.go`\n\n## Structs\n\n| Name | Fields |\n|------|--------|\n| **`Message`** | `Role`, `Content` (both `string`) |\n| **`LLMClient`** | `ModelID`, `BaseURL`, `NumCtx` (`string`, `string`, `int`); `HTTPClient` (`*http.Client`) |\n\n## Functions\n\n| Name | Input / Return | Description |\n|------|---------------|-------------|\n| **`NewLLMClient(modelID, baseURL string, numCtx int)`** | → `*LLMClient` | Factory for creating a new client. |\n\n## Methods on `LLMClient`\n\n| Method | Input | Output / Return | Description |\n|--------|-------|-----------------|-------------|\n| **`CallLLM(ctx context.Context, systemPrompt string, messages []Message, jsonFormat bool)`** | — | `(string, error)` | Blocks until the LLM returns a full response. |\n| **`StreamLLM(ctx context.Context, systemPrompt string, messages []Message, onChunk func(string))`** | — | `error` | Calls the LLM and invokes `onChunk` for each streamed chunk. |\n| **`GetBaseSystemPrompt()`** | — | `string` | Returns the base prompt text (static). |\n| **`GetDefaultSystemPrompt(command string)`** | — | `string` | Dispatches on `command` (\"module_synthesis\", \"architecture\") and returns a composed prompt. |\n\n## Non-Exported Types (Internal)\n\nThese are lowercase and not part of the public surface: `ollamaRequest`, `ollamaOptions`, `ollamaResponse`, `ollamaChunk`, `prepareOllamaRequest`.\n\n#### [BUSINESS_LOGIC]\n## Business Rules \u0026 Domain Concepts\n\nThis file implements a client for interacting with an Ollama-compatible LLM API. The core business logic revolves around two operational modes:\n\n- **Synchronous (non-streaming) calls**: Build a complete request, send it, wait for the full response, parse it once, and return the entire message content or an error.\n- **Streaming calls**: Send a request with streaming enabled, then consume the HTTP response line-by-line as JSON chunks arrive. Each chunk is parsed individually; when content exists, a callback function is invoked immediately. The loop terminates when either the `done` flag arrives or the connection closes (EOF).\n\nA system prompt is always prepended to any user-provided messages before being sent to the API. This base prompt establishes agent identity and defensive operating rules that apply regardless of the task type.\n\nAdditionally, there are two documented command modes that layer different documentation tasks onto the same base:\n- **module_synthesis**: Task is to write technical documentation for a code module based on its internal components; group functions/classes under headings, explain data flow and responsibility, keep it dense.\n- **architecture**: Task is to write global architecture or quickstart documents from module summaries; describe system boundaries and inter-module interactions, provide a developer-friendly overview.\n\n## High-Level Algorithmic Flow\n\n1. Construct an Ollama API request body by combining the configured model ID with any user-supplied messages and a leading system prompt message.\n2. For non-streaming calls: set `Stream` to false; for streaming calls: set `Stream` to true. Optionally enable JSON format output via the `Format` field.\n3. Apply the context window size (`NumCtx`) through Ollama options in every request.\n4. Send the HTTP POST to `/api/chat`; handle success by parsing the response into a structured result, or failure by reading an error status and returning it with the truncated body.\n5. In streaming mode: read each line from the connection, parse as JSON chunk, invoke the consumer callback for non-empty content segments, and exit the loop when `Done` is signaled or the stream ends.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State Analysis: `internal/engine/client.go`\n\n### Mutable State Identified\n\n**None.** No mutable global variables exist in this file. The `LLMClient` struct holds four fields (`ModelID`, `BaseURL`, `NumCtx`, `HTTPClient`), but all are assigned only once during construction via `NewLLMClient()` and never modified by any method on the type.\n\n### Concurrency Mechanisms\n\n**None.** No mutexes, channels, or other synchronization primitives appear in this file. Since no state is mutated after initialization, concurrent reads of the struct fields are safe by virtue of being read-only post-construction, but there is no explicit concurrency protection present.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects (I/O Communication)\n\n| Function | Type of I/O | Details |\n|----------|-------------|---------|\n| `prepareOllamaRequest` | Outbound HTTP | Builds a POST request to `\u003cBaseURL\u003e/api/chat`. Payload is JSON-marshaled in-memory, then sent via `http.NewRequestWithContext` with `\"Content-Type: application/json\"`. |\n| `CallLLM` | Outbound + Inbound HTTP | Performs the same outbound call as above. On success (200), reads the full response body into memory and parses it. On non-200 status, reads up to 1024 bytes of the error response before returning. |\n| `StreamLLM` | Outbound + Inbound HTTP streaming | Performs a streaming POST request. Reads the response body line-by-line using `bufio.NewReader`. Each non-empty JSON line is parsed and passed to the caller-supplied callback (`onChunk`). Loop exits on EOF or `chunk.Done == true`. |\n| `GetBaseSystemPrompt` / `GetDefaultSystemPrompt` | None (pure) | No I/O. Return only string values constructed from static literals and a switch statement. |\n\n## Error Handling\n\n### How errors are returned:\n\n| Location | Behavior |\n|----------|---------|\n| JSON marshal failure in `prepareOllamaRequest` | Wrapped with `fmt.Errorf(\"failed to marshal request: %w\", err)`. |\n| Request creation failure (`http.NewRequestWithContext`) in `prepareOllamaRequest` | Returned directly, unwrapped. |\n| HTTP transport error in `CallLLM` / `StreamLLM` | Returned directly from `c.HTTPClient.Do(req)` without wrapping. |\n| Response body read failure (non-streaming) in `CallLLM` | Wrapped as `\"failed to parse response: %w\"`. |\n| Non-200 response in `CallLLM` | Returns a formatted string error containing status code and up-to-1024-byte partial body. No wrapping; caller receives exactly this string. |\n| Response body read failure (streaming) in `StreamLLM` | Wrapped as `\"error reading stream: %w\"`. |\n| Invalid JSON line inside stream loop in `StreamLLM` | Silently swallowed — the chunk is skipped, no error returned to caller. The caller has no way to know an invalid chunk was encountered unless EOF arrives or a real reader error occurs first. |\n| Stream EOF (`io.EOF`) in `StreamLLM` | Breaks cleanly out of loop; returns `nil`. Not treated as an error. |\n\n### Other observations:\n\n- `c.HTTPClient.Timeout` is set to 10 minutes. This applies to both non-streaming and streaming HTTP calls (the same client instance).\n- The `resp.Body.Close()` deferred call exists in `CallLLM` and `StreamLLM`, so the body is closed even on error paths.\n- `strings.TrimSpace(line)` strips whitespace from each streamed line before parsing — this means bare newlines or carriage returns are skipped without error.\n- The import `strings` appears unused (not referenced anywhere outside of `prepareOllamaRequest`'s URL construction, which uses it indirectly via the trim suffix on `c.BaseURL`)."
    },
    "internal/engine/json_parser.go": {
      "sha256": "917d393076b43674981595c215f7f60d9f25ba3f40a1e9a2caf33e5232bce263",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area\n\n## Functions\n\n| Function | Input(s) | Output |\n|---|---|---|\n| **StripOuterMarkdownFence** | `string` | `string` |\n| **CleanJSONResponse** | `string` | `string` |\n| **UnmarshalJSONResponse** | `string`, `interface{}` | `error` |\n\n#### [BUSINESS_LOGIC]\n## Domain Concepts \u0026 Business Rules\n\n### 1. LLM Response Format Handling\nLLM APIs commonly wrap tool-call responses in markdown code fences (```) with optional language tags like `json` or `markdown`. This file normalizes those raw LLM outputs into parseable JSON before consumption by downstream business logic.\n\n**Rule:** The parser must tolerate and strip surrounding markdown fences regardless of whether a language tag is present, because real-world LLM output includes both ```` ```json ... ``` ` and plain ```` ``` ... ` `` formats.\n\n### 2. Bracket-Depth Aware Extraction\nA raw response may contain JSON embedded within other text (e.g., conversation history containing `{` or `[` from prior messages). The parser cannot simply find the first opening brace—it must walk character-by-character while respecting string boundaries and matching balanced brackets/braces to locate the true start of the target JSON value.\n\n**Rule:** Extraction resumes at the first unescaped `{` or `[`, whichever appears earliest, then walks forward until a matching close is found—skipping over quoted strings where braces appear as literal characters rather than structural delimiters.\n\n### 3. Unified Unmarshal Path\nThe file provides one entry point (`UnmarshalJSONResponse`) that combines cleaning and deserialization. The cleaned output is passed directly to `encoding/json.Unmarshal`, so the caller receives a populated struct without needing to know whether the input came from a markdown-fenced LLM response or was already raw JSON.\n\n### High-Level Algorithmic Flow\n\n1. **Trim whitespace** from the input string.\n2. **Attempt fence removal**: match against a regex pattern that captures content between ```` ``` ` (with optional `markdown`/`json` tag). If matched, return only the captured inner text.\n3. **Locate JSON start**: scan for the first `{` or `[`. If neither is found, return input unchanged. Prefer `{` when present; otherwise use `[`.\n4. **Walk and match brackets**: iterate character-by-character from the start index:\n   - Skip escaped characters (`\\\\`) inside quoted strings to avoid false matches.\n   - Track string boundaries by toggling an `inString` flag at each unescaped `\"`.\n   - When outside a string, push `{`/`[` onto a stack and pop on matching `}`/`]`. Return the substring from start index up to and including the closing character once the stack drains.\n5. **Unmarshal**: pass the cleaned string to the JSON decoder and return any error it produces.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** None. The only package-level variable is `markdownFenceRe` (a compiled `*regexp.Regexp`) which is read-only after initialization and never modified. All function parameters are local to their calls.\n\n**Concurrency Mechanisms:** None. No mutexes, channels, atomics, or other synchronization primitives are present in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 External Communication\n\n**No direct I/O observed.** No disk, network, or database operations are invoked in this file. The only external interaction is through `encoding/json.Unmarshal` (standard library) inside `UnmarshalJSONResponse`, which performs byte-to-value deserialization.\n\n---\n\n## Error Handling\n\n| Function | Behavior |\n|---|---|\n| **StripOuterMarkdownFence** | Returns the trimmed input unchanged when no fence match occurs. No error is signaled. |\n| **CleanJSONResponse** | Returns `string` only. Errors (e.g., malformed JSON, unmatched brackets) are silently swallowed — the function either returns a partial/extrapolated string or the original input. No error path exists. |\n| **UnmarshalJSONResponse** | Wraps the underlying `json.Unmarshal` error with context: `failed to unmarshal JSON (cleaned: %q): \u003coriginal\u003e`. Errors propagate as wrapped values — no panic, no silent swallow. |\n\n---\n\n## Observations\n\n- **Error wrapping is explicit** in one function (`UnmarshalJSONResponse`) but absent in the other two. Callers of `CleanJSONResponse` and `StripOuterMarkdownFence` have no way to detect failures beyond inspecting output shape.\n- **Bracket-matching logic assumes well-formed JSON.** If the cleaned string contains nested braces/brackets that don't close (e.g., `{a: b c}`), the function returns whatever was consumed up to the last unmatched character rather than failing explicitly."
    },
    "internal/engine/orchestrator.go": {
      "sha256": "d4785d5b08206663d1152219bd8ec2b401da64501a26333eab1b472aec11290a",
      "facts": "#### [API_SIGNATURES]\n## Public Surface Area of `internal/engine/orchestrator.go`\n\n### Exported Structs\n\n| Type | Fields / Methods |\n|------|-----------------|\n| **Event** | - `Type string` \u003cbr\u003e - `Message string` |\n\n---\n\n### Methods on `*LLMClient` (type defined elsewhere)\n\nThese methods are invoked in this file but belong to the external type `*LLMClient`. Their signatures, as used here:\n\n| Method | Input Parameters | Return Value |\n|--------|-----------------|--------------|\n| **GenerateStandardDocs** | `(ctx context.Context, repoRoot string, docsDir string, rootSum string, logEvent func(string, string))` | `error` |\n| **RunInit** | `(ctx context.Context, repoRoot string, cfg *config.Config, onEvent func(Event))` | `error` |\n| **RunUpdate** | `(ctx context.Context, repoRoot string, cfg *config.Config, onEvent func(Event))` | `error` |\n\n---\n\n### Internal (unexported) Functions — Not Part of Public Surface\n\n- `logEvent`, `markAllAffected`, `collectDirs`, `determineAffected`, `propagateAffected`, `buildTree`, `synthesizeNode`, `loadMetadataCache`, `saveMetadataCache`, and all other lowercase helpers are **not exported** from this file.\n\n#### [BUSINESS_LOGIC]\n## Domain Concepts \u0026 Business Rules\n\n### Core Purpose\nThis module implements an **automatic documentation generation pipeline** for software repositories. It uses an LLM to analyze source code structure and produce markdown documentation (architecture overview, quickstart guide, per-directory summaries). Documentation is updated incrementally—only when the underlying code changes.\n\n### Key Domain Concepts\n\n| Concept | Description |\n|---|---|\n| **Code Discovery** | Walks a repository root, producing a list of source-code files while respecting `.gitignore`, configured ignore patterns, and excluded file types/paths. |\n| **Hierarchical Tree-Merging** | The discovered files are organized into a directory tree (`DirNode`). An LLM is invoked recursively per node, synthesizing summaries that flow from leaves to root. The root-level synthesis becomes the \"root summary\" used by higher-level docs. |\n| **Metadata Cache** | A `MetadataCache` persists file hashes and module summary paths. It enables incremental detection of changes across runs without re-analyzing unchanged code. |\n| **Affected-Directory Propagation** | When a file is added, modified, or deleted, every ancestor directory is marked as \"affected.\" Only those directories are synthesized in the update run; unaffected ones reuse cached summaries. |\n| **Standard Docs Generation** | Two always-referenced pages (`architecture.md`, `quickstart.md`) are generated from the root summary. They are only regenerated if they don't exist or were impacted by a change. |\n\n### Algorithmic Flow — `RunInit` (Initial Documentation)\n\n1. **Discover code files**: Load `.gitignore`, apply configured ignore lists, scan repository for source-code files.\n2. **Build directory tree**: Convert the flat file list into a hierarchical `DirNode` tree rooted at the repo root.\n3. **Synthesize node summaries**: Walk the tree bottom-up; each node is summarized by the LLM. The root node's summary becomes `rootSum`.\n4. **Generate standard docs**: Invoke the LLM to write `architecture.md` and `quickstart.md`, passing `rootSum` as context.\n5. **Update AGENTS.md**: Append (or create) an \"AI Agent Guidelines\" section pointing agents to the generated documentation locations.\n6. **Persist cache**: Save the final `MetadataCache` with a `\"local\"` commit marker.\n\n### Algorithmic Flow — `RunUpdate` (Incremental Documentation)\n\n1. **Re-discover code files** using identical filtering logic as init.\n2. **Detect changes via SHA256 comparison**: Hash each current file and compare against the cached hash stored in `MetadataCache`. Classify each difference into one of three states: `\"Added\"`, `\"Modified\"`, or `\"Deleted\"`.\n3. **Compute affected directories**: For every changed file, walk upward from its parent to the root directory, marking each ancestor as affected. Deleted files are removed from the cache immediately; modules whose directories are no longer active are also purged.\n4. **Rebuild tree and prune cache** so it reflects only currently existing code.\n5. **Propagate affect markers** through the tree (via `propagateAffected`). If no directories are affected, short-circuit with a \"no modifications\" message and return.\n6. **Synthesize only affected nodes**: Invoke `synthesizeNode` recursively but skip unaffected subtrees entirely—reusing cached summaries for them.\n7. **Regenerate standard docs conditionally**: Regenerate `architecture.md`/`quickstart.md` if they are missing or any top-level directory was affected; otherwise reuse existing files.\n8. **Persist updated cache** with `\"local\"` commit marker.\n\n### Cross-Cutting Rules\n\n- **Gitignore stacking**: `.gitignore`, configured ignore lists, and the `docsDir` itself are all applied together when discovering code files.\n- **Safe file resolution**: All paths are resolved through `security.SafeResolve` before any filesystem operation.\n- **Atomic writes**: Documentation is written via `tools.WriteFileSafely`, implying write-through with error handling rather than in-place edits.\n- **Event logging**: Every step emits a typed event (type + message) so callers can observe pipeline progress without parsing log output.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State Analysis for `internal/engine/orchestrator.go`\n\n### Mutable State Identified\n\n| Variable | Type | Scope | Mutated? | Notes |\n|---|---|---|---|---|\n| `cache` (`*MetadataCache`) | Pointer to struct | Local (passed as parameter) | Yes | Reassigned in both `RunInit` and `RunUpdate` when loading fails or is nil. Fields include `Files`, `Modules`, `LastDocumentedCommit`. Modified via `saveMetadataCache` call after pipeline runs. |\n| `affectedDirs` (`map[string]bool`) | Map | Local | Yes | Populated by recursive tree traversal in `RunInit`. Also populated and mutated in `RunUpdate` during file change processing. Passed to `synthesizeNode` as parameter. |\n| `filteredChanges` (`[]FileChange`) | Slice | Local | Yes | Appended to in multiple places in `RunUpdate`. Returned/used downstream (not visible in this file). |\n| `currentFilesMap` (`map[string]string`) | Map | Local | Yes | Built by iterating `allowedCodeFiles`, then iterated in later loop. |\n| `allowedCodeFiles` (`[]string`) | Slice | Local | Yes | Appended to during iteration of `codeFiles`. |\n| `cachedFiles` (`[]string`) | Slice | Local | Yes | Built from `cache.Files` keys, sorted, then iterated for deletion detection. |\n| `agentFileBytes` (bytes) | Slice | Local | No | Read-only local variable in `RunInit`. |\n| `content` (string) | String | Local | No | Derived from read file bytes; not stored back to cache. |\n\n### Concurrency Mechanisms\n\n**No concurrency primitives detected.** This file contains:\n- No `sync.Mutex`, `sync.RWMutex`, or other `sync` package usage\n- No channels (`chan`)\n- No atomic operations\n- No concurrent map access protections visible\n\nThe code is single-threaded with respect to the state listed above.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 I/O Communication\n\n### Disk I/O\n\n**Writes:**\n\n| Path | Condition | Effect |\n|---|---|---|\n| `docsDir/architecture.md` | Always (in both pipelines) | Written via `tools.WriteFileSafely`, content stripped of outer markdown fences. |\n| `docsDir/quickstart.md` | Always (in `RunInit`) / Conditional in `RunUpdate` | Same write semantics as above. In `RunUpdate`, only written if `affectedDirs[\".\"]` is true or either file doesn't exist. |\n| `AGENTS.md` | Always (in both pipelines) | If file exists and does NOT contain \"AI Agent Guidelines\", appends a separator + the full guidelines block. If missing, creates it fresh. Errors swallowed. |\n| `docsDir/modules/\u003csafe-filename\u003e.md` | When module is no longer in `activeDirs` cache | Removed via `os.Remove(absModuleFile)`. Error from removal is ignored (`_ = os.Remove`). |\n\n**Reads:**\n\n| Path | Condition | Effect |\n|---|---|---|\n| `\u003crepoRoot\u003e/.gitignore` | Both pipelines | Read via `tools.LoadGitignore(repoRoot)`. Error logged as warning, NOT returned. Swallowed. |\n| `\u003crepoRoot\u003e/AGENTS.md` | `RunInit`: read via `tools.ReadFileSafely`; `RunUpdate`: not read (file created if missing). | In `RunInit`, error swallowed (`_ = tools.ReadFileSafely`). If exists and contains \"AI Agent Guidelines\", skipped. Otherwise appended to. |\n| `docsDir/architecture.md` / `docsDir/quickstart.md` | Only in `RunUpdate` | Existence checked via `security.SafeResolve` + `os.Stat`. Errors from resolve/stat swallowed. |\n\n**Other:**\n\n- `modulesDir` created with mode `0755` if not present (`os.MkdirAll`). Error wrapped and returned.\n- All file writes go through `tools.WriteFileSafely`; all reads through `tools.ReadFileSafely`. Neither signature is visible here, but errors from them are either wrapped into return values or swallowed with `_`.\n\n### Network I/O (LLM Calls)\n\n| Function | Effect | Error Handling |\n|---|---|---|\n| `c.CallLLM` (inside `GenerateStandardDocs`) | Called twice per invocation: once for architecture.md, once for quickstart.md. | Wrapped via `fmt.Errorf(\"failed to generate ...: %w\", err)`. Propagated up as error. |\n| `synthesizeNode(ctx, c, tree, repoRoot, cfg, cache, affectedDirs, logEvent)` | Internal recursive LLM synthesis over the code tree. | Error propagated directly (`return err`). In `RunInit`, returned to caller. In `RunUpdate`, wrapped via `fmt.Errorf` and then re-wrapped in a parent error at `if err := synthesizeNode(...); err != nil { return err }`. |\n\n### Sentinel / Panic\n\n- **No panics** detected in this file. All errors are either returned as typed errors, wrapped with `fmt.Errorf`, or swallowed via `_ = \u003cerr\u003e`.\n- **No sentinel values.** Errors carry context via `%w` wrapping chains. The \"no changes\" path uses `len(affectedDirs) == 0` boolean check, not an error sentinel.\n\n## Error Handling Summary\n\n| Location | Behavior |\n|---|---|\n| `.gitignore` load failure | Logged as warning (`logEvent(\"status\", fmt.Sprintf(\"Warning: ...\"))`). Swallowed. Pipeline continues. |\n| `DiscoverCodeFiles` failure | Returned directly to caller. Stops pipeline. |\n| `os.MkdirAll` failure | Wrapped with `%w`. Returns error. Stops pipeline. |\n| Metadata cache load failure or nil result | Logged as warning. Fresh cache constructed in memory (`\u0026MetadataCache{...}`). Swallowed. Pipeline continues. |\n| AGENTS.md read failure | Swallowed (`_ = ...`). Falls through to write path (creates file if missing). |\n| LLM call failure (architecture / quickstart) | Wrapped with descriptive message + `%w`. Propagated up. Stops pipeline at that step. |\n| `synthesizeNode` failure | In `RunInit`: propagated directly. In `RunUpdate`: returned as-is to caller, stopping pipeline. |\n| Module cleanup removal (`os.Remove`) | Ignored (`_ = os.Remove`). Effect observed or not; no error surfaced. |"
    },
    "internal/engine/runner.go": {
      "sha256": "50177f41ec89d76e503c57760f5f77f32cd49a4476e3c06d76314cce95577116",
      "facts": "#### [API_SIGNATURES]\n### Structs\n\n- **`Runner`**\n  - Fields (internal): `cfg *config.Config`\n\n### Methods (on `Runner`)\n\n- **`NewRunner(cfg *config.Config) *Runner`**\n- **`Run(ctx context.Context, repoRoot string, mode string, onEvent func(Event)) error`**\n\n#### [BUSINESS_LOGIC]\n### Business Logic and Domain Concepts\n\n*   **Pipeline Orchestration:** The `Runner` struct acts as an orchestrator for code analysis or documentation pipelines. It manages the lifecycle of execution from initialization through completion, handling state transitions based on a provided mode string.\n*   **Repository Safety Protocols:** Before executing any pipeline logic, the system ensures repository integrity by verifying that the lockfile is excluded from version control (`EnsureGitignoreHasLockfile`). This is treated as a best-effort safety check; if it fails, the operation logs a warning but does not halt execution.\n*   **Concurrency Control:** The runner acquires an exclusive lock on the repository via `security.AcquireLock`. This prevents concurrent execution of pipeline steps against the same repository root. A deferred unlock ensures resource release regardless of how the function exits.\n*   **LLM Client Initialization:** Upon entry, a client is instantiated to communicate with an external large language model (likely Ollama-based), configured via fields like `ModelID`, `OllamaBaseURL`, and `OllamaNumCtx`.\n*   **Mode-Based Execution Strategy:** The runner delegates logic to specific pipeline methods based on the input string:\n    *   `\"init\"` triggers an initialization pipeline.\n    *   `\"update\"` triggers a refresh/update pipeline.\n    *   Any other value results in a fatal error indicating unsupported configuration.\n\n### High-Level Algorithmic Flow\n\n1.  **Context Setup:** A callback function (`onEvent`) is defined to handle event logging if provided by the caller.\n2.  **Safety Pre-flight Check:** The system verifies that the lockfile entry exists in `.gitignore`. If this check fails, a warning is logged via the event handler, but processing continues.\n3.  **Exclusive Lock Acquisition:** The runner calls `security.AcquireLock(repoRoot)`. If the acquisition fails, it returns an immediate error containing the underlying cause.\n4.  **Deferred Release:** A deferred function is attached to ensure the lock acquired in step 3 is released when the function scope exits.\n5.  **Client Instantiation:** An LLM client object is created using configuration parameters stored on the `Runner`.\n6.  **Pipeline Dispatch:** The runner evaluates the `mode` string:\n    *   If `\"init\"`, it calls `client.RunInit(ctx, repoRoot, r.cfg, onEvent)`.\n    *   If `\"update\"`, it calls `client.RunUpdate(ctx, repoRoot, r.cfg, onEvent)`.\n7.  **Error Handling:** If neither mode matches, the function returns a fatal error indicating an unsupported mode was provided.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** None directly observable in this file. The `cfg` field on `Runner` is assigned once via constructor and not reassigned anywhere in this file. No other fields are mutated after initialization.\n\n**Concurrency Mechanisms:** The `lock` returned by `security.AcquireLock(repoRoot)` is acquired before the lockfile check and released via `defer lock.Unlock()` — indicating a locking mechanism (likely file-based or mutex). However, since `internal/security` is not in this source text, the exact concurrency primitive cannot be confirmed.\n\n**Summary:** No directly observable mutable state in this file; one external lock from `security.AcquireLock` is used with deferred release, but its type and protection scope are outside the visible code.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects (Outside-World Communication)\n\n| Operation | I/O Type | Description |\n|---|---|---|\n| `security.EnsureGitignoreHasLockfile(repoRoot)` | **Disk** (read + write) | Reads and modifies `.gitignore` in `repoRoot`. If it fails, the error is swallowed—logged as an event but execution continues. |\n| `security.AcquireLock(repoRoot)` | **Disk** (write) | Writes a lockfile to disk under `repoRoot`. Returns immediately if acquisition fails; no further I/O proceeds. |\n| `defer lock.Unlock()` | **Disk** (read + write, deferred cleanup) | Releases the lock by cleaning up the lockfile on disk. Runs unconditionally after this point in the function scope—cleanup happens even if subsequent steps fail. |\n| `client.RunInit(ctx, ...)` / `client.RunUpdate(ctx, ...)` | **Network** (HTTP) | The actual I/O occurs here: these methods call Ollama over HTTP using `r.cfg.OllamaBaseURL` and `r.cfg.OllamaNumCtx`. No network communication happens in this file directly. |\n\n## Error Handling Summary\n\n| Step | Behavior on Failure |\n|---|---|\n| `EnsureGitignoreHasLockfile` | **Swallowed.** Logged as a warning event via `onEvent`, then execution proceeds normally. Not fatal. |\n| `AcquireLock` | **Fatal, wrapped.** Returns `fmt.Errorf(\"failed to acquire repository lock: %w\", err)`. The caller sees the original error wrapped. |\n| `client.RunInit` / `client.RunUpdate` | **Propagated raw.** Error is returned directly without wrapping. The deferred lock cleanup still runs. |\n| Unsupported mode (`else`) | **Fatal, wrapped.** Returns `fmt.Errorf(\"unsupported mode: %s\", mode)`. |\n| Normal exit path | Returns `nil`. |\n\n## Key Observations\n\n- **Lock acquisition is the gatekeeper.** If the lock can't be acquired, nothing else runs. The deferred unlock still executes (no leak), but no LLM calls happen.\n- **Gitignore fix is best-effort.** Its failure does not abort the pipeline. This is an intentional \"warn-but-continue\" pattern.\n- **No panics** are observed in this file; all errors surface as `error` return values to the caller."
    },
    "internal/engine/synthesize.go": {
      "sha256": "3a6670da0d7ba6a05ac171e44016d0dafbe9b30700bbcb4c0a3ed0ffe5f4ef13",
      "facts": "#### [API_SIGNATURES]\n## Structs: None\n\n## Interfaces: None\n\n## Functions: None (all unexported)\n\n#### [BUSINESS_LOGIC]\n## Domain Concepts \u0026 Business Rules\n\n**Hierarchical Module Synthesis.** The file implements a recursive walk over a directory tree (`DirNode`), producing a synthesized textual summary for each node (file or folder). Each directory's summary is built from its child summaries plus facts extracted from its files—then all components are collapsed into a single module-level document via chunked reduction.\n\n**Change Detection \u0026 Caching.** Before processing any subtree, the function checks `affectedDirs` against the cached state. If no descendant has been affected since last synthesis and a cached summary exists, the entire subtree is skipped entirely—no LLM calls required. This is an optimization rule: *don't re-synthesize unchanged directories*.\n\n**Incremental File Fact Extraction.** For each file within a node:\n- A SHA256 hash of the file on disk determines whether it changed since last run.\n- Unchanged files reuse previously extracted facts from cache.\n- Changed or new files trigger LLM-based extraction through configurable `ExtractionSteps`, where each step appends its own prompt to a base system prompt and asks the model to produce structured \"facts\" about the file's content. Content is truncated before being sent, respecting a dynamic limit derived from context window size.\n\n**Component Aggregation.** After all files and child summaries are processed, they form an ordered list of components (files first, then subdirectories). These components feed into `reduceInChunks` to produce a single condensed summary for the directory.\n\n**Persistence.** The final synthesized summary is written to disk as markdown under `docs/modules/`, keyed by a safe filename derived from the directory path. This makes the synthesis durable across runs and provides an external artifact alongside the in-memory cache.\n\n## High-Level Algorithmic Flow\n\n1. **Context check** — abort early if context is cancelled.\n2. **Cache hit short-circuit** — if node is not affected and has a cached summary, return it immediately.\n3. **Recursive child synthesis** — sort children, then synthesize each one in order; collect non-empty summaries into `childSummaries`.\n4. **File fact extraction loop** — for each file: compute hash, decide cache hit vs. LLM-driven extraction, accumulate facts.\n5. **Component assembly** — concatenate file components followed by child subsystem components into a single ordered slice.\n6. **Chunked reduction** — pass all assembled components to `reduceInChunks` to produce the directory's final summary.\n7. **Cache update \u0026 persist** — store the result in both the metadata cache and on disk under the configured docs path; return the summary string.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State\n\n| Variable | Type | Where Mutated | Details |\n|---|---|---|---|\n| `cache.Files` (map entry) | `FileCacheEntry{SHA256, Facts}` | Inside loop over `node.Files` | `cache.Files[f] = FileCacheEntry{...}` — written when file content differs from cache or extraction steps run. |\n| `cache.Modules` (entry) | `string` | After `reduceInChunks` succeeds | `cache.Modules[node.Path] = finalSum` — written after directory synthesis completes. |\n\n## Concurrency Mechanisms\n\n**No concurrency protection observed.** No `sync.Mutex`, channels, atomic types, or other synchronization primitives are used to guard the mutable state above. Any concurrent callers that modify the same `*MetadataCache` (or share it) could race on writes to `cache.Files[f]` and `cache.Modules[node.Path]`.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects (External Communication)\n\n### Disk I/O\n\n| Operation | Direction | Notes |\n|-----------|-----------|-------|\n| `computeSHA256(repoRoot, f)` | Read | Computes SHA-256 by reading a file from disk. Error swallowed (`continue`). |\n| `tools.ReadFileSafely(repoRoot, f)` | Read | Reads file content for LLM extraction. Error swallowed (`continue`). |\n| `tools.WriteFileSafely(repoRoot, modulePath, ...)` | Write | Persists synthesized module documentation to `\u003crepo\u003e/docs/modules/\u003csafe-filename\u003e.md`. Errors wrapped and returned. |\n\n### Network I/O\n\n| Operation | Direction | Notes |\n|-----------|-----------|-------|\n| `c.CallLLM(ctx, systemPrompt, messages, false)` | Outbound (HTTP) | Called once per extraction step per file. Each call is a network request to an LLM provider. Errors wrapped and returned immediately. |\n\n### In-Memory State Mutations\n\n- `cache.Modules[node.Path]` — module cache updated after successful synthesis\n- `cache.Files[f] = FileCacheEntry{...}` — file cache updated with computed facts + SHA256\n- `affectedDirs` map read (passed by caller)\n\n---\n\n## Error Handling\n\n### Errors Returned Upstream\n\n| Condition | Behavior |\n|-----------|----------|\n| Context cancellation (`ctx.Err()`) at function entry | Returns `(string, error)` — caller sees the error |\n| LLM call failure during extraction step | Wrapped: `fmt.Errorf(\"LLM error extracting %s for %s: %w\", ...)`. Stops all further processing for this node. |\n| Module write failure | Wrapped: `fmt.Errorf(\"failed to write module documentation for %s: %w\", ...)`. Stops the function. |\n\n### Errors Swallowed (Silently Skipped)\n\n| Condition | Behavior |\n|-----------|----------|\n| SHA-256 computation fails (`computeSHA256` error) | `continue` — file hash unknown; cache lookup uses stale entry or falls to re-read path |\n| File read failure (`tools.ReadFileSafely` error) | `continue` — file skipped from component list entirely |\n\n### Error Propagation Characteristics\n\n- **Short-circuit on LLM errors**: A single failed extraction step aborts the entire node synthesis. No partial results are returned.\n- **Partial failure tolerance for files**: If one file fails to read or hash, other files still contribute components. The result may be incomplete but not an error.\n- **No sentinel/panic pattern observed** — all errors flow through the `(string, error)` return signature."
    },
    "internal/engine/tree.go": {
      "sha256": "d8987ef9a59995483f576053a3464667ac915de6dbbacf27e0e4fa2b3f133538",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area: engine/tree.go\n\n## Structs\n\n### FileChange\n```go\ntype FileChange struct {\n    Path   string // \"Added\", \"Modified\", \"Deleted\"\n    Status string // \"Added\", \"Modified\", \"Deleted\"\n}\n```\n\n### DirNode\n```go\ntype DirNode struct {\n    Path     string\n    Files    []string\n    Children map[string]*DirNode\n}\n```\n\n## Functions\n\n### isAllowedFile\n- **Parameters:** `repoRoot` (string), `relPath` (string), `gitIgnore` (*ignore.GitIgnore), `ignoredExtensions` ([]string)\n- **Returns:** bool\n\n#### [BUSINESS_LOGIC]\n**Domain Concepts:**\n\n- **FileChange**: Represents a single file modification event — path plus one of three statuses (Added, Modified, Deleted). This is the input primitive for tracking what changed across the repository.\n- **DirNode tree structure**: A directory hierarchy modeled in memory. Each node holds its own list of files and a map of child subdirectories keyed by name. `buildTree` converts a flat slice of file paths into this nested representation by walking each path's directory components and creating intermediate nodes as needed.\n- **Affected-directory propagation**: The system distinguishes between \"affected\" directories (those containing changed files) and unaffected ones. Propagation rules are:\n  - A directory is affected if any of its direct files appear in the change set.\n  - A directory is also marked affected when a corresponding `modules/\u003cpath-with-separators-replaced-by-underscores\u003e.md` file does not exist at its expected location under docsDir.\n  - A directory is marked affected when it has no cached module entry (cache.Modules[n.Path] == \"\").\n- **Recursive bottom-up propagation**: Once individual directories are flagged as affected, the system walks upward and marks any parent directory as affected if any child below it is already affected. This ensures ancestor nodes reflect changes even when they themselves contain no direct file modifications.\n\n**High-Level Algorithmic Flow:**\n\n1. Receive a list of changed files (each with path + status).\n2. Build a hierarchical DirNode tree from the full set of repository paths — this establishes the directory structure without requiring pre-existing knowledge.\n3. Walk the tree recursively, evaluating each node against three conditions:\n   - Does any file in this directory appear in the change set?\n   - Does the expected module markdown file exist for this path (under docsDir/modules)?\n   - Is there a cached module entry for this path?\n4. If any condition is true, mark the current node as affected and recurse into children to check them independently.\n5. After the full walk completes, propagate affected status upward: if any child of a directory returned \"affected\", the parent becomes affected too.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n| Variable / Field | Location | Type | Mutation Pattern |\n|---|---|---|---|\n| `affectedDirs` | parameter of `propagateAffected`, `determineAffected` | `map[string]bool` | Added to via `affectedDirs[node.Path] = true` in both functions; passed through recursive calls. |\n| `changedFiles` | local inside `determineAffected` | `map[string]bool` | Created fresh per call with `make(map[string]bool)`, then populated with `changedFiles[c.Path] = true`. |\n| `DirNode.Files` | field on `*DirNode` | `[]string` | Appended to in `buildTree` via `curr.Files = append(curr.Files, f)`. |\n| `DirNode.Children` | field on `*DirNode` | `map[string]*DirNode` | Assigned new entries in `buildTree` via `curr.Children[part] = \u0026DirNode{...}`. |\n\n**Concurrency Mechanisms:** None detected in this file. No mutexes, channels, atomics, or goroutines are used. The code is single-threaded.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## I/O Communication\n\n| Function | External System | Direction | Notes |\n|---|---|---|---|\n| `determineAffected` → `security.SafeResolve(repoRoot, modulePath)` | Filesystem (disk) | Read | Resolves a path relative to repo root. Likely performs directory reads internally. Error is **swallowed** — if it fails, the entire block is skipped silently. |\n| `determineAffected` → `os.Stat(absModulePath)` | Filesystem (disk) | Read | Checks existence of generated module docs (`docs/modules/\u003csafeName\u003e.md`). The resulting error is type-checked via `os.IsNotExist(err)` — only \"file not found\" errors trigger the affected flag. Other Stat errors (permission denied, etc.) are treated as non-existence and silently ignored. |\n\n## Error Handling\n\n| Function | Strategy |\n|---|---|\n| `isAllowedFile` | No error handling. Delegates entirely to `tools.ShouldIgnoreFile`. Unchecked return is assumed safe. |\n| `propagateAffected` | No error handling. Recursively walks tree, writes into the passed-in map as a **side effect**. Returns bool based on ancestor/descendant affected state — no error returns possible. |\n| `determineAffected` | Two error paths exist: (1) `SafeResolve` error is caught and ignored (`if err == nil`). (2) `os.Stat` error is only classified as \"not-exist\"; anything else results in a silent no-op. Errors are never returned to the caller. |\n| `buildTree` | No error handling. Pure computation on a file list; panics or errors would come from Go's standard library internals (e.g., `filepath.Dir`, `strings.Split`) and propagate as unhandled. |\n\n## Side Effects\n\n- **`propagateAffected`** — Mutates `affectedDirs` map in place during recursion. Ancestor nodes are marked true if any descendant is affected, but the traversal itself writes entries as it walks (not just at the end).\n- **`determineAffected`** — Writes into `affectedDirs`. Additionally, creates a local `changedFiles` map from input slice; this is transient and not observable externally.\n- All other functions are read-only or pure computation with respect to external state."
    },
    "internal/engine/utils.go": {
      "sha256": "f6a0bf37616a1b30111ee0a6f7b31e8e0fffcb1571a22377c24102c6fd17db8f",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area of `internal/engine/utils.go`\n\n## Exported Functions\n\n### `ToSafeMarkdownFilename`\n- **Signature:** `func ToSafeMarkdownFilename(modulePath string) string`\n- **Input:** `string` (module path)\n- **Output:** `string` (safe filename with `.md` suffix)\n\n#### [BUSINESS_LOGIC]\n## Business Domain Concepts\n\nThis module operates at the boundary between an **internal engine** (`engine` package) and a **documentation generation system**. It maps internal identifiers (module paths) onto safe, human-readable filenames for markdown output. The domain concept is: *a one-to-one mapping from path-like identifiers to document file names*.\n\n## High-Level Algorithmic Flow\n\n1. **Input**: A `modulePath` string — an identifier representing a module within the engine's internal structure.\n2. **Separator Replacement**: All filesystem separator characters in the input are replaced with underscores (`_`). This normalizes platform-specific path separators (e.g., `/`, `\\`) into a single ASCII character, producing a portable filename.\n3. **Empty/Root Guard**: If the resulting string is empty or equal to `\".\"`, it is replaced with the literal string `\"root\"`. This handles edge cases where the input path has no meaningful segments after separator replacement.\n4. **Suffix Append**: A `.md` extension is appended to the sanitized name, signaling that the output is a markdown document file.\n\n## Business Rules\n\n- Module identifiers are treated as opaque strings; their internal structure (if any) is not parsed or validated beyond path-separator handling.\n- The mapping is deterministic and pure — same input always yields the same filename.\n- No error return is emitted for invalid inputs. All transformations are best-effort with silent normalization.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state. This file contains only a pure function that operates on its input parameters without modifying any global variables or struct fields.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis\n\n### Communication with the Outside World (I/O)\n\n- **No external I/O.** This function performs only in-memory string manipulation: replacing `filepath.Separator` with `_`, handling empty/root inputs, and appending `.md`. No network calls, no disk operations, no database queries.\n- Uses standard library `filepath.Separator` to obtain the OS-specific path separator — this is a read-only reference, not an I/O call.\n\n### Error Handling\n\n- **No errors returned.** The function signature returns only a `string`; there is no error channel or sentinel return value.\n- **No panic recovery.** There are no `recover()` calls or deferred functions in scope that would catch panics.\n- **No error wrapping/swallowing patterns** are present — the input string is transformed directly without any validation, type assertions, or fallback mechanisms beyond the explicit empty/root check."
    },
    "internal/security/security.go": {
      "sha256": "0c8fee15e6eda841d064cc28f35d21b1e25b7267803d44be94932e956ad663b5",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area: `internal/security/security.go`\n\n## Functions / Methods\n\n| Signature | Description |\n|-----------|-------------|\n| `SafeResolve(repoRoot, inputPath string) (string, error)` | Resolves and validates an input path against a repository root. Returns the cleaned absolute path or an error on traversal detection. |\n| `AcquireLock(repoRoot string) (*SimpleLock, error)` | Acquires a simple file lock in the repo root. Returns a `*SimpleLock` ready to be unlocked later. |\n| `EnsureGitignoreHasLockfile(repoRoot string) error` | Ensures `.code-reducer.lock` is present in `.gitignore`. Creates `.gitignore` if absent, or appends an entry if it exists. |\n\n## Structs\n\n| Name | Fields |\n|------|--------|\n| `SimpleLock` | `lockPath string`, `file *os.File` |\n\n## Method on SimpleLock\n\n| Signature | Description |\n|-----------|-------------|\n| `(l *SimpleLock) Unlock() error` | Closes the lock file and removes it from disk. Returns an error if either step fails. |\n\n---\n\n\u003e **Note:** `LockFileName` is a package-level constant (`.code-reducer.lock`) but was excluded per scope instructions (only structs, interfaces, methods were requested).\n\n#### [BUSINESS_LOGIC]\n## Domain Concepts and Business Rules\n\nThis module implements three infrastructure contracts:\n\n1. **Path-boundary enforcement** — The `SafeResolve` rule requires any user-supplied path to resolve strictly inside the repository root. If a resolved absolute path escapes the root (detected via the `..` prefix check), the operation fails with an explicit violation error. This is a defense-in-depth rule against directory-traversal attacks.\n\n2. **Process mutual exclusion** — The lock mechanism ensures only one Code-Reducer instance operates on a given repository at a time. Acquisition requires creating the lockfile atomically (via `O_EXCL`); if it already exists, acquisition fails with a \"lock held\" error. Release closes the file and removes the lockfile.\n\n3. **Lockfile version-control exclusion** — The rule that `.code-reducer.lock` must appear in `.gitignore`. If the ignore file doesn't exist or lacks this entry, one is created/updated so the lockfile is not tracked by git.\n\n## High-Level Algorithmic Flow\n\n1. Resolve input path → validate containment within repo root → fail if traversal detected\n2. Acquire lock → open lockfile with exclusive create → write PID → return handle\n3. Release lock → close file → remove lockfile (errors are non-fatal in release)\n4. Manage `.gitignore` → check existence → append lock entry if missing\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n- `SimpleLock.lockPath` (string) — set in `AcquireLock` return literal, read in `Unlock`. No reset after close.\n- `SimpleLock.file` (*os.File) — set in `AcquireLock` return literal. After `Unlock()` the file is closed and removed but the field is never nilled out; subsequent calls to `Unlock()` will close an already-closed file (error swallowed).\n\n**Concurrency Mechanisms:**\n\nNone. No `sync.Mutex`, channels, atomic operations, or other synchronization primitives are used anywhere in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects (I/O)\n\n| Function | I/O Target | Operation | Notes |\n|---|---|---|---|\n| `SafeResolve` | repo filesystem (no direct syscall) | `filepath.Abs`, `filepath.Clean`, `filepath.Rel` | Pure path manipulation; no actual disk read/write. Security check via prefix inspection of relative path. |\n| `AcquireLock` | `\u003crepoRoot\u003e/.code-reducer.lock` | `os.OpenFile(..., O_CREATE\\|O_EXCL)`, then `f.Write()` | Creates lockfile atomically if not present. Writes PID (newline-terminated). |\n| `SimpleLock.Unlock` | Same lockfile path | `l.file.Close()`, `os.Remove(lockPath)` | Closes file handle first, then removes the file from disk. Both operations are side effects on the filesystem. |\n| `EnsureGitignoreHasLockfile` | `\u003crepoRoot\u003e/.gitignore` | `os.Stat`, `bufio.Scanner` reads, then optionally `os.OpenFile(..., O_APPEND\\|O_WRONLY)` + `f.WriteString()` | If `.gitignore` missing: creates it with a single line. Otherwise: scans for existing entry; if not found, appends a commented block plus the lockfile name. |\n\n## Error Handling Patterns\n\n### 1. **Wrap pattern (fmt.Errorf %w)**\nUsed in every failure path — errors are wrapped with context:\n- `SafeResolve`: wraps `filepath.Abs` error as `\"failed to get absolute root path\"`; wraps traversal detection as a hardcoded sentinel message.\n- `AcquireLock`: wraps the raw `os.OpenFile` error when it's not `IsExist`.\n- `EnsureGitignoreHasLockfile`: wraps stat/read/write errors individually.\n\n### 2. **Sentinel / Hardcoded messages**\nNot wrapped with `%w`, returned as-is:\n- `\"security violation: path traversal detected\"` — in `SafeResolve` when relative path starts with `..`.\n- `\"lock at %s is already held by another process...\"` — when `os.IsExist(err)` on OpenFile. This is a domain-specific sentinel for lock contention.\n\n### 3. **Error swallowing**\n- `Unlock()` calls both `Close` and `Remove`, captures each into local vars (`err1`, `err2`), returns whichever is non-nil (or nil if both succeed). The second operation's error may be silently discarded in favor of the first, or vice versa — only one error is ever returned.\n- `EnsureGitignoreHasLockfile` opens `.gitignore` for appending with `O_APPEND\\|O_WRONLY`. If append fails, it returns a wrapped error. No panic recovery; if write fails after opening, the file remains open (deferred close) and no partial state change occurs beyond the open handle — but no explicit cleanup of any partial content is performed.\n\n### 4. **No panic recovery**\nNone of these functions recover from panics. Any unexpected panic propagates to the caller with an empty stack trace in `runtime` (Go's default).\n\n### 5. **Error path: write-after-open failure in AcquireLock**\nIf PID write fails, `f.Close()` and `os.Remove(lockPath)` are called explicitly — this cleans up the partially-created lockfile so another process can retry. This is a deliberate cleanup side effect on error."
    },
    "internal/tools/file_tools.go": {
      "sha256": "b754fd4e54912fda6017fe057d47bd25166b68660002acda578aaa2f273ca10b",
      "facts": "#### [API_SIGNATURES]\n## File: internal/tools/file_tools.go\n\n### Exported Functions (No Structs/Interfaces)\n\n| Function | Input Types | Output Types |\n|---|---|---|\n| `ReadFileSafely` | `(repoRoot string, virtualPath string)` | `([]byte, error)` |\n| `WriteFileSafely` | `(repoRoot string, virtualPath string, content []byte)` | `error` |\n| `LoadGitignore` | `(repoRoot string)` | `([]string, error)` |\n| `ShouldIgnoreFile` | `(repoRoot string, relPath string, gitIgnore *ignore.GitIgnore, ignoredExtensions []string)` | `bool` |\n| `DiscoverCodeFiles` | `(repoRoot string, ignores []string, ignoredExtensions []string)` | `([]string, error)` |\n| `IsBinaryFile` | `(path string)` | `bool` |\n\n### Notes\n- No exported structs or interfaces are defined in this file.\n- All functions are standalone and not methods on any type.\n- The `ignore.GitIgnore` parameter is an external dependency from the `github.com/sabhiram/go-gitignore` package, referenced via alias `ignore`.\n\n#### [BUSINESS_LOGIC]\n## Business Logic \u0026 Domain Concepts\n\nThis file implements **safe, filtered repository traversal** — discovering and reading source code while protecting against path-traversal attacks and symlink races.\n\n### High-Level Flow\n\n1. **DiscoverCodeFiles** walks the repository root recursively. It skips directories matching `.gitignore` or dot-prefixed names (`.build`, `.svn`, etc.), then filters files through `ShouldIgnoreFile`. Valid source file paths are collected into a list.\n\n2. **ShouldIgnoreFile** applies layered ignore rules:\n   - User-configured patterns (`*~`, `package-lock.json`, etc.) via gitignore compilation.\n   - Dot-prefixed path components or `.egg-info` suffixes (build artifacts).\n   - File extension blacklist (e.g., compiled binaries, lock files, images).\n   - Binary file detection for unknown extensions — if an extension isn't in a known-text set, the first 1024 bytes are scanned for null bytes.\n\n3. **ReadFileSafely** resolves a virtual path through `security.SafeResolve` (path traversal guard), then reads it via `os.ReadFile`.\n\n4. **WriteFileSafely** writes content with TOCTOU-safe steps:\n   - Resolve the safe absolute path first.\n   - Create parent directories if missing.\n   - Open for write+create, then `lstat` to verify no symlink was introduced between open and stat calls.\n   - Confirm `SameFile(fiLstat, fiFstat)` — if they differ, a race condition or symlink swap occurred; reject with error.\n   - Truncate after verification, then write content.\n\n5. **LoadGitignore** reads `.gitignore` from the repo root and returns active non-comment patterns (empty file → nil).\n\n### Domain Concepts\n\n- **Virtual path resolution**: files are addressed by a virtual name inside the repo, not absolute paths — `SafeResolve` maps this to disk safely.\n- **Symlink race prevention**: TOCTOU checks (`lstat` after `open`, `SameFile`) stop an attacker from swapping a regular file with a symlink between open and write.\n- **Binary vs text classification**: avoids I/O overhead by maintaining a known-text extension map; only scans for null bytes on unknown extensions.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** None. Every variable in this file is scoped to a single function and none are modified after initialization. The `knownTextExts` map (used by `ShouldIgnoreFile`) is created locally within the function body and never mutated afterward.\n\n**Concurrency Mechanisms:** None. There are no goroutines, channels, or sync primitives used anywhere in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## Side Effects \u0026 Error Handling Analysis: `internal/tools/file_tools.go`\n\n---\n\n### 1. Disk I/O Operations\n\n| Function | Operation | Direction |\n|----------|-----------|-----------|\n| **ReadFileSafely** | `os.ReadFile(safePath)` | Read from disk |\n| **WriteFileSafely** | `os.MkdirAll(dir, 0755)` — creates intermediate dirs | Write to disk |\n| **WriteFileSafely** | `os.OpenFile(safePath, O_WRONLY\\|O_CREATE, 0644)` — opens for write+create | Write to disk |\n| **WriteFileSafely** | `f.Truncate(0)` then `f.Write(content)` — truncates and writes content | Write to disk |\n| **LoadGitignore** | `os.Open(gitignorePath)` → scanner reads lines | Read from disk |\n| **ShouldIgnoreFile** | `security.SafeResolve` + `IsBinaryFile(absPath)` | Read from disk (via Open) |\n| **DiscoverCodeFiles** | `filepath.WalkDir(repoRoot, ...)` — walks entire directory tree | Read from disk |\n| **IsBinaryFile** | `os.Open(path)` → reads 1024 bytes into buf | Read from disk |\n\n---\n\n### 2. TOCTOU Race Mitigation in WriteFileSafely\n\nThe write path performs three sequential checks before truncating:\n\n1. `os.Lstat(safePath)` — stat the file *without* following symlinks\n2. `f.Stat()` — stat via the already-opened fd\n3. `os.SameFile(fiLstat, fiFstat)` — compare both stats for equivalence\n\nIf any step detects a symlink or path mismatch, it returns an error with the prefix `\"security violation\"` rather than proceeding with truncation.\n\n---\n\n### 3. Error Handling Patterns\n\n| Function | Strategy |\n|----------|----------|\n| **ReadFileSafely** | Wraps with `fmt.Errorf(\"failed to read file: %w\", err)` — wraps all errors from SafeResolve and os.ReadFile |\n| **WriteFileSafely** | Contextual wrapping per step: `\"failed to create directory\"`, `\"failed to open file for writing\"`, `\"failed to lstat file\"`, `\"security violation\"` (symlink), `\"TOCTOU symlink race detected\"`, `\"failed to truncate file\"`, `\"failed to write file content\"` — all use `%w` wrapping |\n| **LoadGitignore** | Returns `nil, nil` when `.gitignore` does not exist (`os.IsNotExist`). Wraps everything else. No panic; no sentinel errors |\n| **DiscoverCodeFiles** | Walk error → returns `nil, err`. Individual walk entry errors are swallowed (returns `nil`) — caller cannot distinguish a skipped bad path from a successful skip |\n| **IsBinaryFile** | Opens file; if Open fails, returns `false` (treats unreadable as non-binary). On Read: wraps only `io.EOF` is ignored (expected EOF), other errors return `false`. No wrapping of read errors |\n\n---\n\n### 4. Notable Error-Swallowing Patterns\n\n- **DiscoverCodeFiles**: Walk callback returns `nil` for *any* walk error, making it impossible to tell if a directory entry failed to stat vs was intentionally skipped\n- **IsBinaryFile**: Open failure → returns `false`. Read errors (except EOF) → returns `false`. Caller gets no signal about what went wrong\n- **ShouldIgnoreFile** (`DiscoverCodeFiles`): Walk callback returns `nil` for any per-entry error, swallowing them silently"
    },
    "internal/tools/git_tools.go": {
      "sha256": "f300d22fe8d5d5a847a611f5bad854108e7579588d44b1812e1494b7316afa3e",
      "facts": "#### [API_SIGNATURES]\n## Public Surface Area: `tools` package (from `git_tools.go`)\n\n### Exported Functions\n\n| Function | Input Types | Output / Return Type(s) |\n|---|---|---|\n| `RunGit(repoRoot string, args ...string)` | `repoRoot` `string`, variadic `args` `...string` | `(string, error)` — stdout output and error |\n| `VerifyGitRepo(repoRoot string)` | `repoRoot` `string` | `error` — nil on success |\n\n### Exported Structs / Interfaces\n\nNone.\n\n#### [BUSINESS_LOGIC]\n**Domain Concepts and Business Rules**\n\nThis file provides a utility abstraction for executing version control system commands within a specific working directory. It encapsulates process spawning, output capture, and error handling for Git operations, allowing other parts of the application to interact with repositories without managing subprocess lifecycle directly.\n\nThe file implements two core operations:\n1.  **Command Execution Abstraction**: A generic runner that accepts arbitrary Git arguments, strips pager interference (`--no-pager`), and surfaces both standard output and error streams into a structured return value.\n2.  **Repository Validation**: A check to confirm the target path is recognized as a valid Git worktree by verifying binary availability first, then querying repository state via `rev-parse`.\n\n**High-Level Algorithmic Flow**\n\n1.  **`RunGit`**: Constructs an `exec.Command` for `git`, appending `--no-pager` before user-supplied arguments. It redirects process I/O to buffers, executes the command, and returns trimmed stdout on success. On failure, it trims stderr content and wraps the original error into a descriptive message containing both the exit error and the captured error stream.\n2.  **`VerifyGitRepo`**: First checks system path availability for the `git` binary. If unavailable, it returns an immediate error. Otherwise, it delegates to `RunGit` with the `rev-parse --is-inside-work-tree` query. If that query fails, it returns a specific error indicating the directory is not inside a Git repository.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State\n\n**No mutable state.** All variables (`cmd.Dir`, `cmd.Stdout`, `cmd.Stderr`, `stdout.String()`, `stderr.String()`) are local function-scope buffers created fresh on each call. There is no package-level global variable or struct field that persists across calls.\n\n## Concurrency Mechanisms\n\n**None.** No `sync.Mutex`, channels, atomic operations, or other concurrency primitives are present in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## I/O Communication with the Outside World\n\n### 1. Disk / Filesystem (via git subprocess)\n\n**`RunGit`**:\n- Spawns a `git --no-pager` subprocess via `exec.Command`. The subprocess communicates with the filesystem under `repoRoot` (`cmd.Dir = repoRoot`).\n- Captures stdout into an in-memory `bytes.Buffer`, then reads it back and trims whitespace.\n- Returns the trimmed output string to the caller.\n\n**`VerifyGitRepo`**:\n- Calls `exec.LookPath(\"git\")` — probes the filesystem for a `git` binary on the system PATH.\n- If no binary is found, returns immediately with a wrapped error (no further I/O).\n- Otherwise calls `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")`, which again runs git against the filesystem at `repoRoot`.\n\n### 2. Network / DB\n\n**None.** No network calls or database access are present in this file. All external communication is disk-bound (filesystem via subprocess).\n\n---\n\n## Error Handling\n\n| Function | Failure Mode | Handling Strategy |\n| --- | --- | --- |\n| `RunGit` | `cmd.Run()` returns non-nil error | Wraps original error with stderr text: `fmt.Errorf(\"git command failed: %v, stderr: %s\", err, trimmedErr)` — **wrapping** of the underlying OS/exec error. |\n| `VerifyGitRepo` | `exec.LookPath(\"git\")` fails | Returns a new error directly: `\"git executable not found in system path\"`. No wrapping — fresh error constructed from scratch. |\n| `VerifyGitRepo` | `RunGit(...)` returns non-nil error (from the inner call) | Wraps with: `\"not a git repository (or any of the parent directories)\"`. Original error is **discarded** — only the new message is returned. |\n\n### Summary of patterns used:\n- **Wrapping**: `RunGit` wraps the raw `exec` error + stderr text into a single error value.\n- **Fresh error construction**: `VerifyGitRepo`'s first check constructs a brand-new error with no reference to the original failure (since there is none — it's a missing-binary condition).\n- **Error swallowing / silent discard**: In the second `RunGit` call inside `VerifyGitRepo`, the returned error from that inner call is not passed through; only a new descriptive message is emitted. This means any specific git command failure details are lost to the caller.\n\n### Panic: None. The code never panics. All failures are surfaced as returned errors."
    },
    "main.go": {
      "sha256": "0af846d7b71a30af2292ae0530ff98ed4c31688a4318bf57393597dd135c15c3",
      "facts": "#### [API_SIGNATURES]\n# Public Surface Area of `main.go`\n\n## Structs: None\n\n## Interfaces: None\n\n## Methods:\n\n| Method | Input | Output |\n|--------|-------|--------|\n| `func main()` | `(void)` | `(void)` |\n\n---\n\n**Notes:**\n- `main()` is a package-level entry point. It accepts no arguments and returns nothing visible (no explicit return value).\n- The internal error handling (`fmt.Printf`, `os.Exit(1)`) does not affect the public signature.\n\n#### [BUSINESS_LOGIC]\n**Domain Concepts:** None identified in this file. It contains no business logic or domain-specific code.\n\n**Business Rules:** None present.\n\n**Algorithmic Flow:** The entry point (`main`) delegates entirely to `cmd.RootCmd.Execute()` from a third-party command package. Any error returned is printed and the process exits with status 1; otherwise execution completes normally. No local processing, validation, or state management occurs in this file.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** No mutable state.\n\n**Concurrency Mechanisms:** None detected in the provided source file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### Side Effects and I/O\n\n*   **External Command Execution**: The function calls `cmd.RootCmd.Execute()`, delegating entry-point logic to an external command package (`github.com/arrase/code-reducer/cmd`). No internal implementation is visible in this file.\n*   **stdout Output**: If the delegate returns a non-nil error, it writes a formatted string to stdout using `fmt.Printf`. The output pattern is `\"Execution failed: %v\\n\"`, followed by whatever `cmd.RootCmd.Execute()` returned as an error message.\n*   **Process Termination**: It terminates the process immediately via `os.Exit(1)`.\n\n### Error Handling\n\n*   **Error Propagation**: Errors from `cmd.RootCmd.Execute()` are checked explicitly using a standard `if err != nil` pattern.\n*   **No Panic**: The code does not use `panic()` or recover().\n*   **Error Display**: It prints the error message to stdout but does not wrap the original error object (no `%w` usage).\n*   **Exit Code**: On failure, it exits with status code 1."
    }
  },
  "modules": {
    ".": "# Code-Reducer Engine Architecture\n\n## Module Responsibility\n\nCode-Reducer is an automatic software-documentation generation system driven by Ollama-compatible large language models (LLM). It operates on a repository's source code tree to produce markdown artifacts describing the project's architecture, module boundaries, business logic, state management, error handling patterns, and agent guidelines. The system runs in two modes:\n\n- **`init`** — generates documentation from scratch against the full discovered codebase\n- **`update`** — performs incremental regeneration based on detected file changes\n\nExecution is initiated through `internal/engine/runner.go` (`Runner.Run`). All disk I/O flows through `internal/tools`; all network calls go through the LLM client in `internal/engine/client.go`; concurrency protection is provided by OS-level file locking managed by `security.AcquireLock`.\n\n---\n\n## Subsystem: config — Multi-Source Configuration Resolution\n\n### Responsibility\n\nCentralizes all configuration state for the engine: loading, persisting, resolving, and validating a `config.Config` struct from CLI flags, environment variables, YAML files, or hardcoded defaults in a strict four-tier priority chain. Persisted state lives at `.code-reducer.yaml` in the current working directory with restricted owner-only permissions (`0600`).\n\n### Data Structures\n\n```go\ntype Config struct {\n    ModelID            string   // Ollama model identifier (falls back to gemma4:26b-a4b-it-qat)\n    OllamaBaseURL      string   // Base URL for the Ollama API endpoint\n    OllamaNumCtx       int      // Context window size\n    DocsDir            string   // Path to source documentation directory\n    ExtractionSteps    []ExtractionStep  // Ordered pipeline of analysis tasks\n    Ignore             []string            // Directories to exclude from scanning\n    IgnoreExtensions   []string            // File extensions to skip\n}\n\ntype ExtractionStep struct {\n    Name   string `yaml:\"name\"`\n    Prompt string `yaml:\"prompt\"`\n}\n```\n\nFour extraction steps are defined by default:\n\n| Priority | Step | Purpose |\n|---|---|---|\n| 1 | API_SIGNATURES | Extract exported structs, interfaces, methods and their types |\n| 2 | BUSINESS_LOGIC | Analyze domain concepts and high-level algorithmic flow |\n| 3 | STATE_AND_CONCURRENCY | Identify mutable state and concurrency protections (mutexes, channels) |\n| 4 | ERRORS_AND_SIDE_EFFECTS | Document external communication (I/O) and error handling patterns |\n\nThe step list is overridable in the YAML file; if empty at resolve time it falls back to defaults.\n\n### Module Constants \u0026 Defaults\n\nPackage-level read-only constants consumed exclusively by `ResolveConfig`:\n\n| Variable | Purpose | Reassignable? |\n|---|---|---|\n| `DefaultIgnores` | Hardcoded directories to skip (`.git`, `node_modules`, `venv`, etc.) | No — read-only after init |\n| `DefaultIgnoredExtensions` | Hardcoded file extensions to skip (`.pyc`, `.class`, etc.) | No — read-only after init |\n| `DefaultExtractionSteps` | Ordered list of default analysis tasks | No — read-only after init |\n\n### Functions\n\n**`ConfigExists(cwd string) bool`** — Returns `true` when `.code-reducer.yaml` exists under `cwd`. Treats any `os.Stat` error as \"file does not exist\" — permission denial and I/O failures are silently swallowed.\n\n**`LoadConfig(cwd string) (*Config, error)`** — Reads the persisted config from `\u003ccwd\u003e/.code-reducer.yaml`. Returns a nil pointer with an unwrapped `os.ReadFile` error if the file cannot be read at all. If YAML parsing fails, returns a wrapped error (`failed to parse yaml config`). On success, returns the raw parsed struct without applying overrides — callers must chain this into `ResolveConfig` for final resolution.\n\n**`SaveConfig(cwd string, cfg *Config) error`** — Marshals the provided `cfg` pointer and writes it to `\u003ccwd\u003e/.code-reducer.yaml` with `0600` permissions via `os.WriteFile`. Marshaling errors and write errors are both wrapped with `%w`, preserving the chain for unwrapping. A nil config is not explicitly guarded; marshaling a nil will result in an empty YAML document.\n\n**`MergeAndDeduplicate(a []T, b []T) []T`** (where T is comparable) — Merges two slices into one deduplicated slice using a map-based seen-set pattern. System defaults are merged first, then user-provided config additions follow — this ordering ensures user values take precedence when duplicates exist across the merge boundary. No error return; any panic from non-comparable types would be a caller-side bug.\n\n**`ResolveConfig(repoRoot, modelIdFlag string, numCtxFlag string) *Config`** — Orchestrates the full resolution pipeline: (1) calls `LoadConfig` to read persisted state (errors swallowed — falls back to empty struct); (2) applies CLI flag overrides in order: `modelIdFlag` → `OllamaBaseURL`, then `numCtxFlag` → `OllamaNumCtx`; (3) merges default ignore lists (`DefaultIgnores`, `DefaultIgnoredExtensions`) with user-provided values via `MergeAndDeduplicate`; (4) falls back to hardcoded defaults for extraction steps if the slice is empty; (5) returns a pointer to the final resolved config; no error return — any failure path results in an empty `\u0026Config{}`.\n\n---\n\n## Subsystem: security — Path-Boundary Enforcement and Mutual Exclusion\n\n### Responsibility\n\nImplements three infrastructure contracts for the Code-Reducer agent: path-boundary enforcement against directory-traversal attacks, process mutual exclusion via file locking, and lockfile version-control exclusion through `.gitignore` management. No synchronization primitives (`sync.Mutex`, channels, atomics) are used; concurrency safety is delegated to the OS-level `O_EXCL` semantics on lock acquisition.\n\n### Domain Concepts and Business Rules\n\n1. **Path-boundary enforcement** — `SafeResolve` requires any user-supplied path to resolve strictly inside the repository root. If a resolved absolute path escapes the root (detected via the `..` prefix check), the operation fails with an explicit violation error. This is a defense-in-depth rule against directory-traversal attacks.\n\n2. **Process mutual exclusion** — The lock mechanism ensures only one Code-Reducer instance operates on a given repository at a time. Acquisition requires creating the lockfile atomically (via `O_EXCL`); if it already exists, acquisition fails with a \"lock held\" error. Release closes the file and removes the lockfile.\n\n3. **Lockfile version-control exclusion** — The rule that `.code-reducer.lock` must appear in `.gitignore`. If the ignore file doesn't exist or lacks this entry, one is created/updated so the lockfile is not tracked by git.\n\n### Data Structures\n\n```go\ntype SimpleLock struct {\n    lockPath string // Absolute path to the lock file. Set in AcquireLock; read in Unlock.\n    file     *os.File // File handle opened during acquisition. After Unlock() the underlying file is closed and removed from disk but the field is never nilled out.\n}\n```\n\n**`SimpleLock.Unlock() error`** — Closes the lock file and removes it from disk. Returns an error if either step fails; individual errors are captured into local vars (`err1`, `err2`) and only one is ever returned (the second operation's error may be silently discarded in favor of the first).\n\n### Public Functions\n\n**`SafeResolve(repoRoot, inputPath string) (string, error)`** — Resolves and validates an input path against a repository root. Returns the cleaned absolute path or an error on traversal detection. Uses pure path manipulation: `filepath.Abs`, `filepath.Clean`, `filepath.Rel`. No actual disk read/write occurs during this call. The security check is performed via prefix inspection of the relative path after resolution.\n\n**`AcquireLock(repoRoot string) (*SimpleLock, error)`** — Acquires a simple file lock in the repo root. Requires creating the lockfile atomically (via `O_CREATE|O_EXCL`); if it already exists, acquisition fails with a \"lock held\" sentinel message. On success writes PID (newline-terminated). If PID write subsequently fails, `f.Close()` and `os.Remove(lockPath)` are called explicitly — this cleans up the partially-created lockfile so another process can retry.\n\n**`EnsureGitignoreHasLockfile(repoRoot string) error`** — Ensures `.code-reducer.lock` is present in `.gitignore`. If the ignore file doesn't exist, one is created with a single line. Otherwise: scans for existing entry; if not found, appends a commented block plus the lockfile name. Opens `.gitignore` for appending with `O_APPEND|O_WRONLY`; if append fails, returns a wrapped error. No partial state change occurs beyond the open handle (deferred close); no explicit cleanup of any partial content is performed on write failure after opening.\n\n### Error Handling Patterns\n\n1. **Wrap pattern (`fmt.Errorf %w`)** — Used in every failure path:\n   - `SafeResolve`: wraps `filepath.Abs` error as `\"failed to get absolute root path\"`; wraps traversal detection as a hardcoded sentinel message.\n   - `AcquireLock`: wraps the raw `os.OpenFile` error when it's not `IsExist`.\n   - `EnsureGitignoreHasLockfile`: wraps stat/read/write errors individually.\n\n2. **Sentinel / Hardcoded messages** — Not wrapped with `%w`, returned as-is:\n   - `\"security violation: path traversal detected\"` — in `SafeResolve` when relative path starts with `..`.\n   - `\"lock at %s is already held by another process...\"` — when `os.IsExist(err)` on OpenFile. Domain-specific sentinel for lock contention.\n\n3. **Error swallowing** — `Unlock()` captures each step's error into local vars (`err1`, `err2`) and returns whichever is non-nil (or nil if both succeed). The second operation's error may be silently discarded in favor of the first, or vice versa — only one error is ever returned.\n\n4. **No panic recovery** — None of these functions recover from panics. Any unexpected panic propagates to the caller with an empty stack trace in `runtime` (Go's default).\n\n---\n\n## Subsystem: tools — Repository Traversal, Safe File I/O, and Git Utilities\n\n### Responsibility\n\nProvides three independent capability groups: **repository code discovery**, **safe file read/write operations** with TOCTOU race mitigation, **gitignore-aware filtering**, and **Git subprocess execution**. All functions are standalone (no methods on types); no exported structs or interfaces exist. No goroutines, channels, or sync primitives are used anywhere in the package—every call is synchronous and single-threaded.\n\n### Repository Code Discovery\n\n**`DiscoverCodeFiles(repoRoot string) ([]string, error)`** — Walks `repoRoot` recursively via `filepath.WalkDir`, skipping directories matching `.gitignore` patterns (compiled by `LoadGitignore`) or any dot-prefixed component (`.build`, `.svn`, etc.), then filters each leaf through `ShouldIgnoreFile`. Valid source paths are accumulated into a slice and returned. Walk callback errors from `filepath.WalkDir` are swallowed—individual directory entries that fail to stat return `nil` to the caller, making it impossible to distinguish an intentional skip from a walk failure downstream.\n\n### Safe File Read / Write Operations\n\n**`ReadFileSafely(repoRoot, virtualPath) ([]byte, error)`** — Resolves `virtualPath` through `security.SafeResolve` (path-traversal guard), then reads the resulting absolute path via `os.ReadFile`. Errors from both `SafeResolve` and `os.ReadFile` are wrapped with prefix `\"failed to read file:\"` using `%w`.\n\n**`WriteFileSafely(repoRoot, virtualPath, content []byte) error`** — Performs TOCTOU-safe writes:\n1. Resolve the safe absolute path via `security.SafeResolve`.\n2. Call `os.MkdirAll(dir, 0755)` to create parent directories if missing; wraps with `\"failed to create directory\"`.\n3. Open for write+create (`O_WRONLY|O_CREATE`, mode `0644`); wraps open failure with `\"failed to open file for writing\"`.\n4. Call `os.Lstat(safePath)` (no symlink follow) and `f.Stat()`; compare via `os.SameFile(fiLstat, fiFstat)`. If they differ—indicating a symlink swap or race condition—return an error prefixed `\"security violation\"` or `\"TOCTOU symlink race detected\"`.\n5. Truncate (`f.Truncate(0)`), then write content; wrap each with `\"failed to truncate file:\"` / `\"failed to write file content:\"`.\n\n### Gitignore Handling and Binary Classification\n\n**`LoadGitignore(repoRoot) ([]string, error)`** — Reads `.gitignore` from `repoRoot`, scanner processes line-by-line. Empty file returns `nil, nil`; non-existence (`os.IsNotExist`) also returns `nil, nil`. All other errors are wrapped. Patterns returned are active non-comment entries.\n\n**`ShouldIgnoreFile(repoRoot, relPath string, gitIgnore *ignore.GitIgnore, ignoredExtensions []string) bool`** — Applies layered ignore rules:\n- Compiles user-supplied patterns via the external `github.com/sabhiram/go-gitignore` package (`*ignore.GitIgnore`).\n- Rejects paths with dot-prefixed components or `.egg-info` suffixes (build artifacts).\n- Checks file extension against a provided blacklist.\n- For unknown extensions, reads the first 1024 bytes and scans for null bytes via `IsBinaryFile`. If null bytes are present, returns `true` (binary → ignore).\n\n**`IsBinaryFile(path string) bool`** — Opens the path; if `os.Open` fails, returns `false` (treats unreadable as non-binary). Reads up to 1024 bytes. Only wraps `io.EOF` (expected EOF) without error—other read errors return `false`. No error wrapping for open or other read failures beyond EOF handling.\n\n### Git Subprocess Execution\n\n**`RunGit(repoRoot string, args ...string) (stdout string, err error)`** — Constructs an `exec.Command` for `git`, prepends `--no-pager` to user-supplied arguments, sets `cmd.Dir = repoRoot`. Captures stdout into a `bytes.Buffer`, reads it back, trims whitespace. On success returns trimmed output; on failure wraps the original error with stderr text: `\"git command failed: %v, stderr: %s\"`.\n\n**`VerifyGitRepo(repoRoot string) error`** — Two-stage validation:\n1. Calls `exec.LookPath(\"git\")`; if unavailable, returns a fresh error without wrapping—no reference to any prior failure since none exists at that point.\n2. Otherwise delegates to `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")`. If the inner call fails, discards its returned error entirely and returns a new message: `\"not a git repository (or any of the parent directories)\"`.\n\n### Error Handling Summary\n\n| Function | Failure Mode | Strategy |\n|---|---|---|\n| `ReadFileSafely` | Resolve or read failure | Wrap with `\"failed to read file:\"`, `%w` |\n| `WriteFileSafely` | Directory open, file open, lstat/stat mismatch, truncate, write | Contextual prefix per step; symlink race → `\"security violation\"` / `\"TOCTOU symlink race detected\"` |\n| `LoadGitignore` | Non-existence or read error | `nil, nil` on missing/empty; wrap everything else |\n| `DiscoverCodeFiles` | Walk entry failure | Swallowed—caller sees no signal |\n| `IsBinaryFile` | Open failure or non-EOF read error | Returns `false`; EOF is expected and ignored |\n| `RunGit` | Non-zero exit / exec error | Wrap original + stderr text with `%v`, `%s` |\n| `VerifyGitRepo` | Missing git binary | Fresh error, no wrapping |\n| `VerifyGitRepo` | Git query failure | Discard inner error; return fresh message only |\n\n---\n\n## Subsystem: engine — Documentation Generation Pipeline\n\n### Responsibility\n\nImplements an automatic software-documentation generation pipeline that uses a large language model (Ollama-compatible API) to analyze repository structure and produce markdown artifacts. The system operates in two modes—**init**, which generates fresh documentation from scratch, and **update**, which performs incremental regeneration based on detected code changes.\n\n### LLM Client (`client.go`)\n\nProvides synchronous and streaming communication with an Ollama-compatible LLM API. The client is immutable after construction; all fields are assigned only in `NewLLMClient`. Both modes share a base system prompt that establishes agent identity and operating rules.\n\n**`CallLLM(messages []Message) (*Response, error)`** — Builds a JSON request body combining the configured model ID, user-supplied messages, and a leading system prompt message. The request is POSTed to `\u003cBaseURL\u003e/api/chat` with `\"Content-Type: application/json\"`. On success (200), the full response body is read into memory and parsed once; on non-200 status, up to 1024 bytes of the error body are captured for reporting. The HTTP client has a 10-minute timeout applied globally.\n\n**`StreamLLM(messages []Message, onChunk func(string)) error`** — Performs an identical outbound POST but with streaming enabled. Each line from the response is read via `bufio.NewReader`, trimmed, and parsed as JSON. Non-empty content segments invoke the caller-supplied `onChunk` callback immediately. The loop exits on EOF or when a chunk's `Done` flag is true. Invalid JSON lines inside the stream are silently skipped—no error returned to the caller unless the reader itself fails.\n\n**`GetBaseSystemPrompt() string`** — Returns static base prompt text.\n\n**`GetDefaultSystemPrompt(command) string`** — Switches on `command`: `\"module_synthesis\"` returns a composition for per-module documentation; `\"architecture\"` returns a composition for global architecture documents.\n\n### Response Parsing (`json_parser.go`)\n\nNormalizes raw LLM output into parseable JSON before downstream consumption. The parser tolerates markdown code fences (with or without language tags) and handles bracket/brace mismatches within embedded text by walking character-by-character while respecting string boundaries.\n\n**`StripOuterMarkdownFence(input string) string`** — Trims whitespace at entry, then applies a compiled regex to match content between ```` ``` ` (with optional `markdown`/`json` tag) and captures the inner text. No match returns input unchanged.\n\n**`CleanJSONResponse(input string) (string, error)`** — Scans for the first `{` or `[`. Walks forward tracking an `inString` flag to skip escaped characters inside quoted strings. Pushes opening delimiters onto a stack; pops on matching close. Returns the substring from start index up to and including the closing character once the stack drains. If neither delimiter is found, input passes through unchanged.\n\n**`UnmarshalJSONResponse(input string) (interface{}, error)`** — The cleaned string is passed directly to `encoding/json.Unmarshal`. Errors are wrapped with context: `failed to unmarshal JSON (cleaned: %q): \u003coriginal\u003e`.\n\n### Chunk Reduction (`chunking.go`)\n\nImplements a recursive chunk-reduction algorithm that processes collections of code/architecture items through an LLM-based synthesis pipeline and produces a single consolidated output. Items are grouped into batches bounded by `maxChunkChars` (3× the LLM context window), then each batch is synthesized independently, and intermediate results are concatenated and reduced again until one item remains—a tree-like reduction pattern.\n\n**`ReduceInChunks(items []string) (string, error)`** — Algorithmic flow:\n1. **Empty input** — Returns `(\"\", nil)` immediately.\n2. **Single-item truncation** — When exactly one item exceeds the context window, it is mutated in-place to `\u003citems[0][:maxChunkChars]\u003e\\n...[truncated]\u003e`. No error is returned; processing continues.\n3. **Batching phase** — Items are greedily grouped into batches where each batch's combined length stays within `maxChunkChars`. Overflow starts a new batch.\n4. **Fallback truncation** — If every item exceeds limits individually, each item is proportionally truncated and forced into a single batch. This prevents deadlock on unbatchable inputs.\n5. **Single-batch terminal** — When exactly one batch exists after grouping, the entire set is sent directly to `CallLLM` for synthesis; the result is stripped of markdown fences before return.\n6. **Multi-batch recursion** — Each batch is recursively reduced via `ReduceInChunks`. Intermediate results are concatenated into a single list, which then triggers another recursive call until reaching the base case where all items fit in one context window.\n\nError handling: Recursive-call errors and LLM synthesis errors propagate directly. Synthesis errors are wrapped with `\"LLM error during synthesis: %w\"`. Truncation is a silent side effect, not an error path—no warning logged unless it's the only item (in which case `logEvent` is invoked).\n\n### Hierarchical Synthesis (`synthesize.go`)\n\nRecursively walks a directory tree (`DirNode`), producing synthesized textual summaries for each node. Each directory's summary is built from child summaries plus per-file facts extracted by LLM calls. Unchanged files reuse cached SHA256 hashes and previously extracted facts; changed or new files trigger fresh extraction via configurable `ExtractionSteps`.\n\n**`SynthesizeNode(node *DirNode, cache MetadataCache) (string, error)`** — Algorithmic flow:\n1. **Context check** — Aborts early if context is cancelled.\n2. **Cache hit short-circuit** — If node is not affected (`affectedDirs` map) and a cached summary exists, returns immediately without LLM calls.\n3. **Recursive child synthesis** — Children are sorted; each is synthesized in order. Non-empty summaries accumulate into `childSummaries`.\n4. **File fact extraction loop** — For each file: compute SHA256 via `computeSHA256`; decide cache hit vs. LLM-driven extraction; accumulate facts. Read errors on individual files are swallowed (`continue`).\n5. **Component assembly** — File components followed by child subsystem summaries form an ordered slice.\n6. **Chunked reduction** — All assembled components feed `ReduceInChunks` to produce the directory's final summary.\n7. **Cache update \u0026 persist** — The result is stored in both `MetadataCache.Files[f]` (with new hash and facts) and `MetadataCache.Modules[node.Path]`. Module docs are written under `\u003cdocsDir\u003e/modules/\u003csafe-filename\u003e.md` via `tools.WriteFileSafely`; write errors are wrapped.\n\nError propagation: LLM call failure during extraction is fatal for the node: wrapped as `\"LLM error extracting %s for %s: %w\"` and stops all further processing. No partial results returned. SHA256 computation or file read failures on individual files are swallowed; other files still contribute components. The result may be incomplete but not an error.\n\n### Directory Tree (`tree.go`)\n\nDefines the in-memory `DirNode` hierarchy and implements affected-directory propagation logic that distinguishes between directories containing changed files and unaffected ones, enabling incremental synthesis without re-analyzing unchanged subtrees.\n\n**`FileChange`** — `Path string`, `Status string` (`\"Added\" | \"Modified\" | \"Deleted\"`). Input primitive for tracking changes across the repository.\n\n**`DirNode`** — `Path string`, `Files []string`, `Children map[string]*DirNode`. Each node holds its own file list and a map of child subdirectories keyed by name.\n\n**Affected-Directory Propagation**:\n1. Individual directories are flagged as affected if any direct file appears in the change set, or if no cached module entry exists for the path, or if `modules/\u003csafe-filename\u003e.md` does not exist at the expected location under `docsDir/modules`.\n2. Once individual nodes are flagged, ancestors are recursively marked affected by walking upward—ensuring parent summaries reflect changes even when they themselves contain no direct file modifications.\n\nError handling: `SafeResolve` errors in `determineAffected` are caught and ignored; the entire block is skipped silently. `os.Stat(absModulePath)` errors: only `os.IsNotExist(err)` triggers the affected flag; other Stat errors (permission denied, etc.) are treated as non-existence and silently ignored.\n\n### Orchestrator (`orchestrator.go`)\n\nImplements the automatic documentation generation pipeline for software repositories. Coordinates code discovery, tree building, hierarchical synthesis, metadata cache operations, standard doc regeneration, AGENTS.md maintenance, and module cleanup. Emits typed `Event` objects (type + message) so callers can observe pipeline progress without parsing log output.\n\n**`Event`** — `Type string`, `Message string`. Typed event used for cross-cutting logging.\n\n**RunInit Flow**:\n1. Load `.gitignore`, apply configured ignore lists, scan repository for source-code files.\n2. Convert flat file list into a hierarchical `DirNode` tree rooted at the repo root via `buildTree`.\n3. Walk the tree bottom-up; each node is summarized by LLM invocation. The root node's summary becomes `rootSum`.\n4. Invoke LLM to write `architecture.md` and `quickstart.md`, passing `rootSum` as context. Errors are wrapped with `\"failed to generate ...: %w\"`.\n5. Append or create an \"AI Agent Guidelines\" section in `AGENTS.md`; if file exists and already contains the marker, skip. Write errors are swallowed.\n6. Persist cache via `saveMetadataCache` with a `\"local\"` commit marker.\n\n**RunUpdate Flow**:\n1. Re-discover code files using identical filtering logic.\n2. Detect changes via SHA256 comparison against cached hashes in `MetadataCache.Files`. Differences classify into `\"Added\"`, `\"Modified\"`, or `\"Deleted\"`.\n3. For every changed file, walk upward from its parent to the root directory, marking each ancestor as affected. Deleted files are removed from cache immediately; modules whose directories are no longer active are purged via `os.Remove` (errors ignored).\n4. Rebuild tree and prune cache so it reflects only currently existing code.\n5. Propagate affect markers through the tree via `propagateAffected`. If no directories are affected, short-circuit with a \"no modifications\" message; return without further LLM calls.\n6. Synthesize only affected nodes via `SynthesizeNode`, skipping unaffected subtrees entirely—reusing cached summaries for them.\n7. Regenerate `architecture.md`/`quickstart.md` if they are missing or any top-level directory was affected; otherwise reuse existing files.\n8. Persist updated cache with `\"local\"` commit marker.\n\nCross-cutting rules: **Gitignore stacking** — `.gitignore`, configured ignore lists, and the `docsDir` itself are all applied together when discovering code files. **Safe file resolution** — All paths resolved through `security.SafeResolve` before any filesystem operation. **Atomic writes** — Documentation written via `tools.WriteFileSafely`. **Module cleanup** — Modules whose directories no longer appear in the active set are removed from disk; removal errors are ignored (`_ = os.Remove`).\n\n### Runner (`runner.go`)\n\nActs as the orchestrator for code analysis or documentation pipelines. Manages lifecycle execution from initialization through completion, handling state transitions based on a provided mode string. Ensures repository integrity by verifying that the lockfile is excluded from version control before executing pipeline logic.\n\n**`Runner`** — Internal field `cfg *config.Config`. No exported fields; construction and configuration are external to this file's scope.\n\n**`NewRunner(cfg *config.Config) *Runner`** — Factory for creating a new runner instance.\n\n**`Run(ctx context.Context, repoRoot string, mode string, onEvent func(Event)) error`** — Main entry point. Acquires an exclusive lock on the repository; if acquisition fails, returns immediately with wrapped error. A deferred unlock releases the lock unconditionally regardless of how the function exits.\n\nMode dispatch:\n- `\"init\"` → calls `client.RunInit(ctx, repoRoot, r.cfg, onEvent)`\n- `\"update\"` → calls `client.RunUpdate(ctx, repoRoot, r.cfg, onEvent)`\n- Any other value → returns `fmt.Errorf(\"unsupported mode: %s\", mode)` as a fatal error.\n\nError handling summary:\n\n| Step | Behavior on Failure |\n|---|---|\n| `EnsureGitignoreHasLockfile` | Swallowed; logged as warning via `onEvent`. Execution continues. |\n| `AcquireLock` | Fatal, wrapped: `\"failed to acquire repository lock: %w\"`. Caller sees original error wrapped. |\n| `client.RunInit` / `RunUpdate` | Propagated raw without wrapping. Deferred lock cleanup still runs. |\n| Unsupported mode | Fatal, wrapped: `\"unsupported mode: %s\"`. |\n\n### Utilities (`utils.go`)\n\nMaps internal identifiers (module paths) onto safe, human-readable filenames for markdown output. Operates at the boundary between the engine and the documentation generation system.\n\n**`ToSafeMarkdownFilename(modulePath string) string`**:\n1. All filesystem separator characters are replaced with underscores (`_`). This normalizes platform-specific path separators into a single ASCII character.\n2. If the resulting string is empty or equals `\".\"`, it is replaced with the literal string `\"root\"`.\n3. A `.md` suffix is appended, signaling that the output is a markdown document file.\n\nThe mapping is deterministic and pure—same input always yields the same filename. No error return for invalid inputs. All transformations are best-effort with silent normalization.",
    "cmd": "# cmd Package — Technical Architecture\n\n## Module Responsibility\n\nThe `cmd` package implements a CLI application that scans a repository structure and generates/maintains wiki documentation pages. It acts as the orchestration layer for two operational modes—**initialization** (`init`) and **update**—each routed through a shared dispatcher (`executeCommand`). The module validates preconditions (Git repo presence, configuration existence, credential setup), resolves merged configuration from multiple sources, delegates execution to an engine component, and propagates final errors or surfaces progress events via stdout/stderr.\n\n## Public Surface Area\n\nThe package exposes no exported identifiers. All entry points—`RootCmd`, `initCmd`, `updateCmd`, `setupCmd`—are unexported cobra command instances registered at init time. The only externally callable function is `RunSetupFlow(repoRoot string)`, which serves as the interactive setup wizard entry point.\n\n## Internal Components\n\n### Command Registration Layer (`cmd/init.go`)\n\nPackage-level global variables are initialized once and never mutated within this file:\n\n- `initCmd` — cobra command instance for the `init` subcommand\n- `updateCmd` — cobra command instance for the `update` subcommand (defined in a separate file but referenced here)\n- `RootCmd` — root cobra command, shared across all files in the package\n\nThe `init` subcommand's `.Run` closure delegates to `executeCommand(\"init\")`. The `update` subcommand follows the same delegation pattern. Both commands are registered with `RootCmd.AddCommand` during init; any registration failure is silently swallowed without error propagation.\n\n### Root Command Orchestration (`cmd/root.go`)\n\nThis file implements the primary entry point for all non-setup CLI invocations. It performs a linear sequence of precondition checks and configuration resolution before delegating to an engine runner:\n\n1. **Working directory validation** via `os.Getwd()`—failure prints error to stdout, exits with code 1\n2. **Git repository verification** via `tools.VerifyGitRepo(repoRoot)`—error formatted to stderr, exit code 1\n3. **Configuration existence check** via `config.ConfigExists(repoRoot)`—used only for the TTY guard branch\n4. **Terminal detection** via `isatty.IsTerminal`/`IsCygwinTerminal` on stdin fd—determines whether implicit setup flow is permitted or a non-TTY error path fires\n5. **Implicit setup trigger**—when no config exists and stdin is interactive, invokes the setup wizard before proceeding; otherwise exits with an instruction to run setup first\n6. **Configuration resolution** via `config.ResolveConfig(repoRoot, ...)`—merges persistent flags, environment/file-based config, and user overrides into a single effective config object\n7. **Metadata file read**—`os.ReadFile(metadataPath)` followed by `json.Unmarshal` into a local struct containing `lastSHA` (last documented commit) and other state; parse failures are silently swallowed (no log, no exit)\n8. **Mode isolation validation** between `init` and `update`—rejects running `init` when already initialized (`hasInit` true), rejects `update` when not initialized (`hasInit` false)\n9. **Credential validation**—verifies a model identifier is non-empty; failure triggers setup flag but does not abort\n10. **Engine runner instantiation and execution** via `engine.NewRunner(cfg).Run(ctx, repoRoot, mode, ...)`—any returned error is printed to stderr and causes `os.Exit(1)`\n\nLocal mutable state within `executeCommand` includes `meta`, `cfg`, `repoRoot`, `lastSHA`, `hasInit`—all scoped to the function, single-threaded. Global variables (`modelIdFlag`, `numCtxFlag`) are read-only after their initial assignment in the init closure and never mutated elsewhere in this file.\n\n### Interactive Setup Wizard (`cmd/setup.go`)\n\nImplements incremental configuration generation for `.code-reducer.yaml`. The flow reads existing config first, then prompts per-field with pre-populated values—only changed fields get updated; untouched values persist unchanged:\n\n- **LLM model selection** defaults to `gemma4:26b-a4b-it-qat`; users may override to any Ollama-compatible model ID\n- **Ollama connection parameters**—base URL (defaults to standard port) and context window size in tokens; invalid/empty input falls back to defaults\n- **File/directory exclusion rules** maintained as a list of directories and file extensions; first setup collects fresh values, subsequent runs show existing entries with comma-separated append support; empty input leaves the current list intact\n- **Documentation directory** defaults to `wiki`; user may change freely\n- **Extraction steps**—a fixed sequence loaded once on first setup; preserved without prompting on subsequent runs\n\nThe function reuses a local `reader` (`bufio.NewReader(os.Stdin)` + `ReadString('\\n')`) per call. Configuration is persisted via `config.SaveConfig(repoRoot, newCfg)`.\n\n### Update Command Registration (`cmd/update.go`)\n\nA thin registration layer that wires up the `update` cobra subcommand with `RootCmd.AddCommand` during init. The command's `.Run` closure delegates to `executeCommand(\"update\")`. No business logic, validation rules, or algorithmic steps are implemented in this file—the actual behavior is defined elsewhere. No I/O operations occur here beyond the registration call itself.\n\n## Error Handling Patterns\n\nThe module exhibits three distinct error-handling strategies:\n\n| Pattern | Locations | Behavior |\n|---|---|---|\n| **Sentinel exit** | `root.go` (CWD failure, git check, config-missing+non-TTY, init-already-ran, update-not-init, runner failure); `setup.go` (Getwd failure, SaveConfig failure) | `fmt.Fprintf(os.Stderr, ...)` followed by `os.Exit(1)`. No error wrapping. Process terminates immediately with exit code 1. |\n| **Silent swallow** | `root.go` (metadata file read/unmarshal); `setup.go` (LoadConfig errors, Atoi parse failures); `update.go` (all paths including Run closure return value discarded by delegation) | Errors are captured but not logged or reported; execution continues with empty/zero values. No user notification. |\n| **Warning + fallback** | `setup.go` (stdin read errors in promptAndAppend/promptString) | Warning message printed to stdout, function returns a sensible default rather than propagating the error. Flow continues normally. |\n\nNo panic recovery (`recover()`), no custom sentinel types, no structured error wrapping visible within any of these files. Context cancellation is registered via `signal.NotifyContext(os.Interrupt, syscall.SIGTERM)` in `root.go` with `defer stop()`; cancellation side effects are swallowed unless the engine runner surfaces them.\n\n## Data Flow\n\n```\nUser Input (stdin) ──┬──► Cobra Dispatch (RootCmd.Execute)\n                      │\n                      ├──► initCmd.Run ──► executeCommand(\"init\")\n                      │                       │\n                      │                       ├──► config.ResolveConfig(repoRoot, ...)\n                      │                       ├──► engine.NewRunner(cfg).Run(ctx, repoRoot, \"init\", ...)\n                      │                       └──► Progress events → stdout/stderr via callback\n                      │\n                      ├──► updateCmd.Run ──► executeCommand(\"update\")\n                      │                            (implementation external)\n                      │\n                      └──► setupCmd.Run (RunSetupFlow(repoRoot))\n                                       │\n                                       ├──► config.LoadConfig(repoRoot) ──┐\n                                       ├──► Per-field prompts via reader   │\n                                       ├──► config.SaveConfig(repoRoot, newCfg)\n                                       └──► Success confirmation → stdout\n```\n\nConfiguration resolution merges three sources—persistent flags on the command, environment/file-based config resolution, and user-provided overrides—into a single effective config before any work begins. The metadata file tracks last documented commit SHA for resume capability; its absence or malformed content is treated as a no-op state reset.",
    "internal": "# Code-Reducer Engine Architecture\n\n## Module Overview and Data Flow\n\nThe Code-Reducer engine is an automatic software-documentation generation system driven by an Ollama-compatible large language model (LLM). It operates on a repository's source code tree to produce markdown artifacts describing the project's architecture, module boundaries, business logic, state management, error handling patterns, and agent guidelines. The system runs in two modes—**init**, which generates documentation from scratch against the full discovered codebase, and **update**, which performs incremental regeneration based on detected file changes.\n\nExecution is initiated through `internal/engine/runner.go` (`Runner.Run`), which acquires a repository-exclusive lock via `internal/security`, instantiates an LLM client, and dispatches to either `RunInit` or `RunUpdate`. All disk I/O flows through `internal/tools`; all network calls go through the `LLMClient` in `internal/engine/client.go`; concurrency protection is provided by OS-level file locking (`O_EXCL`) managed by `security.AcquireLock`.\n\nThe data flow for an update run follows this sequence: safety preflight (lockfile gitignore entry verification) → lock acquisition → LLM client construction from configuration → cache restoration from `\u003cdocsDir\u003e/.metadata.json` → code discovery respecting `.gitignore`, configured ignore lists, and `docsDir` exclusion → change detection via SHA256 hashing of current files against cached entries → affected-directory propagation (deleted files trigger immediate cache purging; ancestor directories are marked affected) → hierarchical synthesis walking the directory tree bottom-up with per-file LLM extraction for changed files only → chunked reduction collapsing intermediate summaries into a single module-level document → root documentation regeneration (`architecture.md`, `quickstart.md`) when missing or any top-level directory was affected → cache persistence to `.metadata.json`.\n\nThe init run follows an equivalent flow but skips change detection and affected-directory propagation, synthesizing all nodes from discovered code. Module summaries are persisted under `\u003cdocsDir\u003e/modules/` keyed by a sanitized filename derived via `internal/tools/utils.ToSafeMarkdownFilename`.\n\n---\n\n## Subsystem: config — Multi-Source Configuration Resolution\n\n### Responsibility\n\nCentralizes all configuration state for the engine: loading, persisting, resolving, and validating a `config.Config` struct from CLI flags, environment variables, YAML files, or hardcoded defaults in a strict four-tier priority chain. Persisted state lives at `.code-reducer.yaml` in the current working directory with restricted owner-only permissions (`0600`).\n\n### Data Structures\n\n```go\ntype Config struct {\n    ModelID            string   // Ollama model identifier (falls back to gemma4:26b-a4b-it-qat)\n    OllamaBaseURL      string   // Base URL for the Ollama API endpoint\n    OllamaNumCtx       int      // Context window size\n    DocsDir            string   // Path to source documentation directory\n    ExtractionSteps    []ExtractionStep  // Ordered pipeline of analysis tasks\n    Ignore             []string            // Directories to exclude from scanning\n    IgnoreExtensions   []string            // File extensions to skip\n}\n\ntype ExtractionStep struct {\n    Name   string `yaml:\"name\"`\n    Prompt string `yaml:\"prompt\"`\n}\n```\n\nFour extraction steps are defined by default:\n\n| Priority | Step | Purpose |\n|---|---|---|\n| 1 | API_SIGNATURES | Extract exported structs, interfaces, methods and their types |\n| 2 | BUSINESS_LOGIC | Analyze domain concepts and high-level algorithmic flow |\n| 3 | STATE_AND_CONCURRENCY | Identify mutable state and concurrency protections (mutexes, channels) |\n| 4 | ERRORS_AND_SIDE_EFFECTS | Document external communication (I/O) and error handling patterns |\n\nThe step list is overridable in the YAML file; if empty at resolve time it falls back to defaults.\n\n### Module Constants \u0026 Defaults\n\nPackage-level read-only constants consumed exclusively by `ResolveConfig`:\n\n| Variable | Purpose | Reassignable? |\n|---|---|---|\n| `DefaultIgnores` | Hardcoded directories to skip (`.git`, `node_modules`, `venv`, etc.) | No — read-only after init |\n| `DefaultIgnoredExtensions` | Hardcoded file extensions to skip (`.pyc`, `.class`, etc.) | No — read-only after init |\n| `DefaultExtractionSteps` | Ordered list of default analysis tasks | No — read-only after init |\n\n### Functions\n\n**`ConfigExists(cwd string) bool`** — Returns `true` when `.code-reducer.yaml` exists under `cwd`. Treats any `os.Stat` error as \"file does not exist\" — permission denial and I/O failures are silently swallowed.\n\n**`LoadConfig(cwd string) (*Config, error)`** — Reads the persisted config from `\u003ccwd\u003e/.code-reducer.yaml`. Returns a nil pointer with an unwrapped `os.ReadFile` error if the file cannot be read at all. If YAML parsing fails, returns a wrapped error (`failed to parse yaml config`). On success, returns the raw parsed struct without applying overrides — callers must chain this into `ResolveConfig` for final resolution.\n\n**`SaveConfig(cwd string, cfg *Config) error`** — Marshals the provided `cfg` pointer and writes it to `\u003ccwd\u003e/.code-reducer.yaml` with `0600` permissions via `os.WriteFile`. Marshaling errors and write errors are both wrapped with `%w`, preserving the chain for unwrapping. A nil config is not explicitly guarded; marshaling a nil will result in an empty YAML document.\n\n**`MergeAndDeduplicate(a []T, b []T) []T`** (where T is comparable) — Merges two slices into one deduplicated slice using a map-based seen-set pattern. System defaults are merged first, then user-provided config additions follow — this ordering ensures user values take precedence when duplicates exist across the merge boundary. No error return; any panic from non-comparable types would be a caller-side bug.\n\n**`ResolveConfig(repoRoot, modelIdFlag string, numCtxFlag string) *Config`** — Orchestrates the full resolution pipeline: (1) calls `LoadConfig` to read persisted state (errors swallowed — falls back to empty struct); (2) applies CLI flag overrides in order: `modelIdFlag` → `OllamaBaseURL`, then `numCtxFlag` → `OllamaNumCtx`; (3) merges default ignore lists (`DefaultIgnores`, `DefaultIgnoredExtensions`) with user-provided values via `MergeAndDeduplicate`; (4) falls back to hardcoded defaults for extraction steps if the slice is empty; (5) returns a pointer to the final resolved config; no error return — any failure path results in an empty `\u0026Config{}`.\n\n---\n\n## Subsystem: security — Path-Boundary Enforcement and Mutual Exclusion\n\n### Responsibility\n\nImplements three infrastructure contracts for the Code-Reducer agent: path-boundary enforcement against directory-traversal attacks, process mutual exclusion via file locking, and lockfile version-control exclusion through `.gitignore` management. No synchronization primitives (`sync.Mutex`, channels, atomics) are used; concurrency safety is delegated to the OS-level `O_EXCL` semantics on lock acquisition.\n\n### Domain Concepts and Business Rules\n\n1. **Path-boundary enforcement** — `SafeResolve` requires any user-supplied path to resolve strictly inside the repository root. If a resolved absolute path escapes the root (detected via the `..` prefix check), the operation fails with an explicit violation error. This is a defense-in-depth rule against directory-traversal attacks.\n\n2. **Process mutual exclusion** — The lock mechanism ensures only one Code-Reducer instance operates on a given repository at a time. Acquisition requires creating the lockfile atomically (via `O_EXCL`); if it already exists, acquisition fails with a \"lock held\" error. Release closes the file and removes the lockfile.\n\n3. **Lockfile version-control exclusion** — The rule that `.code-reducer.lock` must appear in `.gitignore`. If the ignore file doesn't exist or lacks this entry, one is created/updated so the lockfile is not tracked by git.\n\n### Data Structures\n\n```go\ntype SimpleLock struct {\n    lockPath string // Absolute path to the lock file. Set in AcquireLock; read in Unlock.\n    file     *os.File // File handle opened during acquisition. After Unlock() the underlying file is closed and removed from disk but the field is never nilled out.\n}\n```\n\n**`SimpleLock.Unlock() error`** — Closes the lock file and removes it from disk. Returns an error if either step fails; individual errors are captured into local vars (`err1`, `err2`) and only one is ever returned (the second operation's error may be silently discarded in favor of the first).\n\n### Public Functions\n\n**`SafeResolve(repoRoot, inputPath string) (string, error)`** — Resolves and validates an input path against a repository root. Returns the cleaned absolute path or an error on traversal detection. Uses pure path manipulation: `filepath.Abs`, `filepath.Clean`, `filepath.Rel`. No actual disk read/write occurs during this call. The security check is performed via prefix inspection of the relative path after resolution.\n\n**`AcquireLock(repoRoot string) (*SimpleLock, error)`** — Acquires a simple file lock in the repo root. Requires creating the lockfile atomically (via `O_CREATE|O_EXCL`); if it already exists, acquisition fails with a \"lock held\" sentinel message. On success writes PID (newline-terminated). If PID write subsequently fails, `f.Close()` and `os.Remove(lockPath)` are called explicitly — this cleans up the partially-created lockfile so another process can retry.\n\n**`EnsureGitignoreHasLockfile(repoRoot string) error`** — Ensures `.code-reducer.lock` is present in `.gitignore`. If the ignore file doesn't exist, one is created with a single line. Otherwise: scans for existing entry; if not found, appends a commented block plus the lockfile name. Opens `.gitignore` for appending with `O_APPEND|O_WRONLY`; if append fails, returns a wrapped error. No partial state change occurs beyond the open handle (deferred close); no explicit cleanup of any partial content is performed on write failure after opening.\n\n### Error Handling Patterns\n\n1. **Wrap pattern (`fmt.Errorf %w`)** — Used in every failure path:\n   - `SafeResolve`: wraps `filepath.Abs` error as `\"failed to get absolute root path\"`; wraps traversal detection as a hardcoded sentinel message.\n   - `AcquireLock`: wraps the raw `os.OpenFile` error when it's not `IsExist`.\n   - `EnsureGitignoreHasLockfile`: wraps stat/read/write errors individually.\n\n2. **Sentinel / Hardcoded messages** — Not wrapped with `%w`, returned as-is:\n   - `\"security violation: path traversal detected\"` — in `SafeResolve` when relative path starts with `..`.\n   - `\"lock at %s is already held by another process...\"` — when `os.IsExist(err)` on OpenFile. Domain-specific sentinel for lock contention.\n\n3. **Error swallowing** — `Unlock()` captures each step's error into local vars (`err1`, `err2`) and returns whichever is non-nil (or nil if both succeed). The second operation's error may be silently discarded in favor of the first, or vice versa — only one error is ever returned.\n\n4. **No panic recovery** — None of these functions recover from panics. Any unexpected panic propagates to the caller with an empty stack trace in `runtime` (Go's default).\n\n---\n\n## Subsystem: tools — Repository Traversal, Safe File I/O, and Git Utilities\n\n### Responsibility\n\nProvides three independent capability groups: **repository code discovery**, **safe file read/write operations** with TOCTOU race mitigation, **gitignore-aware filtering**, and **Git subprocess execution**. All functions are standalone (no methods on types); no exported structs or interfaces exist. No goroutines, channels, or sync primitives are used anywhere in the package—every call is synchronous and single-threaded.\n\n### Repository Code Discovery\n\n**`DiscoverCodeFiles(repoRoot string) ([]string, error)`** — Walks `repoRoot` recursively via `filepath.WalkDir`, skipping directories matching `.gitignore` patterns (compiled by `LoadGitignore`) or any dot-prefixed component (`.build`, `.svn`, etc.), then filters each leaf through `ShouldIgnoreFile`. Valid source paths are accumulated into a slice and returned. Walk callback errors from `filepath.WalkDir` are swallowed—individual directory entries that fail to stat return `nil` to the caller, making it impossible to distinguish an intentional skip from a walk failure downstream.\n\n### Safe File Read / Write Operations\n\n**`ReadFileSafely(repoRoot, virtualPath) ([]byte, error)`** — Resolves `virtualPath` through `security.SafeResolve` (path-traversal guard), then reads the resulting absolute path via `os.ReadFile`. Errors from both `SafeResolve` and `os.ReadFile` are wrapped with prefix `\"failed to read file:\"` using `%w`.\n\n**`WriteFileSafely(repoRoot, virtualPath, content []byte) error`** — Performs TOCTOU-safe writes:\n1. Resolve the safe absolute path via `security.SafeResolve`.\n2. Call `os.MkdirAll(dir, 0755)` to create parent directories if missing; wraps with `\"failed to create directory\"`.\n3. Open for write+create (`O_WRONLY|O_CREATE`, mode `0644`); wraps open failure with `\"failed to open file for writing\"`.\n4. Call `os.Lstat(safePath)` (no symlink follow) and `f.Stat()`; compare via `os.SameFile(fiLstat, fiFstat)`. If they differ—indicating a symlink swap or race condition—return an error prefixed `\"security violation\"` or `\"TOCTOU symlink race detected\"`.\n5. Truncate (`f.Truncate(0)`), then write content; wrap each with `\"failed to truncate file:\"` / `\"failed to write file content:\"`.\n\n### Gitignore Handling and Binary Classification\n\n**`LoadGitignore(repoRoot) ([]string, error)`** — Reads `.gitignore` from `repoRoot`, scanner processes line-by-line. Empty file returns `nil, nil`; non-existence (`os.IsNotExist`) also returns `nil, nil`. All other errors are wrapped. Patterns returned are active non-comment entries.\n\n**`ShouldIgnoreFile(repoRoot, relPath string, gitIgnore *ignore.GitIgnore, ignoredExtensions []string) bool`** — Applies layered ignore rules:\n- Compiles user-supplied patterns via the external `github.com/sabhiram/go-gitignore` package (`*ignore.GitIgnore`).\n- Rejects paths with dot-prefixed components or `.egg-info` suffixes (build artifacts).\n- Checks file extension against a provided blacklist.\n- For unknown extensions, reads the first 1024 bytes and scans for null bytes via `IsBinaryFile`. If null bytes are present, returns `true` (binary → ignore).\n\n**`IsBinaryFile(path string) bool`** — Opens the path; if `os.Open` fails, returns `false` (treats unreadable as non-binary). Reads up to 1024 bytes. Only wraps `io.EOF` (expected EOF) without error—other read errors return `false`. No error wrapping for open or other read failures beyond EOF handling.\n\n### Git Subprocess Execution\n\n**`RunGit(repoRoot string, args ...string) (stdout string, err error)`** — Constructs an `exec.Command` for `git`, prepends `--no-pager` to user-supplied arguments, sets `cmd.Dir = repoRoot`. Captures stdout into a `bytes.Buffer`, reads it back, trims whitespace. On success returns trimmed output; on failure wraps the original error with stderr text: `\"git command failed: %v, stderr: %s\"`.\n\n**`VerifyGitRepo(repoRoot string) error`** — Two-stage validation:\n1. Calls `exec.LookPath(\"git\")`; if unavailable, returns a fresh error without wrapping—no reference to any prior failure since none exists at that point.\n2. Otherwise delegates to `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")`. If the inner call fails, discards its returned error entirely and returns a new message: `\"not a git repository (or any of the parent directories)\"`.\n\n### Error Handling Summary\n\n| Function | Failure Mode | Strategy |\n|---|---|---|\n| `ReadFileSafely` | Resolve or read failure | Wrap with `\"failed to read file:\"`, `%w` |\n| `WriteFileSafely` | Directory open, file open, lstat/stat mismatch, truncate, write | Contextual prefix per step; symlink race → `\"security violation\"` / `\"TOCTOU symlink race detected\"` |\n| `LoadGitignore` | Non-existence or read error | `nil, nil` on missing/empty; wrap everything else |\n| `DiscoverCodeFiles` | Walk entry failure | Swallowed—caller sees no signal |\n| `IsBinaryFile` | Open failure or non-EOF read error | Returns `false`; EOF is expected and ignored |\n| `RunGit` | Non-zero exit / exec error | Wrap original + stderr text with `%v`, `%s` |\n| `VerifyGitRepo` | Missing git binary | Fresh error, no wrapping |\n| `VerifyGitRepo` | Git query failure | Discard inner error; return fresh message only |\n\n---\n\n## Subsystem: engine — Documentation Generation Pipeline\n\n### Responsibility\n\nImplements an automatic software-documentation generation pipeline that uses a large language model (Ollama-compatible API) to analyze repository structure and produce markdown artifacts. The system operates in two modes—**init**, which generates fresh documentation from scratch, and **update**, which performs incremental regeneration based on detected code changes.\n\n### LLM Client (`client.go`)\n\nProvides synchronous and streaming communication with an Ollama-compatible LLM API. The client is immutable after construction; all fields are assigned only in `NewLLMClient`. Both modes share a base system prompt that establishes agent identity and operating rules.\n\n**`CallLLM(messages []Message) (*Response, error)`** — Builds a JSON request body combining the configured model ID, user-supplied messages, and a leading system prompt message. The request is POSTed to `\u003cBaseURL\u003e/api/chat` with `\"Content-Type: application/json\"`. On success (200), the full response body is read into memory and parsed once; on non-200 status, up to 1024 bytes of the error body are captured for reporting. The HTTP client has a 10-minute timeout applied globally.\n\n**`StreamLLM(messages []Message, onChunk func(string)) error`** — Performs an identical outbound POST but with streaming enabled. Each line from the response is read via `bufio.NewReader`, trimmed, and parsed as JSON. Non-empty content segments invoke the caller-supplied `onChunk` callback immediately. The loop exits on EOF or when a chunk's `Done` flag is true. Invalid JSON lines inside the stream are silently skipped—no error returned to the caller unless the reader itself fails.\n\n**`GetBaseSystemPrompt() string`** — Returns static base prompt text.\n\n**`GetDefaultSystemPrompt(command) string`** — Switches on `command`: `\"module_synthesis\"` returns a composition for per-module documentation; `\"architecture\"` returns a composition for global architecture documents.\n\n### Response Parsing (`json_parser.go`)\n\nNormalizes raw LLM output into parseable JSON before downstream consumption. The parser tolerates markdown code fences (with or without language tags) and handles bracket/brace mismatches within embedded text by walking character-by-character while respecting string boundaries.\n\n**`StripOuterMarkdownFence(input string) string`** — Trims whitespace at entry, then applies a compiled regex to match content between ```` ``` ` (with optional `markdown`/`json` tag) and captures the inner text. No match returns input unchanged.\n\n**`CleanJSONResponse(input string) (string, error)`** — Scans for the first `{` or `[`. Walks forward tracking an `inString` flag to skip escaped characters inside quoted strings. Pushes opening delimiters onto a stack; pops on matching close. Returns the substring from start index up to and including the closing character once the stack drains. If neither delimiter is found, input passes through unchanged.\n\n**`UnmarshalJSONResponse(input string) (interface{}, error)`** — The cleaned string is passed directly to `encoding/json.Unmarshal`. Errors are wrapped with context: `failed to unmarshal JSON (cleaned: %q): \u003coriginal\u003e`.\n\n### Chunk Reduction (`chunking.go`)\n\nImplements a recursive chunk-reduction algorithm that processes collections of code/architecture items through an LLM-based synthesis pipeline and produces a single consolidated output. Items are grouped into batches bounded by `maxChunkChars` (3× the LLM context window), then each batch is synthesized independently, and intermediate results are concatenated and reduced again until one item remains—a tree-like reduction pattern.\n\n**`ReduceInChunks(items []string) (string, error)`** — Algorithmic flow:\n1. **Empty input** — Returns `(\"\", nil)` immediately.\n2. **Single-item truncation** — When exactly one item exceeds the context window, it is mutated in-place to `\u003citems[0][:maxChunkChars]\u003e\\n...[truncated]\u003e`. No error is returned; processing continues.\n3. **Batching phase** — Items are greedily grouped into batches where each batch's combined length stays within `maxChunkChars`. Overflow starts a new batch.\n4. **Fallback truncation** — If every item exceeds limits individually, each item is proportionally truncated and forced into a single batch. This prevents deadlock on unbatchable inputs.\n5. **Single-batch terminal** — When exactly one batch exists after grouping, the entire set is sent directly to `CallLLM` for synthesis; the result is stripped of markdown fences before return.\n6. **Multi-batch recursion** — Each batch is recursively reduced via `ReduceInChunks`. Intermediate results are concatenated into a single list, which then triggers another recursive call until reaching the base case where all items fit in one context window.\n\nError handling: Recursive-call errors and LLM synthesis errors propagate directly. Synthesis errors are wrapped with `\"LLM error during synthesis: %w\"`. Truncation is a silent side effect, not an error path—no warning logged unless it's the only item (in which case `logEvent` is invoked).\n\n### Hierarchical Synthesis (`synthesize.go`)\n\nRecursively walks a directory tree (`DirNode`), producing synthesized textual summaries for each node. Each directory's summary is built from child summaries plus per-file facts extracted by LLM calls. Unchanged files reuse cached SHA256 hashes and previously extracted facts; changed or new files trigger fresh extraction via configurable `ExtractionSteps`.\n\n**`SynthesizeNode(node *DirNode, cache MetadataCache) (string, error)`** — Algorithmic flow:\n1. **Context check** — Aborts early if context is cancelled.\n2. **Cache hit short-circuit** — If node is not affected (`affectedDirs` map) and a cached summary exists, returns immediately without LLM calls.\n3. **Recursive child synthesis** — Children are sorted; each is synthesized in order. Non-empty summaries accumulate into `childSummaries`.\n4. **File fact extraction loop** — For each file: compute SHA256 via `computeSHA256`; decide cache hit vs. LLM-driven extraction; accumulate facts. Read errors on individual files are swallowed (`continue`).\n5. **Component assembly** — File components followed by child subsystem summaries form an ordered slice.\n6. **Chunked reduction** — All assembled components feed `ReduceInChunks` to produce the directory's final summary.\n7. **Cache update \u0026 persist** — The result is stored in both `MetadataCache.Files[f]` (with new hash and facts) and `MetadataCache.Modules[node.Path]`. Module docs are written under `\u003cdocsDir\u003e/modules/\u003csafe-filename\u003e.md` via `tools.WriteFileSafely`; write errors are wrapped.\n\nError propagation: LLM call failure during extraction is fatal for the node: wrapped as `\"LLM error extracting %s for %s: %w\"` and stops all further processing. No partial results returned. SHA256 computation or file read failures on individual files are swallowed; other files still contribute components. The result may be incomplete but not an error.\n\n### Directory Tree (`tree.go`)\n\nDefines the in-memory `DirNode` hierarchy and implements affected-directory propagation logic that distinguishes between directories containing changed files and unaffected ones, enabling incremental synthesis without re-analyzing unchanged subtrees.\n\n**`FileChange`** — `Path string`, `Status string` (`\"Added\" | \"Modified\" | \"Deleted\"`). Input primitive for tracking changes across the repository.\n\n**`DirNode`** — `Path string`, `Files []string`, `Children map[string]*DirNode`. Each node holds its own file list and a map of child subdirectories keyed by name.\n\n**Affected-Directory Propagation**:\n1. Individual directories are flagged as affected if any direct file appears in the change set, or if no cached module entry exists for the path, or if `modules/\u003csafe-filename\u003e.md` does not exist at the expected location under `docsDir/modules`.\n2. Once individual nodes are flagged, ancestors are recursively marked affected by walking upward—ensuring parent summaries reflect changes even when they themselves contain no direct file modifications.\n\nError handling: `SafeResolve` errors in `determineAffected` are caught and ignored; the entire block is skipped silently. `os.Stat(absModulePath)` errors: only `os.IsNotExist(err)` triggers the affected flag; other Stat errors (permission denied, etc.) are treated as non-existence and silently ignored.\n\n### Orchestrator (`orchestrator.go`)\n\nImplements the automatic documentation generation pipeline for software repositories. Coordinates code discovery, tree building, hierarchical synthesis, metadata cache operations, standard doc regeneration, AGENTS.md maintenance, and module cleanup. Emits typed `Event` objects (type + message) so callers can observe pipeline progress without parsing log output.\n\n**`Event`** — `Type string`, `Message string`. Typed event used for cross-cutting logging.\n\n**RunInit Flow**:\n1. Load `.gitignore`, apply configured ignore lists, scan repository for source-code files.\n2. Convert flat file list into a hierarchical `DirNode` tree rooted at the repo root via `buildTree`.\n3. Walk the tree bottom-up; each node is summarized by LLM invocation. The root node's summary becomes `rootSum`.\n4. Invoke LLM to write `architecture.md` and `quickstart.md`, passing `rootSum` as context. Errors are wrapped with `\"failed to generate ...: %w\"`.\n5. Append or create an \"AI Agent Guidelines\" section in `AGENTS.md`; if file exists and already contains the marker, skip. Write errors are swallowed.\n6. Persist cache via `saveMetadataCache` with a `\"local\"` commit marker.\n\n**RunUpdate Flow**:\n1. Re-discover code files using identical filtering logic.\n2. Detect changes via SHA256 comparison against cached hashes in `MetadataCache.Files`. Differences classify into `\"Added\"`, `\"Modified\"`, or `\"Deleted\"`.\n3. For every changed file, walk upward from its parent to the root directory, marking each ancestor as affected. Deleted files are removed from cache immediately; modules whose directories are no longer active are purged via `os.Remove` (errors ignored).\n4. Rebuild tree and prune cache so it reflects only currently existing code.\n5. Propagate affect markers through the tree via `propagateAffected`. If no directories are affected, short-circuit with a \"no modifications\" message; return without further LLM calls.\n6. Synthesize only affected nodes via `SynthesizeNode`, skipping unaffected subtrees entirely—reusing cached summaries for them.\n7. Regenerate `architecture.md`/`quickstart.md` if they are missing or any top-level directory was affected; otherwise reuse existing files.\n8. Persist updated cache with `\"local\"` commit marker.\n\nCross-cutting rules: **Gitignore stacking** — `.gitignore`, configured ignore lists, and the `docsDir` itself are all applied together when discovering code files. **Safe file resolution** — All paths resolved through `security.SafeResolve` before any filesystem operation. **Atomic writes** — Documentation written via `tools.WriteFileSafely`. **Module cleanup** — Modules whose directories no longer appear in the active set are removed from disk; removal errors are ignored (`_ = os.Remove`).\n\n### Runner (`runner.go`)\n\nActs as the orchestrator for code analysis or documentation pipelines. Manages lifecycle execution from initialization through completion, handling state transitions based on a provided mode string. Ensures repository integrity by verifying that the lockfile is excluded from version control before executing pipeline logic.\n\n**`Runner`** — Internal field `cfg *config.Config`. No exported fields; construction and configuration are external to this file's scope.\n\n**`NewRunner(cfg *config.Config) *Runner`** — Factory for creating a new runner instance.\n\n**`Run(ctx context.Context, repoRoot string, mode string, onEvent func(Event)) error`** — Main entry point. Acquires an exclusive lock on the repository; if acquisition fails, returns immediately with wrapped error. A deferred unlock releases the lock unconditionally regardless of how the function exits.\n\nMode dispatch:\n- `\"init\"` → calls `client.RunInit(ctx, repoRoot, r.cfg, onEvent)`\n- `\"update\"` → calls `client.RunUpdate(ctx, repoRoot, r.cfg, onEvent)`\n- Any other value → returns `fmt.Errorf(\"unsupported mode: %s\", mode)` as a fatal error.\n\nError handling summary:\n\n| Step | Behavior on Failure |\n|---|---|\n| `EnsureGitignoreHasLockfile` | Swallowed; logged as warning via `onEvent`. Execution continues. |\n| `AcquireLock` | Fatal, wrapped: `\"failed to acquire repository lock: %w\"`. Caller sees original error wrapped. |\n| `client.RunInit` / `RunUpdate` | Propagated raw without wrapping. Deferred lock cleanup still runs. |\n| Unsupported mode | Fatal, wrapped: `\"unsupported mode: %s\"`. |\n\n### Utilities (`utils.go`)\n\nMaps internal identifiers (module paths) onto safe, human-readable filenames for markdown output. Operates at the boundary between the engine and the documentation generation system.\n\n**`ToSafeMarkdownFilename(modulePath string) string`**:\n1. All filesystem separator characters are replaced with underscores (`_`). This normalizes platform-specific path separators into a single ASCII character.\n2. If the resulting string is empty or equals `\".\"`, it is replaced with the literal string `\"root\"`.\n3. A `.md` suffix is appended, signaling that the output is a markdown document file.\n\nThe mapping is deterministic and pure—same input always yields the same filename. No error return for invalid inputs. All transformations are best-effort with silent normalization.",
    "internal/config": "# internal/config — Multi-Source Configuration Resolution Module\n\n## Responsibility \u0026 Data Flow\n\nThe module centralizes all configuration state for the code-reducer engine: loading, persisting, resolving, and validating a `Config` struct from CLI flags, environment variables, YAML files, or hardcoded defaults in a strict four-tier priority chain. Persisted state lives at `.code-reducer.yaml` in the current working directory with restricted owner-only permissions (`0600`).\n\n---\n\n## Data Structures\n\n### Config\n\n```go\ntype Config struct {\n    ModelID            string   // Ollama model identifier (falls back to gemma4:26b-a4b-it-qat)\n    OllamaBaseURL      string   // Base URL for the Ollama API endpoint\n    OllamaNumCtx       int      // Context window size\n    DocsDir            string   // Path to source documentation directory\n    ExtractionSteps    []ExtractionStep  // Ordered pipeline of analysis tasks\n    Ignore             []string            // Directories to exclude from scanning\n    IgnoreExtensions   []string            // File extensions to skip\n}\n```\n\n### ExtractionStep\n\n```go\ntype ExtractionStep struct {\n    Name   string `yaml:\"name\"`\n    Prompt string `yaml:\"prompt\"`\n}\n```\n\nFour extraction steps are defined by default:\n\n| Priority | Step | Purpose |\n|---|---|---|\n| 1 | API_SIGNATURES | Extract exported structs, interfaces, methods and their types |\n| 2 | BUSINESS_LOGIC | Analyze domain concepts and high-level algorithmic flow |\n| 3 | STATE_AND_CONCURRENCY | Identify mutable state and concurrency protections (mutexes, channels) |\n| 4 | ERRORS_AND_SIDE_EFFECTS | Document external communication (I/O) and error handling patterns |\n\nThe step list is overridable in the YAML file; if empty at resolve time it falls back to defaults.\n\n---\n\n## Functions\n\n### ConfigExists(cwd string) bool\n\nReturns `true` when `.code-reducer.yaml` exists under `cwd`. Treats any `os.Stat` error as \"file does not exist\" — permission denial and I/O failures are silently swallowed.\n\n### LoadConfig(cwd string) (*Config, error)\n\nReads the persisted config from `\u003ccwd\u003e/.code-reducer.yaml`. Returns a nil pointer with an unwrapped `os.ReadFile` error if the file cannot be read at all. If YAML parsing fails, returns a wrapped error (`failed to parse yaml config`). On success, returns the raw parsed struct without applying overrides — callers must chain this into `ResolveConfig` for final resolution.\n\n### SaveConfig(cwd string, cfg *Config) error\n\nMarshals the provided `cfg` pointer and writes it to `\u003ccwd\u003e/.code-reducer.yaml` with `0600` permissions via `os.WriteFile`. Marshaling errors and write errors are both wrapped with `%w`, preserving the chain for unwrapping. A nil config is not explicitly guarded; marshaling a nil will result in an empty YAML document.\n\n### MergeAndDeduplicate(a []T, b []T) []T (where T is comparable)\n\nMerges two slices into one deduplicated slice using a map-based seen-set pattern. System defaults are merged first, then user-provided config additions follow — this ordering ensures user values take precedence when duplicates exist across the merge boundary. No error return; any panic from non-comparable types would be a caller-side bug.\n\n### ResolveConfig(repoRoot, modelIdFlag string, numCtxFlag string) *Config\n\nOrchestrates the full resolution pipeline:\n\n1. Calls `LoadConfig` to read persisted state (errors swallowed — falls back to empty struct).\n2. Applies CLI flag overrides in order: `modelIdFlag` → `OllamaBaseURL`, then `numCtxFlag` → `OllamaNumCtx`.\n3. Merges default ignore lists (`DefaultIgnores`, `DefaultIgnoredExtensions`) with user-provided values via `MergeAndDeduplicate`.\n4. Falls back to hardcoded defaults for extraction steps if the slice is empty.\n5. Returns a pointer to the final resolved config; no error return — any failure path results in an empty `\u0026Config{}`.\n\n---\n\n## Module Constants \u0026 Defaults (package-level)\n\n| Variable | Purpose | Reassignable? |\n|---|---|---|\n| `DefaultIgnores` | Hardcoded directories to skip (`.git`, `node_modules`, `venv`, etc.) | No — read-only after init |\n| `DefaultIgnoredExtensions` | Hardcoded file extensions to skip (`.pyc`, `.class`, etc.) | No — read-only after init |\n| `DefaultExtractionSteps` | Ordered list of default analysis tasks | No — read-only after init |\n\nThese are initialized once at package scope and consumed exclusively by `ResolveConfig`.",
    "internal/engine": "# Engine Module Architecture\n\n## Overview and Data Flow\n\nThe `internal/engine` package implements an automatic software-documentation generation pipeline that uses a large language model (Ollama-compatible API) to analyze repository structure and produce markdown artifacts. The system operates in two modes—**init**, which generates fresh documentation from scratch, and **update**, which performs incremental regeneration based on detected code changes.\n\nThe execution entry point is the `Runner` struct in `runner.go`, which acquires an exclusive lock on the repository to prevent concurrent pipeline runs, instantiates an LLM client from configuration, and dispatches to either `RunInit` or `RunUpdate`. All disk I/O is funneled through the `tools.*` package; all network calls go through the `LLMClient`; concurrency protection is provided by a file-based lock acquired via `security.AcquireLock`.\n\nThe data flow for an update run follows this sequence:\n\n1. **Safety preflight** — `EnsureGitignoreHasLockfile` verifies `.gitignore` contains the lockfile entry. Failure logs a warning and continues.\n2. **Lock acquisition** — `security.AcquireLock(repoRoot)` writes a lock file; failure is fatal. A deferred unlock runs unconditionally.\n3. **Client construction** — `NewLLMClient` creates an LLM client using `cfg.ModelID`, `cfg.BaseURL`, and `cfg.NumCtx`.\n4. **Cache restoration** — `loadMetadataCache` reads `\u003cdocsDir\u003e/.metadata.json`. Missing file yields a default-initialized empty cache; parse errors are wrapped for caller inspection.\n5. **Code discovery** — Files under `\u003crepoRoot\u003e` are discovered respecting `.gitignore`, configured ignore lists, and the `docsDir` exclusion.\n6. **Change detection via SHA256** — Each current file is hashed by `computeSHA256`. Hashes are compared against cached entries in `MetadataCache.Files`. Differences produce `\"Added\"`, `\"Modified\"`, or `\"Deleted\"` states (defined as `FileChange` in `tree.go`).\n7. **Affected-directory propagation** — For each changed file, ancestor directories are marked affected. Deleted files trigger immediate cache purging and module-file removal. The tree is rebuilt from current paths; unaffected subtrees reuse cached summaries via a short-circuit check.\n8. **Hierarchical synthesis** — `synthesizeNode` walks the directory tree bottom-up. Each node's summary is built from child summaries plus per-file facts extracted by LLM calls (one call per extraction step per changed file). Components are assembled and collapsed into a single module-level document via `reduceInChunks`.\n9. **Root documentation regeneration** — `architecture.md` and `quickstart.md` are generated if they are missing or any top-level directory was affected. They are written through the same LLM pipeline with the root summary as context.\n10. **Persistence** — The updated `MetadataCache` is marshaled back to `.metadata.json` via `saveMetadataCache`. Module summaries are persisted under `\u003cdocsDir\u003e/modules/` keyed by a sanitized filename (via `ToSafeMarkdownFilename`).\n\nThe init run follows the same flow but skips change detection and affected-directory propagation, synthesizing all nodes from discovered code.\n\n---\n\n## LLM Client (`client.go`)\n\n### Responsibility\nProvides synchronous and streaming communication with an Ollama-compatible LLM API. The client is immutable after construction; all fields are assigned only in `NewLLMClient`. Both modes share a base system prompt that establishes agent identity and operating rules.\n\n### Synchronous Mode — `CallLLM`\nBuilds a JSON request body combining the configured model ID, user-supplied messages, and a leading system prompt message. The request is POSTed to `\u003cBaseURL\u003e/api/chat` with `\"Content-Type: application/json\"`. On success (200), the full response body is read into memory and parsed once; on non-200 status, up to 1024 bytes of the error body are captured for reporting. The HTTP client has a 10-minute timeout applied globally.\n\n### Streaming Mode — `StreamLLM`\nPerforms an identical outbound POST but with streaming enabled. Each line from the response is read via `bufio.NewReader`, trimmed, and parsed as JSON. Non-empty content segments invoke the caller-supplied `onChunk` callback immediately. The loop exits on EOF or when a chunk's `Done` flag is true. Invalid JSON lines inside the stream are silently skipped—no error returned to the caller unless the reader itself fails.\n\n### Prompt Dispatch\n- **`GetBaseSystemPrompt()`** — Returns static base prompt text.\n- **`GetDefaultSystemPrompt(command)`** — Switches on `command`: `\"module_synthesis\"` returns a composition for per-module documentation; `\"architecture\"` returns a composition for global architecture documents.\n\n---\n\n## Response Parsing (`json_parser.go`)\n\n### Responsibility\nNormalizes raw LLM output into parseable JSON before downstream consumption. The parser tolerates markdown code fences (with or without language tags) and handles bracket/brace mismatches within embedded text by walking character-by-character while respecting string boundaries.\n\n### Algorithmic Flow\n1. **Whitespace trim** — Input is trimmed at entry.\n2. **Fence removal** — A compiled regex matches content between ```` ``` ` (with optional `markdown`/`json` tag) and captures the inner text. No match returns input unchanged.\n3. **Bracket-depth extraction** — Scans for the first `{` or `[`. Walks forward tracking an `inString` flag to skip escaped characters inside quoted strings. Pushes opening delimiters onto a stack; pops on matching close. Returns the substring from start index up to and including the closing character once the stack drains. If neither delimiter is found, input passes through unchanged.\n4. **Unmarshal** — The cleaned string is passed directly to `encoding/json.Unmarshal`. Errors are wrapped with context: `failed to unmarshal JSON (cleaned: %q): \u003coriginal\u003e`.\n\n### Error Characteristics\n- `StripOuterMarkdownFence` and `CleanJSONResponse` return strings only; failures beyond the output shape cannot be detected by callers.\n- `UnmarshalJSONResponse` is the only function that surfaces errors, wrapping them explicitly via `%w`.\n\n---\n\n## Chunk Reduction (`chunking.go`)\n\n### Responsibility\nImplements a recursive chunk-reduction algorithm that processes collections of code/architecture items through an LLM-based synthesis pipeline and produces a single consolidated output. Items are grouped into batches bounded by `maxChunkChars` (3× the LLM context window), then each batch is synthesized independently, and intermediate results are concatenated and reduced again until one item remains—a tree-like reduction pattern.\n\n### Algorithmic Flow\n1. **Empty input** — Returns `(\"\", nil)` immediately.\n2. **Single-item truncation** — When exactly one item exceeds the context window, it is mutated in-place to `\u003citems[0][:maxChunkChars]\u003e\\n...[truncated]\u003e`. No error is returned; processing continues.\n3. **Batching phase** — Items are greedily grouped into batches where each batch's combined length stays within `maxChunkChars`. Overflow starts a new batch.\n4. **Fallback truncation** — If every item exceeds limits individually, each item is proportionally truncated and forced into a single batch. This prevents deadlock on unbatchable inputs.\n5. **Single-batch terminal** — When exactly one batch exists after grouping, the entire set is sent directly to `c.CallLLM` for synthesis; the result is stripped of markdown fences before return.\n6. **Multi-batch recursion** — Each batch is recursively reduced via `reduceInChunks`. Intermediate results are concatenated into a single list, which then triggers another recursive call until reaching the base case where all items fit in one context window.\n\n### Error Handling\n- Recursive-call errors and LLM synthesis errors propagate directly. Synthesis errors are wrapped with `\"LLM error during synthesis: %w\"`.\n- Truncation is a silent side effect, not an error path—no warning logged unless it's the only item (in which case `logEvent` is invoked).\n\n---\n\n## Hierarchical Synthesis (`synthesize.go`)\n\n### Responsibility\nRecursively walks a directory tree (`DirNode`), producing synthesized textual summaries for each node. Each directory's summary is built from child summaries plus per-file facts extracted by LLM calls. Unchanged files reuse cached SHA256 hashes and previously extracted facts; changed or new files trigger fresh extraction via configurable `ExtractionSteps`.\n\n### Algorithmic Flow\n1. **Context check** — Aborts early if context is cancelled.\n2. **Cache hit short-circuit** — If node is not affected (`affectedDirs` map) and a cached summary exists, returns immediately without LLM calls.\n3. **Recursive child synthesis** — Children are sorted; each is synthesized in order. Non-empty summaries accumulate into `childSummaries`.\n4. **File fact extraction loop** — For each file: compute SHA256 via `computeSHA256`; decide cache hit vs. LLM-driven extraction; accumulate facts. Read errors on individual files are swallowed (`continue`).\n5. **Component assembly** — File components followed by child subsystem summaries form an ordered slice.\n6. **Chunked reduction** — All assembled components feed `reduceInChunks` to produce the directory's final summary.\n7. **Cache update \u0026 persist** — The result is stored in both `MetadataCache.Files[f]` (with new hash and facts) and `MetadataCache.Modules[node.Path]`. Module docs are written under `\u003cdocsDir\u003e/modules/\u003csafe-filename\u003e.md` via `tools.WriteFileSafately`; write errors are wrapped.\n\n### Error Propagation\n- LLM call failure during extraction is fatal for the node: wrapped as `\"LLM error extracting %s for %s: %w\"` and stops all further processing. No partial results returned.\n- SHA256 computation or file read failures on individual files are swallowed; other files still contribute components. The result may be incomplete but not an error.\n\n---\n\n## Directory Tree (`tree.go`)\n\n### Responsibility\nDefines the in-memory `DirNode` hierarchy and implements affected-directory propagation logic that distinguishes between directories containing changed files and unaffected ones, enabling incremental synthesis without re-analyzing unchanged subtrees.\n\n### Structures\n- **`FileChange`** — `Path string`, `Status string` (`\"Added\" | \"Modified\" | \"Deleted\"`). Input primitive for tracking changes across the repository.\n- **`DirNode`** — `Path string`, `Files []string`, `Children map[string]*DirNode`. Each node holds its own file list and a map of child subdirectories keyed by name.\n\n### Affected-Directory Propagation\n1. Individual directories are flagged as affected if any direct file appears in the change set, or if no cached module entry exists for the path, or if `modules/\u003csafe-filename\u003e.md` does not exist at the expected location under `docsDir/modules`.\n2. Once individual nodes are flagged, ancestors are recursively marked affected by walking upward—ensuring parent summaries reflect changes even when they themselves contain no direct file modifications.\n\n### Error Handling\n- `SafeResolve` errors in `determineAffected` are caught and ignored; the entire block is skipped silently.\n- `os.Stat(absModulePath)` errors: only `os.IsNotExist(err)` triggers the affected flag; other Stat errors (permission denied, etc.) are treated as non-existence and silently ignored.\n\n---\n\n## Orchestrator (`orchestrator.go`)\n\n### Responsibility\nImplements the automatic documentation generation pipeline for software repositories. Coordinates code discovery, tree building, hierarchical synthesis, metadata cache operations, standard doc regeneration, AGENTS.md maintenance, and module cleanup. Emits typed `Event` objects (type + message) so callers can observe pipeline progress without parsing log output.\n\n### Structures\n- **`Event`** — `Type string`, `Message string`. Typed event used for cross-cutting logging.\n\n### RunInit Flow\n1. Load `.gitignore`, apply configured ignore lists, scan repository for source-code files.\n2. Convert flat file list into a hierarchical `DirNode` tree rooted at the repo root via `buildTree`.\n3. Walk the tree bottom-up; each node is summarized by LLM invocation. The root node's summary becomes `rootSum`.\n4. Invoke LLM to write `architecture.md` and `quickstart.md`, passing `rootSum` as context. Errors are wrapped with `\"failed to generate ...: %w\"`.\n5. Append or create an \"AI Agent Guidelines\" section in `AGENTS.md`; if file exists and already contains the marker, skip. Write errors are swallowed.\n6. Persist cache via `saveMetadataCache` with a `\"local\"` commit marker.\n\n### RunUpdate Flow\n1. Re-discover code files using identical filtering logic.\n2. Detect changes via SHA256 comparison against cached hashes in `MetadataCache.Files`. Differences classify into `\"Added\"`, `\"Modified\"`, or `\"Deleted\"`.\n3. For every changed file, walk upward from its parent to the root directory, marking each ancestor as affected. Deleted files are removed from cache immediately; modules whose directories are no longer active are purged via `os.Remove` (errors ignored).\n4. Rebuild tree and prune cache so it reflects only currently existing code.\n5. Propagate affect markers through the tree via `propagateAffected`. If no directories are affected, short-circuit with a \"no modifications\" message; return without further LLM calls.\n6. Synthesize only affected nodes via `synthesizeNode`, skipping unaffected subtrees entirely—reusing cached summaries for them.\n7. Regenerate `architecture.md`/`quickstart.md` if they are missing or any top-level directory was affected; otherwise reuse existing files.\n8. Persist updated cache with `\"local\"` commit marker.\n\n### Cross-Cutting Rules\n- **Gitignore stacking** — `.gitignore`, configured ignore lists, and the `docsDir` itself are all applied together when discovering code files.\n- **Safe file resolution** — All paths resolved through `security.SafeResolve` before any filesystem operation.\n- **Atomic writes** — Documentation written via `tools.WriteFileSafely`.\n- **Module cleanup** — Modules whose directories no longer appear in the active set are removed from disk; removal errors are ignored (`_ = os.Remove`).\n\n---\n\n## Runner (`runner.go`)\n\n### Responsibility\nActs as the orchestrator for code analysis or documentation pipelines. Manages lifecycle execution from initialization through completion, handling state transitions based on a provided mode string. Ensures repository integrity by verifying that the lockfile is excluded from version control before executing pipeline logic.\n\n### Structures\n- **`Runner`** — Internal field `cfg *config.Config`. No exported fields; construction and configuration are external to this file's scope.\n\n### Methods\n- **`NewRunner(cfg *config.Config) *Runner`** — Factory for creating a new runner instance.\n- **`Run(ctx context.Context, repoRoot string, mode string, onEvent func(Event)) error`** — Main entry point. Acquires an exclusive lock on the repository; if acquisition fails, returns immediately with wrapped error. A deferred unlock releases the lock unconditionally regardless of how the function exits.\n\n### Mode Dispatch\n- `\"init\"` → calls `client.RunInit(ctx, repoRoot, r.cfg, onEvent)`\n- `\"update\"` → calls `client.RunUpdate(ctx, repoRoot, r.cfg, onEvent)`\n- Any other value → returns `fmt.Errorf(\"unsupported mode: %s\", mode)` as a fatal error.\n\n### Error Handling Summary\n| Step | Behavior on Failure |\n|---|---|\n| `EnsureGitignoreHasLockfile` | Swallowed; logged as warning via `onEvent`. Execution continues. |\n| `AcquireLock` | Fatal, wrapped: `\"failed to acquire repository lock: %w\"`. Caller sees original error wrapped. |\n| `client.RunInit` / `RunUpdate` | Propagated raw without wrapping. Deferred lock cleanup still runs. |\n| Unsupported mode | Fatal, wrapped: `\"unsupported mode: %s\"`. |\n\n---\n\n## Utilities (`utils.go`)\n\n### Responsibility\nMaps internal identifiers (module paths) onto safe, human-readable filenames for markdown output. Operates at the boundary between the engine and the documentation generation system.\n\n### `ToSafeMarkdownFilename(modulePath string) string`\n1. All filesystem separator characters are replaced with underscores (`_`). This normalizes platform-specific path separators into a single ASCII character.\n2. If the resulting string is empty or equals `\".\"`, it is replaced with the literal string `\"root\"`.\n3. A `.md` suffix is appended, signaling that the output is a markdown document file.\n\nThe mapping is deterministic and pure—same input always yields the same filename. No error return for invalid inputs. All transformations are best-effort with silent normalization.",
    "internal/security": "# internal/security\n\n## Module Responsibility\n\nThis module implements three infrastructure contracts for the Code-Reducer agent: path-boundary enforcement against directory-traversal attacks, process mutual exclusion via file locking, and lockfile version-control exclusion through `.gitignore` management. No synchronization primitives (`sync.Mutex`, channels, atomics) are used; concurrency safety is delegated to the OS-level `O_EXCL` semantics on lock acquisition.\n\n## Domain Concepts and Business Rules\n\n1. **Path-boundary enforcement** — `SafeResolve` requires any user-supplied path to resolve strictly inside the repository root. If a resolved absolute path escapes the root (detected via the `..` prefix check), the operation fails with an explicit violation error. This is a defense-in-depth rule against directory-traversal attacks.\n\n2. **Process mutual exclusion** — The lock mechanism ensures only one Code-Reducer instance operates on a given repository at a time. Acquisition requires creating the lockfile atomically (via `O_EXCL`); if it already exists, acquisition fails with a \"lock held\" error. Release closes the file and removes the lockfile.\n\n3. **Lockfile version-control exclusion** — The rule that `.code-reducer.lock` must appear in `.gitignore`. If the ignore file doesn't exist or lacks this entry, one is created/updated so the lockfile is not tracked by git.\n\n## Data Structures\n\n### `SimpleLock`\n\nA value type representing an acquired lock on a specific repository root.\n\n| Field | Type | Description |\n|-------|------|-------------|\n| `lockPath` | `string` | Absolute path to the lock file. Set in `AcquireLock`; read in `Unlock`. No reset after close. |\n| `file` | `*os.File` | File handle opened during acquisition. After `Unlock()` the underlying file is closed and removed from disk but the field is never nilled out; subsequent calls to `Unlock()` will close an already-closed file (error swallowed). |\n\n### Methods on `SimpleLock`\n\n| Signature | Description |\n|-----------|-------------|\n| `(l *SimpleLock) Unlock() error` | Closes the lock file and removes it from disk. Returns an error if either step fails; individual errors are captured into local vars (`err1`, `err2`) and only one is ever returned (the second operation's error may be silently discarded in favor of the first). |\n\n## Public Functions\n\n### `SafeResolve(repoRoot, inputPath string) (string, error)`\n\nResolves and validates an input path against a repository root. Returns the cleaned absolute path or an error on traversal detection. Uses pure path manipulation: `filepath.Abs`, `filepath.Clean`, `filepath.Rel`. No actual disk read/write occurs during this call. The security check is performed via prefix inspection of the relative path after resolution.\n\n### `AcquireLock(repoRoot string) (*SimpleLock, error)`\n\nAcquires a simple file lock in the repo root. Requires creating the lockfile atomically (via `O_CREATE\\|O_EXCL`); if it already exists, acquisition fails with a \"lock held\" sentinel message. On success writes PID (newline-terminated). If PID write subsequently fails, `f.Close()` and `os.Remove(lockPath)` are called explicitly — this cleans up the partially-created lockfile so another process can retry.\n\n### `EnsureGitignoreHasLockfile(repoRoot string) error`\n\nEnsures `.code-reducer.lock` is present in `.gitignore`. If the ignore file doesn't exist, one is created with a single line. Otherwise: scans for existing entry; if not found, appends a commented block plus the lockfile name. Opens `.gitignore` for appending with `O_APPEND\\|O_WRONLY`; if append fails, returns a wrapped error. No partial state change occurs beyond the open handle (deferred close); no explicit cleanup of any partial content is performed on write failure after opening.\n\n## Error Handling Patterns\n\n1. **Wrap pattern (`fmt.Errorf %w`)** — Used in every failure path:\n   - `SafeResolve`: wraps `filepath.Abs` error as `\"failed to get absolute root path\"`; wraps traversal detection as a hardcoded sentinel message.\n   - `AcquireLock`: wraps the raw `os.OpenFile` error when it's not `IsExist`.\n   - `EnsureGitignoreHasLockfile`: wraps stat/read/write errors individually.\n\n2. **Sentinel / Hardcoded messages** — Not wrapped with `%w`, returned as-is:\n   - `\"security violation: path traversal detected\"` — in `SafeResolve` when relative path starts with `..`.\n   - `\"lock at %s is already held by another process...\"` — when `os.IsExist(err)` on OpenFile. Domain-specific sentinel for lock contention.\n\n3. **Error swallowing** — `Unlock()` captures each step's error into local vars (`err1`, `err2`) and returns whichever is non-nil (or nil if both succeed). The second operation's error may be silently discarded in favor of the first, or vice versa — only one error is ever returned.\n\n4. **No panic recovery** — None of these functions recover from panics. Any unexpected panic propagates to the caller with an empty stack trace in `runtime` (Go's default).",
    "internal/tools": "# internal/tools — Repository Traversal, Safe File I/O, and Git Utilities\n\nThis module provides three independent capability groups: **repository code discovery**, **safe file read/write operations** with TOCTOU race mitigation, **gitignore-aware filtering**, and **Git subprocess execution**. All functions are standalone (no methods on types); no exported structs or interfaces exist. No goroutines, channels, or sync primitives are used anywhere in the package—every call is synchronous and single-threaded.\n\n---\n\n## Repository Code Discovery\n\n`DiscoverCodeFiles` walks `repoRoot` recursively via `filepath.WalkDir`, skipping directories matching `.gitignore` patterns (compiled by `LoadGitignore`) or any dot-prefixed component (`.build`, `.svn`, etc.), then filters each leaf through `ShouldIgnoreFile`. Valid source paths are accumulated into a slice and returned.\n\nWalk callback errors from `filepath.WalkDir` are swallowed—individual directory entries that fail to stat return `nil` to the caller, making it impossible to distinguish an intentional skip from a walk failure downstream.\n\n---\n\n## Safe File Read / Write Operations\n\n### `ReadFileSafely(repoRoot, virtualPath)` → `(data []byte, err error)`\n\nResolves `virtualPath` through `security.SafeResolve` (path-traversal guard), then reads the resulting absolute path via `os.ReadFile`. Errors from both `SafeResolve` and `os.ReadFile` are wrapped with prefix `\"failed to read file:\"` using `%w`.\n\n### `WriteFileSafely(repoRoot, virtualPath, content []byte)` → `error`\n\nPerforms TOCTOU-safe writes:\n\n1. Resolve the safe absolute path via `security.SafeResolve`.\n2. Call `os.MkdirAll(dir, 0755)` to create parent directories if missing; wraps with `\"failed to create directory\"`.\n3. Open for write+create (`O_WRONLY|O_CREATE`, mode `0644`); wraps open failure with `\"failed to open file for writing\"`.\n4. Call `os.Lstat(safePath)` (no symlink follow) and `f.Stat()`; compare via `os.SameFile(fiLstat, fiFstat)`. If they differ—indicating a symlink swap or race condition—return an error prefixed `\"security violation\"` or `\"TOCTOU symlink race detected\"`.\n5. Truncate (`f.Truncate(0)`), then write content; wrap each with `\"failed to truncate file:\"` / `\"failed to write file content:\"`.\n\n---\n\n## Gitignore Handling and Binary Classification\n\n### `LoadGitignore(repoRoot)` → `(patterns []string, err error)`\n\nReads `.gitignore` from `repoRoot`, scanner processes line-by-line. Empty file returns `nil, nil`; non-existence (`os.IsNotExist`) also returns `nil, nil`. All other errors are wrapped. Patterns returned are active non-comment entries.\n\n### `ShouldIgnoreFile(repoRoot, relPath string, gitIgnore *ignore.GitIgnore, ignoredExtensions []string)` → `bool`\n\nApplies layered ignore rules:\n\n- Compiles user-supplied patterns via the external `github.com/sabhiram/go-gitignore` package (`*ignore.GitIgnore`).\n- Rejects paths with dot-prefixed components or `.egg-info` suffixes (build artifacts).\n- Checks file extension against a provided blacklist.\n- For unknown extensions, reads the first 1024 bytes and scans for null bytes via `IsBinaryFile`. If null bytes are present, returns `true` (binary → ignore).\n\n### `IsBinaryFile(path string)` → `bool`\n\nOpens the path; if `os.Open` fails, returns `false` (treats unreadable as non-binary). Reads up to 1024 bytes. Only wraps `io.EOF` (expected EOF) without error—other read errors return `false`. No error wrapping for open or other read failures beyond EOF handling.\n\n---\n\n## Git Subprocess Execution\n\n### `RunGit(repoRoot string, args ...string)` → `(stdout string, err error)`\n\nConstructs an `exec.Command` for `git`, prepends `--no-pager` to user-supplied arguments, sets `cmd.Dir = repoRoot`. Captures stdout into a `bytes.Buffer`, reads it back, trims whitespace. On success returns trimmed output; on failure wraps the original error with stderr text: `\"git command failed: %v, stderr: %s\"`.\n\n### `VerifyGitRepo(repoRoot string)` → `error`\n\nTwo-stage validation:\n\n1. Calls `exec.LookPath(\"git\")`; if unavailable, returns a fresh error without wrapping—no reference to any prior failure since none exists at that point.\n2. Otherwise delegates to `RunGit(repoRoot, \"rev-parse\", \"--is-inside-work-tree\")`. If the inner call fails, discards its returned error entirely and returns a new message: `\"not a git repository (or any of the parent directories)\"`.\n\n---\n\n## Error Handling Summary\n\n| Function | Failure Mode | Strategy |\n|---|---|---|\n| `ReadFileSafely` | Resolve or read failure | Wrap with `\"failed to read file:\"`, `%w` |\n| `WriteFileSafely` | Directory open, file open, lstat/stat mismatch, truncate, write | Contextual prefix per step; symlink race → `\"security violation\"` / `\"TOCTOU symlink race detected\"` |\n| `LoadGitignore` | Non-existence or read error | `nil, nil` on missing/empty; wrap everything else |\n| `DiscoverCodeFiles` | Walk entry failure | Swallowed—caller sees no signal |\n| `IsBinaryFile` | Open failure or non-EOF read error | Returns `false`; EOF is expected and ignored |\n| `RunGit` | Non-zero exit / exec error | Wrap original + stderr text with `%v`, `%s` |\n| `VerifyGitRepo` | Missing git binary | Fresh error, no wrapping |\n| `VerifyGitRepo` | Git query failure | Discard inner error; return fresh message only |"
  }
}